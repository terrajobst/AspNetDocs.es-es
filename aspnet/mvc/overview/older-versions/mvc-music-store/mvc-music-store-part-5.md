---
uid: mvc/overview/older-versions/mvc-music-store/mvc-music-store-part-5
title: 'Parte 5: edición de formularios y plantillas | Microsoft Docs'
author: jongalloway
description: En esta serie de tutoriales se detallan todos los pasos realizados para compilar la aplicación de ejemplo ASP.NET MVC Music Store. La parte 5 trata la edición de formularios y plantillas.
ms.author: riande
ms.date: 04/21/2011
ms.assetid: 6b09413a-6d6a-425a-87c9-629f91b91b28
msc.legacyurl: /mvc/overview/older-versions/mvc-music-store/mvc-music-store-part-5
msc.type: authoredcontent
ms.openlocfilehash: 20b99cbe57b5dfa623205838a5929733a6c2d70d
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 03/06/2020
ms.locfileid: "78450913"
---
# <a name="part-5-edit-forms-and-templating"></a><span data-ttu-id="11823-104">Parte 5: edición de formularios y plantillas</span><span class="sxs-lookup"><span data-stu-id="11823-104">Part 5: Edit Forms and Templating</span></span>

<span data-ttu-id="11823-105">por [Jon Galloway](https://github.com/jongalloway)</span><span class="sxs-lookup"><span data-stu-id="11823-105">by [Jon Galloway](https://github.com/jongalloway)</span></span>

> <span data-ttu-id="11823-106">El almacén de música de MVC es una aplicación de tutorial que presenta y explica paso a paso cómo usar ASP.NET MVC y Visual Studio para el desarrollo web.</span><span class="sxs-lookup"><span data-stu-id="11823-106">The MVC Music Store is a tutorial application that introduces and explains step-by-step how to use ASP.NET MVC and Visual Studio for web development.</span></span>  
>   
> <span data-ttu-id="11823-107">MVC Music Store es una implementación ligera de almacén de ejemplo que vende álbumes musicales en línea e implementa la funcionalidad básica de administración de sitios, Inicio de sesión de usuario y carro de la compra.</span><span class="sxs-lookup"><span data-stu-id="11823-107">The MVC Music Store is a lightweight sample store implementation which sells music albums online, and implements basic site administration, user sign-in, and shopping cart functionality.</span></span>
> 
> <span data-ttu-id="11823-108">En esta serie de tutoriales se detallan todos los pasos realizados para compilar la aplicación de ejemplo ASP.NET MVC Music Store.</span><span class="sxs-lookup"><span data-stu-id="11823-108">This tutorial series details all of the steps taken to build the ASP.NET MVC Music Store sample application.</span></span> <span data-ttu-id="11823-109">La parte 5 trata la edición de formularios y plantillas.</span><span class="sxs-lookup"><span data-stu-id="11823-109">Part 5 covers Edit Forms and Templating.</span></span>

<span data-ttu-id="11823-110">En el capítulo anterior, estábamos cargando datos de nuestra base de datos y mostrándolos.</span><span class="sxs-lookup"><span data-stu-id="11823-110">In the past chapter, we were loading data from our database and displaying it.</span></span> <span data-ttu-id="11823-111">En este capítulo, también se habilitará la edición de los datos.</span><span class="sxs-lookup"><span data-stu-id="11823-111">In this chapter, we'll also enable editing the data.</span></span>

## <a name="creating-the-storemanagercontroller"></a><span data-ttu-id="11823-112">Crear StoreManagerController</span><span class="sxs-lookup"><span data-stu-id="11823-112">Creating the StoreManagerController</span></span>

<span data-ttu-id="11823-113">Comenzaremos por crear un nuevo controlador denominado **StoreManagerController**.</span><span class="sxs-lookup"><span data-stu-id="11823-113">We'll begin by creating a new controller called **StoreManagerController**.</span></span> <span data-ttu-id="11823-114">Para este controlador, se aprovecharán las características de scaffolding disponibles en la actualización de ASP.NET MVC 3 Tools.</span><span class="sxs-lookup"><span data-stu-id="11823-114">For this controller, we will be taking advantage of the Scaffolding features available in the ASP.NET MVC 3 Tools Update.</span></span> <span data-ttu-id="11823-115">Establezca las opciones para el cuadro de diálogo Agregar controlador, como se muestra a continuación.</span><span class="sxs-lookup"><span data-stu-id="11823-115">Set the options for the Add Controller dialog as shown below.</span></span>

![](mvc-music-store-part-5/_static/image1.png)

<span data-ttu-id="11823-116">Al hacer clic en el botón Agregar, verá que el mecanismo de scaffolding ASP.NET MVC 3 realiza una buena cantidad de trabajo:</span><span class="sxs-lookup"><span data-stu-id="11823-116">When you click the Add button, you'll see that the ASP.NET MVC 3 scaffolding mechanism does a good amount of work for you:</span></span>

- <span data-ttu-id="11823-117">Crea el nuevo StoreManagerController con una variable local de Entity Framework</span><span class="sxs-lookup"><span data-stu-id="11823-117">It creates the new StoreManagerController with a local Entity Framework variable</span></span>
- <span data-ttu-id="11823-118">Agrega una carpeta StoreManager a la carpeta views del proyecto.</span><span class="sxs-lookup"><span data-stu-id="11823-118">It adds a StoreManager folder to the project's Views folder</span></span>
- <span data-ttu-id="11823-119">Agrega la vista Create. cshtml, DELETE. cshtml, details. cshtml, Edit. cshtml e index. cshtml, fuertemente tipada a la clase Album.</span><span class="sxs-lookup"><span data-stu-id="11823-119">It adds Create.cshtml, Delete.cshtml, Details.cshtml, Edit.cshtml, and Index.cshtml view, strongly typed to the Album class</span></span>

![](mvc-music-store-part-5/_static/image2.png)

<span data-ttu-id="11823-120">La nueva clase de controlador StoreManager incluye acciones CRUD (crear, leer, actualizar, eliminar) controlador que saben cómo trabajar con la clase de modelo de álbum y usar el contexto de Entity Framework para el acceso a la base de datos.</span><span class="sxs-lookup"><span data-stu-id="11823-120">The new StoreManager controller class includes CRUD (create, read, update, delete) controller actions which know how to work with the Album model class and use our Entity Framework context for database access.</span></span>

## <a name="modifying-a-scaffolded-view"></a><span data-ttu-id="11823-121">Modificar una vista con scaffolding</span><span class="sxs-lookup"><span data-stu-id="11823-121">Modifying a Scaffolded View</span></span>

<span data-ttu-id="11823-122">Es importante recordar que, mientras que este código se generó para nosotros, es el código ASP.NET MVC estándar, al igual que hemos escrito en este tutorial.</span><span class="sxs-lookup"><span data-stu-id="11823-122">It's important to remember that, while this code was generated for us, it's standard ASP.NET MVC code, just like we've been writing throughout this tutorial.</span></span> <span data-ttu-id="11823-123">Está pensada para ahorrarle el tiempo dedicado a escribir código de controlador reutilizable y a crear las vistas fuertemente tipadas manualmente, pero este no es el tipo de código generado que puede haber visto precedido por advertencias de graves en los comentarios sobre cómo no debe cambiar el codifica.</span><span class="sxs-lookup"><span data-stu-id="11823-123">It's intended to save you the time you'd spend on writing boilerplate controller code and creating the strongly typed views manually, but this isn't the kind of generated code you may have seen prefaced with dire warnings in comments about how you mustn't change the code.</span></span> <span data-ttu-id="11823-124">Este es el código y se espera que lo cambie.</span><span class="sxs-lookup"><span data-stu-id="11823-124">This is your code, and you're expected to change it.</span></span>

<span data-ttu-id="11823-125">Por lo tanto, comencemos con una edición rápida de la vista de índice de StoreManager (/Views/StoreManager/Index.cshtml).</span><span class="sxs-lookup"><span data-stu-id="11823-125">So, let's start with a quick edit to the StoreManager Index view (/Views/StoreManager/Index.cshtml).</span></span> <span data-ttu-id="11823-126">Esta vista mostrará una tabla que muestra los álbumes de nuestra tienda con los vínculos editar/detalles/eliminar e incluye las propiedades públicas del álbum.</span><span class="sxs-lookup"><span data-stu-id="11823-126">This view will display a table which lists the Albums in our store with Edit / Details / Delete links, and includes the Album's public properties.</span></span> <span data-ttu-id="11823-127">Vamos a quitar el campo AlbumArtUrl, ya que no es muy útil en esta pantalla.</span><span class="sxs-lookup"><span data-stu-id="11823-127">We'll remove the AlbumArtUrl field, as it's not very useful in this display.</span></span> <span data-ttu-id="11823-128">En &lt;sección&gt; de la tabla del código de vista, quite los elementos &lt;TH&gt; y &lt;TD&gt; que rodean las referencias de AlbumArtUrl, como se indica en las líneas resaltadas a continuación:</span><span class="sxs-lookup"><span data-stu-id="11823-128">In &lt;table&gt; section of the view code, remove the &lt;th&gt; and &lt;td&gt; elements surrounding AlbumArtUrl references, as indicated by the highlighted lines below:</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample1.cshtml)]

<span data-ttu-id="11823-129">El código de vista modificado aparecerá de la siguiente manera:</span><span class="sxs-lookup"><span data-stu-id="11823-129">The modified view code will appear as follows:</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample2.cshtml)]

## <a name="a-first-look-at-the-store-manager"></a><span data-ttu-id="11823-130">Un primer vistazo al administrador de la tienda</span><span class="sxs-lookup"><span data-stu-id="11823-130">A first look at the Store Manager</span></span>

<span data-ttu-id="11823-131">Ahora ejecute la aplicación y vaya a/StoreManager/.</span><span class="sxs-lookup"><span data-stu-id="11823-131">Now run the application and browse to /StoreManager/.</span></span> <span data-ttu-id="11823-132">Esto muestra el índice del administrador de la tienda que acaba de modificar y muestra una lista de los álbumes en el almacén con vínculos para editar, detalles y eliminar.</span><span class="sxs-lookup"><span data-stu-id="11823-132">This displays the Store Manager Index we just modified, showing a list of the albums in the store with links to Edit, Details, and Delete.</span></span>

![](mvc-music-store-part-5/_static/image3.png)

<span data-ttu-id="11823-133">Al hacer clic en el vínculo editar, se muestra un formulario de edición con campos para el álbum, incluidos los menús desplegables de género y artista.</span><span class="sxs-lookup"><span data-stu-id="11823-133">Clicking the Edit link displays an edit form with fields for the Album, including dropdowns for Genre and Artist.</span></span>

![](mvc-music-store-part-5/_static/image4.png)

<span data-ttu-id="11823-134">Haga clic en el vínculo "volver a la lista" de la parte inferior y, a continuación, haga clic en el vínculo detalles de un álbum.</span><span class="sxs-lookup"><span data-stu-id="11823-134">Click the "Back to List" link at the bottom, then click on the Details link for an Album.</span></span> <span data-ttu-id="11823-135">Esto muestra la información detallada de un álbum individual.</span><span class="sxs-lookup"><span data-stu-id="11823-135">This displays the detail information for an individual Album.</span></span>

![](mvc-music-store-part-5/_static/image5.png)

<span data-ttu-id="11823-136">De nuevo, haga clic en el vínculo volver a la lista y, a continuación, haga clic en un vínculo eliminar.</span><span class="sxs-lookup"><span data-stu-id="11823-136">Again, click the Back to List link, then click on a Delete link.</span></span> <span data-ttu-id="11823-137">Esto muestra un cuadro de diálogo de confirmación, que muestra los detalles del álbum y pregunta si estamos seguros de que queremos eliminarlo.</span><span class="sxs-lookup"><span data-stu-id="11823-137">This displays a confirmation dialog, showing the album details and asking if we're sure we want to delete it.</span></span>

![](mvc-music-store-part-5/_static/image6.png)

<span data-ttu-id="11823-138">Al hacer clic en el botón eliminar en la parte inferior, se eliminará el álbum y se le devolverá a la página de índice, que muestra el álbum eliminado.</span><span class="sxs-lookup"><span data-stu-id="11823-138">Clicking the Delete button at the bottom will delete the album and return you to the Index page, which shows the album deleted.</span></span>

<span data-ttu-id="11823-139">No estamos listos con el administrador de la tienda, pero tenemos el controlador de trabajo y el código de vista para que las operaciones CRUD empiecen a partir de.</span><span class="sxs-lookup"><span data-stu-id="11823-139">We're not done with the Store Manager, but we have working controller and view code for the CRUD operations to start from.</span></span>

## <a name="looking-at-the-store-manager-controller-code"></a><span data-ttu-id="11823-140">Examinar el código del controlador del administrador de la tienda</span><span class="sxs-lookup"><span data-stu-id="11823-140">Looking at the Store Manager Controller code</span></span>

<span data-ttu-id="11823-141">El controlador del administrador de la tienda contiene una buena cantidad de código.</span><span class="sxs-lookup"><span data-stu-id="11823-141">The Store Manager Controller contains a good amount of code.</span></span> <span data-ttu-id="11823-142">Vamos a repasarlo de arriba abajo.</span><span class="sxs-lookup"><span data-stu-id="11823-142">Let's go through this from top to bottom.</span></span> <span data-ttu-id="11823-143">El controlador incluye algunos espacios de nombres estándar para un controlador de MVC, así como una referencia al espacio de nombres de los modelos.</span><span class="sxs-lookup"><span data-stu-id="11823-143">The controller includes some standard namespaces for an MVC controller, as well as a reference to our Models namespace.</span></span> <span data-ttu-id="11823-144">El controlador tiene una instancia privada de MusicStoreEntities, usada por cada una de las acciones de controlador para el acceso a los datos.</span><span class="sxs-lookup"><span data-stu-id="11823-144">The controller has a private instance of MusicStoreEntities, used by each of the controller actions for data access.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample3.cs)]

### <a name="store-manager-index-and-details-actions"></a><span data-ttu-id="11823-145">Acciones de índice y detalles de Store Manager</span><span class="sxs-lookup"><span data-stu-id="11823-145">Store Manager Index and Details actions</span></span>

<span data-ttu-id="11823-146">En la vista de índice se recupera una lista de álbumes, incluida la información de intérprete y el género a la que se hace referencia en el álbum, como vimos anteriormente al trabajar en el método de exploración de la tienda.</span><span class="sxs-lookup"><span data-stu-id="11823-146">The index view retrieves a list of Albums, including each album's referenced Genre and Artist information, as we previously saw when working on the Store Browse method.</span></span> <span data-ttu-id="11823-147">La vista de índice sigue las referencias a los objetos vinculados para que pueda mostrar el nombre de género y el nombre del artista de cada álbum, de modo que el controlador sea eficaz y consulte la información de la solicitud original.</span><span class="sxs-lookup"><span data-stu-id="11823-147">The Index view is following the references to the linked objects so that it can display each album's Genre name and Artist name, so the controller is being efficient and querying for this information in the original request.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample4.cs)]

<span data-ttu-id="11823-148">La acción del controlador de detalles del controlador de StoreManager funciona exactamente igual que la acción de detalles del controlador de almacenamiento que hemos escrito anteriormente: consulta el álbum por identificador mediante el método Find () y, a continuación, lo devuelve a la vista.</span><span class="sxs-lookup"><span data-stu-id="11823-148">The StoreManager Controller's Details controller action works exactly the same as the Store Controller Details action we wrote previously - it queries for the Album by ID using the Find() method, then returns it to the view.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample5.cs)]

### <a name="the-create-action-methods"></a><span data-ttu-id="11823-149">Los métodos de acción Create</span><span class="sxs-lookup"><span data-stu-id="11823-149">The Create Action Methods</span></span>

<span data-ttu-id="11823-150">Los métodos de acción Create son ligeramente diferentes de los que hemos visto hasta ahora, ya que administran la entrada de formulario.</span><span class="sxs-lookup"><span data-stu-id="11823-150">The Create action methods are a little different from ones we've seen so far, because they handle form input.</span></span> <span data-ttu-id="11823-151">Cuando un usuario visita por primera vez/StoreManager/Create/, se le mostrará un formulario vacío.</span><span class="sxs-lookup"><span data-stu-id="11823-151">When a user first visits /StoreManager/Create/ they will be shown an empty form.</span></span> <span data-ttu-id="11823-152">Esta página HTML contendrá un formulario &lt;&gt; elemento que contiene los elementos de entrada DropDown y TextBox, donde pueden escribir los detalles del álbum.</span><span class="sxs-lookup"><span data-stu-id="11823-152">This HTML page will contain a &lt;form&gt; element that contains dropdown and textbox input elements where they can enter the album's details.</span></span>

<span data-ttu-id="11823-153">Una vez que el usuario rellena los valores del formulario de álbum, puede presionar el botón "guardar" para enviar estos cambios de vuelta a la aplicación para guardarlos en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="11823-153">After the user fills in the Album form values, they can press the "Save" button to submit these changes back to our application to save within the database.</span></span> <span data-ttu-id="11823-154">Cuando el usuario presiona el botón "guardar", el formulario &lt;&gt; realizará una devolución de datos HTTP en la dirección URL de/StoreManager/Create/y enviará el &lt;formulario&gt; valores como parte de HTTP-POST.</span><span class="sxs-lookup"><span data-stu-id="11823-154">When the user presses the "save" button the &lt;form&gt; will perform an HTTP-POST back to the /StoreManager/Create/ URL and submit the &lt;form&gt; values as part of the HTTP-POST.</span></span>

<span data-ttu-id="11823-155">ASP.NET MVC nos permite dividir fácilmente la lógica de estos dos escenarios de invocación de direcciones URL al permitirnos implementar dos métodos de acción "Create" independientes dentro de nuestra clase StoreManagerController: uno para controlar la búsqueda HTTP-GET inicial en la dirección URL de/StoreManager/Create/y otro para controlar la publicación HTTP de los cambios enviados.</span><span class="sxs-lookup"><span data-stu-id="11823-155">ASP.NET MVC allows us to easily split up the logic of these two URL invocation scenarios by enabling us to implement two separate "Create" action methods within our StoreManagerController class – one to handle the initial HTTP-GET browse to the /StoreManager/Create/ URL, and the other to handle the HTTP-POST of the submitted changes.</span></span>

### <a name="passing-information-to-a-view-using-viewbag"></a><span data-ttu-id="11823-156">Pasar información a una vista mediante ViewBag</span><span class="sxs-lookup"><span data-stu-id="11823-156">Passing information to a View using ViewBag</span></span>

<span data-ttu-id="11823-157">Hemos usado ViewBag anteriormente en este tutorial, pero no ha hablado mucho de ello.</span><span class="sxs-lookup"><span data-stu-id="11823-157">We've used the ViewBag earlier in this tutorial, but haven't talked much about it.</span></span> <span data-ttu-id="11823-158">ViewBag nos permite pasar información a la vista sin usar un objeto de modelo fuertemente tipado.</span><span class="sxs-lookup"><span data-stu-id="11823-158">The ViewBag allows us to pass information to the view without using a strongly typed model object.</span></span> <span data-ttu-id="11823-159">En este caso, la acción editar controlador HTTP-GET debe pasar una lista de géneros y artistas al formulario para rellenar las listas desplegables, y la manera más sencilla de hacerlo es devolverlas como elementos de ViewBag.</span><span class="sxs-lookup"><span data-stu-id="11823-159">In this case, our Edit HTTP-GET controller action needs to pass both a list of Genres and Artists to the form to populate the dropdowns, and the simplest way to do that is to return them as ViewBag items.</span></span>

<span data-ttu-id="11823-160">ViewBag es un objeto dinámico, lo que significa que puede escribir ViewBag. foo o ViewBag. YourNameHere sin escribir código para definir esas propiedades.</span><span class="sxs-lookup"><span data-stu-id="11823-160">The ViewBag is a dynamic object, meaning that you can type ViewBag.Foo or ViewBag.YourNameHere without writing code to define those properties.</span></span> <span data-ttu-id="11823-161">En este caso, el código del controlador utiliza ViewBag. GenreId y ViewBag. ArtistId para que los valores desplegables que se envían con el formulario sean GenreId y ArtistId, que son las propiedades del álbum que van a establecer.</span><span class="sxs-lookup"><span data-stu-id="11823-161">In this case, the controller code uses ViewBag.GenreId and ViewBag.ArtistId so that the dropdown values submitted with the form will be GenreId and ArtistId, which are the Album properties they will be setting.</span></span>

<span data-ttu-id="11823-162">Estos valores desplegables se devuelven al formulario mediante el objeto SelectList, que se crea solo para ese propósito.</span><span class="sxs-lookup"><span data-stu-id="11823-162">These dropdown values are returned to the form using the SelectList object, which is built just for that purpose.</span></span> <span data-ttu-id="11823-163">Esto se hace mediante código como el siguiente:</span><span class="sxs-lookup"><span data-stu-id="11823-163">This is done using code like this:</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample6.cs)]

<span data-ttu-id="11823-164">Como puede ver en el código del método de acción, se usan tres parámetros para crear este objeto:</span><span class="sxs-lookup"><span data-stu-id="11823-164">As you can see from the action method code, three parameters are being used to create this object:</span></span>

- <span data-ttu-id="11823-165">Lista de elementos que se mostrarán en la lista desplegable.</span><span class="sxs-lookup"><span data-stu-id="11823-165">The list of items the dropdown will be displaying.</span></span> <span data-ttu-id="11823-166">Tenga en cuenta que esto no es solo una cadena: vamos a pasar una lista de géneros.</span><span class="sxs-lookup"><span data-stu-id="11823-166">Note that this isn't just a string - we're passing a list of Genres.</span></span>
- <span data-ttu-id="11823-167">El siguiente parámetro que se pasa a SelectList es el valor seleccionado.</span><span class="sxs-lookup"><span data-stu-id="11823-167">The next parameter being passed to the SelectList is the Selected Value.</span></span> <span data-ttu-id="11823-168">Esta es la forma en que el SelectList sabe cómo seleccionar previamente un elemento de la lista.</span><span class="sxs-lookup"><span data-stu-id="11823-168">This how the SelectList knows how to pre-select an item in the list.</span></span> <span data-ttu-id="11823-169">Esto resultará más fácil de entender cuando examinemos el formulario de edición, que es bastante similar.</span><span class="sxs-lookup"><span data-stu-id="11823-169">This will be easier to understand when we look at the Edit form, which is pretty similar.</span></span>
- <span data-ttu-id="11823-170">El último parámetro es la propiedad que se va a mostrar.</span><span class="sxs-lookup"><span data-stu-id="11823-170">The final parameter is the property to be displayed.</span></span> <span data-ttu-id="11823-171">En este caso, esto indica que la propiedad Genre.Name es lo que se mostrará al usuario.</span><span class="sxs-lookup"><span data-stu-id="11823-171">In this case, this is indicating that the Genre.Name property is what will be shown to the user.</span></span>

<span data-ttu-id="11823-172">Con eso en mente, la acción de creación de HTTP-GET es bastante simple: se agregan dos SelectLists a ViewBag y no se pasa ningún objeto de modelo al formulario (ya que aún no se ha creado).</span><span class="sxs-lookup"><span data-stu-id="11823-172">With that in mind, then, the HTTP-GET Create action is pretty simple - two SelectLists are added to the ViewBag, and no model object is passed to the form (since it hasn't been created yet).</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample7.cs)]

### <a name="html-helpers-to-display-the-drop-downs-in-the-create-view"></a><span data-ttu-id="11823-173">Aplicaciones auxiliares de HTML para mostrar las listas desplegables en la vista de creación</span><span class="sxs-lookup"><span data-stu-id="11823-173">HTML Helpers to display the Drop Downs in the Create View</span></span>

<span data-ttu-id="11823-174">Como hemos hablado de cómo se pasan los valores de la lista desplegable a la vista, echemos un vistazo a la vista para ver cómo se muestran esos valores.</span><span class="sxs-lookup"><span data-stu-id="11823-174">Since we've talked about how the drop down values are passed to the view, let's take a quick look at the view to see how those values are displayed.</span></span> <span data-ttu-id="11823-175">En el código de vista (/Views/StoreManager/Create.cshtml), verá que se realiza la siguiente llamada para mostrar la lista desplegable género.</span><span class="sxs-lookup"><span data-stu-id="11823-175">In the view code (/Views/StoreManager/Create.cshtml), you'll see the following call is made to display the Genre drop down.</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample8.cshtml)]

<span data-ttu-id="11823-176">Esto se conoce como aplicación auxiliar HTML: un método de utilidad que realiza una tarea de vista común.</span><span class="sxs-lookup"><span data-stu-id="11823-176">This is known as an HTML Helper - a utility method which performs a common view task.</span></span> <span data-ttu-id="11823-177">Las aplicaciones auxiliares HTML son muy útiles para mantener el código de vista conciso y legible.</span><span class="sxs-lookup"><span data-stu-id="11823-177">HTML Helpers are very useful in keeping our view code concise and readable.</span></span> <span data-ttu-id="11823-178">ASP.NET MVC proporciona la aplicación auxiliar HTML. DropDownList pero, como veremos más adelante, es posible crear nuestras propias aplicaciones auxiliares para ver el código que vamos a usar en nuestra aplicación.</span><span class="sxs-lookup"><span data-stu-id="11823-178">The Html.DropDownList helper is provided by ASP.NET MVC, but as we'll see later it's possible to create our own helpers for view code we'll reuse in our application.</span></span>

<span data-ttu-id="11823-179">La llamada a HTML. DropDownList solo tiene que indicar dos cosas: Dónde obtener la lista para mostrar y qué valor (si existe) se debe preseleccionar.</span><span class="sxs-lookup"><span data-stu-id="11823-179">The Html.DropDownList call just needs to be told two things - where to get the list to display, and what value (if any) should be pre-selected.</span></span> <span data-ttu-id="11823-180">El primer parámetro, GenreId, indica a DropDownList que busque un valor denominado GenreId en el modelo o en ViewBag.</span><span class="sxs-lookup"><span data-stu-id="11823-180">The first parameter, GenreId, tells the DropDownList to look for a value named GenreId in either the model or ViewBag.</span></span> <span data-ttu-id="11823-181">El segundo parámetro se usa para indicar el valor que se va a mostrar como seleccionado inicialmente en la lista desplegable.</span><span class="sxs-lookup"><span data-stu-id="11823-181">The second parameter is used to indicate the value to show as initially selected in the drop down list.</span></span> <span data-ttu-id="11823-182">Dado que este formulario es un formulario de creación, no hay ningún valor que se haya preseleccionado y se pasa String. Empty.</span><span class="sxs-lookup"><span data-stu-id="11823-182">Since this form is a Create form, there's no value to be preselected and String.Empty is passed.</span></span>

### <a name="handling-the-posted-form-values"></a><span data-ttu-id="11823-183">Controlar los valores de formulario publicados</span><span class="sxs-lookup"><span data-stu-id="11823-183">Handling the Posted Form values</span></span>

<span data-ttu-id="11823-184">Como hemos comentado antes, hay dos métodos de acción asociados a cada formulario.</span><span class="sxs-lookup"><span data-stu-id="11823-184">As we discussed before, there are two action methods associated with each form.</span></span> <span data-ttu-id="11823-185">La primera controla la solicitud HTTP-GET y muestra el formulario.</span><span class="sxs-lookup"><span data-stu-id="11823-185">The first handles the HTTP-GET request and displays the form.</span></span> <span data-ttu-id="11823-186">El segundo controla la solicitud HTTP-POST, que contiene los valores de formulario enviados.</span><span class="sxs-lookup"><span data-stu-id="11823-186">The second handles the HTTP-POST request, which contains the submitted form values.</span></span> <span data-ttu-id="11823-187">Observe que la acción del controlador tiene un atributo [HttpPost], que indica a ASP.NET MVC que solo debe responder a solicitudes HTTP POST.</span><span class="sxs-lookup"><span data-stu-id="11823-187">Notice that controller action has an [HttpPost] attribute, which tells ASP.NET MVC that it should only respond to HTTP-POST requests.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample9.cs)]

<span data-ttu-id="11823-188">Esta acción tiene cuatro responsabilidades:</span><span class="sxs-lookup"><span data-stu-id="11823-188">This action has four responsibilities:</span></span>

- 1. <span data-ttu-id="11823-189">Leer los valores del formulario</span><span class="sxs-lookup"><span data-stu-id="11823-189">Read the form values</span></span>
- 2. <span data-ttu-id="11823-190">Comprobar si los valores del formulario pasan las reglas de validación</span><span class="sxs-lookup"><span data-stu-id="11823-190">Check if the form values pass any validation rules</span></span>
- 3. <span data-ttu-id="11823-191">Si el envío del formulario es válido, guarde los datos y muestre la lista actualizada.</span><span class="sxs-lookup"><span data-stu-id="11823-191">If the form submission is valid, save the data and display the updated list</span></span>
- 4. <span data-ttu-id="11823-192">Si el envío del formulario no es válido, vuelva a mostrar el formulario con errores de validación.</span><span class="sxs-lookup"><span data-stu-id="11823-192">If the form submission is not valid, redisplay the form with validation errors</span></span>

#### <a name="reading-form-values-with-model-binding"></a><span data-ttu-id="11823-193">Leer valores de formulario con el enlace de modelos</span><span class="sxs-lookup"><span data-stu-id="11823-193">Reading Form Values with Model Binding</span></span>

<span data-ttu-id="11823-194">La acción de controlador está procesando un envío de formulario que incluye valores para GenreId y ArtistId (de la lista desplegable) y valores de cuadro de texto para título, precio y AlbumArtUrl.</span><span class="sxs-lookup"><span data-stu-id="11823-194">The controller action is processing a form submission that includes values for GenreId and ArtistId (from the drop down list) and textbox values for Title, Price, and AlbumArtUrl.</span></span> <span data-ttu-id="11823-195">Aunque es posible acceder directamente a los valores de formulario, un enfoque mejor es usar las capacidades de enlace de modelos integradas en ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="11823-195">While it's possible to directly access form values, a better approach is to use the Model Binding capabilities built into ASP.NET MVC.</span></span> <span data-ttu-id="11823-196">Cuando una acción de controlador toma un tipo de modelo como un parámetro, ASP.NET MVC intentará rellenar un objeto de ese tipo mediante entradas de formulario (así como valores de ruta y QueryString).</span><span class="sxs-lookup"><span data-stu-id="11823-196">When a controller action takes a model type as a parameter, ASP.NET MVC will attempt to populate an object of that type using form inputs (as well as route and querystring values).</span></span> <span data-ttu-id="11823-197">Para ello, busca valores cuyos nombres coinciden con las propiedades del objeto de modelo; por ejemplo, al establecer el valor de GenreId del nuevo objeto album, busca una entrada con el nombre GenreId.</span><span class="sxs-lookup"><span data-stu-id="11823-197">It does this by looking for values whose names match properties of the model object, e.g. when setting the new Album object's GenreId value, it looks for an input with the name GenreId.</span></span> <span data-ttu-id="11823-198">Al crear vistas con los métodos estándar en ASP.NET MVC, los formularios siempre se representarán con los nombres de propiedad como nombres de campo de entrada, por lo que los nombres de campo solo coincidirán.</span><span class="sxs-lookup"><span data-stu-id="11823-198">When you create views using the standard methods in ASP.NET MVC, the forms will always be rendered using property names as input field names, so this the field names will just match up.</span></span>

#### <a name="validating-the-model"></a><span data-ttu-id="11823-199">Validar el modelo</span><span class="sxs-lookup"><span data-stu-id="11823-199">Validating the Model</span></span>

<span data-ttu-id="11823-200">El modelo se valida con una llamada simple a ModelState. IsValid.</span><span class="sxs-lookup"><span data-stu-id="11823-200">The model is validated with a simple call to ModelState.IsValid.</span></span> <span data-ttu-id="11823-201">Todavía no hemos agregado ninguna regla de validación a nuestra clase de álbum; lo haremos en un poco, de modo que ahora esta comprobación no tiene mucho que hacer.</span><span class="sxs-lookup"><span data-stu-id="11823-201">We haven't added any validation rules to our Album class yet - we'll do that in a bit - so right now this check doesn't have much to do.</span></span> <span data-ttu-id="11823-202">Lo importante es que esta comprobación de ModelStat. IsValid se adaptará a las reglas de validación que se colocan en nuestro modelo, por lo que los cambios futuros en las reglas de validación no requerirán ninguna actualización del código de acción del controlador.</span><span class="sxs-lookup"><span data-stu-id="11823-202">What's important is that this ModelStat.IsValid check will adapt to the validation rules we put on our model, so future changes to validation rules won't require any updates to the controller action code.</span></span>

#### <a name="saving-the-submitted-values"></a><span data-ttu-id="11823-203">Guardar los valores enviados</span><span class="sxs-lookup"><span data-stu-id="11823-203">Saving the submitted values</span></span>

<span data-ttu-id="11823-204">Si el envío del formulario pasa la validación, es el momento de guardar los valores en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="11823-204">If the form submission passes validation, it's time to save the values to the database.</span></span> <span data-ttu-id="11823-205">Con Entity Framework, eso solo requiere agregar el modelo a la colección de álbumes y llamar a SaveChanges.</span><span class="sxs-lookup"><span data-stu-id="11823-205">With Entity Framework, that just requires adding the model to the Albums collection and calling SaveChanges.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample10.cs)]

<span data-ttu-id="11823-206">Entity Framework genera los comandos SQL adecuados para conservar el valor.</span><span class="sxs-lookup"><span data-stu-id="11823-206">Entity Framework generates the appropriate SQL commands to persist the value.</span></span> <span data-ttu-id="11823-207">Después de guardar los datos, redirigimos de nuevo a la lista de álbumes para que podamos ver la actualización.</span><span class="sxs-lookup"><span data-stu-id="11823-207">After saving the data, we redirect back to the list of Albums so we can see our update.</span></span> <span data-ttu-id="11823-208">Para ello, se devuelve RedirectToAction con el nombre de la acción de controlador que se desea mostrar.</span><span class="sxs-lookup"><span data-stu-id="11823-208">This is done by returning RedirectToAction with the name of the controller action we want displayed.</span></span> <span data-ttu-id="11823-209">En este caso, este es el método de índice.</span><span class="sxs-lookup"><span data-stu-id="11823-209">In this case, that's the Index method.</span></span>

#### <a name="displaying-invalid-form-submissions-with-validation-errors"></a><span data-ttu-id="11823-210">Mostrar envíos de formulario no válidos con errores de validación</span><span class="sxs-lookup"><span data-stu-id="11823-210">Displaying invalid form submissions with Validation Errors</span></span>

<span data-ttu-id="11823-211">En el caso de una entrada de formulario no válida, los valores de la lista desplegable se agregan a ViewBag (como en el caso HTTP-GET) y los valores del modelo enlazado se devuelven a la vista para su presentación.</span><span class="sxs-lookup"><span data-stu-id="11823-211">In the case of invalid form input, the dropdown values are added to the ViewBag (as in the HTTP-GET case) and the bound model values are passed back to the view for display.</span></span> <span data-ttu-id="11823-212">Los errores de validación se muestran automáticamente mediante la aplicación auxiliar HTML @Html.ValidationMessageFor.</span><span class="sxs-lookup"><span data-stu-id="11823-212">Validation errors are automatically displayed using the @Html.ValidationMessageFor HTML Helper.</span></span>

#### <a name="testing-the-create-form"></a><span data-ttu-id="11823-213">Probar el formulario de creación</span><span class="sxs-lookup"><span data-stu-id="11823-213">Testing the Create Form</span></span>

<span data-ttu-id="11823-214">Para probar esto, ejecute la aplicación y vaya a/StoreManager/Create/, que le mostrará el formulario en blanco que ha devuelto el método StoreController Create HTTP-GET.</span><span class="sxs-lookup"><span data-stu-id="11823-214">To test this out, run the application and browse to /StoreManager/Create/ - this will show you the blank form which was returned by the StoreController Create HTTP-GET method.</span></span>

<span data-ttu-id="11823-215">Rellene algunos valores y haga clic en el botón crear para enviar el formulario.</span><span class="sxs-lookup"><span data-stu-id="11823-215">Fill in some values and click the Create button to submit the form.</span></span>

![](mvc-music-store-part-5/_static/image7.png)

![](mvc-music-store-part-5/_static/image8.png)

### <a name="handling-edits"></a><span data-ttu-id="11823-216">Control de ediciones</span><span class="sxs-lookup"><span data-stu-id="11823-216">Handling Edits</span></span>

<span data-ttu-id="11823-217">El par de acciones de edición (HTTP-GET y HTTP-POST) son muy similares a los métodos de acción de creación que se acaban de examinar.</span><span class="sxs-lookup"><span data-stu-id="11823-217">The Edit action pair (HTTP-GET and HTTP-POST) are very similar to the Create action methods we just looked at.</span></span> <span data-ttu-id="11823-218">Dado que el escenario de edición implica trabajar con un álbum existente, el método Edit HTTP-GET carga el álbum basándose en el parámetro "ID", que se pasa a través de la ruta.</span><span class="sxs-lookup"><span data-stu-id="11823-218">Since the edit scenario involves working with an existing album, the Edit HTTP-GET method loads the Album based on the "id" parameter, passed in via the route.</span></span> <span data-ttu-id="11823-219">Este código para recuperar un álbum por AlbumId es el mismo que hemos examinado anteriormente en la acción del controlador de detalles.</span><span class="sxs-lookup"><span data-stu-id="11823-219">This code for retrieving an album by AlbumId is the same as we've previously looked at in the Details controller action.</span></span> <span data-ttu-id="11823-220">Al igual que con el método Create/HTTP-GET, los valores de la lista desplegable se devuelven a través de ViewBag.</span><span class="sxs-lookup"><span data-stu-id="11823-220">As with the Create / HTTP-GET method, the drop down values are returned via the ViewBag.</span></span> <span data-ttu-id="11823-221">Esto nos permite devolver un álbum como nuestro objeto de modelo a la vista (que está fuertemente tipada a la clase album) mientras se pasan datos adicionales (por ejemplo, una lista de géneros) a través de ViewBag.</span><span class="sxs-lookup"><span data-stu-id="11823-221">This allows us to return an Album as our model object to the view (which is strongly typed to the Album class) while passing additional data (e.g. a list of Genres) via the ViewBag.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample11.cs)]

<span data-ttu-id="11823-222">La acción editar HTTP-POST es muy similar a la acción crear HTTP-POST.</span><span class="sxs-lookup"><span data-stu-id="11823-222">The Edit HTTP-POST action is very similar to the Create HTTP-POST action.</span></span> <span data-ttu-id="11823-223">La única diferencia es que, en lugar de agregar un nuevo álbum a la base de BD. Álbumes, estamos buscando la instancia actual del álbum con dB. Entrada (álbum) y establecer su estado en modificado.</span><span class="sxs-lookup"><span data-stu-id="11823-223">The only difference is that instead of adding a new album to the db.Albums collection, we're finding the current instance of the Album using db.Entry(album) and setting its state to Modified.</span></span> <span data-ttu-id="11823-224">Esto indica Entity Framework que estamos modificando un álbum existente en lugar de crear uno nuevo.</span><span class="sxs-lookup"><span data-stu-id="11823-224">This tells Entity Framework that we are modifying an existing album as opposed to creating a new one.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample12.cs)]

<span data-ttu-id="11823-225">Para probar esto, ejecute la aplicación y vaya a/StoreManger/y, a continuación, haga clic en el vínculo editar de un álbum.</span><span class="sxs-lookup"><span data-stu-id="11823-225">We can test this out by running the application and browsing to /StoreManger/, then clicking the Edit link for an album.</span></span>

![](mvc-music-store-part-5/_static/image9.png)

<span data-ttu-id="11823-226">Esto muestra el formulario de edición que se muestra en el método Edit HTTP-GET.</span><span class="sxs-lookup"><span data-stu-id="11823-226">This displays the Edit form shown by the Edit HTTP-GET method.</span></span> <span data-ttu-id="11823-227">Rellene algunos valores y haga clic en el botón Guardar.</span><span class="sxs-lookup"><span data-stu-id="11823-227">Fill in some values and click the Save button.</span></span>

![](mvc-music-store-part-5/_static/image10.png)

<span data-ttu-id="11823-228">De este modo, se envía el formulario, se guardan los valores y se devuelven a la lista de álbumes, donde se muestran los valores que se actualizaron.</span><span class="sxs-lookup"><span data-stu-id="11823-228">This posts the form, saves the values, and returns us to the Album list, showing that the values were updated.</span></span>

![](mvc-music-store-part-5/_static/image11.png)

### <a name="handling-deletion"></a><span data-ttu-id="11823-229">Controlar la eliminación</span><span class="sxs-lookup"><span data-stu-id="11823-229">Handling Deletion</span></span>

<span data-ttu-id="11823-230">La eliminación sigue el mismo patrón que editar y crear, mediante una acción de controlador para mostrar el formulario de confirmación y otra acción de controlador para controlar el envío del formulario.</span><span class="sxs-lookup"><span data-stu-id="11823-230">Deletion follows the same pattern as Edit and Create, using one controller action to display the confirmation form, and another controller action to handle the form submission.</span></span>

<span data-ttu-id="11823-231">La acción HTTP-GET Delete Controller es exactamente la misma que la acción anterior del controlador de detalles del administrador de la tienda.</span><span class="sxs-lookup"><span data-stu-id="11823-231">The HTTP-GET Delete controller action is exactly the same as our previous Store Manager Details controller action.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample13.cs)]

<span data-ttu-id="11823-232">Se muestra un formulario fuertemente tipado a un tipo de álbum mediante la plantilla eliminar contenido de vista.</span><span class="sxs-lookup"><span data-stu-id="11823-232">We display a form that's strongly typed to an Album type, using the Delete view content template.</span></span>

![](mvc-music-store-part-5/_static/image12.png)

<span data-ttu-id="11823-233">La plantilla de eliminación muestra todos los campos del modelo, pero se puede simplificar bastante.</span><span class="sxs-lookup"><span data-stu-id="11823-233">The Delete template shows all the fields for the model, but we can simplify that down quite a bit.</span></span> <span data-ttu-id="11823-234">Cambie el código de vista de/Views/StoreManager/Delete.cshtml a lo siguiente.</span><span class="sxs-lookup"><span data-stu-id="11823-234">Change the view code in /Views/StoreManager/Delete.cshtml to the following.</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample14.cshtml)]

<span data-ttu-id="11823-235">Esto muestra una confirmación de eliminación simplificada.</span><span class="sxs-lookup"><span data-stu-id="11823-235">This displays a simplified Delete confirmation.</span></span>

![](mvc-music-store-part-5/_static/image13.png)

<span data-ttu-id="11823-236">Al hacer clic en el botón eliminar, el formulario se devuelve al servidor, que ejecuta la acción DeleteConfirmed.</span><span class="sxs-lookup"><span data-stu-id="11823-236">Clicking the Delete button causes the form to be posted back to the server, which executes the DeleteConfirmed action.</span></span>

[!code-csharp[Main](mvc-music-store-part-5/samples/sample15.cs)]

<span data-ttu-id="11823-237">Nuestra acción HTTP-POST Delete Controller realiza las siguientes acciones:</span><span class="sxs-lookup"><span data-stu-id="11823-237">Our HTTP-POST Delete Controller Action takes the following actions:</span></span>

- 1. <span data-ttu-id="11823-238">Carga el álbum por identificador</span><span class="sxs-lookup"><span data-stu-id="11823-238">Loads the Album by ID</span></span>
- 2. <span data-ttu-id="11823-239">Elimina el álbum y guarda los cambios.</span><span class="sxs-lookup"><span data-stu-id="11823-239">Deletes it the album and save changes</span></span>
- 3. <span data-ttu-id="11823-240">Redirige al índice, mostrando que el álbum se ha quitado de la lista.</span><span class="sxs-lookup"><span data-stu-id="11823-240">Redirects to the Index, showing that the Album was removed from the list</span></span>

<span data-ttu-id="11823-241">Para probar esto, ejecute la aplicación y vaya a/StoreManager.</span><span class="sxs-lookup"><span data-stu-id="11823-241">To test this, run the application and browse to /StoreManager.</span></span> <span data-ttu-id="11823-242">Seleccione un álbum de la lista y haga clic en el vínculo eliminar.</span><span class="sxs-lookup"><span data-stu-id="11823-242">Select an album from the list and click the Delete link.</span></span>

![](mvc-music-store-part-5/_static/image14.png)

<span data-ttu-id="11823-243">Se mostrará la pantalla de confirmación de eliminación.</span><span class="sxs-lookup"><span data-stu-id="11823-243">This displays our Delete confirmation screen.</span></span>

![](mvc-music-store-part-5/_static/image15.png)

<span data-ttu-id="11823-244">Al hacer clic en el botón eliminar, se quita el álbum y se devuelve a la página de índice del administrador de la tienda, que muestra que el álbum se ha eliminado.</span><span class="sxs-lookup"><span data-stu-id="11823-244">Clicking the Delete button removes the album and returns us to the Store Manager Index page, which shows that the album has been deleted.</span></span>

![](mvc-music-store-part-5/_static/image16.png)

### <a name="using-a-custom-html-helper-to-truncate-text"></a><span data-ttu-id="11823-245">Usar una aplicación auxiliar HTML personalizada para truncar el texto</span><span class="sxs-lookup"><span data-stu-id="11823-245">Using a custom HTML Helper to truncate text</span></span>

<span data-ttu-id="11823-246">Tenemos un posible problema con nuestra página de índice del administrador de la tienda.</span><span class="sxs-lookup"><span data-stu-id="11823-246">We've got one potential issue with our Store Manager Index page.</span></span> <span data-ttu-id="11823-247">Las propiedades del título del álbum y el nombre del intérprete pueden ser lo suficientemente largas como para que se produzca el formato de la tabla.</span><span class="sxs-lookup"><span data-stu-id="11823-247">Our Album Title and Artist Name properties can both be long enough that they could throw off our table formatting.</span></span> <span data-ttu-id="11823-248">Crearemos una aplicación auxiliar HTML personalizada para que podamos truncar fácilmente estas y otras propiedades en nuestras vistas.</span><span class="sxs-lookup"><span data-stu-id="11823-248">We'll create a custom HTML Helper to allow us to easily truncate these and other properties in our Views.</span></span>

![](mvc-music-store-part-5/_static/image17.png)

<span data-ttu-id="11823-249">La sintaxis de @helper de Razor ha facilitado la creación de sus propias funciones auxiliares para su uso en las vistas.</span><span class="sxs-lookup"><span data-stu-id="11823-249">Razor's @helper syntax has made it pretty easy to create your own helper functions for use in your views.</span></span> <span data-ttu-id="11823-250">Abra la vista/Views/StoreManager/Index.cshtml y agregue el código siguiente directamente después de la línea de @model.</span><span class="sxs-lookup"><span data-stu-id="11823-250">Open the /Views/StoreManager/Index.cshtml view and add the following code directly after the @model line.</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample16.cshtml)]

<span data-ttu-id="11823-251">Este método auxiliar toma una cadena y la longitud máxima permitida.</span><span class="sxs-lookup"><span data-stu-id="11823-251">This helper method takes a string and a maximum length to allow.</span></span> <span data-ttu-id="11823-252">Si el texto proporcionado es más corto que la longitud especificada, el ayudante lo envía tal cual.</span><span class="sxs-lookup"><span data-stu-id="11823-252">If the text supplied is shorter than the length specified, the helper outputs it as-is.</span></span> <span data-ttu-id="11823-253">Si es mayor, trunca el texto y representa "..." para el resto.</span><span class="sxs-lookup"><span data-stu-id="11823-253">If it is longer, then it truncates the text and renders "…" for the remainder.</span></span>

<span data-ttu-id="11823-254">Ahora podemos usar nuestra aplicación auxiliar de truncamiento para asegurarse de que las propiedades del título del álbum y del nombre del intérprete tengan menos de 25 caracteres.</span><span class="sxs-lookup"><span data-stu-id="11823-254">Now we can use our Truncate helper to ensure that both the Album Title and Artist Name properties are less than 25 characters.</span></span> <span data-ttu-id="11823-255">A continuación se muestra el código de vista completo que usa nuestra nueva aplicación auxiliar de truncamiento.</span><span class="sxs-lookup"><span data-stu-id="11823-255">The complete view code using our new Truncate helper appears below.</span></span>

[!code-cshtml[Main](mvc-music-store-part-5/samples/sample17.cshtml)]

<span data-ttu-id="11823-256">Ahora, cuando examinemos la dirección URL de/StoreManager/, los álbumes y los títulos se mantienen por debajo de las longitudes máximas.</span><span class="sxs-lookup"><span data-stu-id="11823-256">Now when we browse the /StoreManager/ URL, the albums and titles are kept below our maximum lengths.</span></span>

![](mvc-music-store-part-5/_static/image18.png)

<span data-ttu-id="11823-257">Nota: muestra el caso simple de crear y usar una aplicación auxiliar en una vista.</span><span class="sxs-lookup"><span data-stu-id="11823-257">Note: This shows the simple case of creating and using a helper in one view.</span></span> <span data-ttu-id="11823-258">Para obtener más información sobre la creación de aplicaciones auxiliares que puede usar en todo el sitio, consulte mi entrada de blog: [http://bit.ly/mvc3-helper-options](http://bit.ly/mvc3-helper-options)</span><span class="sxs-lookup"><span data-stu-id="11823-258">To learn more about creating helpers that you can use throughout your site, see my blog post: [http://bit.ly/mvc3-helper-options](http://bit.ly/mvc3-helper-options)</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="11823-259">[Anterior](mvc-music-store-part-4.md)
> [Siguiente](mvc-music-store-part-6.md)</span><span class="sxs-lookup"><span data-stu-id="11823-259">[Previous](mvc-music-store-part-4.md)
[Next](mvc-music-store-part-6.md)</span></span>
