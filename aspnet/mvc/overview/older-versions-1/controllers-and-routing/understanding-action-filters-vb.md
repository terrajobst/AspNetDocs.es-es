---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
title: Descripción de los filtros de acción (VB) | Microsoft Docs
author: microsoft
description: El objetivo de este tutorial es explicar los filtros de acción. Un filtro de acción es un atributo que se puede aplicar a una acción de controlador o un controlador todo...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: e83812f2-c53e-4a43-a7c1-d64c59ecf694
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
msc.type: authoredcontent
ms.openlocfilehash: fb3ed252a9232a2f5a1ad4257156a142bbe5b174
ms.sourcegitcommit: 51b01b6ff8edde57d8243e4da28c9f1e7f1962b2
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 05/06/2019
ms.locfileid: "65123174"
---
# <a name="understanding-action-filters-vb"></a><span data-ttu-id="fcb41-104">Descripción de los filtros de acción (VB)</span><span class="sxs-lookup"><span data-stu-id="fcb41-104">Understanding Action Filters (VB)</span></span>

<span data-ttu-id="fcb41-105">por [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="fcb41-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="fcb41-106">Descargar PDF</span><span class="sxs-lookup"><span data-stu-id="fcb41-106">Download PDF</span></span>](http://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_VB.pdf)

> <span data-ttu-id="fcb41-107">El objetivo de este tutorial es explicar los filtros de acción.</span><span class="sxs-lookup"><span data-stu-id="fcb41-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="fcb41-108">Un filtro de acción es un atributo que se puede aplicar a una acción de controlador o un controlador de todo, que modifica la forma en que se ejecuta la acción.</span><span class="sxs-lookup"><span data-stu-id="fcb41-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="fcb41-109">Descripción de los filtros de acción</span><span class="sxs-lookup"><span data-stu-id="fcb41-109">Understanding Action Filters</span></span>

<span data-ttu-id="fcb41-110">El objetivo de este tutorial es explicar los filtros de acción.</span><span class="sxs-lookup"><span data-stu-id="fcb41-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="fcb41-111">Un filtro de acción es un atributo que se puede aplicar a una acción de controlador o un controlador de todo, que modifica la forma en que se ejecuta la acción.</span><span class="sxs-lookup"><span data-stu-id="fcb41-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="fcb41-112">El marco de ASP.NET MVC incluye varios filtros de acción:</span><span class="sxs-lookup"><span data-stu-id="fcb41-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="fcb41-113">OutputCache – este filtro de acción almacena en caché el resultado de una acción de controlador para un período de tiempo especificado.</span><span class="sxs-lookup"><span data-stu-id="fcb41-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="fcb41-114">HandleError – este filtro de acción controla los errores que se genera cuando se ejecuta una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="fcb41-115">Autorizar: este filtro de acción le permite restringir el acceso a un usuario determinado o un rol.</span><span class="sxs-lookup"><span data-stu-id="fcb41-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="fcb41-116">También puede crear sus propios filtros de acción personalizada.</span><span class="sxs-lookup"><span data-stu-id="fcb41-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="fcb41-117">Por ejemplo, puede crear un filtro de acción personalizado con el fin de implementar un sistema de autenticación personalizada.</span><span class="sxs-lookup"><span data-stu-id="fcb41-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="fcb41-118">O bien, si desea crear un filtro de acción que modifica los datos de vista devueltos por una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="fcb41-119">En este tutorial, aprenderá a crear un filtro de acción desde el principio.</span><span class="sxs-lookup"><span data-stu-id="fcb41-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="fcb41-120">Creamos un filtro de acción de registro que registra las distintas fases del procesamiento de una acción a la ventana de salida de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="fcb41-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="fcb41-121">Uso de un filtro de acción</span><span class="sxs-lookup"><span data-stu-id="fcb41-121">Using an Action Filter</span></span>

<span data-ttu-id="fcb41-122">Un filtro de acción es un atributo.</span><span class="sxs-lookup"><span data-stu-id="fcb41-122">An action filter is an attribute.</span></span> <span data-ttu-id="fcb41-123">Puede aplicar la mayoría de los filtros de acción para una acción de controlador individuales o todo un controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="fcb41-124">Por ejemplo, el controlador de datos en el listado 1 expone una acción denominada `Index()` que devuelve la hora actual.</span><span class="sxs-lookup"><span data-stu-id="fcb41-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="fcb41-125">Esta acción se decora con el `OutputCache` filtro de acción.</span><span class="sxs-lookup"><span data-stu-id="fcb41-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="fcb41-126">Este filtro hace que el valor devuelto por la acción que se almacenará en caché durante 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="fcb41-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="fcb41-127">**Listado 1: `Controllers\DataController.vb`**</span><span class="sxs-lookup"><span data-stu-id="fcb41-127">**Listing 1 – `Controllers\DataController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample1.vb)]

<span data-ttu-id="fcb41-128">Si se invoca varias veces el `Index()` acción al introducir la dirección URL operaciones/Data/índice en la barra de direcciones del explorador y presionar la actualización de botón varias veces, verá la misma hora durante 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="fcb41-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="fcb41-129">La salida de la `Index()` acción se almacena en caché durante 10 segundos (consulte la figura 1).</span><span class="sxs-lookup"><span data-stu-id="fcb41-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="fcb41-130">[![Tiempo en caché](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="fcb41-130">[![Cached time](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span></span>

<span data-ttu-id="fcb41-131">**Figura 01**: Almacenar en caché de tiempo ([haga clic aquí para ver imagen en tamaño completo](understanding-action-filters-vb/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="fcb41-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-vb/_static/image3.png))</span></span>

<span data-ttu-id="fcb41-132">En el listado 1, un filtro de acción única: el `OutputCache` filtro de acción: se aplica a la `Index()` método.</span><span class="sxs-lookup"><span data-stu-id="fcb41-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="fcb41-133">Si necesita, puede aplicar varios filtros de acción a la misma acción.</span><span class="sxs-lookup"><span data-stu-id="fcb41-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="fcb41-134">Por ejemplo, desea aplicar tanto la `OutputCache` y `HandleError` los filtros de acción a la misma acción.</span><span class="sxs-lookup"><span data-stu-id="fcb41-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="fcb41-135">En el listado 1, el `OutputCache` filtro de acción se aplica a la `Index()` acción.</span><span class="sxs-lookup"><span data-stu-id="fcb41-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="fcb41-136">También podría aplicar este atributo a la `DataController` propia clase.</span><span class="sxs-lookup"><span data-stu-id="fcb41-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="fcb41-137">En ese caso, el resultado devuelto por cualquier acción expuesta por el controlador almacenaría en caché durante 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="fcb41-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="fcb41-138">Los diferentes tipos de filtros</span><span class="sxs-lookup"><span data-stu-id="fcb41-138">The Different Types of Filters</span></span>

<span data-ttu-id="fcb41-139">El marco de ASP.NET MVC admite cuatro tipos diferentes de filtros:</span><span class="sxs-lookup"><span data-stu-id="fcb41-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="fcb41-140">Filtros de autorización: implementa la `IAuthorizationFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="fcb41-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="fcb41-141">Implementa los filtros de acción el `IActionFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="fcb41-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="fcb41-142">Como resultado de filtros: implementa la `IResultFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="fcb41-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="fcb41-143">Filtros de excepciones: implementa la `IExceptionFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="fcb41-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="fcb41-144">Los filtros se ejecutan en el orden mostrado anteriormente.</span><span class="sxs-lookup"><span data-stu-id="fcb41-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="fcb41-145">Por ejemplo, los filtros de autorización se ejecutan siempre antes de los filtros de acción y los filtros de excepciones siempre se ejecutan después de todos los demás tipos de filtro.</span><span class="sxs-lookup"><span data-stu-id="fcb41-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="fcb41-146">Los filtros de autorización se usan para implementar la autenticación y autorización de acciones del controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="fcb41-147">Por ejemplo, el filtro de autorización es un ejemplo de un filtro de autorización.</span><span class="sxs-lookup"><span data-stu-id="fcb41-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="fcb41-148">Los filtros de acción contienen lógica que se ejecuta antes y después se ejecuta una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="fcb41-149">Puede usar un filtro de acción, por ejemplo, para modificar los datos de vista que devuelve una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="fcb41-150">Filtros de resultados contienen lógica que se ejecuta antes y después de ejecutar un resultado de la vista.</span><span class="sxs-lookup"><span data-stu-id="fcb41-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="fcb41-151">Por ejemplo, desea modificar una vista de resultados justo antes de la vista se presenta en el explorador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="fcb41-152">Los filtros de excepciones son el último tipo de filtro se ejecute.</span><span class="sxs-lookup"><span data-stu-id="fcb41-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="fcb41-153">Puede usar un filtro de excepciones para controlar errores generados por sus acciones de controlador o los resultados de acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="fcb41-154">También puede usar los filtros de excepciones para registrar los errores.</span><span class="sxs-lookup"><span data-stu-id="fcb41-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="fcb41-155">Cada tipo de filtro diferentes se ejecuta en un orden concreto.</span><span class="sxs-lookup"><span data-stu-id="fcb41-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="fcb41-156">Si desea controlar el orden en que se ejecutan los filtros del mismo tipo, a continuación, puede establecer la propiedad de orden de un filtro.</span><span class="sxs-lookup"><span data-stu-id="fcb41-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="fcb41-157">La clase base para todos los filtros de acción es la `System.Web.Mvc.FilterAttribute` clase.</span><span class="sxs-lookup"><span data-stu-id="fcb41-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="fcb41-158">Si desea implementar un tipo determinado de filtro, a continuación, deberá crear una clase que hereda de la clase base del filtro e implementa uno o varios de los IAuthorizationFilter, IActionFilter, IResultFilter o ExceptionFilter interfaces.</span><span class="sxs-lookup"><span data-stu-id="fcb41-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the IAuthorizationFilter, IActionFilter, IResultFilter, or ExceptionFilter interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="fcb41-159">La clase ActionFilterAttribute Base</span><span class="sxs-lookup"><span data-stu-id="fcb41-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="fcb41-160">Para que resulte más fácil de implementar un filtro de acción personalizada, el marco ASP.NET MVC incluye una base `ActionFilterAttribute` clase.</span><span class="sxs-lookup"><span data-stu-id="fcb41-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="fcb41-161">Esta clase implementa tanto la `IActionFilter` y `IResultFilter` e interfaces hereda el `Filter` clase.</span><span class="sxs-lookup"><span data-stu-id="fcb41-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="fcb41-162">La terminología de este artículo no es totalmente coherente.</span><span class="sxs-lookup"><span data-stu-id="fcb41-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="fcb41-163">Técnicamente, una clase que hereda de la clase ActionFilterAttribute es un filtro de acción y un filtro de resultados.</span><span class="sxs-lookup"><span data-stu-id="fcb41-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="fcb41-164">Sin embargo, en el sentido flexible, el filtro de acción de word se usa para referirse a cualquier tipo de filtro en el marco de MVC de ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="fcb41-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="fcb41-165">La clase ActionFilterAttribute base tiene los siguientes métodos que se pueden reemplazar:</span><span class="sxs-lookup"><span data-stu-id="fcb41-165">The base ActionFilterAttribute class has the following methods that you can override:</span></span>

- <span data-ttu-id="fcb41-166">OnActionExecuting: este método se llama antes de ejecuta una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="fcb41-167">OnActionExecuted: este método se llama después de ejecuta una acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="fcb41-168">OnResultExecuting: este método se llama antes de que se ejecute un resultado de acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="fcb41-169">OnResultExecuted: este método se llama después de ejecuta un resultado de acción de controlador.</span><span class="sxs-lookup"><span data-stu-id="fcb41-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="fcb41-170">En la siguiente sección, veremos cómo puede implementar cada uno de estos métodos diferentes.</span><span class="sxs-lookup"><span data-stu-id="fcb41-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="fcb41-171">Crear un filtro de acción de registro</span><span class="sxs-lookup"><span data-stu-id="fcb41-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="fcb41-172">Para ilustrar cómo puede crear un filtro de acción personalizado, vamos a crear un filtro de acción personalizada que registra las fases de procesamiento de una acción de controlador a la ventana de salida de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="fcb41-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="fcb41-173">Nuestro `LogActionFilter` se encuentra en el listado 2.</span><span class="sxs-lookup"><span data-stu-id="fcb41-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="fcb41-174">**Listado 2: `ActionFilters\LogActionFilter.vb`**</span><span class="sxs-lookup"><span data-stu-id="fcb41-174">**Listing 2 – `ActionFilters\LogActionFilter.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample2.vb)]

<span data-ttu-id="fcb41-175">En el listado 2, el `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, y `OnResultExecuted()` todos los métodos llaman a la `Log()` método.</span><span class="sxs-lookup"><span data-stu-id="fcb41-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="fcb41-176">El nombre del método y los datos de ruta actual se pasa a la `Log()` método.</span><span class="sxs-lookup"><span data-stu-id="fcb41-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="fcb41-177">El `Log()` método escribe un mensaje en la ventana de salida de Visual Studio (consulte la figura 2).</span><span class="sxs-lookup"><span data-stu-id="fcb41-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="fcb41-178">[![Escribir en la ventana de salida de Visual Studio](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="fcb41-178">[![Writing to the Visual Studio Output window](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span></span>

<span data-ttu-id="fcb41-179">**Figura 02**: Escribir en la ventana de salida de Visual Studio ([haga clic aquí para ver imagen en tamaño completo](understanding-action-filters-vb/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="fcb41-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-vb/_static/image6.png))</span></span>

<span data-ttu-id="fcb41-180">El controlador Home en el listado 3 muestra cómo puede aplicar el filtro de acción de registro a una clase de controlador completo.</span><span class="sxs-lookup"><span data-stu-id="fcb41-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="fcb41-181">Cada vez que cualquiera de las acciones expuestas por el controlador Home se invocan: ya sea el `Index()` método o la `About()` método – las fases de procesamiento de la acción se registran en la ventana de salida de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="fcb41-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="fcb41-182">**Listado 3: `Controllers\HomeController.vb`**</span><span class="sxs-lookup"><span data-stu-id="fcb41-182">**Listing 3 – `Controllers\HomeController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample3.vb)]

### <a name="summary"></a><span data-ttu-id="fcb41-183">Resumen</span><span class="sxs-lookup"><span data-stu-id="fcb41-183">Summary</span></span>

<span data-ttu-id="fcb41-184">En este tutorial, se presentaron a los filtros de acción de MVC de ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="fcb41-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="fcb41-185">Ha aprendido acerca de los cuatro tipos diferentes de filtros: filtros de autorización, los filtros de acción, filtros de resultados y los filtros de excepciones.</span><span class="sxs-lookup"><span data-stu-id="fcb41-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="fcb41-186">También ha aprendido acerca de la base `ActionFilterAttribute` clase.</span><span class="sxs-lookup"><span data-stu-id="fcb41-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="fcb41-187">Por último, ha aprendido cómo implementar un filtro de acción simple.</span><span class="sxs-lookup"><span data-stu-id="fcb41-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="fcb41-188">Hemos creado un filtro de acción de registro que registra las fases de procesamiento de una acción de controlador a la ventana de salida de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="fcb41-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="fcb41-189">[Anterior](asp-net-mvc-routing-overview-vb.md)
> [Siguiente](improving-performance-with-output-caching-vb.md)</span><span class="sxs-lookup"><span data-stu-id="fcb41-189">[Previous](asp-net-mvc-routing-overview-vb.md)
[Next](improving-performance-with-output-caching-vb.md)</span></span>
