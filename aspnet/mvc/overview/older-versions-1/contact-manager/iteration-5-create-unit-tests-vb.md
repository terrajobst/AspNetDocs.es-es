---
uid: mvc/overview/older-versions-1/contact-manager/iteration-5-create-unit-tests-vb
title: 'Iteración #5: crear pruebas unitarias (VB) | Microsoft Docs'
author: microsoft
description: En la iteración quinta, realizamos nuestra aplicación más fácil de mantener y modificar mediante la adición de las pruebas unitarias. Nos simular nuestras clases de modelo de datos y generar pruebas unitarias para o...
ms.author: riande
ms.date: 02/20/2009
ms.assetid: c6e5c036-2265-4fa7-a9eb-47f197bdc262
msc.legacyurl: /mvc/overview/older-versions-1/contact-manager/iteration-5-create-unit-tests-vb
msc.type: authoredcontent
ms.openlocfilehash: 476e5529e4fd53c6166c53766409268939a83c72
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 04/09/2019
ms.locfileid: "59380670"
---
# <a name="iteration-5--create-unit-tests-vb"></a><span data-ttu-id="d85a6-104">Iteración #5: crear pruebas unitarias (VB)</span><span class="sxs-lookup"><span data-stu-id="d85a6-104">Iteration #5 – Create unit tests (VB)</span></span>

<span data-ttu-id="d85a6-105">por [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="d85a6-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="d85a6-106">Descargue el código</span><span class="sxs-lookup"><span data-stu-id="d85a6-106">Download Code</span></span>](iteration-5-create-unit-tests-vb/_static/contactmanager_5_vb1.zip)

> <span data-ttu-id="d85a6-107">En la iteración quinta, realizamos nuestra aplicación más fácil de mantener y modificar mediante la adición de las pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-107">In the fifth iteration, we make our application easier to maintain and modify by adding unit tests.</span></span> <span data-ttu-id="d85a6-108">Hemos simular nuestras clases de modelo de datos y generar pruebas unitarias para nuestros controladores y la lógica de validación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-108">We mock our data model classes and build unit tests for our controllers and validation logic.</span></span>


## <a name="building-a-contact-management-aspnet-mvc-application-vb"></a><span data-ttu-id="d85a6-109">Creación de una aplicación de ASP.NET MVC (VB) de administración de contactos</span><span class="sxs-lookup"><span data-stu-id="d85a6-109">Building a Contact Management ASP.NET MVC Application (VB)</span></span>

<span data-ttu-id="d85a6-110">En esta serie de tutoriales, creamos una aplicación de administración de contactos completa desde el principio para finalizar.</span><span class="sxs-lookup"><span data-stu-id="d85a6-110">In this series of tutorials, we build an entire Contact Management application from start to finish.</span></span> <span data-ttu-id="d85a6-111">La aplicación de Contact Manager permite almacenar información de contacto - nombres, números de teléfono y direcciones de correo electrónico: para obtener una lista de personas.</span><span class="sxs-lookup"><span data-stu-id="d85a6-111">The Contact Manager application enables you to store contact information - names, phone numbers and email addresses - for a list of people.</span></span>

<span data-ttu-id="d85a6-112">Creamos la aplicación a través de varias iteraciones.</span><span class="sxs-lookup"><span data-stu-id="d85a6-112">We build the application over multiple iterations.</span></span> <span data-ttu-id="d85a6-113">Con cada iteración, mejorar gradualmente la aplicación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-113">With each iteration, we gradually improve the application.</span></span> <span data-ttu-id="d85a6-114">Es el objetivo de este enfoque de varias iteraciones para que pueda entender el motivo de cada cambio.</span><span class="sxs-lookup"><span data-stu-id="d85a6-114">The goal of this multiple iteration approach is to enable you to understand the reason for each change.</span></span>

- <span data-ttu-id="d85a6-115">Iteración #1: crear la aplicación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-115">Iteration #1 - Create the application.</span></span> <span data-ttu-id="d85a6-116">En la primera iteración, se creará el Administrador de contactos de la manera más sencilla posible.</span><span class="sxs-lookup"><span data-stu-id="d85a6-116">In the first iteration, we create the Contact Manager in the simplest way possible.</span></span> <span data-ttu-id="d85a6-117">Se agrega compatibilidad para las operaciones de base de datos básica: Crear, leer, actualizar y eliminar (CRUD).</span><span class="sxs-lookup"><span data-stu-id="d85a6-117">We add support for basic database operations: Create, Read, Update, and Delete (CRUD).</span></span>

- <span data-ttu-id="d85a6-118">Iteración #2: hacer que la aplicación parezca interesante.</span><span class="sxs-lookup"><span data-stu-id="d85a6-118">Iteration #2 - Make the application look nice.</span></span> <span data-ttu-id="d85a6-119">En esta iteración, mejorar la apariencia de la aplicación mediante la modificación de la página principal de la vista de MVC de ASP.NET predeterminada y en cascada de hoja de estilos.</span><span class="sxs-lookup"><span data-stu-id="d85a6-119">In this iteration, we improve the appearance of the application by modifying the default ASP.NET MVC view master page and cascading style sheet.</span></span>

- <span data-ttu-id="d85a6-120">Iteración #3: agregar validación de formulario.</span><span class="sxs-lookup"><span data-stu-id="d85a6-120">Iteration #3 - Add form validation.</span></span> <span data-ttu-id="d85a6-121">En la tercera iteración, se agrega una validación de formulario básico.</span><span class="sxs-lookup"><span data-stu-id="d85a6-121">In the third iteration, we add basic form validation.</span></span> <span data-ttu-id="d85a6-122">Evitamos personas desde el envío de un formulario sin completar los campos obligatorios.</span><span class="sxs-lookup"><span data-stu-id="d85a6-122">We prevent people from submitting a form without completing required form fields.</span></span> <span data-ttu-id="d85a6-123">También hemos validar direcciones de correo electrónico y números de teléfono.</span><span class="sxs-lookup"><span data-stu-id="d85a6-123">We also validate email addresses and phone numbers.</span></span>

- <span data-ttu-id="d85a6-124">Iteración #4: hacer que la aplicación tenga un acoplamiento.</span><span class="sxs-lookup"><span data-stu-id="d85a6-124">Iteration #4 - Make the application loosely coupled.</span></span> <span data-ttu-id="d85a6-125">En este cuarta iteración, aprovechamos de varios patrones de diseño de software para que sea más fácil de mantener y modificar la aplicación de Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="d85a6-125">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="d85a6-126">Por ejemplo, se refactoriza nuestra aplicación para usar el patrón de repositorio y la inserción de dependencias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-126">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

- <span data-ttu-id="d85a6-127">Iteración #5: crear pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-127">Iteration #5 - Create unit tests.</span></span> <span data-ttu-id="d85a6-128">En la iteración quinta, realizamos nuestra aplicación más fácil de mantener y modificar mediante la adición de las pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-128">In the fifth iteration, we make our application easier to maintain and modify by adding unit tests.</span></span> <span data-ttu-id="d85a6-129">Hemos simular nuestras clases de modelo de datos y generar pruebas unitarias para nuestros controladores y la lógica de validación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-129">We mock our data model classes and build unit tests for our controllers and validation logic.</span></span>

- <span data-ttu-id="d85a6-130">Iteración #6: usar el desarrollo controlado por pruebas.</span><span class="sxs-lookup"><span data-stu-id="d85a6-130">Iteration #6 - Use test-driven development.</span></span> <span data-ttu-id="d85a6-131">En esta iteración sexta, se agregan nuevas funciones a nuestra aplicación escribir pruebas unitarias en primer lugar y escribir código en las pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-131">In this sixth iteration, we add new functionality to our application by writing unit tests first and writing code against the unit tests.</span></span> <span data-ttu-id="d85a6-132">En esta iteración, se agrega grupos de contactos.</span><span class="sxs-lookup"><span data-stu-id="d85a6-132">In this iteration, we add contact groups.</span></span>

- <span data-ttu-id="d85a6-133">Iteración #7: agregar funcionalidad Ajax.</span><span class="sxs-lookup"><span data-stu-id="d85a6-133">Iteration #7 - Add Ajax functionality.</span></span> <span data-ttu-id="d85a6-134">En la iteración séptima, mejorar la capacidad de respuesta y el rendimiento de nuestra aplicación agregando compatibilidad con Ajax.</span><span class="sxs-lookup"><span data-stu-id="d85a6-134">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>


## <a name="this-iteration"></a><span data-ttu-id="d85a6-135">Esta iteración</span><span class="sxs-lookup"><span data-stu-id="d85a6-135">This Iteration</span></span>

<span data-ttu-id="d85a6-136">En la iteración anterior de la aplicación de Contact Manager, hemos refactorizado la aplicación para tener más de un acoplamiento flexible.</span><span class="sxs-lookup"><span data-stu-id="d85a6-136">In the previous iteration of the Contact Manager application, we refactored the application to be more loosely coupled.</span></span> <span data-ttu-id="d85a6-137">Se separan la aplicación en un controlador distinto, servicio y capas de repositorio.</span><span class="sxs-lookup"><span data-stu-id="d85a6-137">We separated the application into distinct controller, service, and repository layers.</span></span> <span data-ttu-id="d85a6-138">Cada capa interactúa con la capa debajo de ella a través de interfaces.</span><span class="sxs-lookup"><span data-stu-id="d85a6-138">Each layer interacts with the layer beneath it through interfaces.</span></span>

<span data-ttu-id="d85a6-139">Hemos refactorizado la aplicación para hacer más fácil de mantener y modificar la aplicación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-139">We refactored the application to make the application easier to maintain and modify.</span></span> <span data-ttu-id="d85a6-140">Por ejemplo, si es necesario usar una nueva tecnología de acceso de datos, podemos simplemente cambiamos la capa de repositorio sin tocar el controlador o el nivel de servicio.</span><span class="sxs-lookup"><span data-stu-id="d85a6-140">For example, if we need to use a new data access technology, we can simply change the repository layer without touching the controller or service layer.</span></span> <span data-ttu-id="d85a6-141">Haciendo que el Administrador de contactos de correspondencia imprecisa, se ve facilitado la aplicación sea más resistente a cambiar.</span><span class="sxs-lookup"><span data-stu-id="d85a6-141">By making the Contact Manager loosely coupled, we ve made the application more resilient to change.</span></span>

<span data-ttu-id="d85a6-142">Pero, ¿qué ocurre cuando sea necesario agregar una nueva característica a la aplicación de Contact Manager?</span><span class="sxs-lookup"><span data-stu-id="d85a6-142">But, what happens when we need to add a new feature to the Contact Manager application?</span></span> <span data-ttu-id="d85a6-143">O bien, ¿qué ocurre cuando se corrija un error?</span><span class="sxs-lookup"><span data-stu-id="d85a6-143">Or, what happens when we fix a bug?</span></span> <span data-ttu-id="d85a6-144">Una verdad triste pero bien probada de escribir código es cada vez que toque código que cree el riesgo de introducir nuevos errores.</span><span class="sxs-lookup"><span data-stu-id="d85a6-144">A sad, but well proven, truth of writing code is that whenever you touch code you create the risk of introducing new bugs.</span></span>

<span data-ttu-id="d85a6-145">Por ejemplo, un día bien, el administrador podría pedirle que agregar una nueva característica al administrador de contactos.</span><span class="sxs-lookup"><span data-stu-id="d85a6-145">For example, one fine day, your manager might ask you to add a new feature to the Contact Manager.</span></span> <span data-ttu-id="d85a6-146">Quiere agregar compatibilidad para grupos de contactos.</span><span class="sxs-lookup"><span data-stu-id="d85a6-146">She wants you to add support for Contact Groups.</span></span> <span data-ttu-id="d85a6-147">Quiere permitir que los usuarios organizar sus contactos en grupos como amigos, negocios y así sucesivamente.</span><span class="sxs-lookup"><span data-stu-id="d85a6-147">She wants you to enable users to organize their contacts into groups such as Friends, Business, and so on.</span></span>

<span data-ttu-id="d85a6-148">Para implementar esta nueva característica, deberá modificar los tres niveles de la aplicación de Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="d85a6-148">In order to implement this new feature, you'll need to modify all three layers of the Contact Manager application.</span></span> <span data-ttu-id="d85a6-149">Deberá agregar nueva funcionalidad a los controladores, la capa de servicio y el repositorio.</span><span class="sxs-lookup"><span data-stu-id="d85a6-149">You'll need to add new functionality to the controllers, the service layer, and the repository.</span></span> <span data-ttu-id="d85a6-150">Tan pronto como se empiece a modificar código, corre el riesgo de romper la funcionalidad que antes funcionaba.</span><span class="sxs-lookup"><span data-stu-id="d85a6-150">As soon as you start modifying code, you risk breaking functionality that worked before.</span></span>

<span data-ttu-id="d85a6-151">Refactorización de nuestra aplicación en capas separadas, como hicimos en la iteración anterior, era una buena opción.</span><span class="sxs-lookup"><span data-stu-id="d85a6-151">Refactoring our application into separate layers, as we did in the previous iteration, was a good thing.</span></span> <span data-ttu-id="d85a6-152">Fue una buena opción porque nos permite realizar cambios en capas completas sin tocar el resto de la aplicación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-152">It was a good thing because it enables us to make changes to entire layers without touching the rest of the application.</span></span> <span data-ttu-id="d85a6-153">Sin embargo, si desea hacer más fácil de mantener y modificar el código dentro de una capa, deberá crear pruebas unitarias para el código.</span><span class="sxs-lookup"><span data-stu-id="d85a6-153">However, if you want to make the code within a layer easier to maintain and modify, you need to create unit tests for the code.</span></span>

<span data-ttu-id="d85a6-154">Usa una unidad prueba a una unidad de código individual.</span><span class="sxs-lookup"><span data-stu-id="d85a6-154">You use a unit test to test an individual unit of code.</span></span> <span data-ttu-id="d85a6-155">Estas unidades de código son menores que las capas de aplicación completo.</span><span class="sxs-lookup"><span data-stu-id="d85a6-155">These units of code are smaller than entire application layers.</span></span> <span data-ttu-id="d85a6-156">Por lo general, utilice una prueba unitaria para comprobar si un método concreto en el código se comporta de la manera en que se espera.</span><span class="sxs-lookup"><span data-stu-id="d85a6-156">Typically, you use a unit test to verify whether a particular method in your code behaves in the way that you expect.</span></span> <span data-ttu-id="d85a6-157">Por ejemplo, podría crear una prueba unitaria para el método CreateContact() expuesta la clase ContactManagerService.</span><span class="sxs-lookup"><span data-stu-id="d85a6-157">For example, you would create a unit test for the CreateContact() method exposed by the ContactManagerService class.</span></span>

<span data-ttu-id="d85a6-158">Las pruebas unitarias para un trabajo de la aplicación, como una red de seguridad.</span><span class="sxs-lookup"><span data-stu-id="d85a6-158">The unit tests for an application work just like a safety net.</span></span> <span data-ttu-id="d85a6-159">Cada vez que modifique el código en una aplicación, puede ejecutar un conjunto de pruebas unitarias para comprobar si la modificación interrumpe la funcionalidad existente.</span><span class="sxs-lookup"><span data-stu-id="d85a6-159">Whenever you modify code in an application, you can run a set of unit tests to check whether the modification breaks existing functionality.</span></span> <span data-ttu-id="d85a6-160">Pruebas unitarias que el código sea segura modificar.</span><span class="sxs-lookup"><span data-stu-id="d85a6-160">Unit tests make your code safe to modify.</span></span> <span data-ttu-id="d85a6-161">Pruebas unitarias que todo el código de la aplicación sea más resistente a cambiar.</span><span class="sxs-lookup"><span data-stu-id="d85a6-161">Unit tests make all of the code in your application more resilient to change.</span></span>

<span data-ttu-id="d85a6-162">En esta iteración, agregamos las pruebas unitarias a nuestra aplicación de Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="d85a6-162">In this iteration, we add unit tests to our Contact Manager application.</span></span> <span data-ttu-id="d85a6-163">De este modo, en la siguiente iteración, podemos agregar grupos de contactos a nuestra aplicación sin preocuparse por romper la funcionalidad existente.</span><span class="sxs-lookup"><span data-stu-id="d85a6-163">That way, in the next iteration, we can add Contact Groups to our application without worrying about breaking existing functionality.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="d85a6-164">Hay una variedad de marcos como NUnit, xUnit.net y MbUnit de pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-164">There are a variety of unit testing frameworks including NUnit, xUnit.net, and MbUnit.</span></span> <span data-ttu-id="d85a6-165">En este tutorial, usamos incluido con Visual Studio del marco de pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-165">In this tutorial, we use the unit testing framework included with Visual Studio.</span></span> <span data-ttu-id="d85a6-166">Sin embargo, podría podríamos usar uno de estos marcos de trabajo alternativos.</span><span class="sxs-lookup"><span data-stu-id="d85a6-166">However, you could just as easily use one of these alternative frameworks.</span></span>


## <a name="what-gets-tested"></a><span data-ttu-id="d85a6-167">¿Qué Obtiene probado</span><span class="sxs-lookup"><span data-stu-id="d85a6-167">What Gets Tested</span></span>

<span data-ttu-id="d85a6-168">En el mundo perfecto, todo el código podría estar cubierto por las pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-168">In the perfect world, all of your code would be covered by unit tests.</span></span> <span data-ttu-id="d85a6-169">En el mundo perfecto, tendría la red de seguridad perfecta.</span><span class="sxs-lookup"><span data-stu-id="d85a6-169">In the perfect world, you would have the perfect safety net.</span></span> <span data-ttu-id="d85a6-170">Sería capaz de modificar cualquier línea de código en la aplicación y sepa al instante, mediante la ejecución de las pruebas unitarias, si el cambio interrumpió la funcionalidad existente.</span><span class="sxs-lookup"><span data-stu-id="d85a6-170">You would be able to modify any line of code in your application and know instantly, by executing your unit tests, whether the change broke existing functionality.</span></span>

<span data-ttu-id="d85a6-171">Sin embargo, no queremos t en directo en un mundo perfecto.</span><span class="sxs-lookup"><span data-stu-id="d85a6-171">However, we don t live in a perfect world.</span></span> <span data-ttu-id="d85a6-172">En la práctica, al escribir pruebas unitarias, concentrarse en escribir pruebas para la lógica de negocios (por ejemplo, la lógica de validación).</span><span class="sxs-lookup"><span data-stu-id="d85a6-172">In practice, when writing unit tests, you concentrate on writing tests for your business logic (for example, validation logic).</span></span> <span data-ttu-id="d85a6-173">En concreto, le *no* escribir pruebas unitarias para los datos de acceso lógica o su lógica de la vista.</span><span class="sxs-lookup"><span data-stu-id="d85a6-173">In particular, you *do not* write unit tests for your data access logic or your view logic.</span></span>

<span data-ttu-id="d85a6-174">Para que sea útil, las pruebas unitarias deben ejecutar muy rápidamente.</span><span class="sxs-lookup"><span data-stu-id="d85a6-174">To be useful, unit tests must execute very quickly.</span></span> <span data-ttu-id="d85a6-175">Puede acumular fácilmente cientos o incluso miles de pruebas unitarias para una aplicación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-175">You easily can accumulate hundreds (or even thousands) of unit tests for an application.</span></span> <span data-ttu-id="d85a6-176">Si las pruebas unitarias tardan mucho tiempo en ejecutarse, a continuación, se evitará ejecutarlas.</span><span class="sxs-lookup"><span data-stu-id="d85a6-176">If the unit tests take a long time to run then you'll avoid executing them.</span></span> <span data-ttu-id="d85a6-177">En otras palabras, cuánto tiempo se ejecutan pruebas unitarias son inútiles para fines de codificación de un día para otro.</span><span class="sxs-lookup"><span data-stu-id="d85a6-177">In other words, long running unit tests are useless for day to day coding purposes.</span></span>

<span data-ttu-id="d85a6-178">Por este motivo, normalmente no escribe pruebas unitarias para código que interactúa con una base de datos.</span><span class="sxs-lookup"><span data-stu-id="d85a6-178">For this reason, you typically do not write unit tests for code that interacts with a database.</span></span> <span data-ttu-id="d85a6-179">Ejecutar cientos de pruebas unitarias en una base de datos serían demasiado lento.</span><span class="sxs-lookup"><span data-stu-id="d85a6-179">Running hundreds of unit tests against a live database would be too slow.</span></span> <span data-ttu-id="d85a6-180">En su lugar, simulación de la base de datos y escribir código que interactúa con la base de datos ficticio (analizamos la simulación de una base de datos a continuación).</span><span class="sxs-lookup"><span data-stu-id="d85a6-180">Instead, you mock your database and write code that interacts with the mock database (we discuss mocking a database below).</span></span>

<span data-ttu-id="d85a6-181">Por un motivo similar, normalmente no escribe pruebas unitarias para las vistas.</span><span class="sxs-lookup"><span data-stu-id="d85a6-181">For a similar reason, you typically do not write unit tests for views.</span></span> <span data-ttu-id="d85a6-182">Para probar una vista, debe poner en marcha un servidor web.</span><span class="sxs-lookup"><span data-stu-id="d85a6-182">In order to test a view, you must spin up a web server.</span></span> <span data-ttu-id="d85a6-183">Dado que poner en marcha un servidor web es un proceso relativamente lento, no se recomienda crear pruebas unitarias para las vistas.</span><span class="sxs-lookup"><span data-stu-id="d85a6-183">Because spinning up a web server is a relatively slow process, creating unit tests for your views is not recommended.</span></span>

<span data-ttu-id="d85a6-184">Si la vista contiene una lógica complicada, a continuación, puede mover la lógica a los métodos auxiliares.</span><span class="sxs-lookup"><span data-stu-id="d85a6-184">If your view contains complicated logic then you should consider moving the logic into Helper methods.</span></span> <span data-ttu-id="d85a6-185">Puede escribir pruebas unitarias para métodos auxiliares que se ejecutan sin poner en marcha un servidor web.</span><span class="sxs-lookup"><span data-stu-id="d85a6-185">You can write unit tests for Helper methods that execute without spinning up a web server.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="d85a6-186">Mientras prueba escribir la lógica de acceso a datos o lógica de la vista no es una buena idea al escribir pruebas unitarias, estas pruebas pueden ser muy valiosas al construcción funcional o la integración de las pruebas.</span><span class="sxs-lookup"><span data-stu-id="d85a6-186">While writing tests for data access logic or view logic is not a good idea when writing unit tests, these tests can be very valuable when building functional or integration tests.</span></span>


> [!NOTE] 
> 
> <span data-ttu-id="d85a6-187">ASP.NET MVC es el motor de vistas de formularios Web.</span><span class="sxs-lookup"><span data-stu-id="d85a6-187">ASP.NET MVC is the Web Forms View Engine.</span></span> <span data-ttu-id="d85a6-188">Mientras el motor de vistas de formularios Web depende de un servidor web, no pueden ser otros motores de vista.</span><span class="sxs-lookup"><span data-stu-id="d85a6-188">While the Web Forms View Engine is dependent on a web server, other view engines might not be.</span></span>


## <a name="using-a-mock-object-framework"></a><span data-ttu-id="d85a6-189">Uso de un marco de objeto ficticio</span><span class="sxs-lookup"><span data-stu-id="d85a6-189">Using a Mock Object Framework</span></span>

<span data-ttu-id="d85a6-190">Al compilar las pruebas unitarias, casi siempre necesita aprovechar las ventajas de un marco de simulación de objeto.</span><span class="sxs-lookup"><span data-stu-id="d85a6-190">When building unit tests, you almost always need to take advantage of a Mock Object framework.</span></span> <span data-ttu-id="d85a6-191">Un marco de simulación de objeto le permite crear simulacros y auxiliares de las clases en la aplicación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-191">A Mock Object framework enables you to create mocks and stubs for the classes in your application.</span></span>

<span data-ttu-id="d85a6-192">Por ejemplo, puede usar un marco de simulación de objeto para generar una versión ficticia de la clase de repositorio.</span><span class="sxs-lookup"><span data-stu-id="d85a6-192">For example, you can use a Mock Object framework to generate a mock version of your repository class.</span></span> <span data-ttu-id="d85a6-193">De este modo, puede usar la clase de repositorio ficticio en lugar de la clase de repositorio reales en las pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-193">That way, you can use the mock repository class instead of the real repository class in your unit tests.</span></span> <span data-ttu-id="d85a6-194">El repositorio ficticio permite evitar la ejecución de código de la base de datos al ejecutar una prueba unitaria.</span><span class="sxs-lookup"><span data-stu-id="d85a6-194">Using the mock repository enables you to avoid executing database code when executing a unit test.</span></span>

<span data-ttu-id="d85a6-195">Visual Studio no incluye un marco de simulación de objeto.</span><span class="sxs-lookup"><span data-stu-id="d85a6-195">Visual Studio does not include a Mock Object framework.</span></span> <span data-ttu-id="d85a6-196">Sin embargo, hay varios marcos de simulación de objeto comercial y de código abierto disponibles para .NET framework:</span><span class="sxs-lookup"><span data-stu-id="d85a6-196">However, there are several commercial and open source Mock Object frameworks available for the .NET framework:</span></span>

1. <span data-ttu-id="d85a6-197">Moq - este marco de trabajo está disponible bajo la licencia BSD de código abierto.</span><span class="sxs-lookup"><span data-stu-id="d85a6-197">Moq - This framework is available under the open source BSD license.</span></span> <span data-ttu-id="d85a6-198">Puede descargar Moq desde [ https://code.google.com/p/moq/ ](https://code.google.com/p/moq/).</span><span class="sxs-lookup"><span data-stu-id="d85a6-198">You can download Moq from [https://code.google.com/p/moq/](https://code.google.com/p/moq/).</span></span>
2. <span data-ttu-id="d85a6-199">Rhino Mocks, este marco de trabajo está disponible bajo la licencia BSD de código abierto.</span><span class="sxs-lookup"><span data-stu-id="d85a6-199">Rhino Mocks - This framework is available under the open source BSD license.</span></span> <span data-ttu-id="d85a6-200">Puede descargar Rhino Mocks desde [ http://ayende.com/projects/rhino-mocks.aspx ](http://ayende.com/projects/rhino-mocks.aspx).</span><span class="sxs-lookup"><span data-stu-id="d85a6-200">You can download Rhino Mocks from [http://ayende.com/projects/rhino-mocks.aspx](http://ayende.com/projects/rhino-mocks.aspx).</span></span>
3. <span data-ttu-id="d85a6-201">Typemock Isolator - se trata de un entorno comercial.</span><span class="sxs-lookup"><span data-stu-id="d85a6-201">Typemock Isolator - This is a commercial framework.</span></span> <span data-ttu-id="d85a6-202">Puede descargar una versión de prueba de [ http://www.typemock.com/ ](http://www.typemock.com/).</span><span class="sxs-lookup"><span data-stu-id="d85a6-202">You can download a trial version from [http://www.typemock.com/](http://www.typemock.com/).</span></span>

<span data-ttu-id="d85a6-203">En este tutorial, decidí usar Moq.</span><span class="sxs-lookup"><span data-stu-id="d85a6-203">In this tutorial, I decided to use Moq.</span></span> <span data-ttu-id="d85a6-204">Sin embargo, podría usar fácilmente Rhino Mocks o Typemock Isolator para crear el simulacro de objetos para la aplicación de Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="d85a6-204">However, you could just as easily use Rhino Mocks or Typemock Isolator to create the Mock objects for the Contact Manager application.</span></span>

<span data-ttu-id="d85a6-205">Para poder usar Moq, es preciso completar los pasos siguientes:</span><span class="sxs-lookup"><span data-stu-id="d85a6-205">Before you can use Moq, you need to complete the following steps:</span></span>

1. <span data-ttu-id="d85a6-206">.</span><span class="sxs-lookup"><span data-stu-id="d85a6-206">.</span></span>
2. <span data-ttu-id="d85a6-207">Antes de descomprimir la descarga, asegúrese de que haga clic en el archivo y haga clic en el botón rotulado **desbloquear** (consulte la figura 1).</span><span class="sxs-lookup"><span data-stu-id="d85a6-207">Before you unzip the download, make sure that you right-click the file and click the button labeled **Unblock** (see Figure 1).</span></span>
3. <span data-ttu-id="d85a6-208">Descomprima la descarga.</span><span class="sxs-lookup"><span data-stu-id="d85a6-208">Unzip the download.</span></span>
4. <span data-ttu-id="d85a6-209">Agregue una referencia al ensamblado Moq al proyecto de prueba seleccionando la opción de menú **proyecto, agregar referencia** para abrir el **Agregar referencia** cuadro de diálogo.</span><span class="sxs-lookup"><span data-stu-id="d85a6-209">Add a reference to the Moq assembly to your Test project by selecting the menu option **Project, Add Reference** to open the **Add Reference** dialog.</span></span> <span data-ttu-id="d85a6-210">En la pestaña Examinar, vaya a la carpeta donde descomprimió Moq y seleccione el ensamblado Moq.dll.</span><span class="sxs-lookup"><span data-stu-id="d85a6-210">Under the Browse tab, browse to the folder where you unzipped Moq and select the Moq.dll assembly.</span></span> <span data-ttu-id="d85a6-211">Haga clic en el **Aceptar** botón (consulte la figura 2).</span><span class="sxs-lookup"><span data-stu-id="d85a6-211">Click the **OK** button (see Figure 2).</span></span>


[![U<span data-ttu-id="d85a6-212">nblocking Moq]</span><span class="sxs-lookup"><span data-stu-id="d85a6-212">nblocking Moq]</span></span>(iteration-5-create-unit-tests-vb/_static/image1.jpg)](iteration-5-create-unit-tests-vb/_static/image1.png)

<span data-ttu-id="d85a6-213">**Figura 01**: Moq desbloqueo ([haga clic aquí para ver imagen en tamaño completo](iteration-5-create-unit-tests-vb/_static/image2.png))</span><span class="sxs-lookup"><span data-stu-id="d85a6-213">**Figure 01**: Unblocking Moq([Click to view full-size image](iteration-5-create-unit-tests-vb/_static/image2.png))</span></span>


[![R<span data-ttu-id="d85a6-214">referencias después de agregar Moq]</span><span class="sxs-lookup"><span data-stu-id="d85a6-214">eferences after adding Moq]</span></span>(iteration-5-create-unit-tests-vb/_static/image2.jpg)](iteration-5-create-unit-tests-vb/_static/image3.png)

<span data-ttu-id="d85a6-215">**Figura 02**: Referencias después de agregar Moq ([haga clic aquí para ver imagen en tamaño completo](iteration-5-create-unit-tests-vb/_static/image4.png))</span><span class="sxs-lookup"><span data-stu-id="d85a6-215">**Figure 02**: References after adding Moq([Click to view full-size image](iteration-5-create-unit-tests-vb/_static/image4.png))</span></span>


## <a name="creating-unit-tests-for-the-service-layer"></a><span data-ttu-id="d85a6-216">Crear pruebas unitarias para el nivel de servicio</span><span class="sxs-lookup"><span data-stu-id="d85a6-216">Creating Unit Tests for the Service Layer</span></span>

<span data-ttu-id="d85a6-217">Permiten s empiece por crear un conjunto de pruebas unitarias para nuestro nivel de servicio de Contact Manager aplicación s.</span><span class="sxs-lookup"><span data-stu-id="d85a6-217">Let s start by creating a set of unit tests for our Contact Manager application s service layer.</span></span> <span data-ttu-id="d85a6-218">Vamos a usar estas pruebas para comprobar nuestra lógica de validación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-218">We'll use these tests to verify our validation logic.</span></span>

<span data-ttu-id="d85a6-219">Cree una nueva carpeta denominada Models en el proyecto ContactManager.Tests.</span><span class="sxs-lookup"><span data-stu-id="d85a6-219">Create a new folder named Models in the ContactManager.Tests project.</span></span> <span data-ttu-id="d85a6-220">A continuación, haga clic en la carpeta modelos y seleccione **Add, nueva prueba**.</span><span class="sxs-lookup"><span data-stu-id="d85a6-220">Next, right-click the Models folder and select **Add, New Test**.</span></span> <span data-ttu-id="d85a6-221">El **agregar nueva prueba** aparece el cuadro de diálogo se muestra en la figura 3.</span><span class="sxs-lookup"><span data-stu-id="d85a6-221">The **Add New Test** dialog shown in Figure 3 appears.</span></span> <span data-ttu-id="d85a6-222">Seleccione el **de prueba unitaria** plantilla y el nombre de la nueva prueba ContactManagerServiceTest.vb.</span><span class="sxs-lookup"><span data-stu-id="d85a6-222">Select the **Unit Test** template and name your new test ContactManagerServiceTest.vb.</span></span> <span data-ttu-id="d85a6-223">Haga clic en el **Aceptar** para agregar la nueva prueba a su proyecto de prueba.</span><span class="sxs-lookup"><span data-stu-id="d85a6-223">Click the **OK** button to add your new test to your Test Project.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="d85a6-224">En general, conviene que la estructura de carpetas del proyecto de prueba para que coincida con la estructura de carpetas del proyecto de ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="d85a6-224">In general, you want the folder structure of your Test Project to match the folder structure of your ASP.NET MVC project.</span></span> <span data-ttu-id="d85a6-225">Por ejemplo, coloque las pruebas del controlador en una carpeta de controladores, las pruebas de modelo en una carpeta Models y así sucesivamente.</span><span class="sxs-lookup"><span data-stu-id="d85a6-225">For example, you place controller tests in a Controllers folder, model tests in a Models folder, and so on.</span></span>


[![M<span data-ttu-id="d85a6-226">odels\ContactManagerServiceTest.cs]</span><span class="sxs-lookup"><span data-stu-id="d85a6-226">odels\ContactManagerServiceTest.cs]</span></span>(iteration-5-create-unit-tests-vb/_static/image3.jpg)](iteration-5-create-unit-tests-vb/_static/image5.png)

<span data-ttu-id="d85a6-227">**Figura 03**: Models\ContactManagerServiceTest.cs ([haga clic aquí para ver imagen en tamaño completo](iteration-5-create-unit-tests-vb/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="d85a6-227">**Figure 03**: Models\ContactManagerServiceTest.cs([Click to view full-size image](iteration-5-create-unit-tests-vb/_static/image6.png))</span></span>


<span data-ttu-id="d85a6-228">Inicialmente, va a probar el método CreateContact() expone la clase ContactManagerService.</span><span class="sxs-lookup"><span data-stu-id="d85a6-228">Initially, we want to test the CreateContact() method exposed by the ContactManagerService class.</span></span> <span data-ttu-id="d85a6-229">Vamos a crear las cinco pruebas siguientes:</span><span class="sxs-lookup"><span data-stu-id="d85a6-229">We'll create the following five tests:</span></span>

- <span data-ttu-id="d85a6-230">CreateContact() - pruebas que CreateContact() devuelve el valor true cuando un contacto válido se pasa al método.</span><span class="sxs-lookup"><span data-stu-id="d85a6-230">CreateContact() - Tests that CreateContact() returns the value true when a valid Contact is passed to the method.</span></span>
- <span data-ttu-id="d85a6-231">CreateContactRequiredFirstName() - pruebas que se agrega un mensaje de error al estado del modelo cuando un contacto con un nombre que faltan se pasa al método CreateContact().</span><span class="sxs-lookup"><span data-stu-id="d85a6-231">CreateContactRequiredFirstName() - Tests that an error message is added to model state when a Contact with a missing first name is passed to the CreateContact() method.</span></span>
- <span data-ttu-id="d85a6-232">CreateContactRequiredLastName() - pruebas que se agrega un mensaje de error al estado del modelo cuando un contacto con un apellido que faltan se pasa al método CreateContact().</span><span class="sxs-lookup"><span data-stu-id="d85a6-232">CreateContactRequiredLastName() - Tests that an error message is added to model state when a Contact with a missing last name is passed to the CreateContact() method.</span></span>
- <span data-ttu-id="d85a6-233">CreateContactInvalidPhone() - pruebas que se agrega un mensaje de error al estado del modelo cuando un contacto con un número de teléfono no válido se pasa al método CreateContact().</span><span class="sxs-lookup"><span data-stu-id="d85a6-233">CreateContactInvalidPhone() - Tests that an error message is added to model state when a Contact with an invalid phone number is passed to the CreateContact() method.</span></span>
- <span data-ttu-id="d85a6-234">CreateContactInvalidEmail() - pruebas que se agrega un mensaje de error al estado del modelo cuando un contacto con una dirección de correo electrónico no válido se pasa al método CreateContact()...</span><span class="sxs-lookup"><span data-stu-id="d85a6-234">CreateContactInvalidEmail() - Tests that an error message is added to model state when a Contact with an invalid email address is passed to the CreateContact() method..</span></span>

<span data-ttu-id="d85a6-235">La primera prueba comprueba que un contacto válido no genera un error de validación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-235">The first test verifies that a valid Contact does not generate a validation error.</span></span> <span data-ttu-id="d85a6-236">Las pruebas restantes comprueban cada una de las reglas de validación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-236">The remaining tests check each of the validation rules.</span></span>

<span data-ttu-id="d85a6-237">El código para estas pruebas se encuentra en el listado 1.</span><span class="sxs-lookup"><span data-stu-id="d85a6-237">The code for these tests is contained in Listing 1.</span></span>

**<span data-ttu-id="d85a6-238">Listado 1 - Models\ContactManagerServiceTest.vb</span><span class="sxs-lookup"><span data-stu-id="d85a6-238">Listing 1 - Models\ContactManagerServiceTest.vb</span></span>**

[!code-vb[Main](iteration-5-create-unit-tests-vb/samples/sample1.vb)]


<span data-ttu-id="d85a6-239">Dado que se usa la clase Contact en el listado 1, tenemos que agregar una referencia a Microsoft Entity Framework para nuestro proyecto de prueba.</span><span class="sxs-lookup"><span data-stu-id="d85a6-239">Because we use the Contact class in Listing 1, we need to add a reference to the Microsoft Entity Framework to our Test project.</span></span> <span data-ttu-id="d85a6-240">Agregue una referencia al ensamblado System.Data.Entity.</span><span class="sxs-lookup"><span data-stu-id="d85a6-240">Add a reference to the System.Data.Entity assembly.</span></span>


<span data-ttu-id="d85a6-241">Listado 1 contiene un método denominado Initialize() decorada con el atributo [TestInitialize].</span><span class="sxs-lookup"><span data-stu-id="d85a6-241">Listing 1 contains a method named Initialize() that is decorated with the [TestInitialize] attribute.</span></span> <span data-ttu-id="d85a6-242">Este método es llamado automáticamente antes de que se ejecuta cada una de las pruebas unitarias (se llama justo antes de cada una de las pruebas unitarias 5 veces).</span><span class="sxs-lookup"><span data-stu-id="d85a6-242">This method is called automatically before each of the unit tests is run (it is called 5 times right before each of the unit tests).</span></span> <span data-ttu-id="d85a6-243">El método Initialize() crea un repositorio ficticio con la siguiente línea de código:</span><span class="sxs-lookup"><span data-stu-id="d85a6-243">The Initialize() method creates a mock repository with the following line of code:</span></span>

[!code-vb[Main](iteration-5-create-unit-tests-vb/samples/sample2.vb)]

<span data-ttu-id="d85a6-244">Esta línea de código usa el marco de trabajo Moq para generar un repositorio simulado desde la interfaz IContactManagerRepository.</span><span class="sxs-lookup"><span data-stu-id="d85a6-244">This line of code uses the Moq framework to generate a mock repository from the IContactManagerRepository interface.</span></span> <span data-ttu-id="d85a6-245">Se usa el repositorio ficticio en lugar de la EntityContactManagerRepository real para evitar el acceso a la base de datos cuando se ejecuta cada prueba unitaria.</span><span class="sxs-lookup"><span data-stu-id="d85a6-245">The mock repository is used instead of the actual EntityContactManagerRepository to avoid accessing the database when each unit test is run.</span></span> <span data-ttu-id="d85a6-246">El repositorio ficticio implementa los métodos de la interfaz IContactManagerRepository, pero los métodos don t hace realmente nada.</span><span class="sxs-lookup"><span data-stu-id="d85a6-246">The mock repository implements the methods of the IContactManagerRepository interface, but the methods don t actually do anything.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="d85a6-247">Cuando se usa el marco de trabajo Moq, hay una diferencia entre \_mockRepository y \_mockRepository.Object.</span><span class="sxs-lookup"><span data-stu-id="d85a6-247">When using the Moq framework, there is a distinction between \_mockRepository and \_mockRepository.Object.</span></span> <span data-ttu-id="d85a6-248">El primero se refiere a la clase de simulacro (de IContactManagerRepository) que contiene métodos para especificar cómo se comportará el repositorio ficticio.</span><span class="sxs-lookup"><span data-stu-id="d85a6-248">The former refers to the Mock(Of IContactManagerRepository) class that contains methods for specifying how the mock repository will behave.</span></span> <span data-ttu-id="d85a6-249">El segundo se refiere al repositorio ficticio real que implementa la interfaz IContactManagerRepository.</span><span class="sxs-lookup"><span data-stu-id="d85a6-249">The latter refers to the actual mock repository that implements the IContactManagerRepository interface.</span></span>


<span data-ttu-id="d85a6-250">Al crear una instancia de la clase ContactManagerService, se usa el repositorio ficticio en el método Initialize().</span><span class="sxs-lookup"><span data-stu-id="d85a6-250">The mock repository is used in the Initialize() method when creating an instance of the ContactManagerService class.</span></span> <span data-ttu-id="d85a6-251">Todas las pruebas unitarias individuales utilizan esta instancia de la clase ContactManagerService.</span><span class="sxs-lookup"><span data-stu-id="d85a6-251">All of the individual unit tests use this instance of the ContactManagerService class.</span></span>

<span data-ttu-id="d85a6-252">Listado 1 contiene cinco métodos que corresponden a cada una de las pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-252">Listing 1 contains five methods that correspond to each of the unit tests.</span></span> <span data-ttu-id="d85a6-253">Cada uno de estos métodos se decora con el atributo [TestMethod].</span><span class="sxs-lookup"><span data-stu-id="d85a6-253">Each of these methods is decorated with the [TestMethod] attribute.</span></span> <span data-ttu-id="d85a6-254">Al ejecutar las pruebas unitarias, se llama a cualquier método que tenga este atributo.</span><span class="sxs-lookup"><span data-stu-id="d85a6-254">When you run the unit tests, any method that has this attribute is called.</span></span> <span data-ttu-id="d85a6-255">En otras palabras, cualquier método esté decorado con el atributo [TestMethod] es una prueba unitaria.</span><span class="sxs-lookup"><span data-stu-id="d85a6-255">In other words, any method that is decorated with the [TestMethod] attribute is a unit test.</span></span>

<span data-ttu-id="d85a6-256">La primera prueba unitaria, denominada CreateContact(), comprueba que una llamada a CreateContact() devuelve el valor true cuando una instancia válida de la clase Contact se pasa al método.</span><span class="sxs-lookup"><span data-stu-id="d85a6-256">The first unit test, named CreateContact(), verifies that calling CreateContact() returns the value true when a valid instance of the Contact class is passed to the method.</span></span> <span data-ttu-id="d85a6-257">La prueba crea una instancia de la clase Contact, llama al método CreateContact() y comprueba que CreateContact() devuelve el valor true.</span><span class="sxs-lookup"><span data-stu-id="d85a6-257">The test creates an instance of the Contact class, calls the CreateContact() method, and verifies that CreateContact() returns the value true.</span></span>

<span data-ttu-id="d85a6-258">Las pruebas restantes comprueban que cuando se llama al método CreateContact() con un contacto no válido, a continuación, el método devuelve false y se agrega el mensaje de error de validación esperado al estado del modelo.</span><span class="sxs-lookup"><span data-stu-id="d85a6-258">The remaining tests verify that when the CreateContact() method is called with an invalid Contact then the method returns false and the expected validation error message is added to model state.</span></span> <span data-ttu-id="d85a6-259">Por ejemplo, la prueba CreateContactRequiredFirstName() crea una instancia de la clase Contact con una cadena vacía para su propiedad FirstName.</span><span class="sxs-lookup"><span data-stu-id="d85a6-259">For example, the CreateContactRequiredFirstName() test creates an instance of the Contact class with an empty string for its FirstName property.</span></span> <span data-ttu-id="d85a6-260">A continuación, se llama al método de CreateContact() con el contacto no válido.</span><span class="sxs-lookup"><span data-stu-id="d85a6-260">Next, the CreateContact() method is called with the invalid Contact.</span></span> <span data-ttu-id="d85a6-261">Por último, la prueba comprueba que CreateContact() devuelve false y el estado del modelo contiene el mensaje de error de validación esperado "First name is required."</span><span class="sxs-lookup"><span data-stu-id="d85a6-261">Finally, the test verifies that CreateContact() returns false and that model state contains the expected validation error message "First name is required."</span></span>

<span data-ttu-id="d85a6-262">Puede ejecutar las pruebas unitarias en el listado 1 seleccionando la opción de menú **pruebas, ejecutar, todas las pruebas de la solución (CTRL + R, A)**.</span><span class="sxs-lookup"><span data-stu-id="d85a6-262">You can run the unit tests in Listing 1 by selecting the menu option **Test, Run, All Tests in Solution (CTRL+R, A)**.</span></span> <span data-ttu-id="d85a6-263">Los resultados de las pruebas se muestran en la ventana Resultados de pruebas (consulte la figura 4).</span><span class="sxs-lookup"><span data-stu-id="d85a6-263">The results of the tests are displayed in the Test Results window (see Figure 4).</span></span>


[![T<span data-ttu-id="d85a6-264">est resultados]</span><span class="sxs-lookup"><span data-stu-id="d85a6-264">est Results]</span></span>(iteration-5-create-unit-tests-vb/_static/image4.jpg)](iteration-5-create-unit-tests-vb/_static/image7.png)

<span data-ttu-id="d85a6-265">**Figura 04**: Los resultados de pruebas ([haga clic aquí para ver imagen en tamaño completo](iteration-5-create-unit-tests-vb/_static/image8.png))</span><span class="sxs-lookup"><span data-stu-id="d85a6-265">**Figure 04**: Test Results ([Click to view full-size image](iteration-5-create-unit-tests-vb/_static/image8.png))</span></span>


## <a name="creating-unit-tests-for-controllers"></a><span data-ttu-id="d85a6-266">Crear pruebas unitarias para los controladores</span><span class="sxs-lookup"><span data-stu-id="d85a6-266">Creating Unit Tests for Controllers</span></span>

<span data-ttu-id="d85a6-267">Aplicación de ASP.NET MVC controlan el flujo de interacción del usuario.</span><span class="sxs-lookup"><span data-stu-id="d85a6-267">ASP.NET MVC application control the flow of user interaction.</span></span> <span data-ttu-id="d85a6-268">Cuando un controlador de pruebas que desea comprobar si el controlador devuelve el resultado y ver datos de la acción adecuada.</span><span class="sxs-lookup"><span data-stu-id="d85a6-268">When testing a controller, you want to test whether the controller returns the right action result and view data.</span></span> <span data-ttu-id="d85a6-269">También es posible que desee comprobar si un controlador interactúa con las clases de modelo de la manera prevista.</span><span class="sxs-lookup"><span data-stu-id="d85a6-269">You also might want to test whether a controller interacts with model classes in the manner expected.</span></span>

<span data-ttu-id="d85a6-270">Por ejemplo, el listado 2 contiene dos pruebas unitarias para el método Create() del controlador de contacto.</span><span class="sxs-lookup"><span data-stu-id="d85a6-270">For example, Listing 2 contains two unit tests for the Contact controller Create() method.</span></span> <span data-ttu-id="d85a6-271">La primera prueba unitaria comprueba si un contacto válido se pasa al método Create(), a continuación, el método Create() redirige a la acción del índice.</span><span class="sxs-lookup"><span data-stu-id="d85a6-271">The first unit test verifies that when a valid Contact is passed to the Create() method then the Create() method redirects to the Index action.</span></span> <span data-ttu-id="d85a6-272">En otras palabras, cuando se pasa un contacto válido, el método Create() debe devolver un RedirectToRouteResult que representa la acción del índice.</span><span class="sxs-lookup"><span data-stu-id="d85a6-272">In other words, when passed a valid Contact, the Create() method should return a RedirectToRouteResult that represents the Index action.</span></span>

<span data-ttu-id="d85a6-273">No queremos t desea probar la capa de servicio ContactManager cuando estamos probando el nivel de controlador.</span><span class="sxs-lookup"><span data-stu-id="d85a6-273">We don t want to test the ContactManager service layer when we are testing the controller layer.</span></span> <span data-ttu-id="d85a6-274">Por lo tanto, nos simular la capa de servicio con el código siguiente en el método Initialize:</span><span class="sxs-lookup"><span data-stu-id="d85a6-274">Therefore, we mock the service layer with the following code in the Initialize method:</span></span>

[!code-vb[Main](iteration-5-create-unit-tests-vb/samples/sample3.vb)]

<span data-ttu-id="d85a6-275">En la prueba unitaria CreateValidContact(), nos simular el comportamiento de la llamada a la capa de servicio CreateContact() método con la siguiente línea de código:</span><span class="sxs-lookup"><span data-stu-id="d85a6-275">In the CreateValidContact() unit test, we mock the behavior of calling the service layer CreateContact() method with the following line of code:</span></span>

[!code-vb[Main](iteration-5-create-unit-tests-vb/samples/sample4.vb)]

<span data-ttu-id="d85a6-276">Esta línea de código hace que el servicio ContactManager ficticio devolver el valor true cuando se llama a su método CreateContact().</span><span class="sxs-lookup"><span data-stu-id="d85a6-276">This line of code causes the mock ContactManager service to return the value true when its CreateContact() method is called.</span></span> <span data-ttu-id="d85a6-277">Por el nivel de servicio de simulación, podemos probar el comportamiento de nuestro controlador sin necesidad de ejecutar cualquier código en el nivel de servicio.</span><span class="sxs-lookup"><span data-stu-id="d85a6-277">By mocking the service layer, we can test the behavior of our controller without needing to execute any code in the service layer.</span></span>

<span data-ttu-id="d85a6-278">La segunda prueba unitaria comprueba que la acción Create() devuelve la vista de creación cuando un contacto no válido se pasa al método.</span><span class="sxs-lookup"><span data-stu-id="d85a6-278">The second unit test verifies that the Create() action returns the Create view when an invalid contact is passed to the method.</span></span> <span data-ttu-id="d85a6-279">Hacemos que el método CreateContact() para devolver el valor false con la siguiente línea de código de nivel de servicio:</span><span class="sxs-lookup"><span data-stu-id="d85a6-279">We cause the service layer CreateContact() method to return the value false with the following line of code:</span></span>

[!code-vb[Main](iteration-5-create-unit-tests-vb/samples/sample5.vb)]

<span data-ttu-id="d85a6-280">Si el método Create() se comporta tal como esperábamos, a continuación, debe devolver la vista de creación cuando el nivel de servicio devuelve el valor false.</span><span class="sxs-lookup"><span data-stu-id="d85a6-280">If the Create() method behaves as we expect then it should return the Create view when the service layer returns the value false.</span></span> <span data-ttu-id="d85a6-281">De este modo, el controlador puede mostrar los mensajes de error de validación en la vista de creación y el usuario tiene la oportunidad de corregir ese propiedades del contacto no válido.</span><span class="sxs-lookup"><span data-stu-id="d85a6-281">That way, the controller can display the validation error messages in the Create view and the user has a chance to correct that invalid Contact properties.</span></span>


<span data-ttu-id="d85a6-282">Si va a crear pruebas unitarias para los controladores debe devolver los nombres de vista explícita de sus acciones de controlador.</span><span class="sxs-lookup"><span data-stu-id="d85a6-282">If you plan to build unit tests for your controllers then you need to return explicit view names from your controller actions.</span></span> <span data-ttu-id="d85a6-283">Por ejemplo, no devuelven una vista similar al siguiente:</span><span class="sxs-lookup"><span data-stu-id="d85a6-283">For example, do not return a view like this:</span></span>

<span data-ttu-id="d85a6-284">Devolver View()</span><span class="sxs-lookup"><span data-stu-id="d85a6-284">Return View()</span></span>

<span data-ttu-id="d85a6-285">En su lugar, devuelve la vista similar al siguiente:</span><span class="sxs-lookup"><span data-stu-id="d85a6-285">Instead, return the view like this:</span></span>

<span data-ttu-id="d85a6-286">Devolver View("Create")</span><span class="sxs-lookup"><span data-stu-id="d85a6-286">Return View("Create")</span></span>

<span data-ttu-id="d85a6-287">Si no es explícito al devolver una vista de la propiedad ViewResult.ViewName devuelve una cadena vacía.</span><span class="sxs-lookup"><span data-stu-id="d85a6-287">If you are not explicit when returning a view then the ViewResult.ViewName property returns an empty string.</span></span>


**<span data-ttu-id="d85a6-288">Listado 2 - Controllers\ContactControllerTest.vb</span><span class="sxs-lookup"><span data-stu-id="d85a6-288">Listing 2 - Controllers\ContactControllerTest.vb</span></span>**

[!code-vb[Main](iteration-5-create-unit-tests-vb/samples/sample6.vb)]

## <a name="summary"></a><span data-ttu-id="d85a6-289">Resumen</span><span class="sxs-lookup"><span data-stu-id="d85a6-289">Summary</span></span>

<span data-ttu-id="d85a6-290">En esta iteración, hemos creado las pruebas unitarias para nuestra aplicación de Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="d85a6-290">In this iteration, we created unit tests for our Contact Manager application.</span></span> <span data-ttu-id="d85a6-291">Podemos ejecutar estas pruebas unitarias en cualquier momento para comprobar que nuestra aplicación todavía se comporta de la manera en que se espera.</span><span class="sxs-lookup"><span data-stu-id="d85a6-291">We can run these unit tests at any time to verify that our application still behaves in the manner that we expect.</span></span> <span data-ttu-id="d85a6-292">Las pruebas unitarias actúan como una red de seguridad para nuestra aplicación lo que nos permite modificar de forma segura nuestra aplicación en el futuro.</span><span class="sxs-lookup"><span data-stu-id="d85a6-292">The unit tests act as a safety net for our application enabling us to safely modify our application in the future.</span></span>

<span data-ttu-id="d85a6-293">Hemos creado dos conjuntos de pruebas unitarias.</span><span class="sxs-lookup"><span data-stu-id="d85a6-293">We created two sets of unit tests.</span></span> <span data-ttu-id="d85a6-294">En primer lugar, hemos probado nuestra lógica de validación mediante la creación de pruebas unitarias para nuestro nivel de servicio.</span><span class="sxs-lookup"><span data-stu-id="d85a6-294">First, we tested our validation logic by creating unit tests for our service layer.</span></span> <span data-ttu-id="d85a6-295">A continuación, hemos probado nuestra lógica de flujo de control mediante la creación de pruebas unitarias para nuestro nivel de controlador.</span><span class="sxs-lookup"><span data-stu-id="d85a6-295">Next, we tested our flow control logic by creating unit tests for our controller layer.</span></span> <span data-ttu-id="d85a6-296">Al probar nuestro nivel de servicio, nuestras pruebas nuestro nivel de servicio se habían aislada de la capa de repositorio mediante la simulación de la capa de repositorio.</span><span class="sxs-lookup"><span data-stu-id="d85a6-296">When testing our service layer, we isolated our tests for our service layer from our repository layer by mocking our repository layer.</span></span> <span data-ttu-id="d85a6-297">Al probar el nivel de controlador, hemos aislado nuestras pruebas para nuestro nivel de controlador mediante la capa de servicio de simulación.</span><span class="sxs-lookup"><span data-stu-id="d85a6-297">When testing the controller layer, we isolated our tests for our controller layer by mocking the service layer.</span></span>

<span data-ttu-id="d85a6-298">En la siguiente iteración, se modificará la aplicación de Contact Manager para que admita los grupos de contactos.</span><span class="sxs-lookup"><span data-stu-id="d85a6-298">In the next iteration, we modify the Contact Manager application so that it supports Contact Groups.</span></span> <span data-ttu-id="d85a6-299">Vamos a agregar esta nueva funcionalidad a la aplicación mediante un proceso de diseño de software denominado desarrollo controlado por pruebas.</span><span class="sxs-lookup"><span data-stu-id="d85a6-299">We'll add this new functionality to our application using a software design process called test-driven development.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="d85a6-300">[Anterior](iteration-4-make-the-application-loosely-coupled-vb.md)
> [Siguiente](iteration-6-use-test-driven-development-vb.md)</span><span class="sxs-lookup"><span data-stu-id="d85a6-300">[Previous](iteration-4-make-the-application-loosely-coupled-vb.md)
[Next](iteration-6-use-test-driven-development-vb.md)</span></span>
