---
uid: signalr/overview/security/introduction-to-security
title: Introducción a la seguridad de SignalR | Microsoft Docs
author: bradygaster
description: Describe los problemas de seguridad que debe tener en cuenta al desarrollar una aplicación de SignalR.
ms.author: bradyg
ms.date: 06/10/2014
ms.assetid: ed562717-8591-4936-8e10-c7e63dcb570a
msc.legacyurl: /signalr/overview/security/introduction-to-security
msc.type: authoredcontent
ms.openlocfilehash: 311bfb4279b1c919fddcef2c0aed657083f9c34f
ms.sourcegitcommit: 24b1f6decbb17bb22a45166e5fdb0845c65af498
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 03/01/2019
ms.locfileid: "57034062"
---
<a name="introduction-to-signalr-security"></a><span data-ttu-id="6acba-103">Introducción a la seguridad de SignalR</span><span class="sxs-lookup"><span data-stu-id="6acba-103">Introduction to SignalR Security</span></span>
====================
<span data-ttu-id="6acba-104">por [Patrick Fletcher](https://github.com/pfletcher), [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="6acba-104">by [Patrick Fletcher](https://github.com/pfletcher), [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

[!INCLUDE [Consider ASP.NET Core SignalR](~/includes/signalr/signalr-version-disambiguation.md)]

> <span data-ttu-id="6acba-105">En este artículo se describe los problemas de seguridad que debe tener en cuenta al desarrollar una aplicación de SignalR.</span><span class="sxs-lookup"><span data-stu-id="6acba-105">This article describes the security issues you must consider when developing a SignalR application.</span></span>
>
> ## <a name="software-versions-used-in-this-topic"></a><span data-ttu-id="6acba-106">Versiones de software que se usa en este tema</span><span class="sxs-lookup"><span data-stu-id="6acba-106">Software versions used in this topic</span></span>
>
>
> - [<span data-ttu-id="6acba-107">Visual Studio 2013</span><span class="sxs-lookup"><span data-stu-id="6acba-107">Visual Studio 2013</span></span>](https://my.visualstudio.com/Downloads?q=visual%20studio%202013)
> - <span data-ttu-id="6acba-108">.NET 4.5</span><span class="sxs-lookup"><span data-stu-id="6acba-108">.NET 4.5</span></span>
> - <span data-ttu-id="6acba-109">Versión 2 de SignalR</span><span class="sxs-lookup"><span data-stu-id="6acba-109">SignalR version 2</span></span>
>
>
>
> ## <a name="previous-versions-of-this-topic"></a><span data-ttu-id="6acba-110">Versiones anteriores de este tema.</span><span class="sxs-lookup"><span data-stu-id="6acba-110">Previous versions of this topic</span></span>
>
> <span data-ttu-id="6acba-111">Para obtener información acerca de las versiones anteriores de SignalR, consulte [versiones anteriores de SignalR](../older-versions/index.md).</span><span class="sxs-lookup"><span data-stu-id="6acba-111">For information about earlier versions of SignalR, see [SignalR Older Versions](../older-versions/index.md).</span></span>
>
> ## <a name="questions-and-comments"></a><span data-ttu-id="6acba-112">Preguntas y comentarios</span><span class="sxs-lookup"><span data-stu-id="6acba-112">Questions and comments</span></span>
>
> <span data-ttu-id="6acba-113">Deje comentarios sobre cómo le gustó de este tutorial y que podíamos mejorar en los comentarios en la parte inferior de la página.</span><span class="sxs-lookup"><span data-stu-id="6acba-113">Please leave feedback on how you liked this tutorial and what we could improve in the comments at the bottom of the page.</span></span> <span data-ttu-id="6acba-114">Si tiene preguntas que no están directamente relacionados con el tutorial, puede publicarlos en el [foro de ASP.NET SignalR](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) o [StackOverflow.com](http://stackoverflow.com/).</span><span class="sxs-lookup"><span data-stu-id="6acba-114">If you have questions that are not directly related to the tutorial, you can post them to the [ASP.NET SignalR forum](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) or [StackOverflow.com](http://stackoverflow.com/).</span></span>


## <a name="overview"></a><span data-ttu-id="6acba-115">Información general</span><span class="sxs-lookup"><span data-stu-id="6acba-115">Overview</span></span>

<span data-ttu-id="6acba-116">Este documento contiene las siguientes secciones:</span><span class="sxs-lookup"><span data-stu-id="6acba-116">This document contains the following sections:</span></span>

- [<span data-ttu-id="6acba-117">Conceptos de seguridad de SignalR</span><span class="sxs-lookup"><span data-stu-id="6acba-117">SignalR Security Concepts</span></span>](#concepts)

    - [<span data-ttu-id="6acba-118">Autenticación y autorización</span><span class="sxs-lookup"><span data-stu-id="6acba-118">Authentication and authorization</span></span>](#authentication)
    - [<span data-ttu-id="6acba-119">Token de conexión</span><span class="sxs-lookup"><span data-stu-id="6acba-119">Connection token</span></span>](#connectiontoken)
    - [<span data-ttu-id="6acba-120">Unir a grupos al volver a conectarse</span><span class="sxs-lookup"><span data-stu-id="6acba-120">Rejoining groups when reconnecting</span></span>](#rejoingroup)
- [<span data-ttu-id="6acba-121">¿Cómo evita la falsificación de solicitud entre sitios SignalR</span><span class="sxs-lookup"><span data-stu-id="6acba-121">How SignalR prevents Cross-Site Request Forgery</span></span>](#csrf)
- [<span data-ttu-id="6acba-122">Recomendaciones de seguridad de SignalR</span><span class="sxs-lookup"><span data-stu-id="6acba-122">SignalR Security Recommendations</span></span>](#recommendations)

    - [<span data-ttu-id="6acba-123">Protocolo seguro de las capas de Sockets (SSL)</span><span class="sxs-lookup"><span data-stu-id="6acba-123">Secure Socket Layers (SSL) protocol</span></span>](#ssl)
    - [<span data-ttu-id="6acba-124">No use grupos como un mecanismo de seguridad</span><span class="sxs-lookup"><span data-stu-id="6acba-124">Do not use groups as a security mechanism</span></span>](#groupsecurity)
    - [<span data-ttu-id="6acba-125">Controlar entrada desde clientes de forma segura</span><span class="sxs-lookup"><span data-stu-id="6acba-125">Safely handling input from clients</span></span>](#input)
    - [<span data-ttu-id="6acba-126">Reconciliar un cambio en el estado de usuario con una conexión activa</span><span class="sxs-lookup"><span data-stu-id="6acba-126">Reconciling a change in user status with an active connection</span></span>](#reconcile)
    - [<span data-ttu-id="6acba-127">Archivos del proxy de JavaScript generados automáticamente</span><span class="sxs-lookup"><span data-stu-id="6acba-127">Automatically generated JavaScript proxy files</span></span>](#autogen)
    - [<span data-ttu-id="6acba-128">Excepciones</span><span class="sxs-lookup"><span data-stu-id="6acba-128">Exceptions</span></span>](#exceptions)

<a id="concepts"></a>

## <a name="signalr-security-concepts"></a><span data-ttu-id="6acba-129">Conceptos de seguridad de SignalR</span><span class="sxs-lookup"><span data-stu-id="6acba-129">SignalR Security Concepts</span></span>

<a id="authentication"></a>

### <a name="authentication-and-authorization"></a><span data-ttu-id="6acba-130">Autenticación y autorización</span><span class="sxs-lookup"><span data-stu-id="6acba-130">Authentication and authorization</span></span>

<span data-ttu-id="6acba-131">SignalR no proporciona características para autenticar a los usuarios.</span><span class="sxs-lookup"><span data-stu-id="6acba-131">SignalR does not provide any features for authenticating users.</span></span> <span data-ttu-id="6acba-132">En su lugar, integrar las características de SignalR en la estructura de autenticación existente de una aplicación.</span><span class="sxs-lookup"><span data-stu-id="6acba-132">Instead, you integrate the SignalR features into the existing authentication structure for an application.</span></span> <span data-ttu-id="6acba-133">Autenticar a los usuarios como haría normalmente en la aplicación y trabajar con los resultados de la autenticación en su SignalR el código.</span><span class="sxs-lookup"><span data-stu-id="6acba-133">You authenticate users as you would normally in your application, and work with the results of the authentication in your SignalR code.</span></span> <span data-ttu-id="6acba-134">Por ejemplo, puede autenticar a los usuarios con autenticación de formularios ASP.NET y, a continuación, en su centro, exigir que los usuarios o roles están autorizados para llamar a un método.</span><span class="sxs-lookup"><span data-stu-id="6acba-134">For example, you might authenticate your users with ASP.NET forms authentication, and then in your hub, enforce which users or roles are authorized to call a method.</span></span> <span data-ttu-id="6acba-135">En su centro, también se puede pasar información de autenticación, como nombre de usuario o si un usuario pertenece a un rol, al cliente.</span><span class="sxs-lookup"><span data-stu-id="6acba-135">In your hub, you can also pass authentication information, such as user name or whether a user belongs to a role, to the client.</span></span>

<span data-ttu-id="6acba-136">SignalR proporciona el [Authorize](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) atributo para especificar qué usuarios tienen acceso a un concentrador o un método.</span><span class="sxs-lookup"><span data-stu-id="6acba-136">SignalR provides the [Authorize](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) attribute to specify which users have access to a hub or method.</span></span> <span data-ttu-id="6acba-137">Aplicar el atributo Authorize a un concentrador o determinados métodos en un concentrador.</span><span class="sxs-lookup"><span data-stu-id="6acba-137">You apply the Authorize attribute to either a hub or particular methods in a hub.</span></span> <span data-ttu-id="6acba-138">Sin el atributo Authorize, todos los métodos públicos en el centro están disponibles para un cliente que está conectado al concentrador.</span><span class="sxs-lookup"><span data-stu-id="6acba-138">Without the Authorize attribute, all public methods on the hub are available to a client that is connected to the hub.</span></span> <span data-ttu-id="6acba-139">Para obtener más información sobre los concentradores, vea [autenticación y autorización para los concentradores de SignalR](hub-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="6acba-139">For more information about hubs, see [Authentication and Authorization for SignalR Hubs](hub-authorization.md).</span></span>

<span data-ttu-id="6acba-140">Aplica el `Authorize` atributo concentradores, pero las conexiones no persistentes.</span><span class="sxs-lookup"><span data-stu-id="6acba-140">You apply the `Authorize` attribute to hubs, but not persistent connections.</span></span> <span data-ttu-id="6acba-141">Para aplicar las reglas de autorización cuando se usa un `PersistentConnection` debe invalidar el `AuthorizeRequest` método.</span><span class="sxs-lookup"><span data-stu-id="6acba-141">To enforce authorization rules when using a `PersistentConnection` you must override the `AuthorizeRequest` method.</span></span> <span data-ttu-id="6acba-142">Para obtener más información acerca de las conexiones persistentes, consulte [autenticación y autorización para las conexiones persistentes de SignalR](persistent-connection-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="6acba-142">For more information about persistent connections, see [Authentication and Authorization for SignalR Persistent Connections](persistent-connection-authorization.md).</span></span>

<a id="connectiontoken"></a>

### <a name="connection-token"></a><span data-ttu-id="6acba-143">Token de conexión</span><span class="sxs-lookup"><span data-stu-id="6acba-143">Connection token</span></span>

<span data-ttu-id="6acba-144">SignalR reduce el riesgo de ejecutar comandos malintencionados al validar la identidad del remitente.</span><span class="sxs-lookup"><span data-stu-id="6acba-144">SignalR mitigates the risk of executing malicious commands by validating the identity of the sender.</span></span> <span data-ttu-id="6acba-145">Para cada solicitud, el cliente y servidor pasan un token de conexión que contiene el identificador de conexión y el nombre de usuario para los usuarios autenticados.</span><span class="sxs-lookup"><span data-stu-id="6acba-145">For each request, the client and server pass a connection token which contains the connection id and username for authenticated users.</span></span> <span data-ttu-id="6acba-146">El identificador de conexión identifica de forma única cada cliente conectado.</span><span class="sxs-lookup"><span data-stu-id="6acba-146">The connection id uniquely identifies each connected client.</span></span> <span data-ttu-id="6acba-147">El servidor genera aleatoriamente el identificador de conexión cuando se crea una nueva conexión y conserva ese identificador para la duración de la conexión.</span><span class="sxs-lookup"><span data-stu-id="6acba-147">The server randomly generates the connection id when a new connection is created, and persists that id for the duration of the connection.</span></span> <span data-ttu-id="6acba-148">El mecanismo de autenticación para la aplicación web proporciona el nombre de usuario.</span><span class="sxs-lookup"><span data-stu-id="6acba-148">The authentication mechanism for the web application provides the username.</span></span> <span data-ttu-id="6acba-149">SignalR usa una firma digital y cifrado para proteger el token de conexión.</span><span class="sxs-lookup"><span data-stu-id="6acba-149">SignalR uses encryption and a digital signature to protect the connection token.</span></span>

![](introduction-to-security/_static/image2.png)

<span data-ttu-id="6acba-150">Para cada solicitud, el servidor valida el contenido del token para asegurarse de que la solicitud procede del usuario especificado.</span><span class="sxs-lookup"><span data-stu-id="6acba-150">For each request, the server validates the contents of the token to ensure that the request is coming from the specified user.</span></span> <span data-ttu-id="6acba-151">El nombre de usuario debe corresponder con el identificador de conexión. Al validar el identificador de conexión y el nombre de usuario, SignalR impide que un usuario malintencionado fácilmente suplantación de otro usuario.</span><span class="sxs-lookup"><span data-stu-id="6acba-151">The username must correspond to the connection id. By validating both the connection id and the username, SignalR prevents a malicious user from easily impersonating another user.</span></span> <span data-ttu-id="6acba-152">Si el servidor no puede validar el token de conexión, se produce un error en la solicitud.</span><span class="sxs-lookup"><span data-stu-id="6acba-152">If the server cannot validate the connection token, the request fails.</span></span>

![](introduction-to-security/_static/image4.png)

<span data-ttu-id="6acba-153">Dado que el identificador de conexión es parte del proceso de comprobación, no debe revelar el identificador de conexión de un usuario a otros usuarios o almacenar el valor en el cliente, como en una cookie.</span><span class="sxs-lookup"><span data-stu-id="6acba-153">Because the connection id is part of the verification process, you should not reveal one user's connection id to other users or store the value on the client, such as in a cookie.</span></span>

#### <a name="connection-tokens-vs-other-token-types"></a><span data-ttu-id="6acba-154">Tokens de conexión frente a otros tipos de token</span><span class="sxs-lookup"><span data-stu-id="6acba-154">Connection tokens vs. other token types</span></span>

<span data-ttu-id="6acba-155">Los tokens de la conexión se marcan ocasionalmente mediante herramientas de seguridad porque parecen ser tokens de sesión o tokens de autenticación, lo que supone un riesgo si expone.</span><span class="sxs-lookup"><span data-stu-id="6acba-155">Connection tokens are occasionally flagged by security tools because they appear to be session tokens or authentication tokens, which poses a risk if exposed.</span></span>

<span data-ttu-id="6acba-156">Token de conexión de SignalR no es un token de autenticación.</span><span class="sxs-lookup"><span data-stu-id="6acba-156">SignalR's connection token isn't an authentication token.</span></span> <span data-ttu-id="6acba-157">Sirve para confirmar que el usuario que realiza esta solicitud es el mismo que ha creado la conexión.</span><span class="sxs-lookup"><span data-stu-id="6acba-157">It is used to confirm that the user making this request is the same one that created the connection.</span></span> <span data-ttu-id="6acba-158">El token de conexión es necesario porque SignalR de ASP.NET permite las conexiones mover entre servidores.</span><span class="sxs-lookup"><span data-stu-id="6acba-158">The connection token is necessary because ASP.NET SignalR allows connections to move between servers.</span></span> <span data-ttu-id="6acba-159">El token asocia la conexión a un usuario determinado, pero no confirman la identidad del usuario que realiza la solicitud.</span><span class="sxs-lookup"><span data-stu-id="6acba-159">The token associates the connection with a particular user but doesn't assert the identity of the user making the request.</span></span> <span data-ttu-id="6acba-160">Para que una solicitud SignalR autenticarse correctamente, debe tener algún otro símbolo (token) que valida la identidad del usuario, como una cookie o token de portador.</span><span class="sxs-lookup"><span data-stu-id="6acba-160">For a SignalR request to be properly authenticated, it must have some other token that asserts the identity of the user, such as a cookie or bearer token.</span></span> <span data-ttu-id="6acba-161">Sin embargo, la conexión de token Sí hace ninguna notificación de que la solicitud se realizó por parte de ese usuario, solo que el identificador de conexión contenido en el token está asociada a ese usuario.</span><span class="sxs-lookup"><span data-stu-id="6acba-161">However, the connection token itself makes no claim that the request was made by that user, only that the connection ID contained within the token is associated with that user.</span></span>

<span data-ttu-id="6acba-162">Puesto que el token de conexión proporciona ninguna solicitud de autenticación propios, no se considera una "sesión" o "autenticación" token.</span><span class="sxs-lookup"><span data-stu-id="6acba-162">Since the connection token provides no authentication claim of its own, it isn't considered a "session" or "authentication" token.</span></span> <span data-ttu-id="6acba-163">Tomar la conexión de un usuario determinado token y reproducirlo en una solicitud se autentica como un usuario diferente (o una solicitud no autenticada) se producirá un error, porque no coincide con la identidad del usuario de la solicitud y la identidad almacenada en el token.</span><span class="sxs-lookup"><span data-stu-id="6acba-163">Taking a given user's connection token and replaying it in a request authenticated as a different user (or an unauthenticated request) will fail, because the user identity of the request and the identity stored in the token won't match.</span></span>

<a id="rejoingroup"></a>

### <a name="rejoining-groups-when-reconnecting"></a><span data-ttu-id="6acba-164">Unir a grupos al volver a conectarse</span><span class="sxs-lookup"><span data-stu-id="6acba-164">Rejoining groups when reconnecting</span></span>

<span data-ttu-id="6acba-165">De forma predeterminada, la aplicación de SignalR volver a asignará automáticamente un usuario a los grupos adecuados cuando vuelva a conectar desde una interrupción temporal, por ejemplo, cuando una conexión se quita y vuelve a establecer antes de la conexión agota el tiempo. Al volver a conectarse, el cliente pasa un token de grupo que incluye el identificador de conexión y los grupos asignados.</span><span class="sxs-lookup"><span data-stu-id="6acba-165">By default, the SignalR application will automatically re-assign a user to the appropriate groups when reconnecting from a temporary disruption, such as when a connection is dropped and re-established before the connection times out. When reconnecting, the client passes a group token that includes the connection id and the assigned groups.</span></span> <span data-ttu-id="6acba-166">El token de grupo esté firmado y cifrado.</span><span class="sxs-lookup"><span data-stu-id="6acba-166">The group token is digitally signed and encrypted.</span></span> <span data-ttu-id="6acba-167">El cliente conserva el mismo identificador de conexión después de una reconexión; por lo tanto, el identificador de conexión que se pasa desde el cliente ha vuelto a conectar debe coincidir con el identificador de conexión anterior utilizado por el cliente.</span><span class="sxs-lookup"><span data-stu-id="6acba-167">The client retains the same connection id after a reconnection; therefore, the connection id passed from the reconnected client must match the previous connection id used by the client.</span></span> <span data-ttu-id="6acba-168">Esta comprobación impide que un usuario malintencionado pasar solicitudes para unirse a grupos no autorizados al conectarse de nuevo.</span><span class="sxs-lookup"><span data-stu-id="6acba-168">This verification prevents a malicious user from passing requests to join unauthorized groups when reconnecting.</span></span>

<span data-ttu-id="6acba-169">Sin embargo, es importante tener en cuenta que no expira el token de grupo.</span><span class="sxs-lookup"><span data-stu-id="6acba-169">However, it's important to note, that the group token does not expire.</span></span> <span data-ttu-id="6acba-170">Si un usuario pertenecía a un grupo en el pasado, pero se prohibió de ese grupo, ese usuario puede imitar un token de grupo que incluye el grupo prohibido.</span><span class="sxs-lookup"><span data-stu-id="6acba-170">If a user belonged to a group in the past, but was banned from that group, that user may be able to mimic a group token that includes the prohibited group.</span></span> <span data-ttu-id="6acba-171">Si necesita administrar los usuarios que pertenecen a qué grupos de forma segura, deberá almacenar esos datos en el servidor, como en una base de datos.</span><span class="sxs-lookup"><span data-stu-id="6acba-171">If you need to securely manage which users belong to which groups, you need to store that data on the server, such as in a database.</span></span> <span data-ttu-id="6acba-172">A continuación, agregar lógica a la aplicación que se comprueba en el servidor si un usuario pertenece a un grupo.</span><span class="sxs-lookup"><span data-stu-id="6acba-172">Then, add logic to your application that verifies on the server whether a user belongs to a group.</span></span> <span data-ttu-id="6acba-173">Para obtener un ejemplo de la comprobación de pertenencia a grupos, consulte [trabajar con grupos](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="6acba-173">For an example of verifying group membership, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<span data-ttu-id="6acba-174">Unir automáticamente a los grupos solo se aplica cuando se vuelve a conectar una conexión después de una interrupción temporal.</span><span class="sxs-lookup"><span data-stu-id="6acba-174">Automatically rejoining groups only applies when a connection is reconnected after a temporary disruption.</span></span> <span data-ttu-id="6acba-175">Si un usuario se desconecta por salir de la aplicación o se reinicie la aplicación, la aplicación debe controlar cómo agregar ese usuario a los grupos correctos.</span><span class="sxs-lookup"><span data-stu-id="6acba-175">If a user disconnects by navigating away from the application or the application restarts, your application must handle how to add that user to the correct groups.</span></span> <span data-ttu-id="6acba-176">Para obtener más información, consulte [trabajar con grupos](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="6acba-176">For more information, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="csrf"></a>

## <a name="how-signalr-prevents-cross-site-request-forgery"></a><span data-ttu-id="6acba-177">¿Cómo evita la falsificación de solicitud entre sitios SignalR</span><span class="sxs-lookup"><span data-stu-id="6acba-177">How SignalR prevents Cross-Site Request Forgery</span></span>

<span data-ttu-id="6acba-178">Falsificación de solicitud entre sitios (CSRF) es un ataque en un sitio malintencionado envía una solicitud a un sitio vulnerable donde el usuario ha iniciado en.</span><span class="sxs-lookup"><span data-stu-id="6acba-178">Cross-Site Request Forgery (CSRF) is an attack where a malicious site sends a request to a vulnerable site where the user is currently logged in.</span></span> <span data-ttu-id="6acba-179">SignalR evita CSRF realizando muy poco probable de un sitio malintencionado para crear una solicitud válida para la aplicación de SignalR.</span><span class="sxs-lookup"><span data-stu-id="6acba-179">SignalR prevents CSRF by making it extremely unlikely for a malicious site to create a valid request for your SignalR application.</span></span>

### <a name="description-of-csrf-attack"></a><span data-ttu-id="6acba-180">Descripción de ataque CSRF</span><span class="sxs-lookup"><span data-stu-id="6acba-180">Description of CSRF attack</span></span>

<span data-ttu-id="6acba-181">Este es un ejemplo de un ataque CSRF:</span><span class="sxs-lookup"><span data-stu-id="6acba-181">Here is an example of a CSRF attack:</span></span>

1. <span data-ttu-id="6acba-182">Un usuario inicia sesión en www.example.com, utilizando la autenticación de formularios.</span><span class="sxs-lookup"><span data-stu-id="6acba-182">A user logs into www.example.com, using forms authentication.</span></span>
2. <span data-ttu-id="6acba-183">El servidor autentica al usuario.</span><span class="sxs-lookup"><span data-stu-id="6acba-183">The server authenticates the user.</span></span> <span data-ttu-id="6acba-184">La respuesta del servidor incluye una cookie de autenticación.</span><span class="sxs-lookup"><span data-stu-id="6acba-184">The response from the server includes an authentication cookie.</span></span>
3. <span data-ttu-id="6acba-185">Sin cerrar la sesión, el usuario visita un sitio web malicioso.</span><span class="sxs-lookup"><span data-stu-id="6acba-185">Without logging out, the user visits a malicious web site.</span></span> <span data-ttu-id="6acba-186">Este sitio malintencionado contiene el siguiente formulario HTML:</span><span class="sxs-lookup"><span data-stu-id="6acba-186">This malicious site contains the following HTML form:</span></span>

    [!code-html[Main](introduction-to-security/samples/sample1.html)]

   <span data-ttu-id="6acba-187">Tenga en cuenta que la acción de formulario se publica en el sitio vulnerable, no en el sitio malintencionado.</span><span class="sxs-lookup"><span data-stu-id="6acba-187">Notice that the form action posts to the vulnerable site, not to the malicious site.</span></span> <span data-ttu-id="6acba-188">Esta es la parte "cross-site" de CSRF.</span><span class="sxs-lookup"><span data-stu-id="6acba-188">This is the "cross-site" part of CSRF.</span></span>
4. <span data-ttu-id="6acba-189">El usuario hace clic con el botón Enviar.</span><span class="sxs-lookup"><span data-stu-id="6acba-189">The user clicks the submit button.</span></span> <span data-ttu-id="6acba-190">El explorador incluye la cookie de autenticación con la solicitud.</span><span class="sxs-lookup"><span data-stu-id="6acba-190">The browser includes the authentication cookie with the request.</span></span>
5. <span data-ttu-id="6acba-191">La solicitud se ejecuta en el servidor de ejemplo.com con contexto de autenticación del usuario y puede hacer cualquier cosa que puede hacer un usuario autenticado.</span><span class="sxs-lookup"><span data-stu-id="6acba-191">The request runs on the example.com server with the user's authentication context, and can do anything that an authenticated user is allowed to do.</span></span>

<span data-ttu-id="6acba-192">Aunque este ejemplo requiere que el usuario haga clic en el botón del formulario, la página malintencionada podría tal como ejecutar fácilmente una secuencia de comandos que envía una solicitud AJAX a la aplicación de SignalR.</span><span class="sxs-lookup"><span data-stu-id="6acba-192">Although this example requires the user to click the form button, the malicious page could just as easily run a script that sends an AJAX request to your SignalR application.</span></span> <span data-ttu-id="6acba-193">Además, con SSL no impide que un ataque CSRF, dado que el sitio Web malintencionado puede enviar una solicitud de "https://".</span><span class="sxs-lookup"><span data-stu-id="6acba-193">Moreover, using SSL does not prevent a CSRF attack, because the malicious site can send an "https://" request.</span></span>

<span data-ttu-id="6acba-194">Normalmente, los ataques CSRF son posibles con sitios web que usan cookies para la autenticación, porque los exploradores envían todas las cookies relevantes al sitio web de destino.</span><span class="sxs-lookup"><span data-stu-id="6acba-194">Typically, CSRF attacks are possible against web sites that use cookies for authentication, because browsers send all relevant cookies to the destination web site.</span></span> <span data-ttu-id="6acba-195">Sin embargo, los ataques CSRF no están limitados a aprovechar las cookies.</span><span class="sxs-lookup"><span data-stu-id="6acba-195">However, CSRF attacks are not limited to exploiting cookies.</span></span> <span data-ttu-id="6acba-196">Por ejemplo, la autenticación básica e implícita también son vulnerables.</span><span class="sxs-lookup"><span data-stu-id="6acba-196">For example, Basic and Digest authentication are also vulnerable.</span></span> <span data-ttu-id="6acba-197">Después de que un usuario inicia sesión con la autenticación básica o implícita, el explorador envía automáticamente las credenciales hasta que finaliza la sesión.</span><span class="sxs-lookup"><span data-stu-id="6acba-197">After a user logs in with Basic or Digest authentication, the browser automatically sends the credentials until the session ends.</span></span>

### <a name="csrf-mitigations-taken-by-signalr"></a><span data-ttu-id="6acba-198">Mitigaciones de CSRF realizadas por SignalR</span><span class="sxs-lookup"><span data-stu-id="6acba-198">CSRF mitigations taken by SignalR</span></span>

<span data-ttu-id="6acba-199">SignalR realiza los pasos siguientes para evitar que un sitio malintencionado creen solicitudes válidas para la aplicación.</span><span class="sxs-lookup"><span data-stu-id="6acba-199">SignalR takes the following steps to prevent a malicious site from creating valid requests to your application.</span></span> <span data-ttu-id="6acba-200">SignalR realiza estos pasos de forma predeterminada, no es necesario realizar ninguna acción en el código.</span><span class="sxs-lookup"><span data-stu-id="6acba-200">SignalR takes these steps by default, you do not need to take any action in your code.</span></span>

- <span data-ttu-id="6acba-201">**Deshabilitar las solicitudes entre dominios** SignalR deshabilita las solicitudes entre dominios para evitar que los usuarios de la llamada a un punto de conexión SignalR desde un dominio externo.</span><span class="sxs-lookup"><span data-stu-id="6acba-201">**Disable cross domain requests** SignalR disables cross domain requests to prevent users from calling a SignalR endpoint from an external domain.</span></span> <span data-ttu-id="6acba-202">SignalR se considera que cualquier solicitud de un dominio externo no es válido y bloquea la solicitud.</span><span class="sxs-lookup"><span data-stu-id="6acba-202">SignalR considers any request from an external domain to be invalid and blocks the request.</span></span> <span data-ttu-id="6acba-203">Se recomienda que mantenga este comportamiento predeterminado; en caso contrario, un sitio malintencionado podría engañar a los usuarios enviar comandos a su sitio.</span><span class="sxs-lookup"><span data-stu-id="6acba-203">We recommend that you keep this default behavior; otherwise, a malicious site could trick users into sending commands to your site.</span></span> <span data-ttu-id="6acba-204">Si necesita usar solicitudes entre dominios, consulte [cómo establecer una conexión entre dominios](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) .</span><span class="sxs-lookup"><span data-stu-id="6acba-204">If you need to use cross domain requests, see     [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) .</span></span>
- <span data-ttu-id="6acba-205">**Pasar el token de conexión en la cadena de consulta, cookie no** SignalR pasa el token de conexión como un valor de cadena de consulta, en lugar de como una cookie.</span><span class="sxs-lookup"><span data-stu-id="6acba-205">**Pass connection token in query string, not cookie** SignalR passes the connection token as a query string value, instead of as a cookie.</span></span> <span data-ttu-id="6acba-206">Dado que el explorador puede reenviar accidentalmente el token de conexión cuando se encuentra el código malintencionado no es seguro almacenar el token de conexión en una cookie.</span><span class="sxs-lookup"><span data-stu-id="6acba-206">Storing the connection token in a cookie is unsafe because the browser can inadvertently forward the connection token when malicious code is encountered.</span></span> <span data-ttu-id="6acba-207">Además, pasando el token de conexión en la cadena de consulta impide que el token de conexión almacenar más allá de la conexión actual.</span><span class="sxs-lookup"><span data-stu-id="6acba-207">Also, passing the connection token in the query string prevents the connection token from persisting beyond the current connection.</span></span> <span data-ttu-id="6acba-208">Por lo tanto, un usuario malintencionado no puede realizar una solicitud con credenciales de autenticación de otro usuario.</span><span class="sxs-lookup"><span data-stu-id="6acba-208">Therefore, a malicious user cannot make a request under another user's authentication credentials.</span></span>
- <span data-ttu-id="6acba-209">**Comprobar conexión token** como se describe en el [token de conexión](#connectiontoken) sección, el servidor sabe qué identificador de conexión está asociado a cada usuario autenticado.</span><span class="sxs-lookup"><span data-stu-id="6acba-209">**Verify connection token** As described in the     [Connection token](#connectiontoken) section, the server knows which connection id is associated with each authenticated user.</span></span> <span data-ttu-id="6acba-210">El servidor no procesa cualquier solicitud de un identificador de conexión que no coincide con el nombre de usuario.</span><span class="sxs-lookup"><span data-stu-id="6acba-210">The server does not process any request from a connection id that does not match the user name.</span></span> <span data-ttu-id="6acba-211">No es probable que un usuario malintencionado pueda adivinar una solicitud válida porque el usuario malintencionado tendría que conocer el nombre de usuario y el identificador de conexión generada aleatoriamente actual. Id. de esa conexión deja de ser válido tan pronto como finaliza la conexión.</span><span class="sxs-lookup"><span data-stu-id="6acba-211">It is unlikely a malicious user could guess a valid request because the malicious user would have to know the user name and the current randomly-generated connection id. That connection id becomes invalid as soon as the connection is ended.</span></span> <span data-ttu-id="6acba-212">Los usuarios anónimos no deben tener acceso a información confidencial.</span><span class="sxs-lookup"><span data-stu-id="6acba-212">Anonymous users should not have access to any sensitive information.</span></span>

<a id="recommendations"></a>

## <a name="signalr-security-recommendations"></a><span data-ttu-id="6acba-213">Recomendaciones de seguridad de SignalR</span><span class="sxs-lookup"><span data-stu-id="6acba-213">SignalR Security Recommendations</span></span>

<a id="ssl"></a>

### <a name="secure-socket-layers-ssl-protocol"></a><span data-ttu-id="6acba-214">Protocolo seguro de las capas de Sockets (SSL)</span><span class="sxs-lookup"><span data-stu-id="6acba-214">Secure Socket Layers (SSL) protocol</span></span>

<span data-ttu-id="6acba-215">El protocolo SSL usa cifrado para proteger el transporte de datos entre un cliente y servidor.</span><span class="sxs-lookup"><span data-stu-id="6acba-215">The SSL protocol uses encryption to secure the transport of data between a client and server.</span></span> <span data-ttu-id="6acba-216">Si la aplicación de SignalR transmite información confidencial entre el cliente y servidor, use SSL para el transporte.</span><span class="sxs-lookup"><span data-stu-id="6acba-216">If your SignalR application transmits sensitive information between the client and server, use SSL for the transport.</span></span> <span data-ttu-id="6acba-217">Para obtener más información acerca de cómo configurar SSL, consulte [cómo configurar SSL en IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span><span class="sxs-lookup"><span data-stu-id="6acba-217">For more information about setting up SSL, see [How to set up SSL on IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span></span>

<a id="groupsecurity"></a>

### <a name="do-not-use-groups-as-a-security-mechanism"></a><span data-ttu-id="6acba-218">No use grupos como un mecanismo de seguridad</span><span class="sxs-lookup"><span data-stu-id="6acba-218">Do not use groups as a security mechanism</span></span>

<span data-ttu-id="6acba-219">Los grupos son una manera cómoda de recopilación de usuarios relacionados, pero no son un mecanismo seguro para limitar el acceso a información confidencial.</span><span class="sxs-lookup"><span data-stu-id="6acba-219">Groups are a convenient way of collecting related users, but they are not a secure mechanism for limiting access to sensitive information.</span></span> <span data-ttu-id="6acba-220">Esto es especialmente cierto cuando los usuarios pueden volver a unir automáticamente grupos durante una reconexión.</span><span class="sxs-lookup"><span data-stu-id="6acba-220">This is especially true when users can automatically rejoin groups during a reconnect.</span></span> <span data-ttu-id="6acba-221">En su lugar, considere la posibilidad de agregar los usuarios con privilegios a un rol y limitar el acceso a un método de concentrador a solo los miembros de ese rol.</span><span class="sxs-lookup"><span data-stu-id="6acba-221">Instead, consider adding privileged users to a role and limiting access to a hub method to only members of that role.</span></span> <span data-ttu-id="6acba-222">Para obtener un ejemplo de restringir el acceso basándose en una función, vea [autenticación y autorización para los concentradores de SignalR](hub-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="6acba-222">For an example of restricting access based on a role, see [Authentication and Authorization for SignalR Hubs](hub-authorization.md).</span></span> <span data-ttu-id="6acba-223">Para obtener un ejemplo de la comprobación de acceso de usuario a grupos al volver a conectarse, consulte [trabajar con grupos](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="6acba-223">For an example of checking user access to groups when reconnecting, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="input"></a>

### <a name="safely-handling-input-from-clients"></a><span data-ttu-id="6acba-224">Controlar entrada desde clientes de forma segura</span><span class="sxs-lookup"><span data-stu-id="6acba-224">Safely handling input from clients</span></span>

<span data-ttu-id="6acba-225">Para asegurarse de que un usuario malintencionado no envía la secuencia de comandos a otros usuarios, primero debe codificar todas las entradas de los clientes que está pensada para difusión a otros clientes.</span><span class="sxs-lookup"><span data-stu-id="6acba-225">To ensure that a malicious user does not send script to other users, you must encode all input from clients that is intended for broadcast to other clients.</span></span> <span data-ttu-id="6acba-226">Debe codificar los mensajes en los clientes de recepción en lugar de en el servidor, ya que la aplicación de SignalR puede tener muchos tipos diferentes de los clientes.</span><span class="sxs-lookup"><span data-stu-id="6acba-226">You should encode messages on the receiving clients rather than the server, because your SignalR application may have many different types of clients.</span></span> <span data-ttu-id="6acba-227">Por lo tanto, la codificación HTML funciona para un cliente web, pero no para otros tipos de clientes.</span><span class="sxs-lookup"><span data-stu-id="6acba-227">Therefore, HTML-encoding works for a web client, but not for other types of clients.</span></span> <span data-ttu-id="6acba-228">Por ejemplo, un método de cliente web para mostrar un mensaje de chat con seguridad controlaría el nombre de usuario y el mensaje mediante una llamada a la `html()` función.</span><span class="sxs-lookup"><span data-stu-id="6acba-228">For example, a web client method to display a chat message would safely handle the user name and message by calling the `html()` function.</span></span>

[!code-html[Main](introduction-to-security/samples/sample2.html?highlight=3-4)]

<a id="reconcile"></a>

### <a name="reconciling-a-change-in-user-status-with-an-active-connection"></a><span data-ttu-id="6acba-229">Reconciliar un cambio en el estado de usuario con una conexión activa</span><span class="sxs-lookup"><span data-stu-id="6acba-229">Reconciling a change in user status with an active connection</span></span>

<span data-ttu-id="6acba-230">Si el estado de la autenticación de un usuario cambia mientras existe una conexión activa, el usuario recibirá un error que indica, "la identidad del usuario no puede cambiar durante una conexión SignalR activa".</span><span class="sxs-lookup"><span data-stu-id="6acba-230">If a user's authentication status changes while an active connection exists, the user will receive an error that states, "The user identity cannot change during an active SignalR connection."</span></span> <span data-ttu-id="6acba-231">En ese caso, la aplicación vuelve a debe conectarse al servidor para asegurarse de que el nombre de usuario y el identificador de conexión se coordinan.</span><span class="sxs-lookup"><span data-stu-id="6acba-231">In that case, your application should re-connect to the server to make sure the connection id and username are coordinated.</span></span> <span data-ttu-id="6acba-232">Por ejemplo, si la aplicación permite al usuario cerrar la sesión mientras exista una conexión activa, el nombre de usuario para la conexión ya no coincidirá con el nombre que se pasa en la siguiente solicitud.</span><span class="sxs-lookup"><span data-stu-id="6acba-232">For example, if your application allows the user to log out while an active connection exists, the username for the connection will no longer match the name that is passed in for the next request.</span></span> <span data-ttu-id="6acba-233">Se desea detener la conexión antes de que el usuario cierre sesión y, a continuación, reinícielo.</span><span class="sxs-lookup"><span data-stu-id="6acba-233">You will want to stop the connection before the user logs out, and then restart it.</span></span>

<span data-ttu-id="6acba-234">Sin embargo, es importante tener en cuenta que la mayoría de las aplicaciones no necesitarán detener e iniciar la conexión manualmente.</span><span class="sxs-lookup"><span data-stu-id="6acba-234">However, it is important to note that most applications will not need to manually stop and start the connection.</span></span> <span data-ttu-id="6acba-235">Si la aplicación redirige a los usuarios a una página independiente después de iniciar sesión, por ejemplo, el comportamiento predeterminado en una aplicación de formularios Web Forms o MVC, o actualiza la página actual después de cerrar sesión, la conexión activa se desconecta automáticamente y no lo hace requerir ninguna acción adicional.</span><span class="sxs-lookup"><span data-stu-id="6acba-235">If your application redirects users to a separate page after logging out, such as the default behavior in a Web Forms application or MVC application, or refreshes the current page after logging out, the active connection is automatically disconnected and does not require any additional action.</span></span>

<span data-ttu-id="6acba-236">El ejemplo siguiente muestra cómo detener e iniciar una conexión cuando ha cambiado el estado de usuario.</span><span class="sxs-lookup"><span data-stu-id="6acba-236">The following example shows how to stop and start a connection when the user status has changed.</span></span>

[!code-html[Main](introduction-to-security/samples/sample3.html)]

<span data-ttu-id="6acba-237">O bien, puede cambiar el estado de la autenticación del usuario si su sitio utiliza una expiración variable con autenticación de formularios, y no hay ninguna actividad para mantener la cookie de autenticación válido.</span><span class="sxs-lookup"><span data-stu-id="6acba-237">Or, the user's authentication status may change if your site uses sliding expiration with Forms Authentication, and there is no activity to keep the authentication cookie valid.</span></span> <span data-ttu-id="6acba-238">En ese caso, el usuario se cerrará y el nombre de usuario ya no coincidirá con el nombre de usuario en el token de conexión.</span><span class="sxs-lookup"><span data-stu-id="6acba-238">In that case, the user will be logged out and the user name will no longer match the user name in the connection token.</span></span> <span data-ttu-id="6acba-239">Puede corregir este problema mediante la adición de un script que solicita periódicamente un recurso en el servidor web para mantener la cookie de autenticación válida.</span><span class="sxs-lookup"><span data-stu-id="6acba-239">You can fix this problem by adding some script that periodically requests a resource on the web server to keep the authentication cookie valid.</span></span> <span data-ttu-id="6acba-240">El ejemplo siguiente muestra cómo solicitar un recurso cada 30 minutos.</span><span class="sxs-lookup"><span data-stu-id="6acba-240">The following example shows how to request a resource every 30 minutes.</span></span>

[!code-javascript[Main](introduction-to-security/samples/sample4.js)]

<a id="autogen"></a>

### <a name="automatically-generated-javascript-proxy-files"></a><span data-ttu-id="6acba-241">Archivos del proxy de JavaScript generados automáticamente</span><span class="sxs-lookup"><span data-stu-id="6acba-241">Automatically generated JavaScript proxy files</span></span>

<span data-ttu-id="6acba-242">Si no desea incluir todos los concentradores y métodos en el archivo de proxy de JavaScript para cada usuario, puede deshabilitar la generación automática del archivo.</span><span class="sxs-lookup"><span data-stu-id="6acba-242">If you do not want to include all of the hubs and methods in the JavaScript proxy file for each user, you can disable the automatic generation of the file.</span></span> <span data-ttu-id="6acba-243">Puede elegir esta opción si tiene varios concentradores y métodos, pero no desea que todos los usuarios a tener en cuenta todos los métodos.</span><span class="sxs-lookup"><span data-stu-id="6acba-243">You might choose this option if you have multiple hubs and methods, but do not want every user to be aware of all of the methods.</span></span> <span data-ttu-id="6acba-244">Deshabilitar la generación automática estableciendo **EnableJavaScriptProxies** a **false**.</span><span class="sxs-lookup"><span data-stu-id="6acba-244">You disable automatic generation by setting **EnableJavaScriptProxies** to **false**.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample5.cs)]

<span data-ttu-id="6acba-245">Para obtener más información acerca de los archivos del proxy de JavaScript, consulte [el proxy generado y lo que hace por usted](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span><span class="sxs-lookup"><span data-stu-id="6acba-245">For more information about the JavaScript proxy files, see [The generated proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span></span> <a id="exceptions"></a>

### <a name="exceptions"></a><span data-ttu-id="6acba-246">Excepciones</span><span class="sxs-lookup"><span data-stu-id="6acba-246">Exceptions</span></span>

<span data-ttu-id="6acba-247">Debe evitar pasar objetos de excepción para los clientes porque los objetos pueden exponer información confidencial a los clientes.</span><span class="sxs-lookup"><span data-stu-id="6acba-247">You should avoid passing exception objects to clients because the objects may expose sensitive information to the clients.</span></span> <span data-ttu-id="6acba-248">En su lugar, llame a un método en el cliente que muestra el mensaje de error relevantes.</span><span class="sxs-lookup"><span data-stu-id="6acba-248">Instead, call a method on the client that displays the relevant error message.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample6.cs)]
