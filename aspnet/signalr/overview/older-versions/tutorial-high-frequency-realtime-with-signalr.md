---
uid: signalr/overview/older-versions/tutorial-high-frequency-realtime-with-signalr
title: Alta frecuencia en tiempo real con SignalR 1.x | Microsoft Docs
author: bradygaster
description: Este tutorial muestra cómo crear una aplicación web que usa SignalR de ASP.NET para proporcionar funcionalidad de mensajería de alta frecuencia. Alta frecuencia de mensajería en...
ms.author: bradyg
ms.date: 04/16/2013
ms.assetid: ad2a5da5-2e79-40ea-bc84-028d327f5982
msc.legacyurl: /signalr/overview/older-versions/tutorial-high-frequency-realtime-with-signalr
msc.type: authoredcontent
ms.openlocfilehash: e37e63a410952ec170cbbaadeeb54eae7e82b3b5
ms.sourcegitcommit: 51b01b6ff8edde57d8243e4da28c9f1e7f1962b2
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 05/06/2019
ms.locfileid: "65113700"
---
# <a name="high-frequency-realtime-with-signalr-1x"></a><span data-ttu-id="578dd-104">Alta frecuencia en tiempo real con SignalR 1.x</span><span class="sxs-lookup"><span data-stu-id="578dd-104">High-Frequency Realtime with SignalR 1.x</span></span>

<span data-ttu-id="578dd-105">por [Patrick Fletcher](https://github.com/pfletcher)</span><span class="sxs-lookup"><span data-stu-id="578dd-105">by [Patrick Fletcher](https://github.com/pfletcher)</span></span>

[!INCLUDE [Consider ASP.NET Core SignalR](~/includes/signalr/signalr-version-disambiguation.md)]

> <span data-ttu-id="578dd-106">Este tutorial muestra cómo crear una aplicación web que usa SignalR de ASP.NET para proporcionar funcionalidad de mensajería de alta frecuencia.</span><span class="sxs-lookup"><span data-stu-id="578dd-106">This tutorial shows how to create a web application that uses ASP.NET SignalR to provide high-frequency messaging functionality.</span></span> <span data-ttu-id="578dd-107">En este caso la mensajería de alta frecuencia significa que las actualizaciones que se envían a una tarifa fija; en el caso de esta aplicación, hasta 10 mensajes por segundo.</span><span class="sxs-lookup"><span data-stu-id="578dd-107">High-frequency messaging in this case means updates that are sent at a fixed rate; in the case of this application, up to 10 messages a second.</span></span>
> 
> <span data-ttu-id="578dd-108">La aplicación que creará en este tutorial muestra una forma que los usuarios pueden arrastrar.</span><span class="sxs-lookup"><span data-stu-id="578dd-108">The application you'll create in this tutorial displays a shape that users can drag.</span></span> <span data-ttu-id="578dd-109">La posición de la forma en todos los demás exploradores conectados, a continuación, se actualizará para que coincida con la posición de la forma arrastrada mediante actualizaciones periódicas.</span><span class="sxs-lookup"><span data-stu-id="578dd-109">The position of the shape in all other connected browsers will then be updated to match the position of the dragged shape using timed updates.</span></span>
> 
> <span data-ttu-id="578dd-110">Los conceptos presentados en este tutorial tienen aplicaciones de juegos en tiempo real y otras aplicaciones de la simulación.</span><span class="sxs-lookup"><span data-stu-id="578dd-110">Concepts introduced in this tutorial have applications in real-time gaming and other simulation applications.</span></span>
> 
> <span data-ttu-id="578dd-111">Comentarios en el tutorial son bienvenidos.</span><span class="sxs-lookup"><span data-stu-id="578dd-111">Comments on the tutorial are welcome.</span></span> <span data-ttu-id="578dd-112">Si tiene preguntas que no están directamente relacionados con el tutorial, puede publicarlos en el [foro de ASP.NET SignalR](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) o [StackOverflow.com](http://stackoverflow.com).</span><span class="sxs-lookup"><span data-stu-id="578dd-112">If you have questions that are not directly related to the tutorial, you can post them to the [ASP.NET SignalR forum](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) or [StackOverflow.com](http://stackoverflow.com).</span></span>

## <a name="overview"></a><span data-ttu-id="578dd-113">Información general</span><span class="sxs-lookup"><span data-stu-id="578dd-113">Overview</span></span>

<span data-ttu-id="578dd-114">Este tutorial muestra cómo crear una aplicación que comparte el estado de un objeto con otros exploradores en tiempo real.</span><span class="sxs-lookup"><span data-stu-id="578dd-114">This tutorial demonstrates how to create an application that shares the state of an object with other browsers in real time.</span></span> <span data-ttu-id="578dd-115">La aplicación que vamos a crear se denomina MoveShape.</span><span class="sxs-lookup"><span data-stu-id="578dd-115">The application we'll create is called MoveShape.</span></span> <span data-ttu-id="578dd-116">Mostrará la página de MoveShape un elemento HTML Div que el usuario puede arrastrar; Cuando el usuario arrastra el elemento Div, la nueva posición se enviarán al servidor, que indicará que todos los clientes conectados para actualizar la posición de la forma para que coincida con.</span><span class="sxs-lookup"><span data-stu-id="578dd-116">The MoveShape page will display an HTML Div element that the user can drag; when the user drags the Div, its new position will be sent to the server, which will then tell all other connected clients to update the shape's position to match.</span></span>

![La ventana de la aplicación](tutorial-high-frequency-realtime-with-signalr/_static/image1.png)

<span data-ttu-id="578dd-118">La aplicación creada en este tutorial se basa en una demostración de Damian Edwards.</span><span class="sxs-lookup"><span data-stu-id="578dd-118">The application created in this tutorial is based on a demo by Damian Edwards.</span></span> <span data-ttu-id="578dd-119">Se puede ver un vídeo que contiene esta demostración [aquí](https://channel9.msdn.com/Series/Building-Web-Apps-with-ASP-NET-Jump-Start/Building-Web-Apps-with-ASPNET-Jump-Start-08-Real-time-Communication-with-SignalR).</span><span class="sxs-lookup"><span data-stu-id="578dd-119">A video containing this demo can be seen [here](https://channel9.msdn.com/Series/Building-Web-Apps-with-ASP-NET-Jump-Start/Building-Web-Apps-with-ASPNET-Jump-Start-08-Real-time-Communication-with-SignalR).</span></span>

<span data-ttu-id="578dd-120">Para empezar el tutorial, que demuestra cómo enviar mensajes de SignalR en cada evento que se desencadena a medida que se arrastra la forma.</span><span class="sxs-lookup"><span data-stu-id="578dd-120">The tutorial will start by demonstrating how to send SignalR messages from each event that fires as the shape is dragged.</span></span> <span data-ttu-id="578dd-121">Cada cliente conectado, a continuación, actualizará la posición de la versión local de la forma cada vez que se recibe un mensaje.</span><span class="sxs-lookup"><span data-stu-id="578dd-121">Each connected client will then update the position of the local version of the shape each time a message is received.</span></span>

<span data-ttu-id="578dd-122">Aunque la aplicación funcionará con este método, no es un modelo de programación recomendado, ya que no habría ningún límite superior para el número de mensajes enviados al obtener, por lo que los clientes y el servidor podrían obtener experimentando con mensajes y podría degradar el rendimiento .</span><span class="sxs-lookup"><span data-stu-id="578dd-122">While the application will function using this method, this is not a recommended programming model, since there would be no upper limit to the number of messages getting sent, so the clients and server could get overwhelmed with messages and performance would degrade.</span></span> <span data-ttu-id="578dd-123">La animación mostrada en el cliente también sería separado, como la forma se pasaría al instante por cada método, en lugar de mover sin problemas a cada nueva ubicación.</span><span class="sxs-lookup"><span data-stu-id="578dd-123">The displayed animation on the client would also be disjointed, as the shape would be moved instantly by each method, rather than moving smoothly to each new location.</span></span> <span data-ttu-id="578dd-124">En secciones posteriores de este tutorial se muestran cómo crear una función de temporizador que restringe la velocidad máxima a la que los mensajes se envían por el cliente o el servidor y cómo mover la forma sin problemas entre las ubicaciones.</span><span class="sxs-lookup"><span data-stu-id="578dd-124">Later sections of the tutorial will demonstrate how to create a timer function that restricts the maximum rate at which messages are sent by either the client or server, and how to move the shape smoothly between locations.</span></span> <span data-ttu-id="578dd-125">La versión final de la aplicación creada en este tutorial se puede descargar desde [Galería de código](https://code.msdn.microsoft.com/SignalR-MoveShape-demo-3366dac6).</span><span class="sxs-lookup"><span data-stu-id="578dd-125">The final version of the application created in this tutorial can be downloaded from [Code Gallery](https://code.msdn.microsoft.com/SignalR-MoveShape-demo-3366dac6).</span></span>

<span data-ttu-id="578dd-126">Este tutorial contiene las siguientes secciones:</span><span class="sxs-lookup"><span data-stu-id="578dd-126">This tutorial contains the following sections:</span></span>

- [<span data-ttu-id="578dd-127">Requisitos previos</span><span class="sxs-lookup"><span data-stu-id="578dd-127">Prerequisites</span></span>](#prerequisites)
- [<span data-ttu-id="578dd-128">Crear el proyecto</span><span class="sxs-lookup"><span data-stu-id="578dd-128">Create the project</span></span>](#createtheproject)
- [<span data-ttu-id="578dd-129">Agregue los paquetes de JQuery.UI NuGet y ASP.NET SignalR</span><span class="sxs-lookup"><span data-stu-id="578dd-129">Add the ASP.NET SignalR and JQuery.UI NuGet packages</span></span>](#nugetpackages)
- [<span data-ttu-id="578dd-130">Crear la aplicación básica</span><span class="sxs-lookup"><span data-stu-id="578dd-130">Create the base application</span></span>](#baseapp)
- [<span data-ttu-id="578dd-131">Agregar el bucle de cliente</span><span class="sxs-lookup"><span data-stu-id="578dd-131">Add the client loop</span></span>](#clientloop)
- [<span data-ttu-id="578dd-132">Agregar bucle de servidor</span><span class="sxs-lookup"><span data-stu-id="578dd-132">Add the server loop</span></span>](#serverloop)
- [<span data-ttu-id="578dd-133">Agregar una animación fluida en el cliente</span><span class="sxs-lookup"><span data-stu-id="578dd-133">Add smooth animation on the client</span></span>](#animation)
- [<span data-ttu-id="578dd-134">Pasos adicionales</span><span class="sxs-lookup"><span data-stu-id="578dd-134">Further Steps</span></span>](#furthersteps)

<a id="prerequisites"></a>

## <a name="prerequisites"></a><span data-ttu-id="578dd-135">Requisitos previos</span><span class="sxs-lookup"><span data-stu-id="578dd-135">Prerequisites</span></span>

<span data-ttu-id="578dd-136">Este tutorial requiere Visual Studio 2012 o Visual Studio 2010.</span><span class="sxs-lookup"><span data-stu-id="578dd-136">This tutorial requires Visual Studio 2012 or Visual Studio 2010.</span></span> <span data-ttu-id="578dd-137">Si se usa Visual Studio 2010, se usará el proyecto de .NET Framework 4 en lugar de .NET Framework 4.5.</span><span class="sxs-lookup"><span data-stu-id="578dd-137">If Visual Studio 2010 is used, the project will use .NET Framework 4 rather than .NET Framework 4.5.</span></span>

<span data-ttu-id="578dd-138">Si utiliza Visual Studio 2012, se recomienda que instale el [actualización ASP.NET y Web Tools 2012.2](https://go.microsoft.com/fwlink/?LinkId=282650).</span><span class="sxs-lookup"><span data-stu-id="578dd-138">If you are using Visual Studio 2012, it's recommended that you install the [ASP.NET and Web Tools 2012.2 update](https://go.microsoft.com/fwlink/?LinkId=282650).</span></span> <span data-ttu-id="578dd-139">Esta actualización contiene nuevas características, como mejoras en la publicación, la nueva funcionalidad y las nuevas plantillas.</span><span class="sxs-lookup"><span data-stu-id="578dd-139">This update contains new features such as enhancements to publishing, new functionality, and new templates.</span></span>

<span data-ttu-id="578dd-140">Si tiene Visual Studio 2010, asegúrese de que [NuGet](https://visualstudiogallery.msdn.microsoft.com/27077b70-9dad-4c64-adcf-c7cf6bc9970c) está instalado.</span><span class="sxs-lookup"><span data-stu-id="578dd-140">If you have Visual Studio 2010, make sure that [NuGet](https://visualstudiogallery.msdn.microsoft.com/27077b70-9dad-4c64-adcf-c7cf6bc9970c) is installed.</span></span>

<a id="createtheproject"></a>

## <a name="create-the-project"></a><span data-ttu-id="578dd-141">Crear el proyecto</span><span class="sxs-lookup"><span data-stu-id="578dd-141">Create the project</span></span>

<span data-ttu-id="578dd-142">En esta sección, vamos a crear el proyecto en Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="578dd-142">In this section, we'll create the project in Visual Studio.</span></span>

1. <span data-ttu-id="578dd-143">Desde el **archivo** menú haga clic en **nuevo proyecto**.</span><span class="sxs-lookup"><span data-stu-id="578dd-143">From the **File** menu click **New Project**.</span></span>
2. <span data-ttu-id="578dd-144">En el **nuevo proyecto** cuadro de diálogo, expanda **C#** en **plantillas** y seleccione **Web**.</span><span class="sxs-lookup"><span data-stu-id="578dd-144">In the **New Project** dialog box, expand **C#** under **Templates** and select **Web**.</span></span>
3. <span data-ttu-id="578dd-145">Seleccione el **aplicación Web ASP.NET vacía** plantilla, el nombre del proyecto *MoveShapeDemo*y haga clic en **Aceptar**.</span><span class="sxs-lookup"><span data-stu-id="578dd-145">Select the **ASP.NET Empty Web Application** template, name the project *MoveShapeDemo*, and click **OK**.</span></span>

    ![Creación del nuevo proyecto](tutorial-high-frequency-realtime-with-signalr/_static/image2.png)

<a id="nugetpackages"></a>

## <a name="add-the-signalr-and-jqueryui-nuget-packages"></a><span data-ttu-id="578dd-147">Agregue los paquetes de JQuery.UI NuGet y SignalR</span><span class="sxs-lookup"><span data-stu-id="578dd-147">Add the SignalR and JQuery.UI NuGet Packages</span></span>

<span data-ttu-id="578dd-148">Puede agregar funcionalidad de SignalR a un proyecto mediante la instalación de un paquete de NuGet.</span><span class="sxs-lookup"><span data-stu-id="578dd-148">You can add SignalR functionality to a project by installing a NuGet package.</span></span> <span data-ttu-id="578dd-149">Este tutorial también utilizará el paquete JQuery.UI para permitir la forma poder arrastrar y anima.</span><span class="sxs-lookup"><span data-stu-id="578dd-149">This tutorial will also use the JQuery.UI package for allowing the shape to be dragged and animated.</span></span>

1. <span data-ttu-id="578dd-150">Haga clic en **herramientas | Administrador de paquetes de NuGet | Consola de administrador de paquetes**.</span><span class="sxs-lookup"><span data-stu-id="578dd-150">Click **Tools | NuGet Package Manager | Package Manager Console**.</span></span>
2. <span data-ttu-id="578dd-151">Escriba el siguiente comando en el Administrador de paquetes.</span><span class="sxs-lookup"><span data-stu-id="578dd-151">Enter the following command in the package manager.</span></span>

    [!code-powershell[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample1.ps1)]

    <span data-ttu-id="578dd-152">El paquete de SignalR instala a una serie de otros paquetes de NuGet como dependencias.</span><span class="sxs-lookup"><span data-stu-id="578dd-152">The SignalR package installs a number of other NuGet packages as dependencies.</span></span> <span data-ttu-id="578dd-153">Cuando finalice la instalación tienen todos los componentes de servidor y cliente necesarios para usar SignalR en una aplicación ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="578dd-153">When the installation is finished you have all of the server and client components required to use SignalR in an ASP.NET application.</span></span>
3. <span data-ttu-id="578dd-154">Escriba el siguiente comando en la consola del Administrador de paquetes para instalar los paquetes de JQuery y JQuery.UI.</span><span class="sxs-lookup"><span data-stu-id="578dd-154">Enter the following command into the package manager console to install the JQuery and JQuery.UI packages.</span></span>

    [!code-powershell[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample2.ps1)]

<a id="baseapp"></a>

## <a name="create-the-base-application"></a><span data-ttu-id="578dd-155">Crear la aplicación básica</span><span class="sxs-lookup"><span data-stu-id="578dd-155">Create the base application</span></span>

<span data-ttu-id="578dd-156">En esta sección, vamos a crear una aplicación de explorador que envía la ubicación de la forma en el servidor durante cada evento de mover el mouse.</span><span class="sxs-lookup"><span data-stu-id="578dd-156">In this section, we'll create a browser application that sends the location of the shape to the server during each mouse move event.</span></span> <span data-ttu-id="578dd-157">El servidor transmite, a continuación, esta información para todos los demás clientes conectados cuando se reciben.</span><span class="sxs-lookup"><span data-stu-id="578dd-157">The server then broadcasts this information to all other connected clients as it is received.</span></span> <span data-ttu-id="578dd-158">Se expandirán en esta aplicación en secciones posteriores.</span><span class="sxs-lookup"><span data-stu-id="578dd-158">We'll expand on this application in later sections.</span></span>

1. <span data-ttu-id="578dd-159">En **el Explorador de soluciones**, haga doble clic en el proyecto y seleccione **agregar**, **clase...** . Nombre de la clase **MoveShapeHub** y haga clic en **agregar**.</span><span class="sxs-lookup"><span data-stu-id="578dd-159">In **Solution Explorer**, right-click on the project and select **Add**, **Class...**. Name the class **MoveShapeHub** and click **Add**.</span></span>
2. <span data-ttu-id="578dd-160">Reemplace el código en el nuevo **MoveShapeHub** con el código siguiente.</span><span class="sxs-lookup"><span data-stu-id="578dd-160">Replace the code in the new **MoveShapeHub** class with the following code.</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample3.cs)]

    <span data-ttu-id="578dd-161">La `MoveShapeHub` clase anterior es una implementación de un concentrador SignalR.</span><span class="sxs-lookup"><span data-stu-id="578dd-161">The `MoveShapeHub` class above is an implementation of a SignalR hub.</span></span> <span data-ttu-id="578dd-162">Como en el [Introducción a SignalR](index.md) tutorial, el concentrador tiene un método que los clientes llamen directamente.</span><span class="sxs-lookup"><span data-stu-id="578dd-162">As in the [Getting Started with SignalR](index.md) tutorial, the hub has a method that the clients will call directly.</span></span> <span data-ttu-id="578dd-163">En este caso, el cliente enviará un objeto que contiene las nuevas coordenadas X e Y de la forma en el servidor, que, a continuación, obtiene difunden a todos los demás clientes conectados.</span><span class="sxs-lookup"><span data-stu-id="578dd-163">In this case, the client will send an object containing the new X and Y coordinates of the shape to the server, which then gets broadcasted to all other connected clients.</span></span> <span data-ttu-id="578dd-164">SignalR serializará automáticamente este objeto mediante JSON.</span><span class="sxs-lookup"><span data-stu-id="578dd-164">SignalR will automatically serialize this object using JSON.</span></span>

    <span data-ttu-id="578dd-165">El objeto que se enviará al cliente (`ShapeModel`) contiene miembros para almacenar la posición de la forma.</span><span class="sxs-lookup"><span data-stu-id="578dd-165">The object that will be sent to the client (`ShapeModel`) contains members to store the position of the shape.</span></span> <span data-ttu-id="578dd-166">La versión del objeto en el servidor también contiene un miembro para realizar un seguimiento de los datos del cliente que se va a almacenar, para que un cliente determinado no se envíe a sus propios datos.</span><span class="sxs-lookup"><span data-stu-id="578dd-166">The version of the object on the server also contains a member to track which client's data is being stored, so that a given client won't be sent their own data.</span></span> <span data-ttu-id="578dd-167">Este miembro se usa el `JsonIgnore` atributo para impedir que se va a serializar y enviar al cliente.</span><span class="sxs-lookup"><span data-stu-id="578dd-167">This member uses the `JsonIgnore` attribute to keep it from being serialized and sent to the client.</span></span>
3. <span data-ttu-id="578dd-168">A continuación, configuraremos el concentrador cuando se inicia la aplicación.</span><span class="sxs-lookup"><span data-stu-id="578dd-168">Next, we'll set up the hub when the application starts.</span></span> <span data-ttu-id="578dd-169">En **el Explorador de soluciones**, haga clic en el proyecto y luego haga clic en **Add | Clase de aplicación global**.</span><span class="sxs-lookup"><span data-stu-id="578dd-169">In **Solution Explorer**, right-click the project, then click **Add | Global Application Class**.</span></span> <span data-ttu-id="578dd-170">Acepte el nombre predeterminado de *Global* y haga clic en **Aceptar**.</span><span class="sxs-lookup"><span data-stu-id="578dd-170">Accept the default name of *Global* and click **OK**.</span></span>

    ![Agregar clase de aplicación Global](tutorial-high-frequency-realtime-with-signalr/_static/image3.png)
4. <span data-ttu-id="578dd-172">Agregue el siguiente `using` instrucción después proporcionado **mediante** las instrucciones de la clase de Global.asax.cs.</span><span class="sxs-lookup"><span data-stu-id="578dd-172">Add the following `using` statement after the provided **using** statements in the Global.asax.cs class.</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample4.cs)]
5. <span data-ttu-id="578dd-173">Agregue la siguiente línea de código en el `Application_Start` método de la clase Global para registrar la ruta predeterminada para SignalR.</span><span class="sxs-lookup"><span data-stu-id="578dd-173">Add the following line of code in the `Application_Start` method of the Global class to register the default route for SignalR.</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample5.cs)]

    <span data-ttu-id="578dd-174">El archivo global.asax debe tener el siguiente aspecto:</span><span class="sxs-lookup"><span data-stu-id="578dd-174">Your global.asax file should look like the following:</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample6.cs)]
6. <span data-ttu-id="578dd-175">A continuación, vamos a agregar al cliente.</span><span class="sxs-lookup"><span data-stu-id="578dd-175">Next, we'll add the client.</span></span> <span data-ttu-id="578dd-176">En **el Explorador de soluciones**, haga clic en el proyecto y luego haga clic en **Add | Nuevo elemento**.</span><span class="sxs-lookup"><span data-stu-id="578dd-176">In **Solution Explorer**, right-click the project, then click **Add | New Item**.</span></span> <span data-ttu-id="578dd-177">En el **Agregar nuevo elemento** cuadro de diálogo, seleccione **página Html**.</span><span class="sxs-lookup"><span data-stu-id="578dd-177">In the **Add New Item** dialog, select **Html Page**.</span></span> <span data-ttu-id="578dd-178">Asigne un nombre adecuado a la página (como **Default.html**) y haga clic en **agregar**.</span><span class="sxs-lookup"><span data-stu-id="578dd-178">Give the page an appropriate name (like **Default.html**) and click **Add**.</span></span>
7. <span data-ttu-id="578dd-179">En **el Explorador de soluciones**, haga clic en la página que acaba de crear y haga clic en **establecer como página de inicio**.</span><span class="sxs-lookup"><span data-stu-id="578dd-179">In **Solution Explorer**, right-click the page you just created and click **Set as Start Page**.</span></span>
8. <span data-ttu-id="578dd-180">Reemplace el código predeterminado en la página HTML con el siguiente fragmento de código.</span><span class="sxs-lookup"><span data-stu-id="578dd-180">Replace the default code in the HTML page with the following code snippet.</span></span>

    > [!NOTE]
    > <span data-ttu-id="578dd-181">Comprobar que el script hace referencia a continuación de la coincidencia de los paquetes que se agrega al proyecto en la carpeta Scripts.</span><span class="sxs-lookup"><span data-stu-id="578dd-181">Verify that the script references below match the packages added to your project in the Scripts folder.</span></span> <span data-ttu-id="578dd-182">En Visual Studio 2010, la versión de JQuery y SignalR que se agrega al proyecto no puede coincidir con los números de versión siguientes.</span><span class="sxs-lookup"><span data-stu-id="578dd-182">In Visual Studio 2010, the version of JQuery and SignalR added to the project may not match the version numbers below.</span></span>

    [!code-html[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample7.html)]

    <span data-ttu-id="578dd-183">El código HTML y JavaScript anterior crea un elemento Div rojo llamado de forma, habilita el comportamiento de arrastrar la forma mediante la biblioteca de jQuery y utiliza la forma `drag` eventos a la posición de la forma de envío al servidor.</span><span class="sxs-lookup"><span data-stu-id="578dd-183">The above HTML and JavaScript code creates a red Div called Shape, enables the shape's dragging behavior using the jQuery library, and uses the shape's `drag` event to send the shape's position to the server.</span></span>
9. <span data-ttu-id="578dd-184">Inicie la aplicación presionando F5.</span><span class="sxs-lookup"><span data-stu-id="578dd-184">Start the application by pressing F5.</span></span> <span data-ttu-id="578dd-185">Copiar dirección URL de la página y pegarlo en otra ventana del explorador.</span><span class="sxs-lookup"><span data-stu-id="578dd-185">Copy the page's URL, and paste it into a second browser window.</span></span> <span data-ttu-id="578dd-186">Arrastre la forma de una de las ventanas del explorador; debe mover la forma en la ventana del explorador.</span><span class="sxs-lookup"><span data-stu-id="578dd-186">Drag the shape in one of the browser windows; the shape in the other browser window should move.</span></span>

    ![La ventana de la aplicación](tutorial-high-frequency-realtime-with-signalr/_static/image4.png)

<a id="clientloop"></a>

## <a name="add-the-client-loop"></a><span data-ttu-id="578dd-188">Agregar el bucle de cliente</span><span class="sxs-lookup"><span data-stu-id="578dd-188">Add the client loop</span></span>

<span data-ttu-id="578dd-189">Dado que el envío de la ubicación de la forma en cada evento de mover el mouse, se creará un volumen de tráfico de red innecesario, los mensajes desde el cliente deben estar limitado.</span><span class="sxs-lookup"><span data-stu-id="578dd-189">Since sending the location of the shape on every mouse move event will create an unnecessary amount of network traffic, the messages from the client need to be throttled.</span></span> <span data-ttu-id="578dd-190">Vamos a usar el código javascript `setInterval` función para establecer un bucle que envía información de posición de nuevo al servidor con una tarifa fija.</span><span class="sxs-lookup"><span data-stu-id="578dd-190">We'll use the javascript `setInterval` function to set up a loop that sends new position information to the server at a fixed rate.</span></span> <span data-ttu-id="578dd-191">Este bucle es una representación muy básica de un "bucle de juego", una función varias veces a que controla toda la funcionalidad de un juego o una simulación de otro.</span><span class="sxs-lookup"><span data-stu-id="578dd-191">This loop is a very basic representation of a "game loop", a repeatedly called function that drives all of the functionality of a game or other simulation.</span></span>

1. <span data-ttu-id="578dd-192">Actualice el código de cliente en la página HTML para que coincida con el siguiente fragmento de código.</span><span class="sxs-lookup"><span data-stu-id="578dd-192">Update the client code in the HTML page to match the following code snippet.</span></span>

    [!code-html[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample8.html)]

    <span data-ttu-id="578dd-193">La actualización anterior agrega el `updateServerModel` función, que se invoca con una frecuencia fija.</span><span class="sxs-lookup"><span data-stu-id="578dd-193">The above update adds the `updateServerModel` function, which gets called on a fixed frequency.</span></span> <span data-ttu-id="578dd-194">Esta función envía los datos de la posición en el servidor cada vez que el `moved` marca indica que hay nuevos datos de posición para enviar.</span><span class="sxs-lookup"><span data-stu-id="578dd-194">This function sends the position data to the server whenever the `moved` flag indicates that there is new position data to send.</span></span>
2. <span data-ttu-id="578dd-195">Inicie la aplicación presionando F5.</span><span class="sxs-lookup"><span data-stu-id="578dd-195">Start the application by pressing F5.</span></span> <span data-ttu-id="578dd-196">Copiar dirección URL de la página y pegarlo en otra ventana del explorador.</span><span class="sxs-lookup"><span data-stu-id="578dd-196">Copy the page's URL, and paste it into a second browser window.</span></span> <span data-ttu-id="578dd-197">Arrastre la forma de una de las ventanas del explorador; debe mover la forma en la ventana del explorador.</span><span class="sxs-lookup"><span data-stu-id="578dd-197">Drag the shape in one of the browser windows; the shape in the other browser window should move.</span></span> <span data-ttu-id="578dd-198">Puesto que se limitará el número de mensajes que se envían al servidor, la animación no aparecerán como smooth, como se muestra en la sección anterior.</span><span class="sxs-lookup"><span data-stu-id="578dd-198">Since the number of messages that get sent to the server will be throttled, the animation will not appear as smooth as in the previous section.</span></span>

    ![La ventana de la aplicación](tutorial-high-frequency-realtime-with-signalr/_static/image5.png)

<a id="serverloop"></a>

## <a name="add-the-server-loop"></a><span data-ttu-id="578dd-200">Agregar bucle de servidor</span><span class="sxs-lookup"><span data-stu-id="578dd-200">Add the server loop</span></span>

<span data-ttu-id="578dd-201">En la aplicación actual, los mensajes enviados desde el servidor al cliente salen con tanta frecuencia como se reciben.</span><span class="sxs-lookup"><span data-stu-id="578dd-201">In the current application, messages sent from the server to the client go out as often as they are received.</span></span> <span data-ttu-id="578dd-202">Esto presenta un problema similar como se vio en el cliente; los mensajes pueden enviarse con más frecuencia que son necesarios y la conexión podría inundarse como resultado.</span><span class="sxs-lookup"><span data-stu-id="578dd-202">This presents a similar problem as was seen on the client; messages can be sent more often than they are needed, and the connection could become flooded as a result.</span></span> <span data-ttu-id="578dd-203">En esta sección se describe cómo actualizar el servidor para implementar un temporizador que limita la velocidad de los mensajes salientes.</span><span class="sxs-lookup"><span data-stu-id="578dd-203">This section describes how to update the server to implement a timer that throttles the rate of the outgoing messages.</span></span>

1. <span data-ttu-id="578dd-204">Reemplace el contenido de `MoveShapeHub.cs` con el siguiente fragmento de código.</span><span class="sxs-lookup"><span data-stu-id="578dd-204">Replace the contents of `MoveShapeHub.cs` with the following code snippet.</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample9.cs)]

    <span data-ttu-id="578dd-205">El código anterior expande el cliente para agregar la `Broadcaster` (clase), que limita la salida de mensajes mediante el `Timer` clase a partir de .NET framework.</span><span class="sxs-lookup"><span data-stu-id="578dd-205">The above code expands the client to add the `Broadcaster` class, which throttles the outgoing messages using the `Timer` class from the .NET framework.</span></span>

    <span data-ttu-id="578dd-206">Puesto que el propio centro es transitorio (se crea cada vez que sea necesario), el `Broadcaster` se creará como un singleton.</span><span class="sxs-lookup"><span data-stu-id="578dd-206">Since the hub itself is transitory (it is created every time it is needed), the `Broadcaster` will be created as a singleton.</span></span> <span data-ttu-id="578dd-207">La inicialización diferida (introducida en .NET 4) se usa para diferir su creación hasta que sea necesario, lo que garantiza que la primera instancia de concentrador se crea por completo antes de que se inicia el temporizador.</span><span class="sxs-lookup"><span data-stu-id="578dd-207">Lazy initialization (introduced in .NET 4) is used to defer its creation until it is needed, ensuring that the first hub instance is completely created before the timer is started.</span></span>

    <span data-ttu-id="578dd-208">La llamada a los clientes `UpdateShape` función, a continuación, se mueve fuera del centro `UpdateModel` método, por lo que TI ya no se denomina inmediatamente cada vez que se reciben los mensajes entrantes.</span><span class="sxs-lookup"><span data-stu-id="578dd-208">The call to the clients' `UpdateShape` function is then moved out of the hub's `UpdateModel` method, so that it is no longer called immediately whenever incoming messages are received.</span></span> <span data-ttu-id="578dd-209">En su lugar, se enviará a una velocidad de 25 llamadas por segundo, los mensajes a los clientes administrados por el `_broadcastLoop` temporizador desde el `Broadcaster` clase.</span><span class="sxs-lookup"><span data-stu-id="578dd-209">Instead, the messages to the clients will be sent at a rate of 25 calls per second, managed by the `_broadcastLoop` timer from within the `Broadcaster` class.</span></span>

    <span data-ttu-id="578dd-210">Por último, en lugar de llamar al método de cliente desde el centro directamente, la `Broadcaster` clase necesita obtener una referencia al centro de funcionamiento actualmente (`_hubContext`) mediante el `GlobalHost`.</span><span class="sxs-lookup"><span data-stu-id="578dd-210">Lastly, instead of calling the client method from the hub directly, the `Broadcaster` class needs to obtain a reference to the currently operating hub (`_hubContext`) using the `GlobalHost`.</span></span>
2. <span data-ttu-id="578dd-211">Inicie la aplicación presionando F5.</span><span class="sxs-lookup"><span data-stu-id="578dd-211">Start the application by pressing F5.</span></span> <span data-ttu-id="578dd-212">Copiar dirección URL de la página y pegarlo en otra ventana del explorador.</span><span class="sxs-lookup"><span data-stu-id="578dd-212">Copy the page's URL, and paste it into a second browser window.</span></span> <span data-ttu-id="578dd-213">Arrastre la forma de una de las ventanas del explorador; debe mover la forma en la ventana del explorador.</span><span class="sxs-lookup"><span data-stu-id="578dd-213">Drag the shape in one of the browser windows; the shape in the other browser window should move.</span></span> <span data-ttu-id="578dd-214">No habrá una diferencia visible en el Explorador de la sección anterior, pero se limitará el número de mensajes que se envían al cliente.</span><span class="sxs-lookup"><span data-stu-id="578dd-214">There will not be a visible difference in the browser from the previous section, but the number of messages that get sent to the client will be throttled.</span></span>

    ![La ventana de la aplicación](tutorial-high-frequency-realtime-with-signalr/_static/image6.png)

<a id="animation"></a>

## <a name="add-smooth-animation-on-the-client"></a><span data-ttu-id="578dd-216">Agregar una animación fluida en el cliente</span><span class="sxs-lookup"><span data-stu-id="578dd-216">Add smooth animation on the client</span></span>

<span data-ttu-id="578dd-217">La aplicación está casi completada, pero podríamos realizar una mayor mejora, el movimiento de la forma en el cliente cuando se desplace en respuesta a los mensajes del servidor.</span><span class="sxs-lookup"><span data-stu-id="578dd-217">The application is almost complete, but we could make one more improvement, in the motion of the shape on the client as it is moved in response to server messages.</span></span> <span data-ttu-id="578dd-218">En lugar de establecer la posición de la forma a la nueva ubicación proporcionada por el servidor, vamos a usar la biblioteca de JQuery UI `animate` función para mover la forma sin problemas entre su posición actual y nuevo.</span><span class="sxs-lookup"><span data-stu-id="578dd-218">Rather than setting the position of the shape to the new location given by the server, we'll use the JQuery UI library's `animate` function to move the shape smoothly between its current and new position.</span></span>

1. <span data-ttu-id="578dd-219">Actualizar el cliente `updateShape` , como método para buscar el código resaltado siguiente:</span><span class="sxs-lookup"><span data-stu-id="578dd-219">Update the client's `updateShape` method to look like the highlighted code below:</span></span>

    [!code-html[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample10.html?highlight=35-42)]

    <span data-ttu-id="578dd-220">El código anterior, mueve la forma de la ubicación anterior a la nueva proporcionado por el servidor en el transcurso del intervalo de animación (en este caso 100 milisegundos).</span><span class="sxs-lookup"><span data-stu-id="578dd-220">The above code moves the shape from the old location to the new one given by the server over the course of the animation interval (in this case, 100 milliseconds).</span></span> <span data-ttu-id="578dd-221">Cualquier animación anterior que se ejecutan en la forma se borra antes de que comience la nueva animación.</span><span class="sxs-lookup"><span data-stu-id="578dd-221">Any previous animation running on the shape is cleared before the new animation starts.</span></span>
2. <span data-ttu-id="578dd-222">Inicie la aplicación presionando F5.</span><span class="sxs-lookup"><span data-stu-id="578dd-222">Start the application by pressing F5.</span></span> <span data-ttu-id="578dd-223">Copiar dirección URL de la página y pegarlo en otra ventana del explorador.</span><span class="sxs-lookup"><span data-stu-id="578dd-223">Copy the page's URL, and paste it into a second browser window.</span></span> <span data-ttu-id="578dd-224">Arrastre la forma de una de las ventanas del explorador; debe mover la forma en la ventana del explorador.</span><span class="sxs-lookup"><span data-stu-id="578dd-224">Drag the shape in one of the browser windows; the shape in the other browser window should move.</span></span> <span data-ttu-id="578dd-225">El movimiento de la forma en la ventana debe aparecer menos irregular como su movimiento se interpola a través del tiempo, en lugar de que se va a establecer una vez por cada mensaje entrante.</span><span class="sxs-lookup"><span data-stu-id="578dd-225">The movement of the shape in the other window should appear less jerky as its movement is interpolated over time rather than being set once per incoming message.</span></span>

    ![La ventana de la aplicación](tutorial-high-frequency-realtime-with-signalr/_static/image7.png)

<a id="furthersteps"></a>

## <a name="further-steps"></a><span data-ttu-id="578dd-227">Pasos adicionales</span><span class="sxs-lookup"><span data-stu-id="578dd-227">Further Steps</span></span>

<span data-ttu-id="578dd-228">En este tutorial, ha aprendido cómo programar una aplicación de SignalR que envía mensajes de alta frecuencia entre clientes y servidores.</span><span class="sxs-lookup"><span data-stu-id="578dd-228">In this tutorial, you've learned how to program a SignalR application that sends high-frequency messages between clients and servers.</span></span> <span data-ttu-id="578dd-229">Este paradigma de comunicación es útil para el desarrollo de juegos en línea y otras simulaciones, tales como [el juego ShootR creado con SignalR](http://shootr.signalr.net).</span><span class="sxs-lookup"><span data-stu-id="578dd-229">This communication paradigm is useful for developing online games and other simulations, such as [the ShootR game created with SignalR](http://shootr.signalr.net).</span></span>

<span data-ttu-id="578dd-230">La aplicación completa creada en este tutorial se puede descargar desde [Galería de código](https://code.msdn.microsoft.com/SignalR-MoveShape-demo-3366dac6).</span><span class="sxs-lookup"><span data-stu-id="578dd-230">The complete application created in this tutorial can be downloaded from [Code Gallery](https://code.msdn.microsoft.com/SignalR-MoveShape-demo-3366dac6).</span></span>

<span data-ttu-id="578dd-231">Para obtener más información sobre los conceptos de desarrollo de SignalR, visite los sitios siguientes para recursos y código fuente de SignalR:</span><span class="sxs-lookup"><span data-stu-id="578dd-231">To learn more about SignalR development concepts, visit the following sites for SignalR source code and resources:</span></span>

- [<span data-ttu-id="578dd-232">SignalR Project</span><span class="sxs-lookup"><span data-stu-id="578dd-232">SignalR Project</span></span>](http://signalr.net)
- [<span data-ttu-id="578dd-233">SignalR Github y ejemplos</span><span class="sxs-lookup"><span data-stu-id="578dd-233">SignalR Github and Samples</span></span>](https://github.com/SignalR/SignalR)
- [<span data-ttu-id="578dd-234">SignalR Wiki</span><span class="sxs-lookup"><span data-stu-id="578dd-234">SignalR Wiki</span></span>](https://github.com/SignalR/SignalR/wiki)
