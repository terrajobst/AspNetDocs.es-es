---
uid: signalr/overview/older-versions/troubleshooting
title: Solución de problemas de signalr (Signalr 1. x) | Microsoft Docs
author: bradygaster
description: En este artículo se describen los problemas comunes relacionados con el desarrollo de aplicaciones de Signalr.
ms.author: bradyg
ms.date: 06/05/2013
ms.assetid: 347210ba-c452-4feb-886f-b51d89f58971
msc.legacyurl: /signalr/overview/older-versions/troubleshooting
msc.type: authoredcontent
ms.openlocfilehash: 3dbf091ac2daa4da477b405852bb4d1316584fcd
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 03/06/2020
ms.locfileid: "78468103"
---
# <a name="signalr-troubleshooting-signalr-1x"></a><span data-ttu-id="fc28d-103">Solución de problemas de SignalR (SignalR 1.x)</span><span class="sxs-lookup"><span data-stu-id="fc28d-103">SignalR Troubleshooting (SignalR 1.x)</span></span>

<span data-ttu-id="fc28d-104">por [Patrick Fletcher](https://github.com/pfletcher)</span><span class="sxs-lookup"><span data-stu-id="fc28d-104">by [Patrick Fletcher](https://github.com/pfletcher)</span></span>

[!INCLUDE [Consider ASP.NET Core SignalR](~/includes/signalr/signalr-version-disambiguation.md)]

> <span data-ttu-id="fc28d-105">En este documento se describen los problemas comunes de la solución de problemas con Signalr.</span><span class="sxs-lookup"><span data-stu-id="fc28d-105">This document describes common troubleshooting issues with SignalR.</span></span>

<span data-ttu-id="fc28d-106">Este documento contiene las siguientes secciones.</span><span class="sxs-lookup"><span data-stu-id="fc28d-106">This document contains the following sections.</span></span>

- [<span data-ttu-id="fc28d-107">No se puede llamar a métodos entre el cliente y el servidor de forma silenciosa</span><span class="sxs-lookup"><span data-stu-id="fc28d-107">Calling methods between the client and server silently fails</span></span>](#connection)
- [<span data-ttu-id="fc28d-108">Otros problemas de conexión</span><span class="sxs-lookup"><span data-stu-id="fc28d-108">Other connection issues</span></span>](#other)
- [<span data-ttu-id="fc28d-109">Errores de compilación y de servidor</span><span class="sxs-lookup"><span data-stu-id="fc28d-109">Compilation and server-side errors</span></span>](#server)
- [<span data-ttu-id="fc28d-110">Problemas de Visual Studio</span><span class="sxs-lookup"><span data-stu-id="fc28d-110">Visual Studio issues</span></span>](#vs)
- [<span data-ttu-id="fc28d-111">Problemas de Internet Information Services</span><span class="sxs-lookup"><span data-stu-id="fc28d-111">Internet Information Services issues</span></span>](#iis)
- [<span data-ttu-id="fc28d-112">Problemas de Azure</span><span class="sxs-lookup"><span data-stu-id="fc28d-112">Azure issues</span></span>](#azure)

<a id="connection"></a>

## <a name="calling-methods-between-the-client-and-server-silently-fails"></a><span data-ttu-id="fc28d-113">No se puede llamar a métodos entre el cliente y el servidor de forma silenciosa</span><span class="sxs-lookup"><span data-stu-id="fc28d-113">Calling methods between the client and server silently fails</span></span>

<span data-ttu-id="fc28d-114">En esta sección se describen las posibles causas de que se produzca un error en una llamada de método entre el cliente y el servidor sin un mensaje de error significativo.</span><span class="sxs-lookup"><span data-stu-id="fc28d-114">This section describes possible causes for a method call between client and server to fail without a meaningful error message.</span></span> <span data-ttu-id="fc28d-115">En una aplicación Signalr, el servidor no tiene información sobre los métodos que implementa el cliente; Cuando el servidor invoca un método de cliente, el nombre del método y los datos de parámetro se envían al cliente, y el método se ejecuta solo si existe en el formato especificado por el servidor.</span><span class="sxs-lookup"><span data-stu-id="fc28d-115">In a SignalR application, the server has no information about the methods that the client implements; when the server invokes a client method, the method name and parameter data are sent to the client, and the method is executed only if it exists in the format that the server specified.</span></span> <span data-ttu-id="fc28d-116">Si no se encuentra ningún método coincidente en el cliente, no sucede nada y no se genera ningún mensaje de error en el servidor.</span><span class="sxs-lookup"><span data-stu-id="fc28d-116">If no matching method is found on the client, nothing happens, and no error message is raised on the server.</span></span>

<span data-ttu-id="fc28d-117">Para investigar aún más los métodos de cliente que no se llaman, puede activar el registro antes de llamar al método Start en el concentrador para ver qué llamadas provienen del servidor.</span><span class="sxs-lookup"><span data-stu-id="fc28d-117">To further investigate client methods not getting called, you can turn on logging before the calling the start method on the hub to see what calls are coming from the server.</span></span> <span data-ttu-id="fc28d-118">Para habilitar el registro en una aplicación de JavaScript, consulte [Cómo habilitar el registro del lado cliente (versión del cliente JavaScript)](../guide-to-the-api/hubs-api-guide-javascript-client.md#logging).</span><span class="sxs-lookup"><span data-stu-id="fc28d-118">To enable logging in a JavaScript application, see [How to enable client-side logging (JavaScript client version)](../guide-to-the-api/hubs-api-guide-javascript-client.md#logging).</span></span> <span data-ttu-id="fc28d-119">Para habilitar el registro en una aplicación cliente .NET, consulte [Cómo habilitar el registro del lado cliente (versión del cliente .net)](../guide-to-the-api/hubs-api-guide-net-client.md#logging).</span><span class="sxs-lookup"><span data-stu-id="fc28d-119">To enable logging in a .NET client application, see [How to enable client-side logging (.NET Client version)](../guide-to-the-api/hubs-api-guide-net-client.md#logging).</span></span>

### <a name="misspelled-method-incorrect-method-signature-or-incorrect-hub-name"></a><span data-ttu-id="fc28d-120">Método mal escrito, firma de método incorrecta o nombre de centro incorrecto</span><span class="sxs-lookup"><span data-stu-id="fc28d-120">Misspelled method, incorrect method signature, or incorrect hub name</span></span>

<span data-ttu-id="fc28d-121">Si el nombre o la firma de un método llamado no coincide exactamente con un método adecuado en el cliente, se producirá un error en la llamada.</span><span class="sxs-lookup"><span data-stu-id="fc28d-121">If the name or signature of a called method does not exactly match an appropriate method on the client, the call will fail.</span></span> <span data-ttu-id="fc28d-122">Compruebe que el nombre del método al que llama el servidor coincide con el nombre del método en el cliente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-122">Verify that the method name called by the server matches the name of the method on the client.</span></span> <span data-ttu-id="fc28d-123">Además, Signalr crea el proxy de concentrador mediante métodos con grafía Camel, como es adecuado en JavaScript, por lo que un método denominado `SendMessage` en el servidor se denominará `sendMessage` en el proxy del cliente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-123">Also, SignalR creates the hub proxy using camel-cased methods, as is appropriate in JavaScript, so a method called `SendMessage` on the server would be called `sendMessage` in the client proxy.</span></span> <span data-ttu-id="fc28d-124">Si usa el atributo `HubName` en el código del lado servidor, compruebe que el nombre utilizado coincide con el nombre usado para crear el concentrador en el cliente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-124">If you use the `HubName` attribute in your server-side code, verify that the name used matches the name used to create the hub on the client.</span></span> <span data-ttu-id="fc28d-125">Si no usa el atributo `HubName`, compruebe que el nombre del concentrador en un cliente de JavaScript tiene mayúsculas y minúsculas Camel, como chatHub en lugar de ChatHub.</span><span class="sxs-lookup"><span data-stu-id="fc28d-125">If you do not use the `HubName` attribute, verify that the name of the hub in a JavaScript client is camel-cased, such as chatHub instead of ChatHub.</span></span>

### <a name="duplicate-method-name-on-client"></a><span data-ttu-id="fc28d-126">Nombre de método duplicado en el cliente</span><span class="sxs-lookup"><span data-stu-id="fc28d-126">Duplicate method name on client</span></span>

<span data-ttu-id="fc28d-127">Compruebe que no tiene un método duplicado en el cliente que se diferencie solo por el uso de mayúsculas y minúsculas.</span><span class="sxs-lookup"><span data-stu-id="fc28d-127">Verify that you do not have a duplicate method on the client that differs only by case.</span></span> <span data-ttu-id="fc28d-128">Si la aplicación cliente tiene un método denominado `sendMessage`, compruebe que tampoco hay también un método denominado `SendMessage`.</span><span class="sxs-lookup"><span data-stu-id="fc28d-128">If your client application has a method called `sendMessage`, verify that there isn't also a method called `SendMessage` as well.</span></span>

### <a name="missing-json-parser-on-the-client"></a><span data-ttu-id="fc28d-129">Falta el analizador JSON en el cliente</span><span class="sxs-lookup"><span data-stu-id="fc28d-129">Missing JSON parser on the client</span></span>

<span data-ttu-id="fc28d-130">Signalr requiere que haya un analizador JSON presente para serializar las llamadas entre el servidor y el cliente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-130">SignalR requires a JSON parser to be present to serialize calls between the server and the client.</span></span> <span data-ttu-id="fc28d-131">Si el cliente no tiene un analizador JSON integrado (por ejemplo, Internet Explorer 7), deberá incluir uno en la aplicación.</span><span class="sxs-lookup"><span data-stu-id="fc28d-131">If your client doesn't have a built-in JSON parser (such as Internet Explorer 7), you'll need to include one in your application.</span></span> <span data-ttu-id="fc28d-132">Puede descargar el analizador JSON [aquí](http://nuget.org/packages/json2).</span><span class="sxs-lookup"><span data-stu-id="fc28d-132">You can download the JSON parser [here](http://nuget.org/packages/json2).</span></span>

### <a name="mixing-hub-and-persistentconnection-syntax"></a><span data-ttu-id="fc28d-133">Combinación de la sintaxis de Hub y PersistentConnection</span><span class="sxs-lookup"><span data-stu-id="fc28d-133">Mixing Hub and PersistentConnection syntax</span></span>

<span data-ttu-id="fc28d-134">Signalr usa dos modelos de comunicación: hubs y PersistentConnections.</span><span class="sxs-lookup"><span data-stu-id="fc28d-134">SignalR uses two communication models: Hubs and PersistentConnections.</span></span> <span data-ttu-id="fc28d-135">La sintaxis para llamar a estos dos modelos de comunicación es diferente en el código de cliente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-135">The syntax for calling these two communication models is different in the client code.</span></span> <span data-ttu-id="fc28d-136">Si ha agregado un concentrador en el código del servidor, compruebe que todo el código de cliente utiliza la sintaxis de concentrador adecuada.</span><span class="sxs-lookup"><span data-stu-id="fc28d-136">If you have added a hub in your server code, verify that all of your client code uses the proper hub syntax.</span></span>

<span data-ttu-id="fc28d-137">**Código de cliente de JavaScript que crea un PersistentConnection en un cliente de JavaScript**</span><span class="sxs-lookup"><span data-stu-id="fc28d-137">**JavaScript client code that creates a PersistentConnection in a JavaScript client**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample1.js)]

<span data-ttu-id="fc28d-138">**Código de cliente de JavaScript que crea un proxy de concentrador en un cliente de JavaScript**</span><span class="sxs-lookup"><span data-stu-id="fc28d-138">**JavaScript client code that creates a Hub Proxy in a Javascript client**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample2.js)]

<span data-ttu-id="fc28d-139">**C#código de servidor que asigna una ruta a un PersistentConnection**</span><span class="sxs-lookup"><span data-stu-id="fc28d-139">**C# server code that maps a route to a PersistentConnection**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample3.cs)]

<span data-ttu-id="fc28d-140">**C#código de servidor que asigna una ruta a un concentrador o a varios centros si tiene varias aplicaciones**</span><span class="sxs-lookup"><span data-stu-id="fc28d-140">**C# server code that maps a route to a Hub, or to multiple hubs if you have multiple applications**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample4.cs)]

### <a name="connection-started-before-subscriptions-are-added"></a><span data-ttu-id="fc28d-141">Conexión iniciada antes de agregar las suscripciones</span><span class="sxs-lookup"><span data-stu-id="fc28d-141">Connection started before subscriptions are added</span></span>

<span data-ttu-id="fc28d-142">Si se inicia la conexión del concentrador antes de que se agreguen al proxy los métodos a los que se puede llamar desde el servidor, no se recibirán los mensajes.</span><span class="sxs-lookup"><span data-stu-id="fc28d-142">If the Hub's connection is started before methods that can be called from the server are added to the proxy, messages will not be received.</span></span> <span data-ttu-id="fc28d-143">El siguiente código de JavaScript no iniciará correctamente el concentrador:</span><span class="sxs-lookup"><span data-stu-id="fc28d-143">The following JavaScript code will not start the hub properly:</span></span>

<span data-ttu-id="fc28d-144">**Código de cliente de JavaScript incorrecto que no permitirá la recepción de mensajes de concentradores**</span><span class="sxs-lookup"><span data-stu-id="fc28d-144">**Incorrect JavaScript client code that will not allow Hubs messages to be received**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample5.js)]

<span data-ttu-id="fc28d-145">En su lugar, agregue las suscripciones de método antes de llamar a Start:</span><span class="sxs-lookup"><span data-stu-id="fc28d-145">Instead, add the method subscriptions before calling Start:</span></span>

<span data-ttu-id="fc28d-146">**Código de cliente de JavaScript que agrega correctamente suscripciones a un centro**</span><span class="sxs-lookup"><span data-stu-id="fc28d-146">**JavaScript client code that correctly adds subscriptions to a hub**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample6.js)]

### <a name="missing-method-name-on-the-hub-proxy"></a><span data-ttu-id="fc28d-147">Falta el nombre de método en el proxy de concentrador</span><span class="sxs-lookup"><span data-stu-id="fc28d-147">Missing method name on the hub proxy</span></span>

<span data-ttu-id="fc28d-148">Compruebe que el método definido en el servidor está suscrito a en el cliente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-148">Verify that the method defined on the server is subscribed to on the client.</span></span> <span data-ttu-id="fc28d-149">Aunque el servidor define el método, todavía debe agregarse al proxy del cliente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-149">Even though the server defines the method, it must still be added to the client proxy.</span></span> <span data-ttu-id="fc28d-150">Los métodos se pueden agregar al proxy de cliente de las maneras siguientes (tenga en cuenta que el método se agrega al miembro `client` del concentrador, no al centro directamente):</span><span class="sxs-lookup"><span data-stu-id="fc28d-150">Methods can be added to the client proxy in the following ways (Note that the method is added to the `client` member of the hub, not the hub directly):</span></span>

<span data-ttu-id="fc28d-151">**Código de cliente de JavaScript que agrega métodos a un proxy de concentrador**</span><span class="sxs-lookup"><span data-stu-id="fc28d-151">**JavaScript client code that adds methods to a hub proxy**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample7.js)]

### <a name="hub-or-hub-methods-not-declared-as-public"></a><span data-ttu-id="fc28d-152">Métodos centrales o concentradores no declarados como públicos</span><span class="sxs-lookup"><span data-stu-id="fc28d-152">Hub or hub methods not declared as Public</span></span>

<span data-ttu-id="fc28d-153">Para que sea visible en el cliente, la implementación y los métodos del concentrador se deben declarar como `public`.</span><span class="sxs-lookup"><span data-stu-id="fc28d-153">To be visible on the client, the hub implementation and methods must be declared as `public`.</span></span>

### <a name="accessing-hub-from-a-different-application"></a><span data-ttu-id="fc28d-154">Acceder a la central desde una aplicación diferente</span><span class="sxs-lookup"><span data-stu-id="fc28d-154">Accessing hub from a different application</span></span>

<span data-ttu-id="fc28d-155">Solo se puede tener acceso a los concentradores de signalr a través de aplicaciones que implementan clientes de Signalr.</span><span class="sxs-lookup"><span data-stu-id="fc28d-155">SignalR Hubs can only be accessed through applications that implement SignalR clients.</span></span> <span data-ttu-id="fc28d-156">Signalr no puede interoperar con otras bibliotecas de comunicación (como SOAP o servicios Web WCF). Si no hay ningún cliente de Signalr disponible para la plataforma de destino, no puede tener acceso directamente al punto de conexión del servidor.</span><span class="sxs-lookup"><span data-stu-id="fc28d-156">SignalR cannot interoperate with other communication libraries (like SOAP or WCF web services.) If there is no SignalR client available for your target platform, you cannot access the server's endpoint directly.</span></span>

### <a name="manually-serializing-data"></a><span data-ttu-id="fc28d-157">Serializar datos manualmente</span><span class="sxs-lookup"><span data-stu-id="fc28d-157">Manually serializing data</span></span>

<span data-ttu-id="fc28d-158">Signalr usará automáticamente JSON para serializar los parámetros de método, por lo que no es necesario hacerlo usted mismo.</span><span class="sxs-lookup"><span data-stu-id="fc28d-158">SignalR will automatically use JSON to serialize your method parameters- there's no need to do it yourself.</span></span>

### <a name="remote-hub-method-not-executed-on-client-in-ondisconnected-function"></a><span data-ttu-id="fc28d-159">Método de concentrador remoto no ejecutado en el cliente en la función OnDisconnection</span><span class="sxs-lookup"><span data-stu-id="fc28d-159">Remote Hub method not executed on client in OnDisconnected function</span></span>

<span data-ttu-id="fc28d-160">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="fc28d-160">This behavior is by design.</span></span> <span data-ttu-id="fc28d-161">Cuando se llama a `OnDisconnected`, el concentrador ya ha entrado en el estado `Disconnected`, lo que no permite llamar a más métodos de concentrador.</span><span class="sxs-lookup"><span data-stu-id="fc28d-161">When `OnDisconnected` is called, the hub has already entered the `Disconnected` state, which does not allow further hub methods to be called.</span></span>

<span data-ttu-id="fc28d-162">**C#código de servidor que ejecuta correctamente el código en el evento OnDisconnection**</span><span class="sxs-lookup"><span data-stu-id="fc28d-162">**C# server code that correctly executes code in the OnDisconnected event**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample8.cs)]

### <a name="connection-limit-reached"></a><span data-ttu-id="fc28d-163">Se alcanzó el límite de conexiones</span><span class="sxs-lookup"><span data-stu-id="fc28d-163">Connection limit reached</span></span>

<span data-ttu-id="fc28d-164">Cuando se usa la versión completa de IIS en un sistema operativo cliente como Windows 7, se impone un límite de 10 conexiones.</span><span class="sxs-lookup"><span data-stu-id="fc28d-164">When using the full version of IIS on a client operating system like Windows 7, a 10-connection limit is imposed.</span></span> <span data-ttu-id="fc28d-165">Al utilizar un sistema operativo de cliente, use IIS Express en su lugar para evitar este límite.</span><span class="sxs-lookup"><span data-stu-id="fc28d-165">When using a client OS, use IIS Express instead to avoid this limit.</span></span>

### <a name="cross-domain-connection-not-set-up-properly"></a><span data-ttu-id="fc28d-166">La conexión entre dominios no está configurada correctamente</span><span class="sxs-lookup"><span data-stu-id="fc28d-166">Cross-domain connection not set up properly</span></span>

<span data-ttu-id="fc28d-167">Si una conexión entre dominios (una conexión para la que la dirección URL de Signalr no está en el mismo dominio que la página de hospedaje) no está configurada correctamente, se puede producir un error en la conexión sin un mensaje de error.</span><span class="sxs-lookup"><span data-stu-id="fc28d-167">If a cross-domain connection (a connection for which the SignalR URL is not in the same domain as the hosting page) is not set up correctly, the connection may fail without an error message.</span></span> <span data-ttu-id="fc28d-168">Para obtener información sobre cómo habilitar la comunicación entre dominios, vea [cómo establecer una conexión entre dominios](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span><span class="sxs-lookup"><span data-stu-id="fc28d-168">For information on how to enable cross-domain communication, see [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span></span>

### <a name="connection-using-ntlm-active-directory-not-working-in-net-client"></a><span data-ttu-id="fc28d-169">La conexión con NTLM (Active Directory) no funciona en el cliente de .NET</span><span class="sxs-lookup"><span data-stu-id="fc28d-169">Connection using NTLM (Active Directory) not working in .NET client</span></span>

<span data-ttu-id="fc28d-170">Si la conexión no está configurada correctamente, se puede producir un error en una conexión en una aplicación cliente .NET que use la seguridad del dominio.</span><span class="sxs-lookup"><span data-stu-id="fc28d-170">A connection in a .NET client application that uses Domain security may fail if the connection is not configured properly.</span></span> <span data-ttu-id="fc28d-171">Para usar Signalr en un entorno de dominio, establezca la propiedad de conexión necesaria como se indica a continuación:</span><span class="sxs-lookup"><span data-stu-id="fc28d-171">To use SignalR in a domain environment, set the requisite connection property as follows:</span></span>

<span data-ttu-id="fc28d-172">**C#código de cliente que implementa credenciales de conexión**</span><span class="sxs-lookup"><span data-stu-id="fc28d-172">**C# client code that implements connection credentials**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample9.cs)]

<a id="other"></a>

## <a name="other-connection-issues"></a><span data-ttu-id="fc28d-173">Otros problemas de conexión</span><span class="sxs-lookup"><span data-stu-id="fc28d-173">Other connection issues</span></span>

<span data-ttu-id="fc28d-174">En esta sección se describen las causas y soluciones de los síntomas o mensajes de error específicos que se producen durante una conexión.</span><span class="sxs-lookup"><span data-stu-id="fc28d-174">This section describes the causes and solutions for specific symptoms or error messages that occur during a connection.</span></span>

### <a name="start-must-be-called-before-data-can-be-sent-error"></a><span data-ttu-id="fc28d-175">Error "se debe llamar a iniciar antes de enviar datos"</span><span class="sxs-lookup"><span data-stu-id="fc28d-175">"Start must be called before data can be sent" error</span></span>

<span data-ttu-id="fc28d-176">Este error se suele observar si el código hace referencia a objetos de Signalr antes de que se inicie la conexión.</span><span class="sxs-lookup"><span data-stu-id="fc28d-176">This error is commonly seen if code references SignalR objects before the connection is started.</span></span> <span data-ttu-id="fc28d-177">El wireup para los controladores y el like que llamarán a los métodos definidos en el servidor deben agregarse después de que se complete la conexión.</span><span class="sxs-lookup"><span data-stu-id="fc28d-177">The wireup for handlers and the like that will call methods defined on the server must be added after the connection completes.</span></span> <span data-ttu-id="fc28d-178">Tenga en cuenta que la llamada a `Start` es asincrónica, por lo que el código después de la llamada se puede ejecutar antes de que se complete.</span><span class="sxs-lookup"><span data-stu-id="fc28d-178">Note that the call to `Start` is asynchronous, so code after the call may be executed before it completes.</span></span> <span data-ttu-id="fc28d-179">La mejor manera de agregar controladores después de que una conexión se inicia por completo es colocarlos en una función de devolución de llamada que se pasa como un parámetro al método de Inicio:</span><span class="sxs-lookup"><span data-stu-id="fc28d-179">The best way to add handlers after a connection starts completely is to put them into a callback function that is passed as a parameter to the start method:</span></span>

<span data-ttu-id="fc28d-180">**Código de cliente de JavaScript que agrega correctamente controladores de eventos que hacen referencia a objetos de Signalr**</span><span class="sxs-lookup"><span data-stu-id="fc28d-180">**JavaScript client code that correctly adds event handlers that reference SignalR objects**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample10.js?highlight=1)]

<span data-ttu-id="fc28d-181">Este error también se verá si se detiene una conexión mientras todavía se hace referencia a los objetos de Signalr.</span><span class="sxs-lookup"><span data-stu-id="fc28d-181">This error will also be seen if a connection stops while SignalR objects are still being referenced.</span></span>

### <a name="301-moved-permanently-or-302-moved-temporarily-error"></a><span data-ttu-id="fc28d-182">error "301 se ha cambiado permanentemente" o "302 se ha pasado temporalmente"</span><span class="sxs-lookup"><span data-stu-id="fc28d-182">"301 Moved Permanently" or "302 Moved Temporarily" error</span></span>

<span data-ttu-id="fc28d-183">Este error puede aparecer si el proyecto contiene una carpeta llamada Signalr, que interferirá con el proxy creado automáticamente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-183">This error may be seen if the project contains a folder called SignalR, which will interfere with the automatically-created proxy.</span></span> <span data-ttu-id="fc28d-184">Para evitar este error, no use una carpeta llamada `SignalR` en la aplicación o Active la generación automática de proxy.</span><span class="sxs-lookup"><span data-stu-id="fc28d-184">To avoid this error, do not use a folder called `SignalR` in your application, or turn automatic proxy generation off.</span></span> <span data-ttu-id="fc28d-185">Vea [el proxy generado y lo que hace para](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy) obtener más detalles.</span><span class="sxs-lookup"><span data-stu-id="fc28d-185">See [The Generated Proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy) for more details.</span></span>

### <a name="403-forbidden-error-in-net-or-silverlight-client"></a><span data-ttu-id="fc28d-186">error "403 prohibido" en .NET o en el cliente de Silverlight</span><span class="sxs-lookup"><span data-stu-id="fc28d-186">"403 Forbidden" error in .NET or Silverlight client</span></span>

<span data-ttu-id="fc28d-187">Este error puede producirse en entornos entre dominios en los que la comunicación entre dominios no se ha habilitado correctamente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-187">This error may occur in cross-domain environments where cross-domain communication is not properly enabled.</span></span> <span data-ttu-id="fc28d-188">Para obtener información sobre cómo habilitar la comunicación entre dominios, vea [cómo establecer una conexión entre dominios](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span><span class="sxs-lookup"><span data-stu-id="fc28d-188">For information on how to enable cross-domain communication, see [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span></span> <span data-ttu-id="fc28d-189">Para establecer una conexión entre dominios en un cliente de Silverlight, vea [conexiones entre dominios desde clientes de Silverlight](../guide-to-the-api/hubs-api-guide-net-client.md#slcrossdomain).</span><span class="sxs-lookup"><span data-stu-id="fc28d-189">To establish a cross-domain connection in a Silverlight client, see [Cross-domain connections from Silverlight clients](../guide-to-the-api/hubs-api-guide-net-client.md#slcrossdomain).</span></span>

### <a name="404-not-found-error"></a><span data-ttu-id="fc28d-190">error "404 no encontrado"</span><span class="sxs-lookup"><span data-stu-id="fc28d-190">"404 Not Found" error</span></span>

<span data-ttu-id="fc28d-191">Hay varias causas para este problema.</span><span class="sxs-lookup"><span data-stu-id="fc28d-191">There are several causes for this issue.</span></span> <span data-ttu-id="fc28d-192">Compruebe lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="fc28d-192">Verify all of the following:</span></span>

- <span data-ttu-id="fc28d-193">**Referencia de dirección de proxy de concentrador no formateada correctamente:** Este error suele producirse si la referencia a la dirección del proxy del concentrador generada no tiene el formato correcto.</span><span class="sxs-lookup"><span data-stu-id="fc28d-193">**Hub proxy address reference not formatted correctly:** This error is commonly seen if the reference to the generated hub proxy address is not formatted correctly.</span></span> <span data-ttu-id="fc28d-194">Compruebe que la referencia a la dirección del concentrador se ha realizado correctamente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-194">Verify that the reference to the hub address is made properly.</span></span> <span data-ttu-id="fc28d-195">Vea [Cómo hacer referencia al proxy generado dinámicamente](../guide-to-the-api/hubs-api-guide-javascript-client.md#dynamicproxy) para obtener más información.</span><span class="sxs-lookup"><span data-stu-id="fc28d-195">See [How to reference the dynamically generated proxy](../guide-to-the-api/hubs-api-guide-javascript-client.md#dynamicproxy) for details.</span></span>
- <span data-ttu-id="fc28d-196">**Agregar rutas a la aplicación antes de agregar la ruta del concentrador:** Si la aplicación utiliza otras rutas, compruebe que la primera ruta agregada es la llamada a `MapHubs`.</span><span class="sxs-lookup"><span data-stu-id="fc28d-196">**Adding routes to application before adding the hub route:** If your application uses other routes, verify that the first route added is the call to `MapHubs`.</span></span>

### <a name="500-internal-server-error"></a><span data-ttu-id="fc28d-197">"500 error interno del servidor"</span><span class="sxs-lookup"><span data-stu-id="fc28d-197">"500 Internal Server Error"</span></span>

<span data-ttu-id="fc28d-198">Se trata de un error muy genérico que podría tener una gran variedad de causas.</span><span class="sxs-lookup"><span data-stu-id="fc28d-198">This is a very generic error that could have a wide variety of causes.</span></span> <span data-ttu-id="fc28d-199">Los detalles del error deben aparecer en el registro de eventos del servidor o pueden encontrarse a través de la depuración del servidor.</span><span class="sxs-lookup"><span data-stu-id="fc28d-199">The details of the error should appear in the server's event log, or can be found through debugging the server.</span></span> <span data-ttu-id="fc28d-200">Se puede obtener información de error más detallada si se activan los errores detallados en el servidor.</span><span class="sxs-lookup"><span data-stu-id="fc28d-200">More detailed error information may be obtained by turning on detailed errors on the server.</span></span> <span data-ttu-id="fc28d-201">Para obtener más información, vea [Cómo controlar errores en la clase hub](../guide-to-the-api/hubs-api-guide-server.md#handleErrors).</span><span class="sxs-lookup"><span data-stu-id="fc28d-201">For more information, see [How to handle errors in the Hub class](../guide-to-the-api/hubs-api-guide-server.md#handleErrors).</span></span>

### <a name="typeerror-lthubtypegt-is-undefined-error"></a><span data-ttu-id="fc28d-202">Error "TypeError: &lt;hubType&gt; no está definido"</span><span class="sxs-lookup"><span data-stu-id="fc28d-202">"TypeError: &lt;hubType&gt; is undefined" error</span></span>

<span data-ttu-id="fc28d-203">Este error se producirá si la llamada a `MapHubs` no se realiza correctamente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-203">This error will result if the call to `MapHubs` is not made properly.</span></span> <span data-ttu-id="fc28d-204">Vea [Cómo registrar la ruta de signalr y configurar las opciones de signalr](../guide-to-the-api/hubs-api-guide-server.md#route) para obtener más información.</span><span class="sxs-lookup"><span data-stu-id="fc28d-204">See [How to register the SignalR route and configure SignalR options](../guide-to-the-api/hubs-api-guide-server.md#route) for more information.</span></span>

### <a name="jsonserializationexception-was-unhandled-by-user-code"></a><span data-ttu-id="fc28d-205">El código de usuario no controló JsonSerializationException</span><span class="sxs-lookup"><span data-stu-id="fc28d-205">JsonSerializationException was unhandled by user code</span></span>

<span data-ttu-id="fc28d-206">Compruebe que los parámetros que envía a los métodos no incluyen tipos no serializables (como identificadores de archivo o conexiones de bases de datos).</span><span class="sxs-lookup"><span data-stu-id="fc28d-206">Verify that the parameters you send to your methods do not include non-serializable types (like file handles or database connections).</span></span> <span data-ttu-id="fc28d-207">Si necesita usar miembros en un objeto del servidor que no desea que se envíe al cliente (ya sea por motivos de seguridad o por motivos de serialización), use el atributo `JSONIgnore`.</span><span class="sxs-lookup"><span data-stu-id="fc28d-207">If you need to use members on a server-side object that you don't want to be sent to the client (either for security or for reasons of serialization), use the `JSONIgnore` attribute.</span></span>

### <a name="protocol-error-unknown-transport-error"></a><span data-ttu-id="fc28d-208">Error "error de protocolo: transporte desconocido"</span><span class="sxs-lookup"><span data-stu-id="fc28d-208">"Protocol error: Unknown transport" error</span></span>

<span data-ttu-id="fc28d-209">Este error puede producirse si el cliente no admite los transportes que utiliza Signalr.</span><span class="sxs-lookup"><span data-stu-id="fc28d-209">This error may occur if the client does not support the transports that SignalR uses.</span></span> <span data-ttu-id="fc28d-210">Consulte [transportes y reservas](../getting-started/introduction-to-signalr.md#transports) para obtener información sobre los exploradores que se pueden usar con signalr.</span><span class="sxs-lookup"><span data-stu-id="fc28d-210">See [Transports and Fallbacks](../getting-started/introduction-to-signalr.md#transports) for information on which browsers can be used with SignalR.</span></span>

### <a name="javascript-hub-proxy-generation-has-been-disabled"></a><span data-ttu-id="fc28d-211">"Se ha deshabilitado la generación de proxy de concentrador de JavaScript".</span><span class="sxs-lookup"><span data-stu-id="fc28d-211">"JavaScript Hub proxy generation has been disabled."</span></span>

<span data-ttu-id="fc28d-212">Este error se producirá si se establece `DisableJavaScriptProxies` y se incluye también una referencia al proxy generado dinámicamente en `signalr/hubs`.</span><span class="sxs-lookup"><span data-stu-id="fc28d-212">This error will occur if `DisableJavaScriptProxies` is set while also including a reference to the dynamically generated proxy at `signalr/hubs`.</span></span> <span data-ttu-id="fc28d-213">Para obtener más información sobre la creación manual del proxy, consulte [el proxy generado y lo que hace](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span><span class="sxs-lookup"><span data-stu-id="fc28d-213">For more information on creating the proxy manually, see [The generated proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span></span>

### <a name="the-connection-id-is-in-the-incorrect-format-or-the-user-identity-cannot-change-during-an-active-signalr-connection-error"></a><span data-ttu-id="fc28d-214">"El identificador de conexión tiene el formato incorrecto" o "el error de identidad de usuario no puede cambiar durante una conexión de Signalr activa"</span><span class="sxs-lookup"><span data-stu-id="fc28d-214">"The connection ID is in the incorrect format" or "The user identity cannot change during an active SignalR connection" error</span></span>

<span data-ttu-id="fc28d-215">Este error puede aparecer si se utiliza la autenticación y el cliente se ha cerrado la sesión antes de que se detenga la conexión.</span><span class="sxs-lookup"><span data-stu-id="fc28d-215">This error may be seen if authentication is being used, and the client is logged out before the connection is stopped.</span></span> <span data-ttu-id="fc28d-216">La solución consiste en detener la conexión de Signalr antes de cerrar la sesión del cliente.</span><span class="sxs-lookup"><span data-stu-id="fc28d-216">The solution is to stop the SignalR connection before logging the client out.</span></span>

### <a name="uncaught-error-signalr-jquery-not-found-please-ensure-jquery-is-referenced-before-the-signalrjs-file-error"></a><span data-ttu-id="fc28d-217">"Error no detectado: Signalr: no se encontró jQuery.</span><span class="sxs-lookup"><span data-stu-id="fc28d-217">"Uncaught Error: SignalR: jQuery not found.</span></span> <span data-ttu-id="fc28d-218">Asegúrese de que se hace referencia a jQuery antes del error del archivo Signalr. js.</span><span class="sxs-lookup"><span data-stu-id="fc28d-218">Please ensure jQuery is referenced before the SignalR.js file" error</span></span>

<span data-ttu-id="fc28d-219">El cliente de Signalr JavaScript requiere que jQuery se ejecute.</span><span class="sxs-lookup"><span data-stu-id="fc28d-219">The SignalR JavaScript client requires jQuery to run.</span></span> <span data-ttu-id="fc28d-220">Compruebe que la referencia a jQuery es correcta, que la ruta de acceso usada es válida y que la referencia a jQuery es anterior a la referencia a Signalr.</span><span class="sxs-lookup"><span data-stu-id="fc28d-220">Verify that your reference to jQuery is correct, that the path used is valid, and that the reference to jQuery is before the reference to SignalR.</span></span>

### <a name="uncaught-typeerror-cannot-read-property-ltpropertygt-of-undefined-error"></a><span data-ttu-id="fc28d-221">"No se detectó el TypeError: no se puede leer la propiedad '&lt;la propiedad&gt;' de un error no definido '</span><span class="sxs-lookup"><span data-stu-id="fc28d-221">"Uncaught TypeError: Cannot read property '&lt;property&gt;' of undefined" error</span></span>

<span data-ttu-id="fc28d-222">Este error se produce porque no se hace referencia correctamente a jQuery o al proxy de los concentradores.</span><span class="sxs-lookup"><span data-stu-id="fc28d-222">This error results from not having jQuery or the hubs proxy referenced properly.</span></span> <span data-ttu-id="fc28d-223">Compruebe que la referencia a jQuery y el proxy de hubs es correcta, que la ruta de acceso usada es válida y que la referencia a jQuery es anterior a la referencia al proxy de los concentradores.</span><span class="sxs-lookup"><span data-stu-id="fc28d-223">Verify that your reference to jQuery and the hubs proxy is correct, that the path used is valid, and that the reference to jQuery is before the reference to the hubs proxy.</span></span> <span data-ttu-id="fc28d-224">La referencia predeterminada al proxy de los concentradores debe ser similar a la siguiente:</span><span class="sxs-lookup"><span data-stu-id="fc28d-224">The default reference to the hubs proxy should look like the following:</span></span>

<span data-ttu-id="fc28d-225">**Código HTML del lado cliente que hace referencia correctamente al proxy de los concentradores**</span><span class="sxs-lookup"><span data-stu-id="fc28d-225">**HTML client-side code that correctly references the Hubs proxy**</span></span>

[!code-html[Main](troubleshooting/samples/sample11.html)]

### <a name="runtimebinderexception-was-unhandled-by-user-code-error"></a><span data-ttu-id="fc28d-226">Error "RuntimeBinderException no controlado por el código de usuario"</span><span class="sxs-lookup"><span data-stu-id="fc28d-226">"RuntimeBinderException was unhandled by user code" error</span></span>

<span data-ttu-id="fc28d-227">Este error puede producirse cuando se utiliza la sobrecarga incorrecta de `Hub.On`.</span><span class="sxs-lookup"><span data-stu-id="fc28d-227">This error may occur when the incorrect overload of `Hub.On` is used.</span></span> <span data-ttu-id="fc28d-228">Si el método tiene un valor devuelto, el tipo de valor devuelto debe especificarse como un parámetro de tipo genérico:</span><span class="sxs-lookup"><span data-stu-id="fc28d-228">If the method has a return value, the return type must be specified as a generic type parameter:</span></span>

<span data-ttu-id="fc28d-229">**Método definido en el cliente (sin el proxy generado)**</span><span class="sxs-lookup"><span data-stu-id="fc28d-229">**Method defined on the client (without generated proxy)**</span></span>

[!code-html[Main](troubleshooting/samples/sample12.html?highlight=1)]

### <a name="connection-id-is-inconsistent-or-connection-breaks-between-page-loads"></a><span data-ttu-id="fc28d-230">El ID. de conexión es incoherente o se interrumpe la conexión entre cargas de páginas</span><span class="sxs-lookup"><span data-stu-id="fc28d-230">Connection ID is inconsistent or connection breaks between page loads</span></span>

<span data-ttu-id="fc28d-231">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="fc28d-231">This behavior is by design.</span></span> <span data-ttu-id="fc28d-232">Puesto que el objeto de concentrador se hospeda en el objeto de página, el concentrador se destruye cuando se actualiza la página.</span><span class="sxs-lookup"><span data-stu-id="fc28d-232">Since the hub object is hosted in the page object, the hub is destroyed when the page refreshes.</span></span> <span data-ttu-id="fc28d-233">Una aplicación de varias páginas debe mantener la asociación entre los usuarios y los identificadores de conexión para que sean coherentes entre cargas de páginas.</span><span class="sxs-lookup"><span data-stu-id="fc28d-233">A multi-page application needs to maintain the association between users and connection IDs so that they will be consistent between page loads.</span></span> <span data-ttu-id="fc28d-234">Los identificadores de conexión se pueden almacenar en el servidor en un objeto `ConcurrentDictionary` o en una base de datos.</span><span class="sxs-lookup"><span data-stu-id="fc28d-234">The connection IDs can be stored on the server in either a `ConcurrentDictionary` object or a database.</span></span>

### <a name="value-cannot-be-null-error"></a><span data-ttu-id="fc28d-235">Error "el valor no puede ser nulo"</span><span class="sxs-lookup"><span data-stu-id="fc28d-235">"Value cannot be null" error</span></span>

<span data-ttu-id="fc28d-236">Actualmente no se admiten los métodos del servidor con parámetros opcionales; Si se omite el parámetro opcional, se producirá un error en el método.</span><span class="sxs-lookup"><span data-stu-id="fc28d-236">Server-side methods with optional parameters are not currently supported; if the optional parameter is omitted, the method will fail.</span></span> <span data-ttu-id="fc28d-237">Para obtener más información, vea [parámetros opcionales](https://github.com/SignalR/SignalR/issues/324).</span><span class="sxs-lookup"><span data-stu-id="fc28d-237">For more information, see [Optional Parameters](https://github.com/SignalR/SignalR/issues/324).</span></span>

### <a name="firefox-cant-establish-a-connection-to-the-server-at-ltaddressgt-error-in-firebug"></a><span data-ttu-id="fc28d-238">"Firefox no puede establecer una conexión con el servidor en &lt;dirección&gt;" error en Firebug</span><span class="sxs-lookup"><span data-stu-id="fc28d-238">"Firefox can't establish a connection to the server at &lt;address&gt;" error in Firebug</span></span>

<span data-ttu-id="fc28d-239">Este mensaje de error puede aparecer en Firebug si se produce un error en la negociación del transporte de WebSocket y en su lugar se usa otro transporte.</span><span class="sxs-lookup"><span data-stu-id="fc28d-239">This error message can be seen in Firebug if negotiation of the WebSocket transport fails and another transport is used instead.</span></span> <span data-ttu-id="fc28d-240">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="fc28d-240">This behavior is by design.</span></span>

### <a name="the-remote-certificate-is-invalid-according-to-the-validation-procedure-error-in-net-client-application"></a><span data-ttu-id="fc28d-241">"El certificado remoto no es válido según el procedimiento de validación" en la aplicación cliente de .NET</span><span class="sxs-lookup"><span data-stu-id="fc28d-241">"The remote certificate is invalid according to the validation procedure" error in .NET client application</span></span>

<span data-ttu-id="fc28d-242">Si el servidor requiere certificados de cliente personalizados, puede Agregar un certificado X509 a la conexión antes de que se realice la solicitud.</span><span class="sxs-lookup"><span data-stu-id="fc28d-242">If your server requires custom client certificates, then you can add an x509certificate to the connection before the request is made.</span></span> <span data-ttu-id="fc28d-243">Agregue el certificado a la conexión mediante `Connection.AddClientCertificate`.</span><span class="sxs-lookup"><span data-stu-id="fc28d-243">Add the certificate to the connection using `Connection.AddClientCertificate`.</span></span>

### <a name="connection-drops-after-authentication-times-out"></a><span data-ttu-id="fc28d-244">La conexión se interrumpe después del tiempo de espera de la autenticación</span><span class="sxs-lookup"><span data-stu-id="fc28d-244">Connection drops after authentication times out</span></span>

<span data-ttu-id="fc28d-245">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="fc28d-245">This behavior is by design.</span></span> <span data-ttu-id="fc28d-246">Las credenciales de autenticación no se pueden modificar mientras haya una conexión activa; para actualizar las credenciales, la conexión debe detenerse y reiniciarse.</span><span class="sxs-lookup"><span data-stu-id="fc28d-246">Authentication credentials cannot be modified while a connection is active; to refresh credentials, the connection must be stopped and restarted.</span></span>

### <a name="onconnected-gets-called-twice-when-using-jquery-mobile"></a><span data-ttu-id="fc28d-247">Se llama a alconnected dos veces al usar jQuery Mobile</span><span class="sxs-lookup"><span data-stu-id="fc28d-247">OnConnected gets called twice when using jQuery Mobile</span></span>

<span data-ttu-id="fc28d-248">la función `initializePage` de jQuery Mobile obliga a que se vuelvan a ejecutar los scripts de cada página, con lo que se crea una segunda conexión.</span><span class="sxs-lookup"><span data-stu-id="fc28d-248">jQuery Mobile's `initializePage` function forces the scripts in each page to be re-executed, thus creating a second connection.</span></span> <span data-ttu-id="fc28d-249">Las soluciones para este problema incluyen:</span><span class="sxs-lookup"><span data-stu-id="fc28d-249">Solutions for this issue include:</span></span>

- <span data-ttu-id="fc28d-250">Incluya la referencia a jQuery Mobile antes del archivo de JavaScript.</span><span class="sxs-lookup"><span data-stu-id="fc28d-250">Include the reference to jQuery Mobile before your JavaScript file.</span></span>
- <span data-ttu-id="fc28d-251">Deshabilite la función `initializePage` estableciendo `$.mobile.autoInitializePage = false`.</span><span class="sxs-lookup"><span data-stu-id="fc28d-251">Disable the `initializePage` function by setting `$.mobile.autoInitializePage = false`.</span></span>
- <span data-ttu-id="fc28d-252">Espere a que finalice la inicialización de la página antes de iniciar la conexión.</span><span class="sxs-lookup"><span data-stu-id="fc28d-252">Wait for the page to finish initializing before starting the connection.</span></span>

### <a name="messages-are-delayed-in-silverlight-applications-using-server-sent-events"></a><span data-ttu-id="fc28d-253">Los mensajes se retrasan en las aplicaciones de Silverlight mediante eventos enviados del servidor</span><span class="sxs-lookup"><span data-stu-id="fc28d-253">Messages are delayed in Silverlight applications using Server Sent Events</span></span>

<span data-ttu-id="fc28d-254">Los mensajes se retrasan cuando se usan eventos enviados por el servidor en Silverlight.</span><span class="sxs-lookup"><span data-stu-id="fc28d-254">Messages are delayed when using server sent events on Silverlight.</span></span> <span data-ttu-id="fc28d-255">Para forzar el uso del sondeo largo en su lugar, use lo siguiente al iniciar la conexión:</span><span class="sxs-lookup"><span data-stu-id="fc28d-255">To force long polling to be used instead, use the following when starting the connection:</span></span>

[!code-css[Main](troubleshooting/samples/sample13.css)]

### <a name="permission-denied-using-forever-frame-protocol"></a><span data-ttu-id="fc28d-256">"Permiso denegado" con el protocolo de trama Forever</span><span class="sxs-lookup"><span data-stu-id="fc28d-256">"Permission Denied" using Forever Frame protocol</span></span>

<span data-ttu-id="fc28d-257">Este es un problema conocido, que se describe [aquí](https://github.com/SignalR/SignalR/issues/1963).</span><span class="sxs-lookup"><span data-stu-id="fc28d-257">This is a known issue, described [here](https://github.com/SignalR/SignalR/issues/1963).</span></span> <span data-ttu-id="fc28d-258">Este síntoma puede aparecer con la biblioteca de JQuery más reciente. la solución alternativa es cambiar la aplicación a JQuery 1.8.2.</span><span class="sxs-lookup"><span data-stu-id="fc28d-258">This symptom may be seen using the latest JQuery library; the workaround is to downgrade your application to JQuery 1.8.2.</span></span>

<a id="server"></a>

## <a name="compilation-and-server-side-errors"></a><span data-ttu-id="fc28d-259">Errores de compilación y de servidor</span><span class="sxs-lookup"><span data-stu-id="fc28d-259">Compilation and server-side errors</span></span>

 <span data-ttu-id="fc28d-260">La siguiente sección contiene posibles soluciones para errores en tiempo de ejecución del compilador y del servidor.</span><span class="sxs-lookup"><span data-stu-id="fc28d-260">The following section contains possible solutions to compiler and server-side runtime errors.</span></span> 

### <a name="reference-to-hub-instance-is-null"></a><span data-ttu-id="fc28d-261">La referencia a la instancia de Hub es null</span><span class="sxs-lookup"><span data-stu-id="fc28d-261">Reference to Hub instance is null</span></span>

<span data-ttu-id="fc28d-262">Dado que se crea una instancia de concentrador para cada conexión, no se puede crear una instancia de un concentrador en el código.</span><span class="sxs-lookup"><span data-stu-id="fc28d-262">Since a hub instance is created for each connection, you can't create an instance of a hub in your code yourself.</span></span> <span data-ttu-id="fc28d-263">Para llamar a métodos en un centro desde fuera del propio concentrador, consulte [cómo llamar a métodos de cliente y administrar grupos desde fuera de la clase hub](../guide-to-the-api/hubs-api-guide-server.md#callfromoutsidehub) para obtener una referencia al contexto del concentrador.</span><span class="sxs-lookup"><span data-stu-id="fc28d-263">To call methods on a hub from outside the hub itself, see [How to call client methods and manage groups from outside the Hub class](../guide-to-the-api/hubs-api-guide-server.md#callfromoutsidehub) for how to obtain a reference to the hub context.</span></span>

### <a name="httpcontextcurrentsession-is-null"></a><span data-ttu-id="fc28d-264">HTTPContext. Current. Session es null</span><span class="sxs-lookup"><span data-stu-id="fc28d-264">HTTPContext.Current.Session is null</span></span>

<span data-ttu-id="fc28d-265">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="fc28d-265">This behavior is by design.</span></span> <span data-ttu-id="fc28d-266">Signalr no admite el estado de sesión ASP.NET, ya que al habilitar el estado de sesión se interrumpiría la mensajería dúplex.</span><span class="sxs-lookup"><span data-stu-id="fc28d-266">SignalR does not support the ASP.NET session state, since enabling the session state would break duplex messaging.</span></span>

### <a name="no-suitable-method-to-override"></a><span data-ttu-id="fc28d-267">No hay ningún método adecuado para invalidar</span><span class="sxs-lookup"><span data-stu-id="fc28d-267">No suitable method to override</span></span>

<span data-ttu-id="fc28d-268">Es posible que vea este error si utiliza código de documentación o blogs más antiguos.</span><span class="sxs-lookup"><span data-stu-id="fc28d-268">You may see this error if you are using code from older documentation or blogs.</span></span> <span data-ttu-id="fc28d-269">Compruebe que no hace referencia a los nombres de los métodos que han cambiado o han quedado en desuso (como `OnConnectedAsync`).</span><span class="sxs-lookup"><span data-stu-id="fc28d-269">Verify that you are not referencing names of methods that have been changed or deprecated (like `OnConnectedAsync`).</span></span>

### <a name="hostcontextextensionswebsocketserverurl-is-null"></a><span data-ttu-id="fc28d-270">HostContextExtensions. WebSocketServerUrl es NULL.</span><span class="sxs-lookup"><span data-stu-id="fc28d-270">HostContextExtensions.WebSocketServerUrl is null</span></span>

<span data-ttu-id="fc28d-271">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="fc28d-271">This behavior is by design.</span></span> <span data-ttu-id="fc28d-272">Este miembro está en desuso y no debe usarse.</span><span class="sxs-lookup"><span data-stu-id="fc28d-272">This member is deprecated and should not be used.</span></span>

### <a name="a-route-named-signalrhubs-is-already-in-the-route-collection-error"></a><span data-ttu-id="fc28d-273">"Una ruta denominada ' signalr. hubs ' ya está en el error de la colección de rutas.</span><span class="sxs-lookup"><span data-stu-id="fc28d-273">"A route named 'signalr.hubs' is already in the route collection" error</span></span>

<span data-ttu-id="fc28d-274">Este error se verá si la aplicación llama a `MapHubs` dos veces.</span><span class="sxs-lookup"><span data-stu-id="fc28d-274">This error will be seen if `MapHubs` is called twice by your application.</span></span> <span data-ttu-id="fc28d-275">Algunas aplicaciones de ejemplo llaman a `MapHubs` directamente en el archivo de aplicación global; otros realizan la llamada en una clase contenedora.</span><span class="sxs-lookup"><span data-stu-id="fc28d-275">Some example applications call `MapHubs` directly in the global application file; others make the call in a wrapper class.</span></span> <span data-ttu-id="fc28d-276">Asegúrese de que la aplicación no realiza ambas tareas.</span><span class="sxs-lookup"><span data-stu-id="fc28d-276">Ensure that your application does not do both.</span></span>

<a id="vs"></a>

## <a name="visual-studio-issues"></a><span data-ttu-id="fc28d-277">Problemas de Visual Studio</span><span class="sxs-lookup"><span data-stu-id="fc28d-277">Visual Studio issues</span></span>

<span data-ttu-id="fc28d-278">En esta sección se describen los problemas que se encuentran en Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="fc28d-278">This section describes issues encountered in Visual Studio.</span></span>

### <a name="script-documents-node-does-not-appear-in-solution-explorer"></a><span data-ttu-id="fc28d-279">El nodo documentos de script no aparece en Explorador de soluciones</span><span class="sxs-lookup"><span data-stu-id="fc28d-279">Script Documents node does not appear in Solution Explorer</span></span>

<span data-ttu-id="fc28d-280">Algunos de nuestros tutoriales le dirigen al nodo "documentos de script" en Explorador de soluciones durante la depuración.</span><span class="sxs-lookup"><span data-stu-id="fc28d-280">Some of our tutorials direct you to the "Script Documents" node in Solution Explorer while debugging.</span></span> <span data-ttu-id="fc28d-281">Este nodo se genera mediante el depurador de JavaScript y solo aparecerá mientras se depuran los clientes del explorador en Internet Explorer. el nodo no aparecerá si se usan Chrome o Firefox.</span><span class="sxs-lookup"><span data-stu-id="fc28d-281">This node is produced by the JavaScript debugger, and will only appear while debugging browser clients in Internet Explorer; the node will not appear if Chrome or Firefox are used.</span></span> <span data-ttu-id="fc28d-282">El depurador de JavaScript tampoco se ejecutará si se está ejecutando otro depurador del cliente, como el depurador de Silverlight.</span><span class="sxs-lookup"><span data-stu-id="fc28d-282">The JavaScript debugger will also not run if another client debugger is running, such as the Silverlight debugger.</span></span>

### <a name="signalr-does-not-work-on-visual-studio-2008-or-earlier"></a><span data-ttu-id="fc28d-283">Signalr no funciona en Visual Studio 2008 o versiones anteriores</span><span class="sxs-lookup"><span data-stu-id="fc28d-283">SignalR does not work on Visual Studio 2008 or earlier</span></span>

<span data-ttu-id="fc28d-284">Este comportamiento está diseñado así.</span><span class="sxs-lookup"><span data-stu-id="fc28d-284">This behavior is by design.</span></span> <span data-ttu-id="fc28d-285">Signalr requiere .NET Framework 4 o posterior; Esto requiere que las aplicaciones de Signalr se desarrollen en Visual Studio 2010 o posterior.</span><span class="sxs-lookup"><span data-stu-id="fc28d-285">SignalR requires .NET Framework 4 or later; this requires that SignalR applications be developed in Visual Studio 2010 or later.</span></span>

<a id="iis"></a>

## <a name="iis-issues"></a><span data-ttu-id="fc28d-286">Problemas de IIS</span><span class="sxs-lookup"><span data-stu-id="fc28d-286">IIS issues</span></span>

<span data-ttu-id="fc28d-287">Esta sección contiene problemas con Internet Information Services.</span><span class="sxs-lookup"><span data-stu-id="fc28d-287">This section contains issues with Internet Information Services.</span></span>

### <a name="web-site-crashes-after-maphubs-call"></a><span data-ttu-id="fc28d-288">El sitio web se bloquea después de llamar a MapHubs</span><span class="sxs-lookup"><span data-stu-id="fc28d-288">Web site crashes after MapHubs call</span></span>

<span data-ttu-id="fc28d-289">Este problema se ha corregido en la versión más reciente de Signalr.</span><span class="sxs-lookup"><span data-stu-id="fc28d-289">This issue has been fixed in the latest version of SignalR.</span></span> <span data-ttu-id="fc28d-290">Compruebe que está usando la versión más reciente de Signalr mediante la actualización de la instalación mediante NuGet.</span><span class="sxs-lookup"><span data-stu-id="fc28d-290">Verify that you are using the latest released version of SignalR by updating your installation using NuGet.</span></span>

<a id="azure"></a>

## <a name="azure-issues"></a><span data-ttu-id="fc28d-291">Problemas de Azure</span><span class="sxs-lookup"><span data-stu-id="fc28d-291">Azure issues</span></span>

<span data-ttu-id="fc28d-292">Esta sección contiene problemas con Microsoft Azure.</span><span class="sxs-lookup"><span data-stu-id="fc28d-292">This section contains issues with Microsoft Azure.</span></span>

### <a name="messages-are-not-received-through-the-azure-backplane-after-altering-topic-names"></a><span data-ttu-id="fc28d-293">Los mensajes no se reciben a través del backplane de Azure después de modificar los nombres de los temas</span><span class="sxs-lookup"><span data-stu-id="fc28d-293">Messages are not received through the Azure backplane after altering topic names</span></span>

<span data-ttu-id="fc28d-294">Los temas usados por el backplane de Azure no pretenden ser configurables por el usuario.</span><span class="sxs-lookup"><span data-stu-id="fc28d-294">The topics used by the Azure backplane are not intended to be user-configurable.</span></span>
