---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: 'Introducción a ASP.NET Web Pages: conceptos básicos de formularios HTML | Microsoft Docs'
author: Rick-Anderson
description: Este tutorial muestra los conceptos básicos de cómo crear un formulario de entrada y cómo controlar la entrada del usuario cuando se usa ASP.NET Web Pages (Razor). Y ahora que ya...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f88f7a31551abda029bee0ec16aa35ce2ef5d2f0
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 04/17/2019
ms.locfileid: "59385961"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="2a922-104">Introducción a las páginas Web ASP.NET - conceptos básicos de formularios HTML</span><span class="sxs-lookup"><span data-stu-id="2a922-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="2a922-105">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="2a922-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="2a922-106">Este tutorial muestra los conceptos básicos de cómo crear un formulario de entrada y cómo controlar la entrada del usuario cuando se usa ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="2a922-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="2a922-107">Y ahora que tiene una base de datos, deberá usar sus habilidades de formulario para que los usuarios puedan buscar películas específicas en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="2a922-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="2a922-108">Supone que ha completado la serie a través de [Introducción para mostrar datos utilizando ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="2a922-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="2a922-109">Lo que aprenderá:</span><span class="sxs-lookup"><span data-stu-id="2a922-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="2a922-110">Cómo crear un formulario mediante el uso de los elementos HTML estándar.</span><span class="sxs-lookup"><span data-stu-id="2a922-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="2a922-111">Cómo leer el usuario de la entrada en un formulario.</span><span class="sxs-lookup"><span data-stu-id="2a922-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="2a922-112">Cómo crear una consulta SQL que selectivamente obtiene los datos mediante una búsqueda de términos el usuario proporciona.</span><span class="sxs-lookup"><span data-stu-id="2a922-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="2a922-113">Cómo hacer que los campos en la página "recuerda" el usuario especificado.</span><span class="sxs-lookup"><span data-stu-id="2a922-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="2a922-114">Características y tecnologías tratadas:</span><span class="sxs-lookup"><span data-stu-id="2a922-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="2a922-115">Objeto `Request`.</span><span class="sxs-lookup"><span data-stu-id="2a922-115">The `Request` object.</span></span>
> - <span data-ttu-id="2a922-116">El código SQL `Where` cláusula.</span><span class="sxs-lookup"><span data-stu-id="2a922-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="2a922-117">¿Qué va a crear</span><span class="sxs-lookup"><span data-stu-id="2a922-117">What You'll Build</span></span>

<span data-ttu-id="2a922-118">En el tutorial anterior, creó una base de datos, agrega datos a ella y, a continuación, usa el `WebGrid` auxiliar para mostrar los datos.</span><span class="sxs-lookup"><span data-stu-id="2a922-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="2a922-119">En este tutorial, agregará un cuadro de búsqueda que permite buscar películas de un género concreto o cuyo título contiene cualquier palabra que escriba.</span><span class="sxs-lookup"><span data-stu-id="2a922-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="2a922-120">(Por ejemplo, podrá encontrar todas las películas que pertenezcan al género es "Acción" o cuyo título contiene "Harry" o "Adventure".)</span><span class="sxs-lookup"><span data-stu-id="2a922-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="2a922-121">Cuando haya terminado con este tutorial, tendrá una página como esta:</span><span class="sxs-lookup"><span data-stu-id="2a922-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Página de películas con búsqueda de género y título](form-basics/_static/image1.png)

<span data-ttu-id="2a922-123">La parte de la lista de la página es igual que en el último tutorial &mdash; una cuadrícula.</span><span class="sxs-lookup"><span data-stu-id="2a922-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="2a922-124">La diferencia será que la cuadrícula mostrará solo las películas que va a buscar.</span><span class="sxs-lookup"><span data-stu-id="2a922-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="2a922-125">Acerca de los formularios HTML</span><span class="sxs-lookup"><span data-stu-id="2a922-125">About HTML Forms</span></span>

<span data-ttu-id="2a922-126">(Si tiene experiencia con la creación de formularios HTML y con la diferencia entre `GET` y `POST`, puede omitir esta sección.)</span><span class="sxs-lookup"><span data-stu-id="2a922-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="2a922-127">Un formulario tiene elementos de entrada de usuario &mdash; cuadros de texto, botones, botones de opción, casillas, listas desplegables y así sucesivamente.</span><span class="sxs-lookup"><span data-stu-id="2a922-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="2a922-128">Los usuarios rellenar estos controles o realice las selecciones y, a continuación, envíen el formulario, haga clic en un botón.</span><span class="sxs-lookup"><span data-stu-id="2a922-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="2a922-129">La sintaxis básica de HTML de un formulario se muestra en este ejemplo:</span><span class="sxs-lookup"><span data-stu-id="2a922-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="2a922-130">Cuando este marcado se ejecuta en una página, crea un formulario simple que es similar a esta ilustración:</span><span class="sxs-lookup"><span data-stu-id="2a922-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Formulario HTML básico como representado en el explorador](form-basics/_static/image2.png)

<span data-ttu-id="2a922-132">El `<form>` elemento abarca los elementos HTML que se envíen.</span><span class="sxs-lookup"><span data-stu-id="2a922-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="2a922-133">(Una fácil cometer consiste en Agregar elementos a la página pero se olvida, a continuación, colocarlos dentro un `<form>` elemento.</span><span class="sxs-lookup"><span data-stu-id="2a922-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="2a922-134">En ese caso, no hay nada que se envía.) El `method` atributo indica al explorador cómo enviar la entrada del usuario.</span><span class="sxs-lookup"><span data-stu-id="2a922-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="2a922-135">Se establece en `post` si va a realizar una actualización en el servidor o a `get` si simplemente está obteniendo datos del servidor.</span><span class="sxs-lookup"><span data-stu-id="2a922-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="2a922-136">**GET, POST y seguridad de verbo HTTP**</span><span class="sxs-lookup"><span data-stu-id="2a922-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="2a922-137">HTTP, el protocolo que usan los exploradores y servidores para intercambiar información, es sorprendentemente sencilla en sus operaciones básicas.</span><span class="sxs-lookup"><span data-stu-id="2a922-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="2a922-138">Los exploradores utilizan sólo unos cuantos verbos para realizar solicitudes a los servidores.</span><span class="sxs-lookup"><span data-stu-id="2a922-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="2a922-139">Al escribir código para la web, es útil para comprender estos verbos y usan de cómo el explorador y el servidor.</span><span class="sxs-lookup"><span data-stu-id="2a922-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="2a922-140">De lejos los verbos más usados son los siguientes:</span><span class="sxs-lookup"><span data-stu-id="2a922-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="2a922-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="2a922-141">`GET`.</span></span> <span data-ttu-id="2a922-142">El explorador utiliza este verbo para capturar algo desde el servidor.</span><span class="sxs-lookup"><span data-stu-id="2a922-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="2a922-143">Por ejemplo, cuando escriba una dirección URL en el explorador, el explorador realiza una `GET` operación para solicitar la página que desee.</span><span class="sxs-lookup"><span data-stu-id="2a922-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="2a922-144">Si la página incluye gráficos, el explorador realiza adicionales `GET` operaciones para obtener las imágenes.</span><span class="sxs-lookup"><span data-stu-id="2a922-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="2a922-145">Si el `GET` operación tiene que pasar información al servidor, la información se pasa como parte de la dirección URL en la cadena de consulta.</span><span class="sxs-lookup"><span data-stu-id="2a922-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="2a922-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="2a922-146">`POST`.</span></span> <span data-ttu-id="2a922-147">El explorador envía una `POST` solicitud con el fin de enviar los datos que se agregarán o se cambian en el servidor.</span><span class="sxs-lookup"><span data-stu-id="2a922-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="2a922-148">Por ejemplo, el `POST` verbo se utiliza para crear registros en una base de datos o cambiar las existentes.</span><span class="sxs-lookup"><span data-stu-id="2a922-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="2a922-149">La mayoría de los casos, al rellenar un formulario y haga clic en el botón de envío, el explorador realiza una `POST` operación.</span><span class="sxs-lookup"><span data-stu-id="2a922-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="2a922-150">En un `POST` operación, los datos que se pasan al servidor están en el cuerpo de la página.</span><span class="sxs-lookup"><span data-stu-id="2a922-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="2a922-151">Es una diferencia importante entre estos verbos que un `GET` operación no debe hacer ningún cambio en el servidor, o ponerlo en una forma un poco más abstracta, un `GET` operación no produce un cambio de estado en el servidor.</span><span class="sxs-lookup"><span data-stu-id="2a922-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="2a922-152">Puede realizar un `GET` operación en los mismos recursos tantas veces como desee, y no cambian esos recursos.</span><span class="sxs-lookup"><span data-stu-id="2a922-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="2a922-153">(Un `GET` operación a menudo se dice "seguras", o para usar un término técnico, es *idempotente*.) En cambio, por supuesto, un `POST` solicitud cambia algo en el servidor cada vez que realice la operación.</span><span class="sxs-lookup"><span data-stu-id="2a922-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="2a922-154">Dos ejemplos que le ayudarán a ilustrar esta distinción.</span><span class="sxs-lookup"><span data-stu-id="2a922-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="2a922-155">Al realizar una búsqueda mediante un motor como Bing o Google, rellene un formulario que consta de un cuadro de texto y, a continuación, haga clic en el botón de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="2a922-156">El explorador realiza una `GET` operación, con el valor especificado en el cuadro que se pasa como parte de la dirección URL.</span><span class="sxs-lookup"><span data-stu-id="2a922-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="2a922-157">Mediante un `GET` operación para este tipo de formulario está bien, ya que una operación de búsqueda no cambia los recursos en el servidor, solo captura información.</span><span class="sxs-lookup"><span data-stu-id="2a922-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="2a922-158">Ahora, considere el proceso de pedidos en línea.</span><span class="sxs-lookup"><span data-stu-id="2a922-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="2a922-159">Rellene los detalles del pedido y, a continuación, haga clic en el botón Enviar.</span><span class="sxs-lookup"><span data-stu-id="2a922-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="2a922-160">Esta operación será un `POST` solicitar, porque la operación dará como resultado cambios en el servidor, como un nuevo registro de pedido, un cambio en la información de su cuenta y quizás muchos otros cambios.</span><span class="sxs-lookup"><span data-stu-id="2a922-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="2a922-161">A diferencia de la `GET` operación, no se puede repetir el `POST` solicitud, si lo hiciera, cada vez que se vuelven a enviar la solicitud, se generaría un pedido nuevo en el servidor.</span><span class="sxs-lookup"><span data-stu-id="2a922-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="2a922-162">(En casos como éste, los sitios Web a menudo le advertirá que no haga clic en un botón de envío de más de una vez o deshabilitará el botón Enviar para que no vuelva a enviar el formulario por accidente.)</span><span class="sxs-lookup"><span data-stu-id="2a922-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="2a922-163">En el transcurso de este tutorial, deberá usar un `GET` operación y un `POST` operación para trabajar con formularios HTML.</span><span class="sxs-lookup"><span data-stu-id="2a922-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="2a922-164">Explicaremos en cada caso porqué el verbo que use es el adecuado.</span><span class="sxs-lookup"><span data-stu-id="2a922-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="2a922-165">(Para más información acerca de los verbos HTTP, consulte el [definiciones de método](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) artículo en el sitio de W3C.)</span><span class="sxs-lookup"><span data-stu-id="2a922-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="2a922-166">La mayoría los elementos de entrada de usuario son HTML `<input>` elementos.</span><span class="sxs-lookup"><span data-stu-id="2a922-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="2a922-167">Parecen `<input type="type" name="name">,` donde *tipo* indica el tipo de control de entrada de usuario que desee.</span><span class="sxs-lookup"><span data-stu-id="2a922-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="2a922-168">Estos elementos son los más comunes:</span><span class="sxs-lookup"><span data-stu-id="2a922-168">These elements are the common ones:</span></span>

- <span data-ttu-id="2a922-169">Cuadro de texto: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="2a922-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="2a922-170">Casilla de verificación: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="2a922-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="2a922-171">Botón de radio: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="2a922-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="2a922-172">Botón: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="2a922-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="2a922-173">Botón de envío: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="2a922-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="2a922-174">También puede usar el `<textarea>` elemento para crear un cuadro de texto multilínea y `<select>` elemento para crear una lista desplegable o lista desplazable.</span><span class="sxs-lookup"><span data-stu-id="2a922-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="2a922-175">(Para más información sobre la HTML los elementos del formulario, consulte [formularios HTML y entrada](http://www.w3schools.com/html/html_forms.asp) en el sitio W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="2a922-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="2a922-176">El `name` atributo es muy importante, porque el nombre es cómo se obtendrá el valor del elemento más adelante, como verá en breve.</span><span class="sxs-lookup"><span data-stu-id="2a922-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="2a922-177">La parte más interesante es, el desarrollador de páginas, ¿qué con la entrada del usuario.</span><span class="sxs-lookup"><span data-stu-id="2a922-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="2a922-178">No hay ningún comportamiento integrado asociado con estos elementos.</span><span class="sxs-lookup"><span data-stu-id="2a922-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="2a922-179">En su lugar, tendrá que obtener los valores que el usuario ha entrado o seleccionado y hacer algo con ellos.</span><span class="sxs-lookup"><span data-stu-id="2a922-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="2a922-180">Eso es lo que aprenderá en este tutorial.</span><span class="sxs-lookup"><span data-stu-id="2a922-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="2a922-181">**HTML5 y formularios de entrada**</span><span class="sxs-lookup"><span data-stu-id="2a922-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="2a922-182">Como ya sabe, HTML está en transición y la versión más reciente (HTML5) incluye compatibilidad con las formas más intuitivas para los usuarios escriban información.</span><span class="sxs-lookup"><span data-stu-id="2a922-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="2a922-183">Por ejemplo, en HTML5, (el desarrollador de páginas) puede indicar la página que desea que el usuario que escriba una fecha.</span><span class="sxs-lookup"><span data-stu-id="2a922-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="2a922-184">A continuación, el explorador puede mostrar automáticamente un calendario, en lugar de requerir al usuario que escriba una fecha manualmente.</span><span class="sxs-lookup"><span data-stu-id="2a922-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="2a922-185">Sin embargo, HTML5 es nuevo y aún no se admite en todos los exploradores.</span><span class="sxs-lookup"><span data-stu-id="2a922-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="2a922-186">ASP.NET Web Pages es compatible con HTML5 en la medida en que examinador del usuario hace de entrada.</span><span class="sxs-lookup"><span data-stu-id="2a922-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="2a922-187">Para obtener una idea de los nuevos atributos para el `<input>` el elemento en HTML5, vea [HTML &lt;entrada&gt; atributo type](http://www.w3schools.com/html/html_form_input_types.asp) en el sitio W3Schools.</span><span class="sxs-lookup"><span data-stu-id="2a922-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="2a922-188">Crear el formulario</span><span class="sxs-lookup"><span data-stu-id="2a922-188">Creating the Form</span></span>

<span data-ttu-id="2a922-189">En WebMatrix, en el **archivos** área de trabajo, abra el *Movies.cshtml* página.</span><span class="sxs-lookup"><span data-stu-id="2a922-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="2a922-190">Después del cierre `</h1>` etiqueta y antes de la apertura `<div>` etiqueta de la `grid.GetHtml` llamada, agregue el siguiente marcado:</span><span class="sxs-lookup"><span data-stu-id="2a922-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="2a922-191">Este marcado crea un formulario que tiene un cuadro de texto denominado `searchGenre` y un botón de envío.</span><span class="sxs-lookup"><span data-stu-id="2a922-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="2a922-192">El texto del cuadro y enviar el botón se incluyen en un `<form>` elemento cuyo `method` atributo está establecido en `get`.</span><span class="sxs-lookup"><span data-stu-id="2a922-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="2a922-193">(Recuerde que si no coloca el cuadro de texto y botón dentro de envío un `<form>` elemento, nada se enviarán al hacer clic en el botón.) Usa el `GET` verbo aquí porque se está creando un formulario que no realice cambios en el servidor, solo resulta en una búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="2a922-194">(En el tutorial anterior, ha utilizado un `post` método, que es cómo enviar los cambios realizados en el servidor.</span><span class="sxs-lookup"><span data-stu-id="2a922-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="2a922-195">Verá en el siguiente tutorial nuevo.)</span><span class="sxs-lookup"><span data-stu-id="2a922-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="2a922-196">Ejecute la página.</span><span class="sxs-lookup"><span data-stu-id="2a922-196">Run the page.</span></span> <span data-ttu-id="2a922-197">Aunque no ha definido ningún comportamiento del formulario, puede ver su aspecto:</span><span class="sxs-lookup"><span data-stu-id="2a922-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Página de películas con el cuadro de búsqueda de género](form-basics/_static/image3.png)

<span data-ttu-id="2a922-199">Escriba un valor en el cuadro de texto, como "Comedia".</span><span class="sxs-lookup"><span data-stu-id="2a922-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="2a922-200">A continuación, haga clic en **búsqueda género**.</span><span class="sxs-lookup"><span data-stu-id="2a922-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="2a922-201">Tome nota de la dirección URL de la página.</span><span class="sxs-lookup"><span data-stu-id="2a922-201">Take note of the URL of the page.</span></span> <span data-ttu-id="2a922-202">Porque se configuró el `<form>` del elemento `method` atribuir a `get`, el valor especificado es ahora parte de la cadena de consulta en la dirección URL, similar al siguiente:</span><span class="sxs-lookup"><span data-stu-id="2a922-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="2a922-203">Leer valores de formulario</span><span class="sxs-lookup"><span data-stu-id="2a922-203">Reading Form Values</span></span>

<span data-ttu-id="2a922-204">La página ya contiene código que obtiene de la base de datos y muestra los resultados en una cuadrícula.</span><span class="sxs-lookup"><span data-stu-id="2a922-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="2a922-205">Ahora debe agregar código que lee el valor del cuadro de texto para que pueda ejecutar una consulta SQL que incluye el término de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="2a922-206">Dado que el método del formulario se establece en `get`, puede leer el valor que se especificó en el cuadro de texto utilizando código similar al siguiente:</span><span class="sxs-lookup"><span data-stu-id="2a922-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="2a922-207">El `Request.QueryString` objeto (el `QueryString` propiedad de la `Request` objeto) incluye los valores de los elementos que se enviaron como parte de la `GET` operación.</span><span class="sxs-lookup"><span data-stu-id="2a922-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="2a922-208">El `Request.QueryString` propiedad contiene un *colección* (lista) de los valores que se envían en el formulario.</span><span class="sxs-lookup"><span data-stu-id="2a922-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="2a922-209">Para obtener cualquier valor individual, especifique el nombre del elemento que desee.</span><span class="sxs-lookup"><span data-stu-id="2a922-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="2a922-210">Por eso debe tener un `name` atributo el `<input>` elemento (`searchTerm`) que crea el cuadro de texto.</span><span class="sxs-lookup"><span data-stu-id="2a922-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="2a922-211">(Para más información sobre la `Request` de objetos, consulte el [sidebar](#BKMK_TheRequestObject) más adelante.)</span><span class="sxs-lookup"><span data-stu-id="2a922-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="2a922-212">Es lo suficientemente sencilla para leer el valor del cuadro de texto.</span><span class="sxs-lookup"><span data-stu-id="2a922-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="2a922-213">Sin embargo, si el usuario no escribe nada en absoluto en el cuadro de texto, pero hace clic en **búsqueda** de todos modos, puede omitir ese clic, ya que no hay nada que buscar.</span><span class="sxs-lookup"><span data-stu-id="2a922-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="2a922-214">El código siguiente es un ejemplo que muestra cómo implementar estas condiciones.</span><span class="sxs-lookup"><span data-stu-id="2a922-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="2a922-215">(No tiene que agregar este código aún; lo hará en un momento).</span><span class="sxs-lookup"><span data-stu-id="2a922-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="2a922-216">La prueba se divide de esta manera:</span><span class="sxs-lookup"><span data-stu-id="2a922-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="2a922-217">Obtener el valor de `Request.QueryString["searchGenre"]`, concretamente, el valor que se especificó en el `<input>` elemento denominado `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="2a922-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="2a922-218">Averiguar si está vacío utilizando el `IsEmpty` método.</span><span class="sxs-lookup"><span data-stu-id="2a922-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="2a922-219">Este método es el método estándar para determinar si algo (por ejemplo, un elemento de formulario) contiene un valor.</span><span class="sxs-lookup"><span data-stu-id="2a922-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="2a922-220">Pero en realidad, ocupa sólo si se ha *no* vacía, por lo tanto...</span><span class="sxs-lookup"><span data-stu-id="2a922-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="2a922-221">Agregar el `!` operador delante de la `IsEmpty` probar.</span><span class="sxs-lookup"><span data-stu-id="2a922-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="2a922-222">(El `!` operador significa NOT lógico).</span><span class="sxs-lookup"><span data-stu-id="2a922-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="2a922-223">En términos sencillos, toda la `if` condición se traduce en lo siguiente: *Si el elemento de searchGenre del formulario no está vacío, a continuación...*</span><span class="sxs-lookup"><span data-stu-id="2a922-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="2a922-224">Este bloque prepara el terreno para crear una consulta que utiliza el término de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="2a922-225">Hágalo en la sección siguiente.</span><span class="sxs-lookup"><span data-stu-id="2a922-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="2a922-226">**El objeto de solicitud**</span><span class="sxs-lookup"><span data-stu-id="2a922-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="2a922-227">La `Request` objeto contiene toda la información que el explorador envía a la aplicación cuando se solicita una página o se envían.</span><span class="sxs-lookup"><span data-stu-id="2a922-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="2a922-228">Este objeto incluye toda la información que proporciona el usuario, como los valores del cuadro de texto o un archivo para cargarlo.</span><span class="sxs-lookup"><span data-stu-id="2a922-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="2a922-229">También incluye todo tipo de información adicional, como las cookies, los valores de la cadena de consulta de dirección URL (si existe), la ruta de acceso de la página que se está ejecutando, el tipo de explorador que está usando el usuario, la lista de idiomas que se establecen en el explorador y mucho más.</span><span class="sxs-lookup"><span data-stu-id="2a922-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="2a922-230">El `Request` objeto es un *colección* (lista) de valores.</span><span class="sxs-lookup"><span data-stu-id="2a922-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="2a922-231">Obtiene un valor individual fuera de la colección especificando su nombre:</span><span class="sxs-lookup"><span data-stu-id="2a922-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="2a922-232">La `Request` objeto realmente expone varios subconjuntos.</span><span class="sxs-lookup"><span data-stu-id="2a922-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="2a922-233">Por ejemplo:</span><span class="sxs-lookup"><span data-stu-id="2a922-233">For example:</span></span>
> 
> - <span data-ttu-id="2a922-234">`Request.Form` Proporciona valores de elementos dentro de la enviada `<form>` elemento si la solicitud es un `POST` solicitud.</span><span class="sxs-lookup"><span data-stu-id="2a922-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="2a922-235">`Request.QueryString` proporciona solo los valores en la cadena de consulta de URL.</span><span class="sxs-lookup"><span data-stu-id="2a922-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="2a922-236">(En una dirección URL como `http://mysite/myapp/page?searchGenre=action&page=2`, el `?searchGenre=action&page=2` sección de la dirección URL es la cadena de consulta.)</span><span class="sxs-lookup"><span data-stu-id="2a922-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="2a922-237">`Request.Cookies` colección proporciona acceso a las cookies que se ha enviado el explorador.</span><span class="sxs-lookup"><span data-stu-id="2a922-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="2a922-238">Para obtener un valor que sabe que está en el formulario enviado, puede usar `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="2a922-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="2a922-239">Como alternativa, puede usar las versiones más específicas `Request.Form["name"]` (para `POST` solicitudes) o `Request.QueryString["name"]` (para `GET` solicitudes).</span><span class="sxs-lookup"><span data-stu-id="2a922-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="2a922-240">Por supuesto, *nombre* es el nombre del elemento que se va a obtener.</span><span class="sxs-lookup"><span data-stu-id="2a922-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="2a922-241">El nombre del elemento que desea obtener debe ser único dentro de la colección que está usando.</span><span class="sxs-lookup"><span data-stu-id="2a922-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="2a922-242">Por eso la `Request` objeto proporciona los subconjuntos como `Request.Form` y `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="2a922-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="2a922-243">Supongamos que la página contiene un elemento de formulario denominado `userName` y *también* contiene una cookie denominada `userName`.</span><span class="sxs-lookup"><span data-stu-id="2a922-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="2a922-244">Si obtiene `Request["userName"]`, si desea que el valor del formulario o la cookie es ambiguo.</span><span class="sxs-lookup"><span data-stu-id="2a922-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="2a922-245">Sin embargo, si se produce `Request.Form["userName"]` o `Request.Cookie["userName"]`, le sean explícitos sobre qué valor va a obtener.</span><span class="sxs-lookup"><span data-stu-id="2a922-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="2a922-246">Es una buena práctica para ser específico y usar el subconjunto de `Request` que le interesa, como `Request.Form` o `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="2a922-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="2a922-247">Para las páginas simples que se va a crear en este tutorial, probablemente no realiza realmente cualquier diferencia.</span><span class="sxs-lookup"><span data-stu-id="2a922-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="2a922-248">Sin embargo, como crear páginas más complejas, mediante la versión explícita `Request.Form` o `Request.QueryString` puede ayudarle a evitar problemas que pueden surgir cuando la página contiene un formulario (o varios formularios), las cookies, los valores de cadena de consulta y así sucesivamente.</span><span class="sxs-lookup"><span data-stu-id="2a922-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="2a922-249">Creación de una consulta con un término de búsqueda</span><span class="sxs-lookup"><span data-stu-id="2a922-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="2a922-250">Ahora que sabe cómo obtener el término de búsqueda que el usuario especificado, puede crear una consulta que lo usa.</span><span class="sxs-lookup"><span data-stu-id="2a922-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="2a922-251">Recuerde que para obtener todos los elementos de la película fuera de la base de datos, está usando una consulta SQL que es similar a esta instrucción:</span><span class="sxs-lookup"><span data-stu-id="2a922-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="2a922-252">Para obtener solo determinadas películas, tiene que usar una consulta que incluye un `Where` cláusula.</span><span class="sxs-lookup"><span data-stu-id="2a922-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="2a922-253">Esta cláusula permite establecer una condición en la que se devuelven las filas por la consulta.</span><span class="sxs-lookup"><span data-stu-id="2a922-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="2a922-254">Por ejemplo:</span><span class="sxs-lookup"><span data-stu-id="2a922-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="2a922-255">El formato básico es `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="2a922-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="2a922-256">Puede usar distintos operadores además de simplemente `=`, como `>` (mayor que), `<` (menor que), `<>` (no igual a), `<=` (menor o igual que), etc., según lo está buscando.</span><span class="sxs-lookup"><span data-stu-id="2a922-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="2a922-257">Si se lo está preguntando, las instrucciones SQL no distinguen mayúsculas de minúsculas &mdash; `SELECT` es el mismo que `Select` (o incluso `select`).</span><span class="sxs-lookup"><span data-stu-id="2a922-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="2a922-258">Sin embargo, las personas a menudo aprovechar como palabras clave en una instrucción SQL, `SELECT` y `WHERE`, para que sea más fácil de leer.</span><span class="sxs-lookup"><span data-stu-id="2a922-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="2a922-259">Pasando el término de búsqueda como un parámetro</span><span class="sxs-lookup"><span data-stu-id="2a922-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="2a922-260">Busca un género concreto es bastante fácil (`WHERE Genre = 'Action'`), pero desea poder buscar cualquier género que el usuario escribe.</span><span class="sxs-lookup"><span data-stu-id="2a922-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="2a922-261">Para ello, cree como consulta SQL que incluye un marcador de posición para el valor para buscar.</span><span class="sxs-lookup"><span data-stu-id="2a922-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="2a922-262">Será similar a este comando:</span><span class="sxs-lookup"><span data-stu-id="2a922-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="2a922-263">El marcador de posición es el `@` caracteres seguidos de cero.</span><span class="sxs-lookup"><span data-stu-id="2a922-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="2a922-264">Como puede imaginar, una consulta puede contener varios marcadores de posición, y se denominará `@0`, `@1`, `@2`, etcetera.</span><span class="sxs-lookup"><span data-stu-id="2a922-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="2a922-265">Para configurar la consulta y pásele realmente el valor, utilice el código similar al siguiente:</span><span class="sxs-lookup"><span data-stu-id="2a922-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="2a922-266">Este código es similar a lo que ha hecho ya para mostrar datos en la cuadrícula.</span><span class="sxs-lookup"><span data-stu-id="2a922-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="2a922-267">Las únicas diferencias son:</span><span class="sxs-lookup"><span data-stu-id="2a922-267">The only differences are:</span></span>

- <span data-ttu-id="2a922-268">La consulta contiene un marcador de posición (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="2a922-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="2a922-269">La consulta se coloca en una variable (`selectCommand`); antes, se pasa directamente a la consulta el `db.Query` método.</span><span class="sxs-lookup"><span data-stu-id="2a922-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="2a922-270">Cuando se llama a la `db.Query` método, pase la consulta y el valor que se usará para el marcador de posición.</span><span class="sxs-lookup"><span data-stu-id="2a922-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="2a922-271">(Si la consulta tiene varios marcadores de posición, podría pasar todos como valores independientes para el método.)</span><span class="sxs-lookup"><span data-stu-id="2a922-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="2a922-272">Si coloca todos estos elementos juntos, obtendrá el siguiente código:</span><span class="sxs-lookup"><span data-stu-id="2a922-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="2a922-273">**¡Importante!**</span><span class="sxs-lookup"><span data-stu-id="2a922-273">**Important!**</span></span> <span data-ttu-id="2a922-274">Uso de marcadores de posición (como `@0`) pasar valores a un comando SQL es *extremadamente importante* para la seguridad.</span><span class="sxs-lookup"><span data-stu-id="2a922-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="2a922-275">La manera en que ve aquí, con marcadores de posición para los datos de la variable, es la única manera que se debe construir los comandos SQL.</span><span class="sxs-lookup"><span data-stu-id="2a922-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="2a922-276">Nunca cree una instrucción SQL mediante la colocación de los valores que obtener del usuario y texto literal (concatenar).</span><span class="sxs-lookup"><span data-stu-id="2a922-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="2a922-277">Concatenación de entrada del usuario en una instrucción SQL abre el sitio para un *ataques de inyección SQL* donde un usuario malintencionado envía valores a la página que hack en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="2a922-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="2a922-278">(Encontrará información adicional en el artículo [inyección de código SQL](https://msdn.microsoft.com/library/ms161953.aspx) el sitio Web de MSDN.)</span><span class="sxs-lookup"><span data-stu-id="2a922-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="2a922-279">Actualizar la página de películas con código de búsqueda</span><span class="sxs-lookup"><span data-stu-id="2a922-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="2a922-280">Ahora puede actualizar el código en el *Movies.cshtml* archivo.</span><span class="sxs-lookup"><span data-stu-id="2a922-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="2a922-281">Para empezar, reemplace el código en el bloque de código en la parte superior de la página con este código:</span><span class="sxs-lookup"><span data-stu-id="2a922-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="2a922-282">La diferencia es que ha colocado la consulta en el `selectCommand` variable, que pasará a `db.Query` más adelante.</span><span class="sxs-lookup"><span data-stu-id="2a922-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="2a922-283">Colocar la instrucción SQL en una variable le permite cambiar la instrucción, que es lo haré para llevar a cabo la búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="2a922-284">También se han quitado estas dos líneas, que le vuelve a colocar en versiones posteriores:</span><span class="sxs-lookup"><span data-stu-id="2a922-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="2a922-285">No desea ejecutar la consulta aún (es decir, llamar a `db.Query`) y no desea inicializar el `WebGrid` auxiliar todavía cualquiera.</span><span class="sxs-lookup"><span data-stu-id="2a922-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="2a922-286">Llevará a cabo esas cosas después de que haya descubierto qué instrucción SQL debe ejecutarse.</span><span class="sxs-lookup"><span data-stu-id="2a922-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="2a922-287">Después de este bloque ha vuelto a escribir, puede agregar la nueva lógica para controlar la búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="2a922-288">El código completo tendrá un aspecto similar al siguiente.</span><span class="sxs-lookup"><span data-stu-id="2a922-288">The completed code will look like the following.</span></span> <span data-ttu-id="2a922-289">Actualice el código en la página para que coincida con este ejemplo:</span><span class="sxs-lookup"><span data-stu-id="2a922-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="2a922-290">La página ahora funciona así.</span><span class="sxs-lookup"><span data-stu-id="2a922-290">The page now works like this.</span></span> <span data-ttu-id="2a922-291">Cada vez que se ejecuta la página, el código abre la base de datos y el `selectCommand` variable se establece en la instrucción SQL que obtiene todos los registros desde el `Movies` tabla.</span><span class="sxs-lookup"><span data-stu-id="2a922-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="2a922-292">El código también inicializa la `searchTerm` variable.</span><span class="sxs-lookup"><span data-stu-id="2a922-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="2a922-293">Sin embargo, si la solicitud actual incluye un valor para el `searchGenre` elemento, el código establece `selectCommand` a una consulta diferente, es decir, para que incluya el `Where` cláusula para buscar un género.</span><span class="sxs-lookup"><span data-stu-id="2a922-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="2a922-294">También establece `searchTerm` a lo que se pasó para el cuadro de búsqueda (que puede ser nothing).</span><span class="sxs-lookup"><span data-stu-id="2a922-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="2a922-295">Independientemente de qué SQL instrucción está en `selectCommand`, el código, a continuación, llama a `db.Query` para ejecutar la consulta, pasándole el se encuentra en `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="2a922-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="2a922-296">Si no hay nada `searchTerm`, no importa, porque en ese caso, no hay ningún parámetro para pasar el valor a `selectCommand` de todos modos.</span><span class="sxs-lookup"><span data-stu-id="2a922-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="2a922-297">Por último, el código inicializa el `WebGrid` auxiliar mediante el uso de los resultados de consulta, como antes.</span><span class="sxs-lookup"><span data-stu-id="2a922-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="2a922-298">Puede ver que al colocar la instrucción SQL y el término de búsqueda en variables, se ha agregado flexibilidad al código.</span><span class="sxs-lookup"><span data-stu-id="2a922-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="2a922-299">Como verá más adelante en este tutorial, puede usar este marco de trabajo básico y seguir agregando la lógica para los distintos tipos de búsquedas.</span><span class="sxs-lookup"><span data-stu-id="2a922-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="2a922-300">Probar la característica de búsqueda por género</span><span class="sxs-lookup"><span data-stu-id="2a922-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="2a922-301">En WebMatrix, ejecute el *Movies.cshtml* página.</span><span class="sxs-lookup"><span data-stu-id="2a922-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="2a922-302">Consulte la página con el cuadro de texto para el género.</span><span class="sxs-lookup"><span data-stu-id="2a922-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="2a922-303">Escriba un género que ha especificado para uno de los registros de prueba y luego haga clic en **búsqueda**.</span><span class="sxs-lookup"><span data-stu-id="2a922-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="2a922-304">Esta vez verá una lista de solo las películas que coincidan con ese género:</span><span class="sxs-lookup"><span data-stu-id="2a922-304">This time you see a listing of just the movies that match that genre:</span></span>

![Página de películas lista después de buscar género 'Comedies'](form-basics/_static/image4.png)

<span data-ttu-id="2a922-306">Escriba un género diferentes y buscar de nuevo.</span><span class="sxs-lookup"><span data-stu-id="2a922-306">Enter a different genre and search again.</span></span> <span data-ttu-id="2a922-307">Pruebe a escribir el género mediante el uso de todas las letras mayúsculas o minúsculas para que puedan ver que la búsqueda no distingue mayúsculas de minúsculas.</span><span class="sxs-lookup"><span data-stu-id="2a922-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="2a922-308">"Recordar" el usuario especificado</span><span class="sxs-lookup"><span data-stu-id="2a922-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="2a922-309">Puede que haya observado que después de que ha especificado un género y hace clic en **búsqueda género**, vio una lista de ese género.</span><span class="sxs-lookup"><span data-stu-id="2a922-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="2a922-310">Sin embargo, el cuadro de texto de búsqueda estaba vacío &mdash; en otras palabras, no recordar lo que había escrito.</span><span class="sxs-lookup"><span data-stu-id="2a922-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="2a922-311">Es importante comprender por qué se produce este comportamiento.</span><span class="sxs-lookup"><span data-stu-id="2a922-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="2a922-312">Cuando se envía una página, el explorador envía una solicitud al servidor web.</span><span class="sxs-lookup"><span data-stu-id="2a922-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="2a922-313">Cuando ASP.NET recibe la solicitud, crea una nueva instancia de la página, se ejecuta el código en él y, a continuación, vuelve a presentar la página al explorador.</span><span class="sxs-lookup"><span data-stu-id="2a922-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="2a922-314">En la práctica, sin embargo, la página no sabe que acaba de trabajar con una versión anterior de sí mismo.</span><span class="sxs-lookup"><span data-stu-id="2a922-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="2a922-315">Todo lo que sabe es que TI recibió una solicitud que tenía algunos datos del formulario en ella.</span><span class="sxs-lookup"><span data-stu-id="2a922-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="2a922-316">Cada vez que se solicita una página &mdash; ya sea por primera vez o enviándolo &mdash; le esté sacando una nueva página.</span><span class="sxs-lookup"><span data-stu-id="2a922-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="2a922-317">El servidor web no tiene memoria de la última solicitud.</span><span class="sxs-lookup"><span data-stu-id="2a922-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="2a922-318">Pero tampoco ASP.NET, y tampoco el explorador.</span><span class="sxs-lookup"><span data-stu-id="2a922-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="2a922-319">La única conexión entre estas dos instancias independientes de la página es cualquier dato que transmite entre ellos.</span><span class="sxs-lookup"><span data-stu-id="2a922-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="2a922-320">Por ejemplo, si envía una página, la nueva instancia de la página puede obtener los datos del formulario enviado por la instancia anterior.</span><span class="sxs-lookup"><span data-stu-id="2a922-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="2a922-321">(Otra forma de pasar datos entre páginas es usar cookies).</span><span class="sxs-lookup"><span data-stu-id="2a922-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="2a922-322">Es decir que las páginas web son una manera formal para describir esta situación *sin estado*.</span><span class="sxs-lookup"><span data-stu-id="2a922-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="2a922-323">Los servidores Web y las propias páginas y los elementos de la página no mantienen toda la información sobre el estado de una página anterior.</span><span class="sxs-lookup"><span data-stu-id="2a922-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="2a922-324">La web se diseñó así porque el mantenimiento del estado para las solicitudes individuales podría rápidamente agotar los recursos de servidores web, que a menudo miles, quizás incluso cientos de miles de solicitudes por segundo.</span><span class="sxs-lookup"><span data-stu-id="2a922-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="2a922-325">Por lo que es el motivo por el cuadro de texto está vacío.</span><span class="sxs-lookup"><span data-stu-id="2a922-325">So that's why the text box was empty.</span></span> <span data-ttu-id="2a922-326">Una vez enviada la página, ASP.NET crea una nueva instancia de la página y se ejecutó a través del código y marcado.</span><span class="sxs-lookup"><span data-stu-id="2a922-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="2a922-327">No hubo nada en el código que le indica a ASP.NET para colocar un valor en el cuadro de texto.</span><span class="sxs-lookup"><span data-stu-id="2a922-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="2a922-328">Por lo que ASP.NET no hace nada y el cuadro de texto se representa sin un valor en él.</span><span class="sxs-lookup"><span data-stu-id="2a922-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="2a922-329">Hay realmente una manera sencilla de solucionar este problema.</span><span class="sxs-lookup"><span data-stu-id="2a922-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="2a922-330">El género que ha introducido en el cuadro de texto *es* disponibles en el código &mdash; resulta `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="2a922-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="2a922-331">Actualice el marcado del cuadro de texto para que el `value` atributo obtiene su valor de `searchTerm`, al igual que en este ejemplo:</span><span class="sxs-lookup"><span data-stu-id="2a922-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="2a922-332">En esta página, podría haber establecido también el `value` atributo a la `searchTerm` variable, ya que esa variable también contiene el género que escribió.</span><span class="sxs-lookup"><span data-stu-id="2a922-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="2a922-333">Pero al usar el `Request` objeto para establecer el `value` atributo tal como se muestra aquí es la manera estándar para realizar esta tarea.</span><span class="sxs-lookup"><span data-stu-id="2a922-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="2a922-334">(Suponiendo que incluso desee hacerlo &mdash; en algunas situaciones, es posible que desee presentar la página *sin* valores en los campos.</span><span class="sxs-lookup"><span data-stu-id="2a922-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="2a922-335">Todo depende de lo que está ocurriendo con su aplicación.)</span><span class="sxs-lookup"><span data-stu-id="2a922-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="2a922-336">No se "recuerda" el valor de un cuadro de texto que se usa para las contraseñas.</span><span class="sxs-lookup"><span data-stu-id="2a922-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="2a922-337">Sería una vulnerabilidad de seguridad para permitir que los usuarios rellenar un campo de contraseña mediante el uso de código.</span><span class="sxs-lookup"><span data-stu-id="2a922-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="2a922-338">Vuelva a ejecutar la página, escriba un género y haga clic en **búsqueda género**.</span><span class="sxs-lookup"><span data-stu-id="2a922-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="2a922-339">Esta vez no sólo verá los resultados de la búsqueda, pero el cuadro de texto le recuerda lo que especificó la última vez:</span><span class="sxs-lookup"><span data-stu-id="2a922-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Página que muestra el cuadro de texto se "recuerda" la entrada anterior](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="2a922-341">Buscar cualquier palabra en el título</span><span class="sxs-lookup"><span data-stu-id="2a922-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="2a922-342">Ahora puede buscar cualquier género, pero también puede buscar un título.</span><span class="sxs-lookup"><span data-stu-id="2a922-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="2a922-343">Es difícil obtener un título elaborarlo correctamente al realizar una búsqueda, por lo tanto, que puede buscar una palabra que aparece en cualquier lugar dentro de un título.</span><span class="sxs-lookup"><span data-stu-id="2a922-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="2a922-344">Para ello en SQL, usa el `LIKE` operador y la sintaxis como la siguiente:</span><span class="sxs-lookup"><span data-stu-id="2a922-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="2a922-345">Este comando obtiene todas las películas cuyos títulos contienen "adventure".</span><span class="sxs-lookup"><span data-stu-id="2a922-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="2a922-346">Cuando se usa el `LIKE` operador, incluir el carácter comodín `%` como parte del término de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="2a922-347">La búsqueda `LIKE 'adventure%'` significa "comenzar con 'adventure'".</span><span class="sxs-lookup"><span data-stu-id="2a922-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="2a922-348">(Técnicamente, significa "La cadena"adventure"seguido de nada.") De forma similar, el término de búsqueda `LIKE '%adventure'` "cualquier cosa seguido por la cadena"adventure"", significa que es otra manera de decir "finaliza con"adventure"".</span><span class="sxs-lookup"><span data-stu-id="2a922-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="2a922-349">El término de búsqueda `LIKE '%adventure%'` significa, por tanto, "con"adventure"en cualquier lugar en el título."</span><span class="sxs-lookup"><span data-stu-id="2a922-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="2a922-350">(Técnicamente, "cualquier cosa en el título, seguido de"adventure", seguido de nada".)</span><span class="sxs-lookup"><span data-stu-id="2a922-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="2a922-351">Dentro de la `<form>` elemento, agregue el marcado siguiente justo debajo del cierre `</div>` etiqueta para la búsqueda de género (justo antes de cerrar `</form>` elemento):</span><span class="sxs-lookup"><span data-stu-id="2a922-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="2a922-352">El código para controlar esta búsqueda es similar al código de la búsqueda de género, salvo que lo debe ensamblar el `LIKE` búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="2a922-353">Dentro del bloque de código en la parte superior de la página, agregue esto `if` justo después de bloquear la `if` bloque para la búsqueda de género:</span><span class="sxs-lookup"><span data-stu-id="2a922-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="2a922-354">Este código usa la misma lógica que vio anteriormente, salvo que la búsqueda utiliza un `LIKE` operador y la coloca código "`%`" antes y después el término de búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="2a922-355">Tenga en cuenta cómo resultó muy fácil agregar otra búsqueda a la página.</span><span class="sxs-lookup"><span data-stu-id="2a922-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="2a922-356">Sólo tenía que hacer era:</span><span class="sxs-lookup"><span data-stu-id="2a922-356">All you had to do was:</span></span>

- <span data-ttu-id="2a922-357">Crear un `if` bloque que probar para ver si el cuadro de búsqueda pertinentes tenía un valor.</span><span class="sxs-lookup"><span data-stu-id="2a922-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="2a922-358">Establecer el `selectCommand` variable a una nueva instrucción SQL.</span><span class="sxs-lookup"><span data-stu-id="2a922-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="2a922-359">Establecer el `searchTerm` variable con el valor para pasar a la consulta.</span><span class="sxs-lookup"><span data-stu-id="2a922-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="2a922-360">Este es el bloque de código completo, que contiene la lógica para la búsqueda de un título nuevo:</span><span class="sxs-lookup"><span data-stu-id="2a922-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="2a922-361">Este es un resumen de lo que hace este código:</span><span class="sxs-lookup"><span data-stu-id="2a922-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="2a922-362">Las variables `searchTerm` y `selectCommand` se inicializan en la parte superior.</span><span class="sxs-lookup"><span data-stu-id="2a922-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="2a922-363">Va a establecer estas variables para el término de búsqueda adecuado (si existe) y comando SQL adecuado en función de lo que hace el usuario en la página.</span><span class="sxs-lookup"><span data-stu-id="2a922-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="2a922-364">La búsqueda predeterminada es el caso sencillo de obtener todas las películas de la base de datos.</span><span class="sxs-lookup"><span data-stu-id="2a922-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="2a922-365">En las pruebas para `searchGenre` y `searchTitle`, el código establece `searchTerm` en el valor que desea buscar.</span><span class="sxs-lookup"><span data-stu-id="2a922-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="2a922-366">Los bloques de código también establece `selectCommand` a un comando SQL adecuado para la búsqueda.</span><span class="sxs-lookup"><span data-stu-id="2a922-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="2a922-367">El `db.Query` método se invoca una sola vez, mediante cualquier comando SQL está en `selectedCommand` y cualquier valor que se encuentra en `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="2a922-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="2a922-368">Si no hay ningún término de búsqueda (sin género y no hay ninguna palabra título), el valor de `searchTerm` es una cadena vacía.</span><span class="sxs-lookup"><span data-stu-id="2a922-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="2a922-369">Sin embargo, que no importa, porque en ese caso, la consulta no requiere un parámetro.</span><span class="sxs-lookup"><span data-stu-id="2a922-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="2a922-370">Probar la característica de búsqueda de título</span><span class="sxs-lookup"><span data-stu-id="2a922-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="2a922-371">Ahora puede probar la página de búsqueda completada.</span><span class="sxs-lookup"><span data-stu-id="2a922-371">Now you can test your completed search page.</span></span> <span data-ttu-id="2a922-372">Run *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2a922-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="2a922-373">Escriba un género y haga clic en **búsqueda género**.</span><span class="sxs-lookup"><span data-stu-id="2a922-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="2a922-374">La cuadrícula muestra películas de ese género, al igual que antes.</span><span class="sxs-lookup"><span data-stu-id="2a922-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="2a922-375">Escriba una palabra de título y haga clic en **título de la búsqueda**.</span><span class="sxs-lookup"><span data-stu-id="2a922-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="2a922-376">La cuadrícula muestra películas con esa palabra en el título.</span><span class="sxs-lookup"><span data-stu-id="2a922-376">The grid displays movies that have that word in the title.</span></span>

![Enumerar después de buscar 'El' en el título de página de películas](form-basics/_static/image6.png)

<span data-ttu-id="2a922-378">Deje en blanco ambos cuadros de texto y haga clic en cualquiera de los botones.</span><span class="sxs-lookup"><span data-stu-id="2a922-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="2a922-379">La cuadrícula muestra todas las películas.</span><span class="sxs-lookup"><span data-stu-id="2a922-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="2a922-380">Combinar las consultas</span><span class="sxs-lookup"><span data-stu-id="2a922-380">Combining the Queries</span></span>

<span data-ttu-id="2a922-381">Es posible que tenga en cuenta que las búsquedas que puede realizar son exclusivas.</span><span class="sxs-lookup"><span data-stu-id="2a922-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="2a922-382">No se puede buscar el título y el género al mismo tiempo, incluso si los dos cuadros de búsqueda tienen valores en ellos.</span><span class="sxs-lookup"><span data-stu-id="2a922-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="2a922-383">Por ejemplo, no se puede buscar todas las películas de acción cuyo título contiene "Adventure".</span><span class="sxs-lookup"><span data-stu-id="2a922-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="2a922-384">(Como la página está codificada por ahora, si escribe valores para el género y el título, la búsqueda de título obtiene precedencia). Para crear una búsqueda que combina las condiciones, deberá crear una consulta SQL que tiene una sintaxis similar al siguiente:</span><span class="sxs-lookup"><span data-stu-id="2a922-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="2a922-385">Y tendría que ejecutar la consulta mediante una instrucción similar al siguiente (en términos generales):</span><span class="sxs-lookup"><span data-stu-id="2a922-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="2a922-386">Crear la lógica para permitir las permutaciones de muchos de los criterios de búsqueda puede obtener un poco implicado, como puede ver.</span><span class="sxs-lookup"><span data-stu-id="2a922-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="2a922-387">Por lo tanto, dejaremos de aquí.</span><span class="sxs-lookup"><span data-stu-id="2a922-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="2a922-388">Próxima</span><span class="sxs-lookup"><span data-stu-id="2a922-388">Coming Up Next</span></span>

<span data-ttu-id="2a922-389">En el siguiente tutorial, creará una página que usa un formulario para permitir que los usuarios agregar películas a la base de datos.</span><span class="sxs-lookup"><span data-stu-id="2a922-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="2a922-390">Lista completa de la página de película (actualizada con la búsqueda)</span><span class="sxs-lookup"><span data-stu-id="2a922-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="2a922-391">Recursos adicionales</span><span class="sxs-lookup"><span data-stu-id="2a922-391">Additional Resources</span></span>

- [<span data-ttu-id="2a922-392">Introducción a la programación Web de ASP.NET mediante la sintaxis Razor</span><span class="sxs-lookup"><span data-stu-id="2a922-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="2a922-393">[Cláusula WHERE de SQL](http://www.w3schools.com/sql/sql_where.asp) en el sitio W3Schools</span><span class="sxs-lookup"><span data-stu-id="2a922-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="2a922-394">[Las definiciones de método](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) artículo en el sitio de W3C</span><span class="sxs-lookup"><span data-stu-id="2a922-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="2a922-395">[Anterior](displaying-data.md)
> [Siguiente](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="2a922-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
