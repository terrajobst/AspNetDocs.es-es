---
uid: web-pages/overview/performance-and-traffic/15-caching-to-improve-the-performance-of-your-website
title: Almacenamiento en caché de datos en un sitio ASP.NET Web Pages (Razor) para mejorar el rendimiento | Microsoft Docs
author: Rick-Anderson
description: Puede acelerar el sitio web si lo almacena, es decir, almacenar en caché los resultados de los datos que, normalmente, tardarán un tiempo considerable en recuperar o procesar...
ms.author: riande
ms.date: 02/14/2014
ms.assetid: 961e525b-7700-469e-8a68-d7010b6fb68c
msc.legacyurl: /web-pages/overview/performance-and-traffic/15-caching-to-improve-the-performance-of-your-website
msc.type: authoredcontent
ms.openlocfilehash: 01796d3ca699a6af5d9162b22a926551435c2040
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 03/06/2020
ms.locfileid: "78521161"
---
# <a name="caching-data-in-an-aspnet-web-pages-razor-site-for-better-performance"></a><span data-ttu-id="7b726-103">Almacenamiento en caché de datos en un sitio ASP.NET Web Pages (Razor) para mejorar el rendimiento</span><span class="sxs-lookup"><span data-stu-id="7b726-103">Caching Data in an ASP.NET Web Pages (Razor) Site for Better Performance</span></span>

<span data-ttu-id="7b726-104">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="7b726-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="7b726-105">En este artículo se explica cómo usar una aplicación auxiliar para almacenar en caché información para obtener un rendimiento más rápido en un sitio web de ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="7b726-105">This article explains how to use a helper to cache information for faster performance in an ASP.NET Web Pages (Razor) website.</span></span> <span data-ttu-id="7b726-106">Puede acelerar el sitio web porque lo almacena &#8212; , almacenar en caché &#8212; los resultados de los datos que normalmente tardarían un tiempo considerable en recuperarse o procesarse y que no cambian a menudo.</span><span class="sxs-lookup"><span data-stu-id="7b726-106">You can speed up your website by having it store &#8212; that is, cache &#8212; the results of data that ordinarily would take considerable time to retrieve or process and that does not change often.</span></span>
> 
> <span data-ttu-id="7b726-107">**Lo que aprenderá:**</span><span class="sxs-lookup"><span data-stu-id="7b726-107">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="7b726-108">Cómo usar el almacenamiento en caché para mejorar la capacidad de respuesta de su sitio Web.</span><span class="sxs-lookup"><span data-stu-id="7b726-108">How to use caching to improve the responsiveness of your website.</span></span>
> 
> <span data-ttu-id="7b726-109">Estas son las características de ASP.NET presentadas en el artículo:</span><span class="sxs-lookup"><span data-stu-id="7b726-109">These are the ASP.NET features introduced in the article:</span></span>
> 
> - <span data-ttu-id="7b726-110">Aplicación auxiliar de `WebCache`.</span><span class="sxs-lookup"><span data-stu-id="7b726-110">The `WebCache` helper.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="7b726-111">Versiones de software usadas en el tutorial</span><span class="sxs-lookup"><span data-stu-id="7b726-111">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="7b726-112">ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="7b726-112">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="7b726-113">Este tutorial también funciona con ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="7b726-113">This tutorial also works with ASP.NET Web Pages 2.</span></span>

<span data-ttu-id="7b726-114">Cada vez que alguien solicita una página de su sitio, el servidor Web tiene que hacer algún trabajo para completar la solicitud.</span><span class="sxs-lookup"><span data-stu-id="7b726-114">Every time someone requests a page from your site, the web server has to do some work in order to fulfill the request.</span></span> <span data-ttu-id="7b726-115">En algunas de las páginas, es posible que el servidor tenga que realizar tareas que tarden un período de tiempo (comparativamente), como la recuperación de datos de una base de datos.</span><span class="sxs-lookup"><span data-stu-id="7b726-115">For some of your pages, the server might have to perform tasks that take a (comparatively) long time, such as retrieving data from a database.</span></span> <span data-ttu-id="7b726-116">Incluso si estas tareas no tardan mucho en términos absolutos, si el sitio experimenta una gran cantidad de tráfico, toda una serie de solicitudes individuales que hacen que el servidor Web realice la tarea complicada o lenta puede Agregar un gran número de trabajo.</span><span class="sxs-lookup"><span data-stu-id="7b726-116">Even if these tasks don't take long in absolute terms, if your site experiences a lot of traffic, a whole series of individual requests that cause the web server to perform the complicated or slow task can add up to a lot of work.</span></span> <span data-ttu-id="7b726-117">En última instancia, esto puede afectar al rendimiento del sitio.</span><span class="sxs-lookup"><span data-stu-id="7b726-117">This can ultimately affect the performance of the site.</span></span>

<span data-ttu-id="7b726-118">Una manera de mejorar el rendimiento de su sitio web en circunstancias como esta es almacenar los datos en caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-118">One way to improve the performance of your website in circumstances like this is to cache data.</span></span> <span data-ttu-id="7b726-119">Si el sitio obtiene solicitudes repetidas para la misma información y no es necesario modificar la información para cada persona, y no es sensible a la hora, en lugar de volver a obtenerla o recalcularla, puede capturar los datos una vez y, después, almacenar los resultados.</span><span class="sxs-lookup"><span data-stu-id="7b726-119">If your site gets repeated requests for the same information, and the information does not need to be modified for each person, and it's not time sensitive, instead of re-fetching or recalculating it, you can fetch the data once and then store the results.</span></span> <span data-ttu-id="7b726-120">La próxima vez que llegue una solicitud para esa información, solo tendrá que salir de la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-120">The next time a request comes in for that information, you just get it out of the cache.</span></span>

<span data-ttu-id="7b726-121">En general, se almacena en caché información que no cambia con frecuencia.</span><span class="sxs-lookup"><span data-stu-id="7b726-121">In general, you cache information that doesn't change frequently.</span></span> <span data-ttu-id="7b726-122">Cuando se coloca información en la memoria caché, se almacena en memoria en el servidor Web.</span><span class="sxs-lookup"><span data-stu-id="7b726-122">When you put information in the cache, it's stored in memory on the web server.</span></span> <span data-ttu-id="7b726-123">Puede especificar cuánto tiempo se debe almacenar en caché, de segundos a días.</span><span class="sxs-lookup"><span data-stu-id="7b726-123">You can specify how long it should be cached, from seconds to days.</span></span> <span data-ttu-id="7b726-124">Cuando expire el período de almacenamiento en caché, la información se quitará automáticamente de la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-124">When the caching period expires, the information is automatically removed from the cache.</span></span>

> [!NOTE]
> <span data-ttu-id="7b726-125">Es posible que se quiten las entradas de la memoria caché por motivos distintos de los que han expirado.</span><span class="sxs-lookup"><span data-stu-id="7b726-125">Entries in the cache might be removed for reasons other than that they've expired.</span></span> <span data-ttu-id="7b726-126">Por ejemplo, el servidor Web podría quedarse sin memoria temporalmente y una forma de recuperar memoria es producir entradas fuera de la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-126">For example, the web server might temporarily run low on memory, and one way it can reclaim memory is by throwing entries out of the cache.</span></span> <span data-ttu-id="7b726-127">Como podrá ver, incluso si ha colocado información en la memoria caché, tendrá que comprobar que todavía está allí cuando la necesite.</span><span class="sxs-lookup"><span data-stu-id="7b726-127">As you'll see, even if you've put information into the cache, you have to check to be sure it's still there when you need it.</span></span>

<span data-ttu-id="7b726-128">Imagine que su sitio web tiene una página que muestra la temperatura actual y la previsión meteorológica.</span><span class="sxs-lookup"><span data-stu-id="7b726-128">Imagine your website has a page that displays the current temperature and weather forecast.</span></span> <span data-ttu-id="7b726-129">Para obtener este tipo de información, puede enviar una solicitud a un servicio externo.</span><span class="sxs-lookup"><span data-stu-id="7b726-129">To get this type of information, you might send a request to an external service.</span></span> <span data-ttu-id="7b726-130">Puesto que esta información no cambia mucho (en un período de dos horas, por ejemplo) y dado que las llamadas externas requieren tiempo y ancho de banda, es un buen candidato para el almacenamiento en caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-130">Since this information doesn't change much (within a two-hour time period, for example) and since external calls require time and bandwidth, it's a good candidate for caching.</span></span>

## <a name="adding-caching-to-a-page"></a><span data-ttu-id="7b726-131">Agregar almacenamiento en caché a una página</span><span class="sxs-lookup"><span data-stu-id="7b726-131">Adding Caching to a Page</span></span>

<span data-ttu-id="7b726-132">ASP.NET incluye una aplicación auxiliar `WebCache` que facilita la tarea de agregar almacenamiento en caché al sitio y agregar datos a la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-132">ASP.NET includes a `WebCache` helper that makes it easy to add caching to your site and add data to the cache.</span></span> <span data-ttu-id="7b726-133">En este procedimiento, creará una página que almacena en caché la hora actual.</span><span class="sxs-lookup"><span data-stu-id="7b726-133">In this procedure, you'll create a page that caches the current time.</span></span> <span data-ttu-id="7b726-134">No se trata de un ejemplo real, ya que la hora actual es algo que cambia a menudo y que, además, no es complejo de calcular.</span><span class="sxs-lookup"><span data-stu-id="7b726-134">This isn't a real-world example, since the current time is something that does change often, and that moreover isn't complex to calculate.</span></span> <span data-ttu-id="7b726-135">Sin embargo, es una buena manera de ilustrar el almacenamiento en caché en acción.</span><span class="sxs-lookup"><span data-stu-id="7b726-135">However, it's a good way to illustrate caching in action.</span></span>

1. <span data-ttu-id="7b726-136">Agregue una nueva página denominada *webcache. cshtml* al sitio Web.</span><span class="sxs-lookup"><span data-stu-id="7b726-136">Add a new page named *WebCache.cshtml* to the website.</span></span>
2. <span data-ttu-id="7b726-137">Agregue el código y el marcado siguientes a la página:</span><span class="sxs-lookup"><span data-stu-id="7b726-137">Add the following code and markup to the page:</span></span>

    [!code-cshtml[Main](15-caching-to-improve-the-performance-of-your-website/samples/sample1.cshtml)]

    <span data-ttu-id="7b726-138">Al almacenar datos en caché, se coloca en la memoria caché con un nombre que es único en todo el sitio Web.</span><span class="sxs-lookup"><span data-stu-id="7b726-138">When you cache data, you put it into the cache using a name this is unique across the website.</span></span> <span data-ttu-id="7b726-139">En este caso, usará una entrada de caché denominada `CachedTime`.</span><span class="sxs-lookup"><span data-stu-id="7b726-139">In this case, you'll use a cache entry named `CachedTime`.</span></span> <span data-ttu-id="7b726-140">Este es el `cacheItemKey` que se muestra en el ejemplo de código.</span><span class="sxs-lookup"><span data-stu-id="7b726-140">This is the `cacheItemKey` shown in the code example.</span></span>

    <span data-ttu-id="7b726-141">En primer lugar, el código lee la entrada de caché de `CachedTime`.</span><span class="sxs-lookup"><span data-stu-id="7b726-141">The code first reads the `CachedTime` cache entry.</span></span> <span data-ttu-id="7b726-142">Si se devuelve un valor (es decir, si la entrada de la caché no es null), el código simplemente establece el valor de la variable de tiempo en los datos de la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-142">If a value is returned (that is, if the cache entry isn't null), the code just sets the value of the time variable to the cache data.</span></span>

    <span data-ttu-id="7b726-143">Sin embargo, si la entrada de caché no existe (es decir, es null), el código establece el valor de tiempo, lo agrega a la memoria caché y establece un valor de expiración en un minuto.</span><span class="sxs-lookup"><span data-stu-id="7b726-143">However, if the cache entry doesn't exist (that is, it's null), the code sets the time value, adds it to the cache, and sets an expiration value to one minute.</span></span> <span data-ttu-id="7b726-144">Después de un minuto, se descarta la entrada de caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-144">After one minute, the cache entry is discarded.</span></span> <span data-ttu-id="7b726-145">(El valor de expiración predeterminado para un elemento de la memoria caché es de 20 minutos). El comando `WebCache.Set(cacheItemKey, time, 1, false)` muestra cómo agregar el valor de hora actual a la memoria caché y establecer su expiración en 1 minuto.</span><span class="sxs-lookup"><span data-stu-id="7b726-145">(The default expiration value for an item in the cache is 20 minutes.) The command `WebCache.Set(cacheItemKey, time, 1, false)` shows how to add the current time value to the cache and set its expiration to 1 minute.</span></span> <span data-ttu-id="7b726-146">Establecer el parámetro *SlidingExpiration* en `false` significa que la hora de expiración no se renueva cada vez que se solicita.</span><span class="sxs-lookup"><span data-stu-id="7b726-146">Setting the *slidingExpiration* parameter to `false` means the expiration time is not renewed each time it is requested.</span></span> <span data-ttu-id="7b726-147">Expirará exactamente 1 minuto después de que se agregara originalmente a la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-147">It will expire exactly 1 minute after it was originally added to the cache.</span></span> <span data-ttu-id="7b726-148">Si establece este valor en `true` se restablecerá el tiempo de expiración de 1 minuto cada vez que se solicite el valor de la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-148">If you set this value to `true` the 1 minute expiration time is reset each time the value is requested from the cache.</span></span>

    <span data-ttu-id="7b726-149">Este código muestra el patrón que se debe usar siempre al almacenar datos en caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-149">This code illustrates the pattern you should always use when you cache data.</span></span> <span data-ttu-id="7b726-150">Antes de sacar algo de la memoria caché, compruebe siempre si el método `WebCache.Get` ha devuelto null.</span><span class="sxs-lookup"><span data-stu-id="7b726-150">Before you get something out of the cache, always check first whether the `WebCache.Get` method has returned null.</span></span> <span data-ttu-id="7b726-151">Recuerde que es posible que la entrada de caché haya expirado o que se haya quitado por algún otro motivo, por lo que nunca se garantiza que una entrada determinada esté en la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-151">Remember that the cache entry might have expired or might have been removed for some other reason, so any given entry is never guaranteed to be in the cache.</span></span>
3. <span data-ttu-id="7b726-152">Ejecute *webcache. cshtml* en un explorador.</span><span class="sxs-lookup"><span data-stu-id="7b726-152">Run *WebCache.cshtml* in a browser.</span></span> <span data-ttu-id="7b726-153">(Asegúrese de que la página esté seleccionada en el área de trabajo **archivos** antes de ejecutarla). La primera vez que se solicita la página, los datos de hora no están en la memoria caché y el código tiene que agregar el valor de tiempo a la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-153">(Make sure the page is selected in the **Files** workspace before you run it.) The first time you request the page, the time data isn't in the cache, and the code has to add the time value to the cache.</span></span>

    ![cache-1](15-caching-to-improve-the-performance-of-your-website/_static/image1.jpg)
4. <span data-ttu-id="7b726-155">Actualice *webcache. cshtml* en el explorador.</span><span class="sxs-lookup"><span data-stu-id="7b726-155">Refresh *WebCache.cshtml* in the browser.</span></span> <span data-ttu-id="7b726-156">Esta vez, los datos de hora están en la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-156">This time, the time data is in the cache.</span></span> <span data-ttu-id="7b726-157">Tenga en cuenta que la hora no ha cambiado desde la última vez que vio la página.</span><span class="sxs-lookup"><span data-stu-id="7b726-157">Notice that the time hasn't changed since the last time you viewed the page.</span></span>

    ![cache-2](15-caching-to-improve-the-performance-of-your-website/_static/image2.jpg)
5. <span data-ttu-id="7b726-159">Espere un minuto para que se vacíe la memoria caché y, a continuación, actualice la página.</span><span class="sxs-lookup"><span data-stu-id="7b726-159">Wait one minute for the cache to be emptied, and then refresh the page.</span></span> <span data-ttu-id="7b726-160">La página vuelve a indicar que los datos de hora no se encontraron en la memoria caché y la hora actualizada se agrega a la caché.</span><span class="sxs-lookup"><span data-stu-id="7b726-160">The page again indicates that the time data wasn't found in the cache, and the updated time is added to the cache.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="7b726-161">Recursos adicionales</span><span class="sxs-lookup"><span data-stu-id="7b726-161">Additional Resources</span></span>

- [<span data-ttu-id="7b726-162">Mostrar datos en un gráfico</span><span class="sxs-lookup"><span data-stu-id="7b726-162">Displaying Data in a Chart</span></span>](https://go.microsoft.com/fwlink/?LinkId=202895)
- <span data-ttu-id="7b726-163">[Referencia de API de webcache](https://msdn.microsoft.com/library/system.web.helpers.webcache(v=vs.99).aspx) (MSDN)</span><span class="sxs-lookup"><span data-stu-id="7b726-163">[WebCache API reference](https://msdn.microsoft.com/library/system.web.helpers.webcache(v=vs.99).aspx) (MSDN)</span></span>
