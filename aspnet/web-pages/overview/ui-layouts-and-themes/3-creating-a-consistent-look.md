---
uid: web-pages/overview/ui-layouts-and-themes/3-creating-a-consistent-look
title: Crear un diseño coherente en ASP.NET Web Pages (Razor) sitios | Microsoft Docs
author: Rick-Anderson
description: Para que sea más eficaz para crear páginas web para su sitio, puede crear bloques de contenido (por ejemplo, los encabezados y pies de página) para su sitio Web y c...
ms.author: riande
ms.date: 03/10/2014
ms.assetid: d7bd001b-6db2-4422-9b78-f3d08b743b00
msc.legacyurl: /web-pages/overview/ui-layouts-and-themes/3-creating-a-consistent-look
msc.type: authoredcontent
ms.openlocfilehash: 7ed2f5da62f4521b42db737100230fac5ea71d67
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 04/17/2019
ms.locfileid: "59385986"
---
# <a name="creating-a-consistent-layout-in-aspnet-web-pages-razor-sites"></a><span data-ttu-id="c1c10-103">Crear un diseño coherente en los sitios de ASP.NET Web Pages (Razor)</span><span class="sxs-lookup"><span data-stu-id="c1c10-103">Creating a Consistent Layout in ASP.NET Web Pages (Razor) Sites</span></span>

<span data-ttu-id="c1c10-104">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="c1c10-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="c1c10-105">En este artículo se explica cómo usar las páginas de diseño en un sitio Web de ASP.NET Web Pages (Razor) para crear bloques de contenido (por ejemplo, los encabezados y pies de página) y para crear una apariencia coherente para todas las páginas del sitio.</span><span class="sxs-lookup"><span data-stu-id="c1c10-105">This article explains how you can use layout pages in an ASP.NET Web Pages (Razor) website to create reusable blocks of content (like headers and footers) and to create a consistent look for all the pages in the site.</span></span>
> 
> <span data-ttu-id="c1c10-106">**Lo que aprenderá:**</span><span class="sxs-lookup"><span data-stu-id="c1c10-106">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="c1c10-107">Cómo crear bloques de contenido, como los encabezados y pies de página.</span><span class="sxs-lookup"><span data-stu-id="c1c10-107">How to create reusable blocks of content like headers and footers.</span></span>
> - <span data-ttu-id="c1c10-108">Cómo crear una apariencia coherente para todas las páginas en el sitio con un diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-108">How to create a consistent look for all the pages in your site using a layout.</span></span>
> - <span data-ttu-id="c1c10-109">Cómo pasar datos en tiempo de ejecución a una página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-109">How to pass data at run time to a layout page.</span></span>
> 
> <span data-ttu-id="c1c10-110">Estas son las características ASP.NET incorporadas en el artículo:</span><span class="sxs-lookup"><span data-stu-id="c1c10-110">These are the ASP.NET features introduced in the article:</span></span>
> 
> - <span data-ttu-id="c1c10-111">Bloques de contenido, que son archivos que contienen contenido con formato HTML que deben insertarse en varias páginas.</span><span class="sxs-lookup"><span data-stu-id="c1c10-111">Content blocks, which are files that contain HTML-formatted content to be inserted in multiple pages.</span></span>
> - <span data-ttu-id="c1c10-112">Páginas de diseño, que son las páginas que contienen contenido con formato HTML que se pueda compartir entre las páginas del sitio Web.</span><span class="sxs-lookup"><span data-stu-id="c1c10-112">Layout pages, which are pages that contain HTML-formatted content that can be shared by pages on the website.</span></span>
> - <span data-ttu-id="c1c10-113">El `RenderPage`, `RenderBody`, y `RenderSection` métodos, que indican dónde se va a insertar los elementos de la página ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="c1c10-113">The `RenderPage`, `RenderBody`, and `RenderSection` methods, which tell ASP.NET where to insert page elements.</span></span>
> - <span data-ttu-id="c1c10-114">El `PageData` diccionario que le permite compartir datos entre bloques de contenido y las páginas de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-114">The `PageData` dictionary that lets you share data between content blocks and layout pages.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="c1c10-115">Versiones de software que se usa en el tutorial</span><span class="sxs-lookup"><span data-stu-id="c1c10-115">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="c1c10-116">ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="c1c10-116">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="c1c10-117">Este tutorial también funciona con ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="c1c10-117">This tutorial also works with ASP.NET Web Pages 2.</span></span>


## <a name="about-layout-pages"></a><span data-ttu-id="c1c10-118">Acerca de las páginas de diseño</span><span class="sxs-lookup"><span data-stu-id="c1c10-118">About Layout Pages</span></span>

<span data-ttu-id="c1c10-119">Muchos sitios Web tienen contenido que se muestra en cada página, como un encabezado y pie de página o un cuadro que indica a los usuarios que inician sesión.</span><span class="sxs-lookup"><span data-stu-id="c1c10-119">Many websites have content that's displayed on every page, like a header and footer, or a box that tells users that they're logged in.</span></span> <span data-ttu-id="c1c10-120">ASP.NET permite crear un archivo independiente con un bloque de contenido que puede contener texto, marcado y código, al igual que una página web normal.</span><span class="sxs-lookup"><span data-stu-id="c1c10-120">ASP.NET lets you create a separate file with a content block that can contain text, markup, and code, just like a regular web page.</span></span> <span data-ttu-id="c1c10-121">A continuación, puede insertar el bloque de contenido en otras páginas en el sitio donde desea que aparezca la información.</span><span class="sxs-lookup"><span data-stu-id="c1c10-121">You can then insert the content block in other pages on the site where you want the information to appear.</span></span> <span data-ttu-id="c1c10-122">De este modo que no tiene que copiar y pegar el mismo contenido en cada página.</span><span class="sxs-lookup"><span data-stu-id="c1c10-122">That way you don't have to copy and paste the same content into every page.</span></span> <span data-ttu-id="c1c10-123">Creación de contenido comunes como esta también resulta más fácil actualizar el sitio.</span><span class="sxs-lookup"><span data-stu-id="c1c10-123">Creating common content like this also makes it easier to update your site.</span></span> <span data-ttu-id="c1c10-124">Si necesita cambiar el contenido, puede actualizar simplemente un único archivo y, a continuación, se reflejan los cambios en todas partes se ha insertado el contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-124">If you need to change the content, you can just update a single file, and the changes are then reflected everywhere the content has been inserted.</span></span>

<span data-ttu-id="c1c10-125">El diagrama siguiente muestra cómo contenido bloquea el trabajo.</span><span class="sxs-lookup"><span data-stu-id="c1c10-125">The following diagram shows how content blocks work.</span></span> <span data-ttu-id="c1c10-126">Cuando un explorador solicita una página desde el servidor web, ASP.NET inserta los bloques de contenido en el punto donde el `RenderPage` se llama al método en la página principal.</span><span class="sxs-lookup"><span data-stu-id="c1c10-126">When a browser requests a page from the web server, ASP.NET inserts the content blocks at the point where the `RenderPage` method is called in the main page.</span></span> <span data-ttu-id="c1c10-127">La página final (combina), a continuación, se envía al explorador.</span><span class="sxs-lookup"><span data-stu-id="c1c10-127">The finished (merged) page is then sent to the browser.</span></span>

![Diagrama conceptual que se muestra cómo el método RenderPage inserta una página que se hace referencia en la página actual.](3-creating-a-consistent-look/_static/image1.jpg)

<span data-ttu-id="c1c10-129">En este procedimiento, creará una página que hace referencia a dos bloques de contenido (un encabezado y un pie de página) que se encuentran en archivos independientes.</span><span class="sxs-lookup"><span data-stu-id="c1c10-129">In this procedure, you'll create a page that references two content blocks (a header and a footer) that are located in separate files.</span></span> <span data-ttu-id="c1c10-130">Puede usar estos mismos bloques de contenido en cualquier página del sitio.</span><span class="sxs-lookup"><span data-stu-id="c1c10-130">You can use these same content blocks in any page in your site.</span></span> <span data-ttu-id="c1c10-131">Cuando haya terminado, aparecerá una página similar al siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-131">When you're done, you'll get a page like this:</span></span>

![Captura de pantalla que muestra una página en el explorador que resulta de la ejecución de una página que incluye llamadas al método RenderPage.](3-creating-a-consistent-look/_static/image2.jpg)

1. <span data-ttu-id="c1c10-133">En la carpeta raíz de su sitio Web, cree un archivo denominado *Index.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="c1c10-133">In the root folder of your website, create a file named *Index.cshtml*.</span></span>
2. <span data-ttu-id="c1c10-134">Reemplace el marcado existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-134">Replace the existing markup with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample1.html)]
3. <span data-ttu-id="c1c10-135">En la carpeta raíz, cree una carpeta denominada *Shared*.</span><span class="sxs-lookup"><span data-stu-id="c1c10-135">In the root folder, create a folder named *Shared*.</span></span>

    > [!NOTE]
    > <span data-ttu-id="c1c10-136">Es una práctica común para almacenar los archivos que se comparten entre las páginas web en una carpeta denominada *Shared*.</span><span class="sxs-lookup"><span data-stu-id="c1c10-136">It's common practice to store files that are shared among web pages in a folder named *Shared*.</span></span>
4. <span data-ttu-id="c1c10-137">En el *Shared* carpeta, cree un archivo denominado  *\_Header.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="c1c10-137">In the *Shared* folder, create a file named *\_Header.cshtml*.</span></span>
5. <span data-ttu-id="c1c10-138">Reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-138">Replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample2.html)]

    <span data-ttu-id="c1c10-139">Tenga en cuenta que es el nombre de archivo  *\_Header.cshtml*, con un carácter de subrayado (\_) como prefijo.</span><span class="sxs-lookup"><span data-stu-id="c1c10-139">Notice that the file name is *\_Header.cshtml*, with an underscore (\_) as a prefix.</span></span> <span data-ttu-id="c1c10-140">ASP.NET no enviará una página en el explorador si su nombre comienza con un carácter de subrayado.</span><span class="sxs-lookup"><span data-stu-id="c1c10-140">ASP.NET won't send a page to the browser if its name starts with an underscore.</span></span> <span data-ttu-id="c1c10-141">Esto impide que las personas que soliciten (por accidente o de otro modo) estas páginas directamente.</span><span class="sxs-lookup"><span data-stu-id="c1c10-141">This prevents people from requesting (inadvertently or otherwise) these pages directly.</span></span> <span data-ttu-id="c1c10-142">Es una buena idea usar un carácter de subrayado en las páginas de nombre que tienen los bloques de contenido en ellos, porque realmente no desea que los usuarios puedan solicitar estas páginas &#8212; existen estrictamente para insertarse en otras páginas.</span><span class="sxs-lookup"><span data-stu-id="c1c10-142">It's a good idea to use an underscore to name pages that have content blocks in them, because you don't really want users to be able to request these pages &#8212; they exist strictly to be inserted into other pages.</span></span>
6. <span data-ttu-id="c1c10-143">En el *Shared* carpeta, cree un archivo denominado  *\_Footer.cshtml* y reemplace el contenido por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-143">In the *Shared* folder, create a file named *\_Footer.cshtml* and replace the content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample3.html)]
7. <span data-ttu-id="c1c10-144">En el *Index.cshtml* página, agregue las dos llamadas a la `RenderPage` método, como se muestra aquí:</span><span class="sxs-lookup"><span data-stu-id="c1c10-144">In the *Index.cshtml* page, add two calls to the `RenderPage` method, as shown here:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample4.html)]

    <span data-ttu-id="c1c10-145">Esto muestra cómo insertar un bloque de contenido en una página web.</span><span class="sxs-lookup"><span data-stu-id="c1c10-145">This shows how to insert a content block into a web page.</span></span> <span data-ttu-id="c1c10-146">Se llama a la `RenderPage` método y pásele el nombre del archivo cuyo contenido desea insertar en ese momento.</span><span class="sxs-lookup"><span data-stu-id="c1c10-146">You call the `RenderPage` method and pass it the name of the file whose contents you want to insert at that point.</span></span> <span data-ttu-id="c1c10-147">En este caso, desea insertar el contenido de la  *\_Header.cshtml* y  *\_Footer.cshtml* archivos en el *Index.cshtml* archivo.</span><span class="sxs-lookup"><span data-stu-id="c1c10-147">Here, you're inserting the contents of the *\_Header.cshtml* and *\_Footer.cshtml* files into the *Index.cshtml* file.</span></span>
8. <span data-ttu-id="c1c10-148">Ejecute el *Index.cshtml* página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="c1c10-148">Run the *Index.cshtml* page in a browser.</span></span> <span data-ttu-id="c1c10-149">(En WebMatrix, en el **archivos** área de trabajo, haga clic en el archivo y, a continuación, seleccione **iniciar en el explorador**.)</span><span class="sxs-lookup"><span data-stu-id="c1c10-149">(In WebMatrix, in the **Files** workspace, right-click the file and then select **Launch in browser**.)</span></span>
9. <span data-ttu-id="c1c10-150">En el explorador, vea el origen de la página.</span><span class="sxs-lookup"><span data-stu-id="c1c10-150">In the browser, view the page source.</span></span> <span data-ttu-id="c1c10-151">(Por ejemplo, en Internet Explorer, haga clic en la página y, a continuación, haga clic en **ver código fuente**.)</span><span class="sxs-lookup"><span data-stu-id="c1c10-151">(For example, in Internet Explorer, right-click the page and then click **View Source**.)</span></span>

    <span data-ttu-id="c1c10-152">Esto le permite ver el marcado de página web que se envía al explorador, que combina el marcado de página de índice con los bloques de contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-152">This lets you see the web page markup that's sent to the browser, which combines the index page markup with the content blocks.</span></span> <span data-ttu-id="c1c10-153">El ejemplo siguiente muestra el origen de la página que se represente para *Index.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="c1c10-153">The following example shows the page source that's rendered for *Index.cshtml*.</span></span> <span data-ttu-id="c1c10-154">Las llamadas a `RenderPage` insertadas en *Index.cshtml* se han reemplazado con el contenido real de los archivos de encabezado y pie de página.</span><span class="sxs-lookup"><span data-stu-id="c1c10-154">The calls to `RenderPage` that you inserted into *Index.cshtml* have been replaced with the actual contents of the header and footer files.</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample5.html)]

## <a name="creating-a-consistent-look-using-layout-pages"></a><span data-ttu-id="c1c10-155">Crear una apariencia coherente con las páginas de diseño</span><span class="sxs-lookup"><span data-stu-id="c1c10-155">Creating a Consistent Look Using Layout Pages</span></span>

<span data-ttu-id="c1c10-156">Hasta ahora ha visto que es fácil incluir el mismo contenido en varias páginas.</span><span class="sxs-lookup"><span data-stu-id="c1c10-156">So far you've seen that it's easy to include the same content on multiple pages.</span></span> <span data-ttu-id="c1c10-157">Un enfoque más estructurado para crear una apariencia coherente para un sitio es usar las páginas de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-157">A more structured approach to creating a consistent look for a site is to use layout pages.</span></span> <span data-ttu-id="c1c10-158">Una página de diseño define la estructura de una página web, pero no contiene ningún contenido real.</span><span class="sxs-lookup"><span data-stu-id="c1c10-158">A layout page defines the structure of a web page, but doesn't contain any actual content.</span></span> <span data-ttu-id="c1c10-159">Después de crear una página de diseño, puede crear páginas web que contienen el contenido y, a continuación, vincularlos a la página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-159">After you've created a layout page, you can create web pages that contain the content and then link them to the layout page.</span></span> <span data-ttu-id="c1c10-160">Cuando se muestran estas páginas, se le aplica formato según la página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-160">When these pages are displayed, they'll be formatted according to the layout page.</span></span> <span data-ttu-id="c1c10-161">(En este sentido, una página de diseño actúa como un tipo de plantilla para el contenido que se define en otras páginas).</span><span class="sxs-lookup"><span data-stu-id="c1c10-161">(In this sense, a layout page acts as a kind of template for content that's defined in other pages.)</span></span>

<span data-ttu-id="c1c10-162">La página de diseño es igual que cualquier página HTML, salvo que contiene una llamada a la `RenderBody` método.</span><span class="sxs-lookup"><span data-stu-id="c1c10-162">The layout page is just like any HTML page, except that it contains a call to the `RenderBody` method.</span></span> <span data-ttu-id="c1c10-163">La posición de la `RenderBody` método en la página de diseño determina dónde se incluirá la información de la página de contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-163">The position of the `RenderBody` method in the layout page determines where the information from the content page will be included.</span></span>

<span data-ttu-id="c1c10-164">El siguiente diagrama muestra las páginas de contenido y las páginas de diseño se combinan en tiempo de ejecución para generar la página web finalizada.</span><span class="sxs-lookup"><span data-stu-id="c1c10-164">The following diagram shows how content pages and layout pages are combined at run time to produce the finished web page.</span></span> <span data-ttu-id="c1c10-165">El explorador solicita una página de contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-165">The browser requests a content page.</span></span> <span data-ttu-id="c1c10-166">La página de contenido contiene código que especifica la página de diseño que se usará para la estructura de la página.</span><span class="sxs-lookup"><span data-stu-id="c1c10-166">The content page has code in it that specifies the layout page to use for the page's structure.</span></span> <span data-ttu-id="c1c10-167">En la página de diseño, el contenido se insertará en el punto donde el `RenderBody` se llama al método.</span><span class="sxs-lookup"><span data-stu-id="c1c10-167">In the layout page, the content is inserted at the point where the `RenderBody` method is called.</span></span> <span data-ttu-id="c1c10-168">Contenido de bloques también se pueden insertar en la página de diseño mediante una llamada a la `RenderPage` método, la manera en la sección anterior.</span><span class="sxs-lookup"><span data-stu-id="c1c10-168">Content blocks can also be inserted into the layout page by calling the `RenderPage` method, the way you did in the previous section.</span></span> <span data-ttu-id="c1c10-169">Una vez completada la página web, se envía al explorador.</span><span class="sxs-lookup"><span data-stu-id="c1c10-169">When the web page is complete, it's sent to the browser.</span></span>

![Captura de pantalla que muestra una página en el explorador que resulta de la ejecución de una página que incluye llamadas al método RenderBody.](3-creating-a-consistent-look/_static/image3.jpg)

<span data-ttu-id="c1c10-171">El procedimiento siguiente muestra cómo crear un diseño de páginas de contenido de página y un vínculo a él.</span><span class="sxs-lookup"><span data-stu-id="c1c10-171">The following procedure shows how to create a layout page and link content pages to it.</span></span>

1. <span data-ttu-id="c1c10-172">En el *Shared* carpeta del sitio Web, cree un archivo denominado  *\_Layout1.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="c1c10-172">In the *Shared* folder of your website, create a file named *\_Layout1.cshtml*.</span></span>
2. <span data-ttu-id="c1c10-173">Reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-173">Replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample6.html)]

    <span data-ttu-id="c1c10-174">Usa el `RenderPage` método en una página de diseño para insertar bloques de contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-174">You use the `RenderPage` method in a layout page to insert content blocks.</span></span> <span data-ttu-id="c1c10-175">Una página de diseño puede contener sólo una llamada a la `RenderBody` método.</span><span class="sxs-lookup"><span data-stu-id="c1c10-175">A layout page can contain only one call to the `RenderBody` method.</span></span>
3. <span data-ttu-id="c1c10-176">En el *Shared* carpeta, cree un archivo denominado  *\_Header2.cshtml* y reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-176">In the *Shared* folder, create a file named *\_Header2.cshtml* and replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample7.html)]
4. <span data-ttu-id="c1c10-177">En la carpeta raíz, cree una nueva carpeta y asígnele el nombre *estilos*.</span><span class="sxs-lookup"><span data-stu-id="c1c10-177">In the root folder, create a new folder and name it *Styles*.</span></span>
5. <span data-ttu-id="c1c10-178">En el *estilos* carpeta, cree un archivo denominado *Site.css* y agregue las siguientes definiciones de estilo:</span><span class="sxs-lookup"><span data-stu-id="c1c10-178">In the *Styles* folder, create a file named *Site.css* and add the following style definitions:</span></span>

    [!code-css[Main](3-creating-a-consistent-look/samples/sample8.css)]

    <span data-ttu-id="c1c10-179">Estas definiciones de estilo aquí son solo mostrar cómo se pueden usar las hojas de estilos con las páginas de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-179">These style definitions are here only to show how style sheets can be used with layout pages.</span></span> <span data-ttu-id="c1c10-180">Si lo desea, puede definir sus propios estilos para estos elementos.</span><span class="sxs-lookup"><span data-stu-id="c1c10-180">If you want, you can define your own styles for these elements.</span></span>
6. <span data-ttu-id="c1c10-181">En la carpeta raíz, cree un archivo denominado *Content1.cshtml* y reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-181">In the root folder, create a file named *Content1.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample9.cshtml)]

    <span data-ttu-id="c1c10-182">Se trata de una página que va a usar una página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-182">This is a page that will use a layout page.</span></span> <span data-ttu-id="c1c10-183">El bloque de código en la parte superior de la página indica qué página de diseño se utiliza para dar formato a este contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-183">The code block at the top of the page indicates which layout page to use to format this content.</span></span>
7. <span data-ttu-id="c1c10-184">Ejecute *Content1.cshtml* en un explorador.</span><span class="sxs-lookup"><span data-stu-id="c1c10-184">Run *Content1.cshtml* in a browser.</span></span> <span data-ttu-id="c1c10-185">La página representada utiliza el formato y la hoja de estilos definidos en  *\_Layout1.cshtml* y el texto (contenido) definido en *Content1.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="c1c10-185">The rendered page uses the format and style sheet defined in *\_Layout1.cshtml* and the text (content) defined in *Content1.cshtml*.</span></span>

    ![[image]](3-creating-a-consistent-look/_static/image4.jpg)

    <span data-ttu-id="c1c10-187">Puede repetir el paso 6 para crear páginas de contenido adicionales que, a continuación, se pueden compartir la misma página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-187">You can repeat step 6 to create additional content pages that can then share the same layout page.</span></span>

    > [!NOTE]
    > <span data-ttu-id="c1c10-188">Puede configurar su sitio para que se pueden usar automáticamente la misma página de diseño para todas las páginas de contenido en una carpeta.</span><span class="sxs-lookup"><span data-stu-id="c1c10-188">You can set up your site so that you can automatically use the same layout page for all the content pages in a folder.</span></span> <span data-ttu-id="c1c10-189">Para obtener más información, consulte [personalizar el comportamiento de todo el sitio para ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=202906).</span><span class="sxs-lookup"><span data-stu-id="c1c10-189">For details, see [Customizing Site-Wide Behavior for ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=202906).</span></span>

## <a name="designing-layout-pages-that-have-multiple-content-sections"></a><span data-ttu-id="c1c10-190">Diseñar páginas de diseño que tienen varias secciones de contenido</span><span class="sxs-lookup"><span data-stu-id="c1c10-190">Designing Layout Pages That Have Multiple Content Sections</span></span>

<span data-ttu-id="c1c10-191">Una página de contenido puede tener varias secciones, lo que resulta útil si desea usar diseños que tienen varias áreas con contenido reemplazable.</span><span class="sxs-lookup"><span data-stu-id="c1c10-191">A content page can have multiple sections, which is useful if you want to use layouts that have multiple areas with replaceable content.</span></span> <span data-ttu-id="c1c10-192">En la página de contenido, asigne cada sección de un nombre único.</span><span class="sxs-lookup"><span data-stu-id="c1c10-192">In the content page, you give each section a unique name.</span></span> <span data-ttu-id="c1c10-193">(Se deja la sección predeterminada sin nombre.) En la página de diseño, agregue un `RenderBody` método para especificar dónde debe aparecer la sección sin nombre (valor predeterminado).</span><span class="sxs-lookup"><span data-stu-id="c1c10-193">(The default section is left unnamed.) In the layout page, you add a `RenderBody` method to specify where the unnamed (default) section should appear.</span></span> <span data-ttu-id="c1c10-194">A continuación, agregar independiente `RenderSection` métodos para representar las secciones individualmente.</span><span class="sxs-lookup"><span data-stu-id="c1c10-194">You then add separate `RenderSection` methods in order to render named sections individually.</span></span>

<span data-ttu-id="c1c10-195">El diagrama siguiente muestra cómo controla ASP.NET el contenido que se divide en varias secciones.</span><span class="sxs-lookup"><span data-stu-id="c1c10-195">The following diagram shows how ASP.NET handles content that's divided into multiple sections.</span></span> <span data-ttu-id="c1c10-196">Cada sección con nombre se encuentra en un bloque de sección en la página de contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-196">Each named section is contained in a section block in the content page.</span></span> <span data-ttu-id="c1c10-197">(Nombre que tengan `Header` y `List` en el ejemplo.) El marco de trabajo inserta la sección de contenido en la página de diseño en el punto donde el `RenderSection` se llama al método.</span><span class="sxs-lookup"><span data-stu-id="c1c10-197">(They're named `Header` and `List` in the example.) The framework inserts content section into the layout page at the point where the `RenderSection` method is called.</span></span> <span data-ttu-id="c1c10-198">La sección sin nombre (valor predeterminado) se inserta en el punto donde el `RenderBody` se llama el método, como se vio anteriormente.</span><span class="sxs-lookup"><span data-stu-id="c1c10-198">The unnamed (default) section is inserted at the point where the `RenderBody` method is called, as you saw earlier.</span></span>

![Diagrama conceptual que se muestra cómo el método RenderSection inserta secciones de referencias en la página actual.](3-creating-a-consistent-look/_static/image5.jpg)

<span data-ttu-id="c1c10-200">Este procedimiento muestra cómo crear una página de contenido que tiene varias secciones de contenido y cómo se representan mediante una página de diseño que admita varias secciones de contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-200">This procedure shows how to create a content page that has multiple content sections and how to render it using a layout page that supports multiple content sections.</span></span>

1. <span data-ttu-id="c1c10-201">En el *Shared* carpeta, cree un archivo denominado  *\_Layout2.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="c1c10-201">In the *Shared* folder, create a file named *\_Layout2.cshtml*.</span></span>
2. <span data-ttu-id="c1c10-202">Reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-202">Replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample10.html)]

    <span data-ttu-id="c1c10-203">Usa el `RenderSection` método para representar el encabezado y la lista de secciones.</span><span class="sxs-lookup"><span data-stu-id="c1c10-203">You use the `RenderSection` method to render both the header and list sections.</span></span>
3. <span data-ttu-id="c1c10-204">En la carpeta raíz, cree un archivo denominado *Content2.cshtml* y reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-204">In the root folder, create a file named *Content2.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample11.cshtml)]

    <span data-ttu-id="c1c10-205">Esta página de contenido contiene un bloque de código en la parte superior de la página.</span><span class="sxs-lookup"><span data-stu-id="c1c10-205">This content page contains a code block at the top of the page.</span></span> <span data-ttu-id="c1c10-206">Cada sección con nombre se encuentra en un bloque de sección.</span><span class="sxs-lookup"><span data-stu-id="c1c10-206">Each named section is contained in a section block.</span></span> <span data-ttu-id="c1c10-207">El resto de la página contiene la sección de contenido predeterminado (sin nombre).</span><span class="sxs-lookup"><span data-stu-id="c1c10-207">The rest of the page contains the default (unnamed) content section.</span></span>
4. <span data-ttu-id="c1c10-208">Ejecute *Content2.cshtml* en un explorador.</span><span class="sxs-lookup"><span data-stu-id="c1c10-208">Run *Content2.cshtml* in a browser.</span></span>

    ![Captura de pantalla que muestra una página en el explorador que resulta de la ejecución de una página que incluye llamadas al método RenderSection.](3-creating-a-consistent-look/_static/image6.jpg)

## <a name="making-content-sections-optional"></a><span data-ttu-id="c1c10-210">Hacer que las secciones de contenido opcional</span><span class="sxs-lookup"><span data-stu-id="c1c10-210">Making Content Sections Optional</span></span>

<span data-ttu-id="c1c10-211">Normalmente, las secciones que se crean en una página de contenido necesario para que coincida con las secciones que se definen en la página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-211">Normally, the sections that you create in a content page have to match sections that are defined in the layout page.</span></span> <span data-ttu-id="c1c10-212">Puede obtener errores si se produce alguna de las siguientes acciones:</span><span class="sxs-lookup"><span data-stu-id="c1c10-212">You can get errors if any of the following occur:</span></span>

- <span data-ttu-id="c1c10-213">La página de contenido contiene una sección con ninguna sección correspondiente en la página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-213">The content page contains a section that has no corresponding section in the layout page.</span></span>
- <span data-ttu-id="c1c10-214">La página de diseño contiene una sección para el que no hay ningún contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-214">The layout page contains a section for which there's no content.</span></span>
- <span data-ttu-id="c1c10-215">La página de diseño incluye llamadas de método que intentan representar varias veces la misma sección.</span><span class="sxs-lookup"><span data-stu-id="c1c10-215">The layout page includes method calls that try to render the same section more than once.</span></span>

<span data-ttu-id="c1c10-216">Sin embargo, puede invalidar este comportamiento para una sección con nombre mediante la declaración de la sección para que sea opcional en la página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-216">However, you can override this behavior for a named section by declaring the section to be optional in the layout page.</span></span> <span data-ttu-id="c1c10-217">Esto le permite definir varias páginas de contenido que puede compartir una página de diseño, pero que puede o no tener el contenido de una sección concreta.</span><span class="sxs-lookup"><span data-stu-id="c1c10-217">This lets you define multiple content pages that can share a layout page but that might or might not have content for a specific section.</span></span>

1. <span data-ttu-id="c1c10-218">Abra *Content2.cshtml* y quite la siguiente sección:</span><span class="sxs-lookup"><span data-stu-id="c1c10-218">Open *Content2.cshtml* and remove the following section:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample12.cshtml)]
2. <span data-ttu-id="c1c10-219">Guarde la página y, a continuación, ejecútelo en un explorador.</span><span class="sxs-lookup"><span data-stu-id="c1c10-219">Save the page and then run it in a browser.</span></span> <span data-ttu-id="c1c10-220">Se muestra un mensaje de error, porque la página de contenido no proporciona el contenido de una sección definida en la página de diseño, es decir, la sección de encabezado.</span><span class="sxs-lookup"><span data-stu-id="c1c10-220">An error message is displayed, because the content page doesn't provide content for a section defined in the layout page, namely the header section.</span></span>

    ![Captura de pantalla que muestra el error que se produce si se ejecuta una página que llama al método RenderSection, pero no se proporciona la sección correspondiente.](3-creating-a-consistent-look/_static/image7.jpg)
3. <span data-ttu-id="c1c10-222">En el *Shared* carpeta, abra el  *\_Layout2.cshtml* página y reemplace esta línea:</span><span class="sxs-lookup"><span data-stu-id="c1c10-222">In the *Shared* folder, open the *\_Layout2.cshtml* page and replace this line:</span></span>

    [!code-javascript[Main](3-creating-a-consistent-look/samples/sample13.js)]

    <span data-ttu-id="c1c10-223">con el código siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-223">with the following code:</span></span>

    [!code-javascript[Main](3-creating-a-consistent-look/samples/sample14.js)]

    <span data-ttu-id="c1c10-224">Como alternativa, podría reemplazar la línea de código anterior con el siguiente bloque de código, lo que produce los mismos resultados:</span><span class="sxs-lookup"><span data-stu-id="c1c10-224">As an alternative, you could replace the previous line of code with the following code block, which produces the same results:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample15.cshtml)]
4. <span data-ttu-id="c1c10-225">Ejecute el *Content2.cshtml* página en un explorador nuevo.</span><span class="sxs-lookup"><span data-stu-id="c1c10-225">Run the *Content2.cshtml* page in a browser again.</span></span> <span data-ttu-id="c1c10-226">(Si todavía tiene abierto en el Explorador de esta página, puede simplemente actualizarlo.) Esta vez la página se muestra con ningún error, incluso aunque no tenga ningún encabezado.</span><span class="sxs-lookup"><span data-stu-id="c1c10-226">(If you still have this page open in the browser, you can just refresh it.) This time the page is displayed with no error, even though it has no header.</span></span>

## <a name="passing-data-to-layout-pages"></a><span data-ttu-id="c1c10-227">Pasar datos a las páginas de diseño</span><span class="sxs-lookup"><span data-stu-id="c1c10-227">Passing Data to Layout Pages</span></span>

<span data-ttu-id="c1c10-228">Es posible que tenga datos definidos en la página de contenido que necesita para hacer referencia en una página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-228">You might have data defined in the content page that you need to refer to in a layout page.</span></span> <span data-ttu-id="c1c10-229">Si es así, deberá pasar los datos de la página de contenido a la página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-229">If so, you need to pass the data from the content page to the layout page.</span></span> <span data-ttu-id="c1c10-230">Por ejemplo, desea mostrar el estado de inicio de sesión de un usuario o es posible que desee mostrar u ocultar áreas de contenido basadas en la entrada del usuario.</span><span class="sxs-lookup"><span data-stu-id="c1c10-230">For example, you might want to display the login status of a user, or you might want to show or hide content areas based on user input.</span></span>

<span data-ttu-id="c1c10-231">Para pasar datos de una página de contenido a una página de diseño, puede colocar valores en el `PageData` propiedad de la página de contenido.</span><span class="sxs-lookup"><span data-stu-id="c1c10-231">To pass data from a content page to a layout page, you can put values into the `PageData` property of the content page.</span></span> <span data-ttu-id="c1c10-232">El `PageData` propiedad es una colección de pares de nombre-valor que contienen los datos que se van a pasar entre las páginas.</span><span class="sxs-lookup"><span data-stu-id="c1c10-232">The `PageData` property is a collection of name/value pairs that hold the data that you want to pass between pages.</span></span> <span data-ttu-id="c1c10-233">En la página de diseño, a continuación, puede leer los valores de la `PageData` propiedad.</span><span class="sxs-lookup"><span data-stu-id="c1c10-233">In the layout page, you can then read values out of the `PageData` property.</span></span>

<span data-ttu-id="c1c10-234">Este es otro diagrama.</span><span class="sxs-lookup"><span data-stu-id="c1c10-234">Here's another diagram.</span></span> <span data-ttu-id="c1c10-235">Éste muestra cómo puede usar ASP.NET el `PageData` propiedad para pasar valores de una página de contenido a la página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-235">This one shows how ASP.NET can use the `PageData` property to pass values from a content page to the layout page.</span></span> <span data-ttu-id="c1c10-236">Cuando ASP.NET comienza la creación de la página web, crea el `PageData` colección.</span><span class="sxs-lookup"><span data-stu-id="c1c10-236">When ASP.NET begins building the web page, it creates the `PageData` collection.</span></span> <span data-ttu-id="c1c10-237">En la página de contenido, escribe código para colocar datos en el `PageData` colección.</span><span class="sxs-lookup"><span data-stu-id="c1c10-237">In the content page, you write code to put data in the `PageData` collection.</span></span> <span data-ttu-id="c1c10-238">Los valores en el `PageData` colección también puede obtenerse mediante otras secciones en la página de contenido o bloques de contenido adicionales.</span><span class="sxs-lookup"><span data-stu-id="c1c10-238">Values in the `PageData` collection can also be accessed by other sections in the content page or by additional content blocks.</span></span>

![Diagrama conceptual que se muestra cómo puede rellenar un diccionario PageData y pasar esa información a la página de diseño de una página de contenido.](3-creating-a-consistent-look/_static/image8.jpg)

<span data-ttu-id="c1c10-240">El siguiente procedimiento muestra cómo pasar datos de una página de contenido a una página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-240">The following procedure shows how to pass data from a content page to a layout page.</span></span> <span data-ttu-id="c1c10-241">Cuando se ejecuta la página, muestra un botón que permite al usuario mostrar u ocultar una lista que se define en la página de diseño.</span><span class="sxs-lookup"><span data-stu-id="c1c10-241">When the page runs, it displays a button that lets the user hide or show a list that's defined in the layout page.</span></span> <span data-ttu-id="c1c10-242">Cuando los usuarios, haga clic en el botón, establece un valor de true o false (booleano) en el `PageData` propiedad.</span><span class="sxs-lookup"><span data-stu-id="c1c10-242">When users click the button, it sets a true/false (Boolean) value in the `PageData` property.</span></span> <span data-ttu-id="c1c10-243">La página de diseño lee ese valor y si es false, oculta la lista.</span><span class="sxs-lookup"><span data-stu-id="c1c10-243">The layout page reads that value, and if it's false, hides the list.</span></span> <span data-ttu-id="c1c10-244">El valor también se utiliza en la página de contenido para determinar si se debe mostrar el **Ocultar lista** botón o la **Mostrar lista** botón.</span><span class="sxs-lookup"><span data-stu-id="c1c10-244">The value is also used in the content page to determine whether to display the **Hide List** button or the **Show List** button.</span></span>

![[image]](3-creating-a-consistent-look/_static/image9.jpg)

1. <span data-ttu-id="c1c10-246">En la carpeta raíz, cree un archivo denominado *Content3.cshtml* y reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-246">In the root folder, create a file named *Content3.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample16.cshtml)]

    <span data-ttu-id="c1c10-247">El código almacena dos conjuntos de datos en el `PageData` propiedad &#8212; el título de la página web y true o false para especificar si desea mostrar una lista.</span><span class="sxs-lookup"><span data-stu-id="c1c10-247">The code stores two pieces of data in the `PageData` property &#8212; the title of the web page and true or false to specify whether to display a list.</span></span>

    <span data-ttu-id="c1c10-248">Tenga en cuenta que ASP.NET le permite colocar código HTML en la página de forma condicional mediante un bloque de código.</span><span class="sxs-lookup"><span data-stu-id="c1c10-248">Notice that ASP.NET lets you put HTML markup into the page conditionally using a code block.</span></span> <span data-ttu-id="c1c10-249">Por ejemplo, el `if/else` bloque en el cuerpo de la página determina qué formulario para mostrar dependiendo de si `PageData["ShowList"]` está establecido en true.</span><span class="sxs-lookup"><span data-stu-id="c1c10-249">For example, the `if/else` block in the body of the page determines which form to display depending on whether `PageData["ShowList"]` is set to true.</span></span>
2. <span data-ttu-id="c1c10-250">En el *Shared* carpeta, cree un archivo denominado  *\_Layout3.cshtml* y reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-250">In the *Shared* folder, create a file named *\_Layout3.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample17.cshtml)]

    <span data-ttu-id="c1c10-251">La página de diseño incluye una expresión en el `<title>` elemento que recibe el valor de título de la `PageData` propiedad.</span><span class="sxs-lookup"><span data-stu-id="c1c10-251">The layout page includes an expression in the `<title>` element that gets the title value from the `PageData` property.</span></span> <span data-ttu-id="c1c10-252">También usa el `ShowList` valor de la `PageData` propiedad para determinar si se muestra el bloque de contenido de la lista.</span><span class="sxs-lookup"><span data-stu-id="c1c10-252">It also uses the `ShowList` value of the `PageData` property to determine whether to display the list content block.</span></span>
3. <span data-ttu-id="c1c10-253">En el *Shared* carpeta, cree un archivo denominado  *\_List.cshtml* y reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="c1c10-253">In the *Shared* folder, create a file named *\_List.cshtml* and replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample18.html)]
4. <span data-ttu-id="c1c10-254">Ejecute el *Content3.cshtml* página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="c1c10-254">Run the *Content3.cshtml* page in a browser.</span></span> <span data-ttu-id="c1c10-255">Se abrirá la página con la lista visible en el lado izquierdo de la página y un **Ocultar lista** situado en la parte inferior.</span><span class="sxs-lookup"><span data-stu-id="c1c10-255">The page is displayed with the list visible on the left side of the page and a **Hide List** button at the bottom.</span></span>

    ![Captura de pantalla que muestra la página que incluye la lista y un botón que dice "Ocultar List".](3-creating-a-consistent-look/_static/image10.jpg)
5. <span data-ttu-id="c1c10-257">Haga clic en **Ocultar lista**.</span><span class="sxs-lookup"><span data-stu-id="c1c10-257">Click **Hide List**.</span></span> <span data-ttu-id="c1c10-258">La lista desaparece y el botón cambia a **Mostrar lista**.</span><span class="sxs-lookup"><span data-stu-id="c1c10-258">The list disappears and the button changes to **Show List**.</span></span>

    ![Captura de pantalla que muestra la página que no incluye la lista y un botón que dice 'Mostrar lista'.](3-creating-a-consistent-look/_static/image11.jpg)
6. <span data-ttu-id="c1c10-260">Haga clic en el **Mostrar lista** botón y la lista se muestra de nuevo.</span><span class="sxs-lookup"><span data-stu-id="c1c10-260">Click the **Show List** button, and the list is displayed again.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="c1c10-261">Recursos adicionales</span><span class="sxs-lookup"><span data-stu-id="c1c10-261">Additional Resources</span></span>


[<span data-ttu-id="c1c10-262">Personalizar el comportamiento de todo el sitio de ASP.NET Web Pages</span><span class="sxs-lookup"><span data-stu-id="c1c10-262">Customizing Site-Wide Behavior for ASP.NET Web Pages</span></span>](https://go.microsoft.com/fwlink/?LinkId=202906)
