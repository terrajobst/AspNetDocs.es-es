---
uid: web-pages/overview/data/working-with-files
title: Trabajar con archivos en un sitio de ASP.NET Web Pages (Razor) | Microsoft Docs
author: Rick-Anderson
description: En este capítulo se explica cómo leer, escribir, anexar, eliminar y cargar archivos.
ms.author: riande
ms.date: 02/20/2014
ms.assetid: eee916e4-ba4c-439a-a24e-68df7d45a569
msc.legacyurl: /web-pages/overview/data/working-with-files
msc.type: authoredcontent
ms.openlocfilehash: 684c47a8a8480dc040e5144144577c94c35d39e5
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 03/06/2020
ms.locfileid: "78521887"
---
# <a name="working-with-files-in-an-aspnet-web-pages-razor-site"></a><span data-ttu-id="0becf-103">Trabajar con archivos en un sitio de ASP.NET Web Pages (Razor)</span><span class="sxs-lookup"><span data-stu-id="0becf-103">Working with Files in an ASP.NET Web Pages (Razor) Site</span></span>

<span data-ttu-id="0becf-104">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="0becf-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="0becf-105">En este artículo se explica cómo leer, escribir, anexar, eliminar y cargar archivos en un sitio de ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="0becf-105">This article explains how to read, write, append, delete, and upload files in an ASP.NET Web Pages (Razor) site.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="0becf-106">Si desea cargar imágenes y manipularlas (por ejemplo, voltear o cambiar su tamaño), consulte [trabajar con imágenes en un sitio de ASP.NET Web pages](/aspnet/web-pages/overview/ui-layouts-and-themes/9-working-with-images).</span><span class="sxs-lookup"><span data-stu-id="0becf-106">If you want to upload images and manipulate them (for example, flip or resize them), see [Working with Images in an ASP.NET Web Pages Site](/aspnet/web-pages/overview/ui-layouts-and-themes/9-working-with-images).</span></span>
> 
> 
> <span data-ttu-id="0becf-107">**Lo que aprenderá:**</span><span class="sxs-lookup"><span data-stu-id="0becf-107">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="0becf-108">Cómo crear un archivo de texto y escribir datos en él.</span><span class="sxs-lookup"><span data-stu-id="0becf-108">How to create a text file and write data to it.</span></span>
> - <span data-ttu-id="0becf-109">Cómo anexar datos a un archivo existente.</span><span class="sxs-lookup"><span data-stu-id="0becf-109">How to append data to an existing file.</span></span>
> - <span data-ttu-id="0becf-110">Cómo leer un archivo y mostrarlo desde él.</span><span class="sxs-lookup"><span data-stu-id="0becf-110">How to read a file and display from it.</span></span>
> - <span data-ttu-id="0becf-111">Cómo eliminar archivos de un sitio Web.</span><span class="sxs-lookup"><span data-stu-id="0becf-111">How to delete files from a website.</span></span>
> - <span data-ttu-id="0becf-112">Cómo permitir a los usuarios cargar un archivo o varios archivos.</span><span class="sxs-lookup"><span data-stu-id="0becf-112">How to let users upload one file or multiple files.</span></span>
> 
> <span data-ttu-id="0becf-113">Estas son las características de programación de ASP.NET que se introdujeron en el artículo:</span><span class="sxs-lookup"><span data-stu-id="0becf-113">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="0becf-114">El objeto `File`, que proporciona una manera de administrar archivos.</span><span class="sxs-lookup"><span data-stu-id="0becf-114">The `File` object, which provides a way to manage files.</span></span>
> - <span data-ttu-id="0becf-115">Aplicación auxiliar de `FileUpload`.</span><span class="sxs-lookup"><span data-stu-id="0becf-115">The `FileUpload` helper.</span></span>
> - <span data-ttu-id="0becf-116">El objeto `Path`, que proporciona métodos que permiten manipular nombres de archivo y ruta de acceso.</span><span class="sxs-lookup"><span data-stu-id="0becf-116">The `Path` object, which provides methods that let you manipulate path and file names.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="0becf-117">Versiones de software usadas en el tutorial</span><span class="sxs-lookup"><span data-stu-id="0becf-117">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="0becf-118">ASP.NET Web Pages (Razor) 2</span><span class="sxs-lookup"><span data-stu-id="0becf-118">ASP.NET Web Pages (Razor) 2</span></span>
> - <span data-ttu-id="0becf-119">WebMatrix 2</span><span class="sxs-lookup"><span data-stu-id="0becf-119">WebMatrix 2</span></span>
>   
> 
> <span data-ttu-id="0becf-120">Este tutorial también funciona con WebMatrix 3.</span><span class="sxs-lookup"><span data-stu-id="0becf-120">This tutorial also works with WebMatrix 3.</span></span>

<a id="Creating_a_Text_File"></a>
## <a name="creating-a-text-file-and-writing-data-to-it"></a><span data-ttu-id="0becf-121">Crear un archivo de texto y escribir datos en él</span><span class="sxs-lookup"><span data-stu-id="0becf-121">Creating a Text File and Writing Data to It</span></span>

<span data-ttu-id="0becf-122">Además de usar una base de datos en el sitio web, puede trabajar con archivos.</span><span class="sxs-lookup"><span data-stu-id="0becf-122">In addition to using a database in your website, you might work with files.</span></span> <span data-ttu-id="0becf-123">Por ejemplo, puede usar archivos de texto como una forma sencilla de almacenar datos para el sitio.</span><span class="sxs-lookup"><span data-stu-id="0becf-123">For example, you might use text files as a simple way to store data for the site.</span></span> <span data-ttu-id="0becf-124">(Un archivo de texto que se usa para almacenar datos a veces se denomina *archivo plano*). Los archivos de texto pueden estar en formatos diferentes, como *. txt*, *. XML*o *. csv* (valores delimitados por comas).</span><span class="sxs-lookup"><span data-stu-id="0becf-124">(A text file that's used to store data is sometimes called a *flat file*.) Text files can be in different formats, like *.txt*, *.xml*, or *.csv* (comma-delimited values).</span></span>

<span data-ttu-id="0becf-125">Si desea almacenar los datos en un archivo de texto, puede utilizar el método `File.WriteAllText` para especificar el archivo que se va a crear y los datos que se van a escribir en él.</span><span class="sxs-lookup"><span data-stu-id="0becf-125">If you want to store data in a text file, you can use the `File.WriteAllText` method to specify the file to create and the data to write to it.</span></span> <span data-ttu-id="0becf-126">En este procedimiento, creará una página que contenga un formulario sencillo con tres `input` elementos (nombre, apellidos y dirección de correo electrónico) y un botón **Enviar** .</span><span class="sxs-lookup"><span data-stu-id="0becf-126">In this procedure, you'll create a page that contains a simple form with three `input` elements (first name, last name, and email address) and a **Submit** button.</span></span> <span data-ttu-id="0becf-127">Cuando el usuario envía el formulario, almacenará la entrada del usuario en un archivo de texto.</span><span class="sxs-lookup"><span data-stu-id="0becf-127">When the user submits the form, you'll store the user's input in a text file.</span></span>

1. <span data-ttu-id="0becf-128">Cree una nueva carpeta denominada *App\_Data*, si aún no existe.</span><span class="sxs-lookup"><span data-stu-id="0becf-128">Create a new folder named *App\_Data*, if it doesn't exist already.</span></span>
2. <span data-ttu-id="0becf-129">En la raíz de su sitio web, cree un nuevo archivo llamado *userdata. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0becf-129">At the root of your website, create a new file named *UserData.cshtml*.</span></span>
3. <span data-ttu-id="0becf-130">Reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="0becf-130">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample1.cshtml)]

    <span data-ttu-id="0becf-131">El marcado HTML crea el formulario con los tres cuadros de texto.</span><span class="sxs-lookup"><span data-stu-id="0becf-131">The HTML markup creates the form with the three text boxes.</span></span> <span data-ttu-id="0becf-132">En el código, use la propiedad `IsPost` para determinar si se ha enviado la página antes de comenzar el procesamiento.</span><span class="sxs-lookup"><span data-stu-id="0becf-132">In the code, you use the `IsPost` property to determine whether the page has been submitted before you start processing.</span></span>

    <span data-ttu-id="0becf-133">La primera tarea consiste en obtener la entrada del usuario y asignarla a las variables.</span><span class="sxs-lookup"><span data-stu-id="0becf-133">The first task is to get the user input and assign it to variables.</span></span> <span data-ttu-id="0becf-134">A continuación, el código concatena los valores de las variables independientes en una cadena delimitada por comas, que se almacena en una variable diferente.</span><span class="sxs-lookup"><span data-stu-id="0becf-134">The code then concatenates the values of the separate variables into one comma-delimited string, which is then stored in a different variable.</span></span> <span data-ttu-id="0becf-135">Tenga en cuenta que el separador de comas es una cadena contenida entre comillas (","), porque está insertando literalmente una coma en la cadena grande que está creando.</span><span class="sxs-lookup"><span data-stu-id="0becf-135">Notice that the comma separator is a string contained in quotation marks (","), because you're literally embedding a comma into the big string that you're creating.</span></span> <span data-ttu-id="0becf-136">Al final de los datos que se concatenan juntos, se agrega `Environment.NewLine`.</span><span class="sxs-lookup"><span data-stu-id="0becf-136">At the end of the data that you concatenate together, you add `Environment.NewLine`.</span></span> <span data-ttu-id="0becf-137">Esto agrega un salto de línea (un carácter de nueva línea).</span><span class="sxs-lookup"><span data-stu-id="0becf-137">This adds a line break (a newline character).</span></span> <span data-ttu-id="0becf-138">Lo que va a crear con esta concatenación es una cadena similar a la siguiente:</span><span class="sxs-lookup"><span data-stu-id="0becf-138">What you're creating with all this concatenation is a string that looks like this:</span></span>

    [!code-css[Main](working-with-files/samples/sample2.css)]

    <span data-ttu-id="0becf-139">(Con un salto de línea invisible al final).</span><span class="sxs-lookup"><span data-stu-id="0becf-139">(With an invisible line break at the end.)</span></span>

    <span data-ttu-id="0becf-140">A continuación, cree una variable (`dataFile`) que contenga la ubicación y el nombre del archivo en el que almacenar los datos.</span><span class="sxs-lookup"><span data-stu-id="0becf-140">You then create a variable (`dataFile`) that contains the location and name of the file to store the data in.</span></span> <span data-ttu-id="0becf-141">La configuración de la ubicación requiere un tratamiento especial.</span><span class="sxs-lookup"><span data-stu-id="0becf-141">Setting the location requires some special handling.</span></span> <span data-ttu-id="0becf-142">En sitios web, se recomienda hacer referencia al código en rutas de acceso absolutas como *C:\Folder\File.txt* para los archivos del servidor Web.</span><span class="sxs-lookup"><span data-stu-id="0becf-142">In websites, it's a bad practice to refer in code to absolute paths like *C:\Folder\File.txt* for files on the web server.</span></span> <span data-ttu-id="0becf-143">Si se mueve un sitio web, una ruta de acceso absoluta será incorrecta.</span><span class="sxs-lookup"><span data-stu-id="0becf-143">If a website is moved, an absolute path will be wrong.</span></span> <span data-ttu-id="0becf-144">Además, para un sitio hospedado (en lugar de en su propio equipo), normalmente no es necesario saber cuál es la ruta de acceso correcta al escribir el código.</span><span class="sxs-lookup"><span data-stu-id="0becf-144">Moreover, for a hosted site (as opposed to on your own computer) you typically don't even know what the correct path is when you're writing the code.</span></span>

    <span data-ttu-id="0becf-145">Pero a veces (como ahora, para escribir un archivo) necesita una ruta de acceso completa.</span><span class="sxs-lookup"><span data-stu-id="0becf-145">But sometimes (like now, for writing a file) you do need a complete path.</span></span> <span data-ttu-id="0becf-146">La solución consiste en usar el método `MapPath` del objeto `Server`.</span><span class="sxs-lookup"><span data-stu-id="0becf-146">The solution is to use the `MapPath` method of the `Server` object.</span></span> <span data-ttu-id="0becf-147">Esto devuelve la ruta de acceso completa a su sitio Web.</span><span class="sxs-lookup"><span data-stu-id="0becf-147">This returns the complete path to your website.</span></span> <span data-ttu-id="0becf-148">Para obtener la ruta de acceso de la raíz del sitio web, puede utilizar el operador de `~` (para que REPRES la raíz virtual del sitio) a `MapPath`.</span><span class="sxs-lookup"><span data-stu-id="0becf-148">To get the path for the website root, you user the `~` operator (to represen the site's virtual root) to `MapPath`.</span></span> <span data-ttu-id="0becf-149">(También puede pasarle un nombre de subcarpeta, como *~/App\_Data/* , para obtener la ruta de acceso de esa subcarpeta). Después, puede concatenar información adicional en cualquier objeto que devuelva el método para crear una ruta de acceso completa.</span><span class="sxs-lookup"><span data-stu-id="0becf-149">(You can also pass a subfolder name to it, like *~/App\_Data/*, to get the path for that subfolder.) You can then concatenate additional information onto whatever the method returns in order to create a complete path.</span></span> <span data-ttu-id="0becf-150">En este ejemplo, se agrega un nombre de archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-150">In this example, you add a file name.</span></span> <span data-ttu-id="0becf-151">(Puede obtener más información sobre cómo trabajar con rutas de acceso de archivos y carpetas en [Introducción a la programación de ASP.NET Web pages mediante la sintaxis Razor](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths)).</span><span class="sxs-lookup"><span data-stu-id="0becf-151">(You can read more about how to work with file and folder paths in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths).)</span></span>

    <span data-ttu-id="0becf-152">El archivo se guarda en la carpeta de datos de la\_de la *aplicación* .</span><span class="sxs-lookup"><span data-stu-id="0becf-152">The file is saved in the *App\_Data* folder.</span></span> <span data-ttu-id="0becf-153">Esta carpeta es una carpeta especial de ASP.NET que se usa para almacenar archivos de datos, tal y como se describe en [Introducción al trabajo con una base de datos en ASP.NET Web pages sitios](https://go.microsoft.com/fwlink/?LinkId=195209).</span><span class="sxs-lookup"><span data-stu-id="0becf-153">This folder is a special folder in ASP.NET that's used to store data files, as described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=195209).</span></span>

    <span data-ttu-id="0becf-154">El método `WriteAllText` del objeto `File` escribe los datos en el archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-154">The `WriteAllText` method of the `File` object writes the data to the file.</span></span> <span data-ttu-id="0becf-155">Este método toma dos parámetros: el nombre (con la ruta de acceso) del archivo en el que se va a escribir y los datos reales que se van a escribir.</span><span class="sxs-lookup"><span data-stu-id="0becf-155">This method takes two parameters: the name (with path) of the file to write to, and the actual data to write.</span></span> <span data-ttu-id="0becf-156">Observe que el nombre del primer parámetro tiene un carácter `@` como prefijo.</span><span class="sxs-lookup"><span data-stu-id="0becf-156">Notice that the name of the first parameter has an `@` character as a prefix.</span></span> <span data-ttu-id="0becf-157">Esto indica a ASP.NET que está proporcionando un literal de cadena textual y que los caracteres como "/" no deben interpretarse de manera especial.</span><span class="sxs-lookup"><span data-stu-id="0becf-157">This tells ASP.NET that you're providing a verbatim string literal, and that characters like "/" should not be interpreted in special ways.</span></span> <span data-ttu-id="0becf-158">(Para obtener más información, vea [Introducción a la programación web de ASP.net mediante la sintaxis Razor](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths)).</span><span class="sxs-lookup"><span data-stu-id="0becf-158">(For more information, see [Introduction to ASP.NET Web Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths).)</span></span>

    > [!NOTE]
    > <span data-ttu-id="0becf-159">Para que el código guarde los archivos en la carpeta de datos de la\_de la *aplicación* , la aplicación necesita permisos de lectura y escritura para esa carpeta.</span><span class="sxs-lookup"><span data-stu-id="0becf-159">In order for your code to save files in the *App\_Data* folder, the application needs read-write permissions for that folder.</span></span> <span data-ttu-id="0becf-160">En el equipo de desarrollo, esto no suele ser un problema.</span><span class="sxs-lookup"><span data-stu-id="0becf-160">On your development computer this is not typically an issue.</span></span> <span data-ttu-id="0becf-161">Sin embargo, al publicar el sitio en el servidor Web de un proveedor de hospedaje, puede que tenga que establecer esos permisos explícitamente.</span><span class="sxs-lookup"><span data-stu-id="0becf-161">However, when you publish your site to a hosting provider's web server, you might need to explicitly set those permissions.</span></span> <span data-ttu-id="0becf-162">Si ejecuta este código en el servidor de un proveedor de hospedaje y obtiene errores, consulte con el proveedor de hospedaje para averiguar cómo establecer esos permisos.</span><span class="sxs-lookup"><span data-stu-id="0becf-162">If you run this code on a hosting provider's server and get errors, check with the hosting provider to find out how to set those permissions.</span></span>

- <span data-ttu-id="0becf-163">Ejecute la página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="0becf-163">Run the page in a browser.</span></span> 

    ![](working-with-files/_static/image1.jpg)
- <span data-ttu-id="0becf-164">Escriba los valores en los campos y, a continuación, haga clic en **Enviar**.</span><span class="sxs-lookup"><span data-stu-id="0becf-164">Enter values into the fields and then click **Submit**.</span></span>
- <span data-ttu-id="0becf-165">Cierre el explorador.</span><span class="sxs-lookup"><span data-stu-id="0becf-165">Close the browser.</span></span>
- <span data-ttu-id="0becf-166">Vuelva al proyecto y actualice la vista.</span><span class="sxs-lookup"><span data-stu-id="0becf-166">Return to the project and refresh the view.</span></span>
- <span data-ttu-id="0becf-167">Abra el archivo *Data. txt* .</span><span class="sxs-lookup"><span data-stu-id="0becf-167">Open the *data.txt* file.</span></span> <span data-ttu-id="0becf-168">Los datos que envió en el formulario están en el archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-168">The data you submitted in the form is in the file.</span></span> 

    ![[imagen]](working-with-files/_static/image2.jpg)
- <span data-ttu-id="0becf-170">Cierre el archivo *Data. txt* .</span><span class="sxs-lookup"><span data-stu-id="0becf-170">Close the *data.txt* file.</span></span>

<a id="Appending_Data"></a>
## <a name="appending-data-to-an-existing-file"></a><span data-ttu-id="0becf-171">Anexar datos a un archivo existente</span><span class="sxs-lookup"><span data-stu-id="0becf-171">Appending Data to an Existing File</span></span>

<span data-ttu-id="0becf-172">En el ejemplo anterior, usó `WriteAllText` para crear un archivo de texto que tiene solo un fragmento de datos.</span><span class="sxs-lookup"><span data-stu-id="0becf-172">In the previous example, you used `WriteAllText` to create a text file that's got just one piece of data in it.</span></span> <span data-ttu-id="0becf-173">Si llama de nuevo al método y lo pasa al mismo nombre de archivo, el archivo existente se sobrescribe por completo.</span><span class="sxs-lookup"><span data-stu-id="0becf-173">If you call the method again and pass it the same file name, the existing file is completely overwritten.</span></span> <span data-ttu-id="0becf-174">Sin embargo, después de crear un archivo, a menudo desea agregar nuevos datos al final del archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-174">However, after you've created a file you often want to add new data to the end of the file.</span></span> <span data-ttu-id="0becf-175">Puede hacerlo mediante el método `AppendAllText` del objeto `File`.</span><span class="sxs-lookup"><span data-stu-id="0becf-175">You can do that using the `AppendAllText` method of the `File` object.</span></span>

1. <span data-ttu-id="0becf-176">En el sitio web, realice una copia del archivo *userdata. cshtml* y asígnele el nombre *UserDataMultiple. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0becf-176">In the website, make a copy of the *UserData.cshtml* file and name the copy *UserDataMultiple.cshtml*.</span></span>
2. <span data-ttu-id="0becf-177">Reemplace el bloque de código antes de la etiqueta de apertura `<!DOCTYPE html>` por el siguiente bloque de código:</span><span class="sxs-lookup"><span data-stu-id="0becf-177">Replace the code block before the opening `<!DOCTYPE html>` tag with the following code block:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample3.cshtml)]

    <span data-ttu-id="0becf-178">Este código tiene un cambio en el ejemplo anterior.</span><span class="sxs-lookup"><span data-stu-id="0becf-178">This code has one change in it from the previous example.</span></span> <span data-ttu-id="0becf-179">En lugar de utilizar `WriteAllText`, utiliza `the AppendAllText` método.</span><span class="sxs-lookup"><span data-stu-id="0becf-179">Instead of using `WriteAllText`, it uses `the AppendAllText` method.</span></span> <span data-ttu-id="0becf-180">Los métodos son similares, salvo que `AppendAllText` agrega los datos al final del archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-180">The methods are similar, except that `AppendAllText` adds the data to the end of the file.</span></span> <span data-ttu-id="0becf-181">Como con `WriteAllText`, `AppendAllText` crea el archivo si aún no existe.</span><span class="sxs-lookup"><span data-stu-id="0becf-181">As with `WriteAllText`, `AppendAllText` creates the file if it doesn't already exist.</span></span>
3. <span data-ttu-id="0becf-182">Ejecute la página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="0becf-182">Run the page in a browser.</span></span>
4. <span data-ttu-id="0becf-183">Escriba los valores de los campos y, a continuación, haga clic en **Enviar**.</span><span class="sxs-lookup"><span data-stu-id="0becf-183">Enter values for the fields and then click **Submit**.</span></span>
5. <span data-ttu-id="0becf-184">Agregue más datos y envíe el formulario de nuevo.</span><span class="sxs-lookup"><span data-stu-id="0becf-184">Add more data and submit the form again.</span></span>
6. <span data-ttu-id="0becf-185">Vuelva al proyecto, haga clic con el botón secundario en la carpeta del proyecto y, a continuación, haga clic en **Actualizar**.</span><span class="sxs-lookup"><span data-stu-id="0becf-185">Return to your project, right-click the project folder, and then click **Refresh**.</span></span>
7. <span data-ttu-id="0becf-186">Abra el archivo *Data. txt* .</span><span class="sxs-lookup"><span data-stu-id="0becf-186">Open the *data.txt* file.</span></span> <span data-ttu-id="0becf-187">Ahora contiene los nuevos datos que acaba de escribir.</span><span class="sxs-lookup"><span data-stu-id="0becf-187">It now contains the new data that you just entered.</span></span> 

    ![[imagen]](working-with-files/_static/image3.jpg)

<a id="Reading_and_Displaying_Data"></a>
## <a name="reading-and-displaying-data-from-a-file"></a><span data-ttu-id="0becf-189">Leer y Mostrar datos de un archivo</span><span class="sxs-lookup"><span data-stu-id="0becf-189">Reading and Displaying Data from a File</span></span>

<span data-ttu-id="0becf-190">Incluso si no necesita escribir datos en un archivo de texto, es probable que a veces necesite leer los datos de uno de ellos.</span><span class="sxs-lookup"><span data-stu-id="0becf-190">Even if you don't need to write data to a text file, you'll probably sometimes need to read data from one.</span></span> <span data-ttu-id="0becf-191">Para ello, puede volver a usar el objeto `File`.</span><span class="sxs-lookup"><span data-stu-id="0becf-191">To do this, you can again use the `File` object.</span></span> <span data-ttu-id="0becf-192">Puede usar el objeto `File` para leer cada línea individualmente (separadas por saltos de línea) o para leer el elemento individual, independientemente de cómo estén separados.</span><span class="sxs-lookup"><span data-stu-id="0becf-192">You can use the `File` object to read each line individually (separated by line breaks) or to read individual item no matter how they're separated.</span></span>

<span data-ttu-id="0becf-193">En este procedimiento se muestra cómo leer y mostrar los datos creados en el ejemplo anterior.</span><span class="sxs-lookup"><span data-stu-id="0becf-193">This procedure shows you how to read and display the data that you created in the previous example.</span></span>

1. <span data-ttu-id="0becf-194">En la raíz de su sitio web, cree un nuevo archivo denominado *DisplayData. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0becf-194">At the root of your website, create a new file named *DisplayData.cshtml*.</span></span>
2. <span data-ttu-id="0becf-195">Reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="0becf-195">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample4.cshtml)]

    <span data-ttu-id="0becf-196">El código comienza leyendo el archivo que creó en el ejemplo anterior en una variable denominada `userData`, mediante esta llamada al método:</span><span class="sxs-lookup"><span data-stu-id="0becf-196">The code starts by reading the file that you created in the previous example into a variable named `userData`, using this method call:</span></span>

    [!code-css[Main](working-with-files/samples/sample5.css)]

    <span data-ttu-id="0becf-197">El código para hacerlo está dentro de una instrucción `if`.</span><span class="sxs-lookup"><span data-stu-id="0becf-197">The code to do this is inside an `if` statement.</span></span> <span data-ttu-id="0becf-198">Cuando desea leer un archivo, es una buena idea usar el método `File.Exists` para determinar primero si el archivo está disponible.</span><span class="sxs-lookup"><span data-stu-id="0becf-198">When you want to read a file, it's a good idea to use the `File.Exists` method to determine first whether the file is available.</span></span> <span data-ttu-id="0becf-199">El código también comprueba si el archivo está vacío.</span><span class="sxs-lookup"><span data-stu-id="0becf-199">The code also checks whether the file is empty.</span></span>

    <span data-ttu-id="0becf-200">El cuerpo de la página contiene dos bucles `foreach`, uno anidado dentro de otro.</span><span class="sxs-lookup"><span data-stu-id="0becf-200">The body of the page contains two `foreach` loops, one nested inside the other.</span></span> <span data-ttu-id="0becf-201">El bucle `foreach` externo obtiene una línea a la vez del archivo de datos.</span><span class="sxs-lookup"><span data-stu-id="0becf-201">The outer `foreach` loop gets one line at a time from the data file.</span></span> <span data-ttu-id="0becf-202">En este caso, las líneas se definen mediante saltos de línea en &#8212; el archivo, es decir, cada elemento de datos se encuentra en su propia línea.</span><span class="sxs-lookup"><span data-stu-id="0becf-202">In this case, the lines are defined by line breaks in the file &#8212; that is, each data item is on its own line.</span></span> <span data-ttu-id="0becf-203">El bucle exterior crea un nuevo elemento (`<li>` elemento) dentro de una lista ordenada (elemento`<ol>`).</span><span class="sxs-lookup"><span data-stu-id="0becf-203">The outer loop creates a new item (`<li>` element) inside an ordered list (`<ol>` element).</span></span>

    <span data-ttu-id="0becf-204">El bucle interno divide cada línea de datos en elementos (campos) mediante una coma como delimitador.</span><span class="sxs-lookup"><span data-stu-id="0becf-204">The inner loop splits each data line into items (fields) using a comma as a delimiter.</span></span> <span data-ttu-id="0becf-205">(Según el ejemplo anterior, esto significa que cada línea contiene tres campos &#8212; , el nombre, el apellido y la dirección de correo electrónico, separados por una coma). El bucle interno también crea una lista de `<ul>` y muestra un elemento de lista para cada campo de la línea de datos.</span><span class="sxs-lookup"><span data-stu-id="0becf-205">(Based on the previous example, this means that each line contains three fields &#8212; the first name, last name, and email address, each separated by a comma.) The inner loop also creates a `<ul>` list and displays one list item for each field in the data line.</span></span>

    <span data-ttu-id="0becf-206">El código muestra cómo utilizar dos tipos de datos, una matriz y el tipo de datos `char`.</span><span class="sxs-lookup"><span data-stu-id="0becf-206">The code illustrates how to use two data types, an array and the `char` data type.</span></span> <span data-ttu-id="0becf-207">La matriz es necesaria porque el método `File.ReadAllLines` devuelve datos como una matriz.</span><span class="sxs-lookup"><span data-stu-id="0becf-207">The array is required because the `File.ReadAllLines` method returns data as an array.</span></span> <span data-ttu-id="0becf-208">El tipo de datos `char` es necesario porque el método `Split` devuelve un `array` en el que cada elemento es de tipo `char`.</span><span class="sxs-lookup"><span data-stu-id="0becf-208">The `char` data type is required because the `Split` method returns an `array` in which each element is of the type `char`.</span></span> <span data-ttu-id="0becf-209">(Para obtener información sobre las matrices, vea [Introducción a la programación web de ASP.net mediante la sintaxis Razor](https://go.microsoft.com/fwlink/?LinkId=202890#ID_CollectionsAndObjects)).</span><span class="sxs-lookup"><span data-stu-id="0becf-209">(For information about arrays, see [Introduction to ASP.NET Web Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890#ID_CollectionsAndObjects).)</span></span>
3. <span data-ttu-id="0becf-210">Ejecute la página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="0becf-210">Run the page in a browser.</span></span> <span data-ttu-id="0becf-211">Se muestran los datos especificados para los ejemplos anteriores.</span><span class="sxs-lookup"><span data-stu-id="0becf-211">The data you entered for the previous examples is displayed.</span></span> 

    ![[imagen]](working-with-files/_static/image4.jpg)

> [!TIP] 
> 
> <span data-ttu-id="0becf-213">**Mostrar datos de un archivo delimitado por comas de Microsoft Excel**</span><span class="sxs-lookup"><span data-stu-id="0becf-213">**Displaying Data from a Microsoft Excel Comma-Delimited File**</span></span>
> 
> <span data-ttu-id="0becf-214">Puede usar Microsoft Excel para guardar los datos contenidos en una hoja de cálculo como un archivo delimitado por comas (archivo *. csv* ).</span><span class="sxs-lookup"><span data-stu-id="0becf-214">You can use Microsoft Excel to save the data contained in a spreadsheet as a comma-delimited file (*.csv* file).</span></span> <span data-ttu-id="0becf-215">Cuando lo haga, el archivo se guardará en texto sin formato, no en formato de Excel.</span><span class="sxs-lookup"><span data-stu-id="0becf-215">When you do, the file is saved in plain text, not in Excel format.</span></span> <span data-ttu-id="0becf-216">Cada fila de la hoja de cálculo está separada por un salto de línea en el archivo de texto y cada elemento de datos está separado por una coma.</span><span class="sxs-lookup"><span data-stu-id="0becf-216">Each row in the spreadsheet is separated by a line break in the text file, and each data item is separated by a comma.</span></span> <span data-ttu-id="0becf-217">Puede usar el código que se muestra en el ejemplo anterior para leer un archivo delimitado por comas de Excel simplemente cambiando el nombre del archivo de datos en el código.</span><span class="sxs-lookup"><span data-stu-id="0becf-217">You can use the code shown in the previous example to read an Excel comma-delimited file just by changing the name of the data file in your code.</span></span>

<a id="Deleting_Files"></a>
## <a name="deleting-files"></a><span data-ttu-id="0becf-218">Eliminar archivos</span><span class="sxs-lookup"><span data-stu-id="0becf-218">Deleting Files</span></span>

<span data-ttu-id="0becf-219">Para eliminar archivos del sitio web, puede usar el método `File.Delete`.</span><span class="sxs-lookup"><span data-stu-id="0becf-219">To delete files from your website, you can use the `File.Delete` method.</span></span> <span data-ttu-id="0becf-220">Este procedimiento muestra cómo permitir que los usuarios eliminen una imagen (archivo *. jpg* ) de una carpeta de *imágenes* si conocen el nombre del archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-220">This procedure shows how to let users delete an image (*.jpg* file) from an *images* folder if they know the name of the file.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="0becf-221">**Importante** En un sitio web de producción, normalmente se restringe quién tiene permiso para realizar cambios en los datos.</span><span class="sxs-lookup"><span data-stu-id="0becf-221">**Important** In a production website, you typically restrict who's allowed to make changes to the data.</span></span> <span data-ttu-id="0becf-222">Para obtener información acerca de cómo configurar la pertenencia y sobre cómo autorizar a los usuarios para que realicen tareas en el sitio, consulte [adición de seguridad y pertenencia a un sitio de ASP.NET Web pages](https://go.microsoft.com/fwlink/?LinkId=202904).</span><span class="sxs-lookup"><span data-stu-id="0becf-222">For information about how to set up membership and about ways to authorize users to perform tasks on the site, see [Adding Security and Membership to an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=202904).</span></span>

1. <span data-ttu-id="0becf-223">En el sitio web, cree una subcarpeta denominada *images*.</span><span class="sxs-lookup"><span data-stu-id="0becf-223">In the website, create a subfolder named *images*.</span></span>
2. <span data-ttu-id="0becf-224">Copie uno o varios archivos *. jpg* en la carpeta *images* .</span><span class="sxs-lookup"><span data-stu-id="0becf-224">Copy one or more *.jpg* files into the *images* folder.</span></span>
3. <span data-ttu-id="0becf-225">En la raíz del sitio web, cree un nuevo archivo denominado *FileDelete. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0becf-225">In the root of the website, create a new file named *FileDelete.cshtml*.</span></span>
4. <span data-ttu-id="0becf-226">Reemplace el contenido existente por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="0becf-226">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample6.cshtml)]

    <span data-ttu-id="0becf-227">Esta página contiene un formulario en el que los usuarios pueden escribir el nombre de un archivo de imagen.</span><span class="sxs-lookup"><span data-stu-id="0becf-227">This page contains a form where users can enter the name of an image file.</span></span> <span data-ttu-id="0becf-228">No escriben la extensión de nombre de archivo *. jpg.* al restringir el nombre de archivo de este modo, se evita que los usuarios eliminen archivos arbitrarios en el sitio.</span><span class="sxs-lookup"><span data-stu-id="0becf-228">They don't enter the *.jpg* file-name extension; by restricting the file name like this, you help prevents users from deleting arbitrary files on your site.</span></span>

    <span data-ttu-id="0becf-229">El código lee el nombre de archivo que el usuario ha escrito y, a continuación, construye una ruta de acceso completa.</span><span class="sxs-lookup"><span data-stu-id="0becf-229">The code reads the file name that the user has entered and then constructs a complete path.</span></span> <span data-ttu-id="0becf-230">Para crear la ruta de acceso, el código utiliza la ruta de acceso del sitio web actual (tal como la devuelve el método `Server.MapPath`), el nombre de la carpeta *images* , el nombre que el usuario ha proporcionado y ". jpg" como una cadena literal.</span><span class="sxs-lookup"><span data-stu-id="0becf-230">To create the path, the code uses the current website path (as returned by the `Server.MapPath` method), the *images* folder name, the name that the user has provided, and ".jpg" as a literal string.</span></span>

    <span data-ttu-id="0becf-231">Para eliminar el archivo, el código llama al método `File.Delete`, pasándole la ruta de acceso completa que acaba de crear.</span><span class="sxs-lookup"><span data-stu-id="0becf-231">To delete the file, the code calls the `File.Delete` method, passing it the full path that you just constructed.</span></span> <span data-ttu-id="0becf-232">Al final del marcado, el código muestra un mensaje de confirmación que indica que el archivo se ha eliminado.</span><span class="sxs-lookup"><span data-stu-id="0becf-232">At the end of the markup, code displays a confirmation message that the file was deleted.</span></span>
5. <span data-ttu-id="0becf-233">Ejecute la página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="0becf-233">Run the page in a browser.</span></span> 

    ![[imagen]](working-with-files/_static/image5.jpg)
6. <span data-ttu-id="0becf-235">Escriba el nombre del archivo que desea eliminar y, a continuación, haga clic en **Enviar**.</span><span class="sxs-lookup"><span data-stu-id="0becf-235">Enter the name of the file to delete and then click **Submit**.</span></span> <span data-ttu-id="0becf-236">Si se eliminó el archivo, el nombre del archivo se muestra en la parte inferior de la página.</span><span class="sxs-lookup"><span data-stu-id="0becf-236">If the file was deleted, the name of the file is displayed at the bottom of the page.</span></span>

<a id="Letting_Users_Upload_a_File"></a>
## <a name="letting-users-upload-a-file"></a><span data-ttu-id="0becf-237">Permitir a los usuarios cargar un archivo</span><span class="sxs-lookup"><span data-stu-id="0becf-237">Letting Users Upload a File</span></span>

<span data-ttu-id="0becf-238">La aplicación auxiliar de `FileUpload` permite a los usuarios cargar archivos en el sitio Web.</span><span class="sxs-lookup"><span data-stu-id="0becf-238">The `FileUpload` helper lets users upload files to your website.</span></span> <span data-ttu-id="0becf-239">En el procedimiento siguiente se muestra cómo permitir a los usuarios cargar un único archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-239">The procedure below shows you how to let users upload a single file.</span></span>

1. <span data-ttu-id="0becf-240">Agregue la biblioteca de aplicaciones auxiliares Web de ASP.NET a su sitio web, tal como se describe en [instalación de aplicaciones auxiliares en un sitio de ASP.NET Web pages](https://go.microsoft.com/fwlink/?LinkId=252372), si no lo agregó anteriormente.</span><span class="sxs-lookup"><span data-stu-id="0becf-240">Add the ASP.NET Web Helpers Library to your website as described in [Installing Helpers in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=252372), if you didn't add it previously.</span></span>
2. <span data-ttu-id="0becf-241">En la carpeta *App\_Data* , cree una nueva carpeta y asígnele el nombre *UploadedFiles*.</span><span class="sxs-lookup"><span data-stu-id="0becf-241">In the *App\_Data* folder, create a new a folder and name it *UploadedFiles*.</span></span>
3. <span data-ttu-id="0becf-242">En la raíz, cree un nuevo archivo denominado *FileUpload. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0becf-242">In the root, create a new file named *FileUpload.cshtml*.</span></span>
4. <span data-ttu-id="0becf-243">Reemplace el contenido existente en la página por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="0becf-243">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample7.cshtml)]

    <span data-ttu-id="0becf-244">La parte del cuerpo de la página usa la aplicación auxiliar de `FileUpload` para crear el cuadro de carga y los botones con los que probablemente esté familiarizado:</span><span class="sxs-lookup"><span data-stu-id="0becf-244">The body portion of the page uses the `FileUpload` helper to create the upload box and buttons that you're probably familiar with:</span></span>

    ![[imagen]](working-with-files/_static/image6.jpg)

    <span data-ttu-id="0becf-246">Las propiedades que establezca para la aplicación auxiliar de `FileUpload` especifican que desea que se cargue un solo cuadro para el archivo y que desea que el botón Enviar lea **cargar**.</span><span class="sxs-lookup"><span data-stu-id="0becf-246">The properties that you set for the `FileUpload` helper specify that you want a single box for the file to upload and that you want the submit button to read **Upload**.</span></span> <span data-ttu-id="0becf-247">(Agregará más cuadros más adelante en el artículo).</span><span class="sxs-lookup"><span data-stu-id="0becf-247">(You'll add more boxes later in the article.)</span></span>

    <span data-ttu-id="0becf-248">Cuando el usuario hace clic en **cargar**, el código que se encuentra en la parte superior de la página obtiene el archivo y lo guarda.</span><span class="sxs-lookup"><span data-stu-id="0becf-248">When the user clicks **Upload**, the code at the top of the page gets the file and saves it.</span></span> <span data-ttu-id="0becf-249">El objeto `Request` que normalmente se usa para obtener los valores de los campos de formulario también tiene una matriz de `Files` que contiene el archivo (o archivos) que se ha cargado.</span><span class="sxs-lookup"><span data-stu-id="0becf-249">The `Request` object that you normally use to get values from form fields also has a `Files` array that contains the file (or files) that have been uploaded.</span></span> <span data-ttu-id="0becf-250">Puede obtener archivos individuales de posiciones específicas en la matriz &#8212; , por ejemplo, para obtener el primer archivo cargado, obtener `Request.Files[0]`, para obtener el segundo archivo, obtener `Request.Files[1]`, etc.</span><span class="sxs-lookup"><span data-stu-id="0becf-250">You can get individual files out of specific positions in the array &#8212; for example, to get the first uploaded file, you get `Request.Files[0]`, to get the second file, you get `Request.Files[1]`, and so on.</span></span> <span data-ttu-id="0becf-251">(Recuerde que en la programación, el recuento normalmente comienza en cero).</span><span class="sxs-lookup"><span data-stu-id="0becf-251">(Remember that in programming, counting usually starts at zero.)</span></span>

    <span data-ttu-id="0becf-252">Al capturar un archivo cargado, lo coloca en una variable (aquí, `uploadedFile`) para que pueda manipularlo.</span><span class="sxs-lookup"><span data-stu-id="0becf-252">When you fetch an uploaded file, you put it in a variable (here, `uploadedFile`) so that you can manipulate it.</span></span> <span data-ttu-id="0becf-253">Para determinar el nombre del archivo cargado, solo tiene que obtener su propiedad `FileName`.</span><span class="sxs-lookup"><span data-stu-id="0becf-253">To determine the name of the uploaded file, you just get its `FileName` property.</span></span> <span data-ttu-id="0becf-254">Sin embargo, cuando el usuario carga un archivo, `FileName` contiene el nombre original del usuario, que incluye la ruta de acceso completa.</span><span class="sxs-lookup"><span data-stu-id="0becf-254">However, when the user uploads a file, `FileName` contains the user's original name, which includes the entire path.</span></span> <span data-ttu-id="0becf-255">Podría tener este aspecto:</span><span class="sxs-lookup"><span data-stu-id="0becf-255">It might look like this:</span></span>

    <span data-ttu-id="0becf-256">*C:\Users\Public\Sample.txt*</span><span class="sxs-lookup"><span data-stu-id="0becf-256">*C:\Users\Public\Sample.txt*</span></span>

    <span data-ttu-id="0becf-257">Sin embargo, no desea toda la información de la ruta de acceso, ya que es la ruta de acceso en el equipo del usuario, no en el servidor.</span><span class="sxs-lookup"><span data-stu-id="0becf-257">You don't want all that path information, though, because that's the path on the user's computer, not for your server.</span></span> <span data-ttu-id="0becf-258">Simplemente desea el nombre de archivo real (*sample. txt*).</span><span class="sxs-lookup"><span data-stu-id="0becf-258">You just want the actual file name (*Sample.txt*).</span></span> <span data-ttu-id="0becf-259">Puede quitar solo el archivo de una ruta de acceso mediante el método `Path.GetFileName`, de la siguiente manera:</span><span class="sxs-lookup"><span data-stu-id="0becf-259">You can strip out just the file from a path by using the `Path.GetFileName` method, like this:</span></span>

    [!code-csharp[Main](working-with-files/samples/sample8.cs)]

    <span data-ttu-id="0becf-260">El objeto `Path` es una utilidad que tiene una serie de métodos como este que puede usar para quitar trazados, combinar trazados, etc.</span><span class="sxs-lookup"><span data-stu-id="0becf-260">The `Path` object is a utility that has a number of methods like this that you can use to strip paths, combine paths, and so on.</span></span>

    <span data-ttu-id="0becf-261">Una vez que haya obtenido el nombre del archivo cargado, puede crear una nueva ruta de acceso en la que desea almacenar el archivo cargado en el sitio Web.</span><span class="sxs-lookup"><span data-stu-id="0becf-261">Once you've gotten the name of the uploaded file, you can build a new path for where you want to store the uploaded file in your website.</span></span> <span data-ttu-id="0becf-262">En este caso, se combinan `Server.MapPath`, los nombres de carpeta (*App\_Data/UploadedFiles*) y el nombre de archivo recién eliminado para crear una nueva ruta de acceso.</span><span class="sxs-lookup"><span data-stu-id="0becf-262">In this case, you combine `Server.MapPath`, the folder names (*App\_Data/UploadedFiles*), and the newly stripped file name to create a new path.</span></span> <span data-ttu-id="0becf-263">Después, puede llamar al método `SaveAs` del archivo cargado para guardar realmente el archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-263">You can then call the uploaded file's `SaveAs` method to actually save the file.</span></span>
5. <span data-ttu-id="0becf-264">Ejecute la página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="0becf-264">Run the page in a browser.</span></span> 

    ![[imagen]](working-with-files/_static/image7.jpg)
6. <span data-ttu-id="0becf-266">Haga clic en **examinar** y seleccione un archivo para cargarlo.</span><span class="sxs-lookup"><span data-stu-id="0becf-266">Click **Browse** and then select a file to upload.</span></span> 

    ![[imagen]](working-with-files/_static/image8.jpg)

    <span data-ttu-id="0becf-268">El cuadro de texto situado junto al botón **examinar** contendrá la ruta de acceso y la ubicación del archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-268">The text box next to the **Browse** button will contain the path and file location.</span></span>

    ![[imagen]](working-with-files/_static/image9.jpg)
7. <span data-ttu-id="0becf-270">Haga clic en **Cargar**.</span><span class="sxs-lookup"><span data-stu-id="0becf-270">Click **Upload**.</span></span>
8. <span data-ttu-id="0becf-271">En el sitio web, haga clic con el botón secundario en la carpeta de proyecto y haga clic en **Actualizar**.</span><span class="sxs-lookup"><span data-stu-id="0becf-271">In the website, right-click the project folder and then click **Refresh**.</span></span>
9. <span data-ttu-id="0becf-272">Abra la carpeta *UploadedFiles* .</span><span class="sxs-lookup"><span data-stu-id="0becf-272">Open the *UploadedFiles* folder.</span></span> <span data-ttu-id="0becf-273">El archivo que ha cargado está en la carpeta.</span><span class="sxs-lookup"><span data-stu-id="0becf-273">The file that you uploaded is in the folder.</span></span> 

    ![[imagen]](working-with-files/_static/image10.jpg)

<a id="Letting_Users_Upload_Multiple_Files"></a>
## <a name="letting-users-upload-multiple-files"></a><span data-ttu-id="0becf-275">Permitir a los usuarios cargar varios archivos</span><span class="sxs-lookup"><span data-stu-id="0becf-275">Letting Users Upload Multiple Files</span></span>

<span data-ttu-id="0becf-276">En el ejemplo anterior, permite a los usuarios cargar un archivo.</span><span class="sxs-lookup"><span data-stu-id="0becf-276">In the previous example, you let users upload one file.</span></span> <span data-ttu-id="0becf-277">Sin embargo, puede usar la aplicación auxiliar `FileUpload` para cargar más de un archivo a la vez.</span><span class="sxs-lookup"><span data-stu-id="0becf-277">But you can use the `FileUpload` helper to upload more than one file at a time.</span></span> <span data-ttu-id="0becf-278">Esto resulta útil para escenarios como la carga de fotografías, donde la carga de un archivo a la vez es tediosa.</span><span class="sxs-lookup"><span data-stu-id="0becf-278">This is handy for scenarios like uploading photos, where uploading one file at a time is tedious.</span></span> <span data-ttu-id="0becf-279">(Puede leer acerca de la carga de fotografías [al trabajar con imágenes en un sitio de ASP.NET Web pages](https://go.microsoft.com/fwlink/?LinkId=202897)). Este ejemplo muestra cómo permitir que los usuarios carguen dos a la vez, aunque puede usar la misma técnica para cargar más que eso.</span><span class="sxs-lookup"><span data-stu-id="0becf-279">(You can read about uploading photos in [Working with Images in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=202897).) This example shows how to let users upload two at a time, although you can use the same technique to upload more than that.</span></span>

1. <span data-ttu-id="0becf-280">Agregue la biblioteca de aplicaciones auxiliares Web de ASP.NET a su sitio web, tal como se describe en [instalación de aplicaciones auxiliares en un sitio de ASP.NET Web pages](https://go.microsoft.com/fwlink/?LinkId=252372), si no lo ha hecho ya.</span><span class="sxs-lookup"><span data-stu-id="0becf-280">Add the ASP.NET Web Helpers Library to your website as described in [Installing Helpers in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=252372), if you haven't already.</span></span>
2. <span data-ttu-id="0becf-281">Cree una nueva página denominada *FileUploadMultiple. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0becf-281">Create a new page named *FileUploadMultiple.cshtml*.</span></span>
3. <span data-ttu-id="0becf-282">Reemplace el contenido existente en la página por lo siguiente:</span><span class="sxs-lookup"><span data-stu-id="0becf-282">Replace the existing content in the page with the following:</span></span>  

    [!code-cshtml[Main](working-with-files/samples/sample9.cshtml)]

    <span data-ttu-id="0becf-283">En este ejemplo, la aplicación auxiliar de `FileUpload` en el cuerpo de la página está configurada para permitir que los usuarios carguen dos archivos de forma predeterminada.</span><span class="sxs-lookup"><span data-stu-id="0becf-283">In this example, the `FileUpload` helper in the body of the page is configured to let users upload two files by default.</span></span> <span data-ttu-id="0becf-284">Como `allowMoreFilesToBeAdded` está establecido en `true`, el ayudante representa un vínculo que permite al usuario agregar más cuadros de carga:</span><span class="sxs-lookup"><span data-stu-id="0becf-284">Because `allowMoreFilesToBeAdded` is set to `true`, the helper renders a link that lets user add more upload boxes:</span></span>

    ![[imagen]](working-with-files/_static/image11.jpg)

    <span data-ttu-id="0becf-286">Para procesar los archivos que el usuario carga, el código utiliza la misma técnica básica que usó en el ejemplo &#8212; anterior obtener un archivo de `Request.Files` y, a continuación, guardarlo.</span><span class="sxs-lookup"><span data-stu-id="0becf-286">To process the files that the user uploads, the code uses the same basic technique that you used in the previous example &#8212; get a file from `Request.Files` and then save it.</span></span> <span data-ttu-id="0becf-287">(Incluidas las diversas cosas que debe hacer para obtener el nombre de archivo y la ruta de acceso correctos). La innovación es que es posible que el usuario esté cargando varios archivos y no conozca muchos.</span><span class="sxs-lookup"><span data-stu-id="0becf-287">(Including the various things you need to do to get the right file name and path.) The innovation this time is that the user might be uploading multiple files and you don't know many.</span></span> <span data-ttu-id="0becf-288">Para averiguarlo, puede obtener `Request.Files.Count`.</span><span class="sxs-lookup"><span data-stu-id="0becf-288">To find out, you can get `Request.Files.Count`.</span></span>

    <span data-ttu-id="0becf-289">Con este número a mano, puede recorrer `Request.Files`, capturar cada archivo a su vez y guardarlo.</span><span class="sxs-lookup"><span data-stu-id="0becf-289">With this number in hand, you can loop through `Request.Files`, fetch each file in turn, and save it.</span></span> <span data-ttu-id="0becf-290">Si desea crear un bucle de un número conocido de veces a través de una colección, puede usar un bucle `for`, como el siguiente:</span><span class="sxs-lookup"><span data-stu-id="0becf-290">When you want to loop a known number of times through a collection, you can use a `for` loop, like this:</span></span>

    [!code-csharp[Main](working-with-files/samples/sample10.cs)]

    <span data-ttu-id="0becf-291">La variable `i` es simplemente un contador temporal que va de cero a cualquier límite superior establecido.</span><span class="sxs-lookup"><span data-stu-id="0becf-291">The variable `i` is just a temporary counter that will go from zero to whatever upper limit you set.</span></span> <span data-ttu-id="0becf-292">En este caso, el límite superior es el número de archivos.</span><span class="sxs-lookup"><span data-stu-id="0becf-292">In this case, the upper limit is the number of files.</span></span> <span data-ttu-id="0becf-293">Pero dado que el contador comienza en cero, como es típico para el recuento de escenarios en ASP.NET, el límite superior es realmente uno menos que el número de archivos.</span><span class="sxs-lookup"><span data-stu-id="0becf-293">But because the counter starts at zero, as is typical for counting scenarios in ASP.NET, the upper limit is actually one less than the file count.</span></span> <span data-ttu-id="0becf-294">(Si se cargan tres archivos, el recuento es de cero a 2).</span><span class="sxs-lookup"><span data-stu-id="0becf-294">(If three files are uploaded, the count is zero to 2.)</span></span>

    <span data-ttu-id="0becf-295">La variable `uploadedCount` suma todos los archivos que se han cargado y guardado correctamente.</span><span class="sxs-lookup"><span data-stu-id="0becf-295">The `uploadedCount` variable totals all the files that are successfully uploaded and saved.</span></span> <span data-ttu-id="0becf-296">Este código tiene en cuenta la posibilidad de que un archivo esperado no pueda cargarse.</span><span class="sxs-lookup"><span data-stu-id="0becf-296">This code accounts for the possibility that an expected file may not be able to be uploaded.</span></span>
4. <span data-ttu-id="0becf-297">Ejecute la página en un explorador.</span><span class="sxs-lookup"><span data-stu-id="0becf-297">Run the page in a browser.</span></span> <span data-ttu-id="0becf-298">El explorador muestra la página y sus dos cuadros de carga.</span><span class="sxs-lookup"><span data-stu-id="0becf-298">The browser displays the page and its two upload boxes.</span></span>
5. <span data-ttu-id="0becf-299">Seleccione dos archivos para cargar.</span><span class="sxs-lookup"><span data-stu-id="0becf-299">Select two files to upload.</span></span>
6. <span data-ttu-id="0becf-300">Haga clic en **Agregar otro archivo**.</span><span class="sxs-lookup"><span data-stu-id="0becf-300">Click **Add another file**.</span></span> <span data-ttu-id="0becf-301">La página muestra un nuevo cuadro de carga.</span><span class="sxs-lookup"><span data-stu-id="0becf-301">The page displays a new upload box.</span></span> 

    ![[imagen]](working-with-files/_static/image12.jpg)
7. <span data-ttu-id="0becf-303">Haga clic en **Cargar**.</span><span class="sxs-lookup"><span data-stu-id="0becf-303">Click **Upload**.</span></span>
8. <span data-ttu-id="0becf-304">En el sitio web, haga clic con el botón secundario en la carpeta de proyecto y haga clic en **Actualizar**.</span><span class="sxs-lookup"><span data-stu-id="0becf-304">In the website, right-click the project folder and then click **Refresh**.</span></span>
9. <span data-ttu-id="0becf-305">Abra la carpeta *UploadedFiles* para ver los archivos cargados correctamente.</span><span class="sxs-lookup"><span data-stu-id="0becf-305">Open the *UploadedFiles* folder to see the successfully uploaded files.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="0becf-306">Recursos adicionales</span><span class="sxs-lookup"><span data-stu-id="0becf-306">Additional Resources</span></span>

[<span data-ttu-id="0becf-307">Trabajar con imágenes en un sitio de ASP.NET Web Pages</span><span class="sxs-lookup"><span data-stu-id="0becf-307">Working with Images in an ASP.NET Web Pages Site</span></span>](https://go.microsoft.com/fwlink/?LinkId=202897)

[<span data-ttu-id="0becf-308">Exportación a un archivo CSV</span><span class="sxs-lookup"><span data-stu-id="0becf-308">Exporting to a CSV File</span></span>](https://msdn.microsoft.com/library/ms155919.aspx)
